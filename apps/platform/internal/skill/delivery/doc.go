// Package packaging turns an immutable Skill Version into a downloadable
// package: it builds the zip, writes the manifest, and owns the download
// artifacts and records produced from it.
package packaging
