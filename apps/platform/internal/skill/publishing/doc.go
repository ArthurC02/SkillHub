// Package publishing owns who discloses what under which name: a creator's
// publisher namespace, the publications in it, and the immutable releases
// that pin the version each one shows.
package publishing
