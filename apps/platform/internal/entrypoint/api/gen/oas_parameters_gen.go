// Code generated by ogen, DO NOT EDIT.

package publicapi

import (
	"github.com/google/uuid"
)

// ActOnCreationSessionParams is parameters of actOnCreationSession operation.
type ActOnCreationSessionParams struct {
	SessionID uuid.UUID
}

// AddAcceptanceCriterionParams is parameters of addAcceptanceCriterion operation.
type AddAcceptanceCriterionParams struct {
	ID uuid.UUID
}

// BrowseCatalogParams is parameters of browseCatalog operation.
type BrowseCatalogParams struct {
	Limit OptInt `json:",omitempty,omitzero"`
	// As on `GET /api/skills/search`. Absent = not filtered.
	Script OptBrowseCatalogScript `json:",omitempty,omitzero"`
	// As on `GET /api/skills/search`. Absent = not filtered.
	Validation OptBrowseCatalogValidation `json:",omitempty,omitzero"`
	// As on `GET /api/skills/search`. Absent = not filtered.
	Agent OptBrowseCatalogAgent `json:",omitempty,omitzero"`
	// As on `GET /api/skills/search`. Absent = not filtered.
	Tier OptBrowseCatalogTier `json:",omitempty,omitzero"`
	// As on `GET /api/skills/search`. Absent = not filtered.
	Category OptBrowseCatalogCategory `json:",omitempty,omitzero"`
}

// CancelRunParams is parameters of cancelRun operation.
type CancelRunParams struct {
	ID uuid.UUID
}

// ClearSkillRestrictionParams is parameters of clearSkillRestriction operation.
type ClearSkillRestrictionParams struct {
	ID uuid.UUID
}

// CompareRunsParams is parameters of compareRuns operation.
type CompareRunsParams struct {
	ID uuid.UUID
	// The other run. Must be in the caller's workspace.
	Against uuid.UUID
}

// ConfirmRunPreflightParams is parameters of confirmRunPreflight operation.
type ConfirmRunPreflightParams struct {
	ID uuid.UUID
}

// CreateDownloadArtifactParams is parameters of createDownloadArtifact operation.
type CreateDownloadArtifactParams struct {
	// Skill id. The version must belong to this skill.
	ID        uuid.UUID
	VersionId uuid.UUID
}

// CreateSkillVersionFromSuggestionsParams is parameters of createSkillVersionFromSuggestions operation.
type CreateSkillVersionFromSuggestionsParams struct {
	// Skill id. The evaluation's run must belong to a version of this skill.
	ID uuid.UUID
}

// DecideSuggestionParams is parameters of decideSuggestion operation.
type DecideSuggestionParams struct {
	ID uuid.UUID
}

// DeleteAcceptanceCriterionParams is parameters of deleteAcceptanceCriterion operation.
type DeleteAcceptanceCriterionParams struct {
	ID          uuid.UUID
	CriterionId string
}

// DeleteDatasetParams is parameters of deleteDataset operation.
type DeleteDatasetParams struct {
	ID        uuid.UUID
	DatasetId uuid.UUID
}

// DeleteDownloadArtifactParams is parameters of deleteDownloadArtifact operation.
type DeleteDownloadArtifactParams struct {
	ArtifactId uuid.UUID
}

// DeleteRunArtifactParams is parameters of deleteRunArtifact operation.
type DeleteRunArtifactParams struct {
	ID         uuid.UUID
	ArtifactId uuid.UUID
}

// DeleteSkillParams is parameters of deleteSkill operation.
type DeleteSkillParams struct {
	ID uuid.UUID
}

// DeleteTestCaseParams is parameters of deleteTestCase operation.
type DeleteTestCaseParams struct {
	ID uuid.UUID
}

// DiffSkillVersionsParams is parameters of diffSkillVersions operation.
type DiffSkillVersionsParams struct {
	ID   uuid.UUID
	From uuid.UUID
	To   uuid.UUID
}

// DownloadArtifactContentParams is parameters of downloadArtifactContent operation.
type DownloadArtifactContentParams struct {
	ArtifactId uuid.UUID
}

// FindSkillsForGovernanceParams is parameters of findSkillsForGovernance operation.
type FindSkillsForGovernanceParams struct {
	Q string
}

// FinishGithubLoginParams is parameters of finishGithubLogin operation.
type FinishGithubLoginParams struct {
	Code  string
	State string
}

// ForkSkillParams is parameters of forkSkill operation.
type ForkSkillParams struct {
	ID uuid.UUID
}

// GetCostTrendParams is parameters of getCostTrend operation.
type GetCostTrendParams struct {
	Days OptGetCostTrendDays `json:",omitempty,omitzero"`
}

// GetCreationSessionParams is parameters of getCreationSession operation.
type GetCreationSessionParams struct {
	SessionID uuid.UUID
}

// GetCreditLedgerParams is parameters of getCreditLedger operation.
type GetCreditLedgerParams struct {
	WorkspaceID uuid.UUID
}

// GetCreditTrendParams is parameters of getCreditTrend operation.
type GetCreditTrendParams struct {
	Days OptGetCreditTrendDays `json:",omitempty,omitzero"`
}

// GetDownloadArtifactParams is parameters of getDownloadArtifact operation.
type GetDownloadArtifactParams struct {
	ArtifactId uuid.UUID
}

// GetOperatorActionTrendParams is parameters of getOperatorActionTrend operation.
type GetOperatorActionTrendParams struct {
	Days OptGetOperatorActionTrendDays `json:",omitempty,omitzero"`
}

// GetRunParams is parameters of getRun operation.
type GetRunParams struct {
	ID uuid.UUID
}

// GetRunEvaluationParams is parameters of getRunEvaluation operation.
type GetRunEvaluationParams struct {
	ID uuid.UUID
	// An `evaluation_id` from GET /runs/{id}/evaluation/revisions.
	Revision OptUUID `json:",omitempty,omitzero"`
}

// GetRunPreflightParams is parameters of getRunPreflight operation.
type GetRunPreflightParams struct {
	// Skill id. The version must belong to this skill.
	ID        uuid.UUID
	VersionID uuid.UUID
	// The editable draft, not a snapshot. The summary describes a run that has not started, and a snapshot
	// only exists once one has.
	TestCaseID uuid.UUID
}

// GetRunTraceParams is parameters of getRunTrace operation.
type GetRunTraceParams struct {
	// The platform run_id. A provider's ephemeral id never appears in a URL (iron rule 10).
	ID   uuid.UUID
	Mode OptGetRunTraceMode `json:",omitempty,omitzero"`
	// Advanced-mode ingestion cursor. Omit or use 0 for the first page. Page boundaries follow receipt
	// order so a running producer cannot make an earlier page skip a newly committed event.
	After OptInt64 `json:",omitempty,omitzero"`
}

// GetRunTrendParams is parameters of getRunTrend operation.
type GetRunTrendParams struct {
	Days OptGetRunTrendDays `json:",omitempty,omitzero"`
}

// GetSkillDetailParams is parameters of getSkillDetail operation.
type GetSkillDetailParams struct {
	ID uuid.UUID
	// `embedded` marks a read that is NOT a detail-page view: a surface that needs the same data for its
	// own purpose (packaging, side-by-side comparison). Such a read records no `skill_detail_viewed`,
	// because 01 §11.2's first segment counts sessions in which somebody opened a skill, and Compare was
	// minting that event for skills whose detail page was never opened (04 丙-57 follow-up).
	View OptGetSkillDetailView `json:",omitempty,omitzero"`
}

// GetSkillFilesParams is parameters of getSkillFiles operation.
type GetSkillFilesParams struct {
	ID uuid.UUID
}

// GetSuggestionDiffParams is parameters of getSuggestionDiff operation.
type GetSuggestionDiffParams struct {
	ID uuid.UUID
}

// GetTestCaseParams is parameters of getTestCase operation.
type GetTestCaseParams struct {
	ID uuid.UUID
}

// GrantCreditsParams is parameters of grantCredits operation.
type GrantCreditsParams struct {
	WorkspaceID uuid.UUID
}

// IngestTraceEventsParams is parameters of ingestTraceEvents operation.
type IngestTraceEventsParams struct {
	// The signed per-attempt ingestion credential. Secret material; never logged.
	Token string
}

// ListDatasetsParams is parameters of listDatasets operation.
type ListDatasetsParams struct {
	ID uuid.UUID
}

// ListDownloadRecordsParams is parameters of listDownloadRecords operation.
type ListDownloadRecordsParams struct {
	ArtifactId uuid.UUID
}

// ListOperatorAuditLogParams is parameters of listOperatorAuditLog operation.
type ListOperatorAuditLogParams struct {
	Limit  OptInt `json:",omitempty,omitzero"`
	Offset OptInt `json:",omitempty,omitzero"`
}

// ListRunArtifactsParams is parameters of listRunArtifacts operation.
type ListRunArtifactsParams struct {
	ID uuid.UUID
}

// ListRunEvaluationRevisionsParams is parameters of listRunEvaluationRevisions operation.
type ListRunEvaluationRevisionsParams struct {
	ID uuid.UUID
}

// ListRunSuggestionsParams is parameters of listRunSuggestions operation.
type ListRunSuggestionsParams struct {
	ID uuid.UUID
}

// ListRunsParams is parameters of listRuns operation.
type ListRunsParams struct {
	// Only runs of this test case — the "執行歷史" of one draft, which is what closes the 建立 →
	// 試跑 → 回來看 loop. Matched against the test case the run's snapshot was frozen from, so a
	// run stays in the list after the draft has been edited. Workspace scoped like the unfiltered list;
	// another workspace's id matches nothing (WS-006).
	TestCaseID OptUUID `json:",omitempty,omitzero"`
	// Refused with a 400 when outside the schema, not clamped: both bounds are inclusive and an
	// out-of-range value is not replaced by the default.
	Limit  OptInt `json:",omitempty,omitzero"`
	Offset OptInt `json:",omitempty,omitzero"`
}

// ListSkillVersionsParams is parameters of listSkillVersions operation.
type ListSkillVersionsParams struct {
	ID uuid.UUID
}

// ListTestCasesParams is parameters of listTestCases operation.
type ListTestCasesParams struct {
	// Only this skill's test cases. Answers "which test cases have I written for this skill", which the
	// Skill detail page asks.
	SkillID OptUUID `json:",omitempty,omitzero"`
	Limit   OptInt  `json:",omitempty,omitzero"`
	Offset  OptInt  `json:",omitempty,omitzero"`
}

// LookupAccountParams is parameters of lookupAccount operation.
type LookupAccountParams struct {
	Email string
}

// PreviewPackagingParams is parameters of previewPackaging operation.
type PreviewPackagingParams struct {
	// Skill id. The version must belong to this skill.
	ID        uuid.UUID
	VersionId uuid.UUID
	// Which target to preview. Required and not defaulted: the answer differs per target, and a default
	// would let a user read the standard package's verdict as a profile's.
	Target PackagingTargetId
	// Preview with the PACK-005 test cases included. The excluded list is returned either way, so a user
	// can see what would be left out before deciding.
	IncludeTestCases OptBool `json:",omitempty,omitzero"`
}

// PublicSearchSkillsParams is parameters of publicSearchSkills operation.
type PublicSearchSkillsParams struct {
	// Natural language task description.
	Q     string
	Limit OptInt `json:",omitempty,omitzero"`
	// Why this search is being made, when it is not the user looking for a Skill to use. `reference` is
	// GEN-006's reference picker: the caller is choosing worked examples for a generation, not expressing
	// an intent the funnel measures. The server then writes no `search_performed` event (01 §11.2 segment
	// 1 counts intents, and this is not one) and makes no match-reason model call (the reason a hit
	// matched is DISC-002's answer to "should I use this", which the picker does not ask). Retrieval
	// itself is unchanged. Absent = an ordinary search.
	Purpose OptPublicSearchSkillsPurpose `json:",omitempty,omitzero"`
	// DISC-003 filter on whether the package carries runnable code, as recorded by the import scan: a
	// script file in the tree, or code embedded in SKILL.md itself (SKILL-003). Absent = not filtered.
	//
	// A row the projection holds no scan for matches neither value. It is not known to have a script and
	// it is not known to be free of one, and answering `no` for it would be the
	// 不得自行推定為通過 that DISC-004 forbids. Such rows leave a filtered page and return when
	// the filter is cleared.
	Script OptPublicSearchSkillsScript `json:",omitempty,omitzero"`
	// DISC-003 filter on the spec-validation axis of `compatibility`. `passed` is a skill with a saved
	// version — static validation blocks the import on any error-level finding, so a stored version is
	// the evidence. `unverified` is a skill with no saved content, which is never reported as failed.
	// Absent = not filtered.
	Validation OptPublicSearchSkillsValidation `json:",omitempty,omitzero"`
	// DISC-002's Agent dimension, live since migration 0022. It filters the `runtime` axis of
	// `compatibility` and only that axis, matching the measured verdict exactly rather than as a boolean:
	// `unverified` is a value a caller can ask for, and a "not native" filter would silently mean
	// transpiled-or-failed-or-never-measured, which are three different things to someone choosing a
	// skill. Absent = not filtered.
	//
	// The `capability` axis is shown but not filterable. Every measured skill in the catalogue came back
	// `activated` (45/45 in the M2 baseline), so a control on it separates nothing; it becomes a filter
	// when a `not_activated` row exists and not before.
	Agent OptPublicSearchSkillsAgent `json:",omitempty,omitzero"`
	// DISC-002's 來源層級 dimension, live since migration 0042. Absent = not filtered.
	//
	// `curated` means the PDM-002 nine-item review passed and the version it examined is still the newest
	// one; a curated skill whose content has moved on answers `indexed` here, because five of those nine
	// checks are about specific bytes. So `indexed` is not "never reviewed" — it is "not currently
	// carrying a review of what you are looking at", which is the question a reader is actually asking.
	//
	// `external` is not accepted. An external result was never imported and has no row, so it is a state
	// of the search rather than a value this filter can select.
	Tier OptPublicSearchSkillsTier `json:",omitempty,omitzero"`
	// DISC-002's 類別 dimension (PDM-001's three shelves), live since migration 0053. Absent = not
	// filtered.
	//
	// Matches the skill's stored category exactly. A skill with no category — every user-imported skill
	// today, because the platform has not decided how one gets a category (05 R-19) — matches none of
	// the three values; it is not silently filed under any of them, and its row says 尚未定值 rather
	// than a guess (02:DISC-004).
	Category OptPublicSearchSkillsCategory `json:",omitempty,omitzero"`
}

// SaveSkillVersionParams is parameters of saveSkillVersion operation.
type SaveSkillVersionParams struct {
	ID uuid.UUID
}

// SearchSkillsParams is parameters of searchSkills operation.
type SearchSkillsParams struct {
	Q     string
	Limit OptInt `json:",omitempty,omitzero"`
}

// SetEvaluationFeedbackParams is parameters of setEvaluationFeedback operation.
type SetEvaluationFeedbackParams struct {
	ID uuid.UUID
}

// SetSkillCategoryParams is parameters of setSkillCategory operation.
type SetSkillCategoryParams struct {
	ID uuid.UUID
}

// SetSkillCurationTierParams is parameters of setSkillCurationTier operation.
type SetSkillCurationTierParams struct {
	ID uuid.UUID
}

// SetSkillRedistributionParams is parameters of setSkillRedistribution operation.
type SetSkillRedistributionParams struct {
	ID uuid.UUID
}

// SetSkillRestrictionParams is parameters of setSkillRestriction operation.
type SetSkillRestrictionParams struct {
	ID uuid.UUID
}

// StartRunParams is parameters of startRun operation.
type StartRunParams struct {
	// Skill id. The version must belong to this skill.
	ID uuid.UUID
}

// StreamCreationSessionParams is parameters of streamCreationSession operation.
type StreamCreationSessionParams struct {
	SessionID uuid.UUID
	// The revision of the last event this client already has; the server replays from the next one, so a
	// reconnect loses nothing and repeats nothing. Absent means 「everything this session has」. Named
	// as the SSE specification names it — HTTP header names are case-insensitive, and a browser's own
	// reconnect sends exactly this.
	LastEventID OptInt64 `json:",omitempty,omitzero"`
}

// SuggestAcceptanceCriteriaParams is parameters of suggestAcceptanceCriteria operation.
type SuggestAcceptanceCriteriaParams struct {
	ID uuid.UUID
}

// TakedownSkillParams is parameters of takedownSkill operation.
type TakedownSkillParams struct {
	ID uuid.UUID
}

// TakedownSkillAsOperatorParams is parameters of takedownSkillAsOperator operation.
type TakedownSkillAsOperatorParams struct {
	ID uuid.UUID
}

// UpdateAcceptanceCriterionParams is parameters of updateAcceptanceCriterion operation.
type UpdateAcceptanceCriterionParams struct {
	ID          uuid.UUID
	CriterionId string
}

// UpdateTestCaseParams is parameters of updateTestCase operation.
type UpdateTestCaseParams struct {
	ID uuid.UUID
}

// UploadDatasetParams is parameters of uploadDataset operation.
type UploadDatasetParams struct {
	ID uuid.UUID
}
