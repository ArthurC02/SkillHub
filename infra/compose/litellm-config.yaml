model_list:
  - model_name: gpt-5.4-mini #試跑預設 (PDM-003 v5)
    litellm_params:
      model: openai/gpt-5.4-mini
      api_key: os.environ/OPENAI_API_KEY
      # This route does not honor drop_params/additional_drop_params globally,
      # so unsupported params must be dropped per model here.
      additional_drop_params: ["reasoning_effort", "thinking"]
  - model_name: gpt-5.6-sol # flagship
    litellm_params:
      model: openai/gpt-5.6-sol
      api_key: os.environ/OPENAI_API_KEY
      additional_drop_params: ["reasoning_effort", "thinking", "temperature"]
  - model_name: gpt-5.6-terra # judge tier, separated from the generating model
    litellm_params:
      model: openai/gpt-5.6-terra
      api_key: os.environ/OPENAI_API_KEY
      additional_drop_params: ["reasoning_effort", "thinking", "temperature"]
  - model_name: gpt-5.6-luna # match-reason tier
    litellm_params:
      model: openai/gpt-5.6-luna
      api_key: os.environ/OPENAI_API_KEY
      additional_drop_params: ["reasoning_effort", "thinking", "temperature"]
  - model_name: text-embedding-3-small
    litellm_params:
      model: openai/text-embedding-3-small
      api_key: os.environ/OPENAI_API_KEY

general_settings:
  master_key: os.environ/LITELLM_MASTER_KEY
  database_url: os.environ/DATABASE_URL
  proxy_batch_write_at: 1
  # Reservation charges each in-flight request's theoretical max cost (full input
  # rate plus max_tokens at output rate) against the budget, not actual spend;
  # disabling it enforces against recorded spend instead.
  disable_budget_reservation: true

litellm_settings:
  drop_params: true
  max_budget: 50
  budget_duration: 1d
