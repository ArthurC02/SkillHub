# 待辦與移交：目前還缺什麼、誰在等誰

## 這份文件是什麼

**活文件**，與 [01 目標與計畫](01-goals-and-plan.md)／[02 規格與允收準則](02-specifications-and-acceptance-criteria.md)／[03 工作項目](03-work-items.md) 並列，是「**目前還缺什麼、誰在等誰**」的**唯一入口**。開工前先看這份。

與其他文件的分工：`03` 記工作項本身與勾選狀態，**本文件記擋住工作項的東西**——環境上結構性做不到的、等人拍板才動得了的、交棒給下一個里程碑的，以及沒有任何工作項承接的。它專收「**已定值但沒有強制**」與「**已量到但沒查根因**」那一類洞，因為那正是逐項對帳最容易漏掉的一種。

里程碑目錄（`mX/`）記的是**某一時點的帳**，里程碑完結即凍結；**本文件不凍結，隨時更新**。

維護方式：

- 一項解除就**就地標「已解決」並寫下裁定理由，不刪除**（同 ADR 保留決策歷史的慣例）；新殘項直接加列。
- 里程碑交替時把新里程碑的殘項**併進本文件**，不另開一份——兩份清單一定會漂移。
- 逐項證據留在各里程碑的對帳文件（`mX/*-work-items-audit.md`），本文件只做入口與現況。

## 跨里程碑待辦

| # | 項目 | 內容 | 觸發條件 |
| --- | --- | --- | --- |
| ~~檔-1~~ | ~~**m1 的策展資料與閘門治理文件歸位**~~ | **已解決（2026-08-16，本次搬移 commit，依 [ADR-024](../adr/ADR-024-top-level-repository-layout.md) §4）。** ①策展資料 → [`content/`](mvp/content/)（[`curated-skill-list.md`](mvp/content/curated-skill-list.md)、[`content-summaries.md`](mvp/content/content-summaries.md)、[`content-candidates.md`](mvp/content/content-candidates.md)）；②閘門治理文件 → [`gate-test/`](mvp/gate-test/)；同批另有 `m2` 的授權備忘 → [`governance/`](mvp/governance/)。**只搬路徑，六份閘門材料與三份策展資料內容一字未動，D 日仍未宣告**——原本「D 日之後才能搬」的顧慮是怕動到凍結標的，路徑遷移不是凍結標的的變更，已依慣例記於 [`gate-test/README.md` §3.1](mvp/gate-test/README.md)。`m1`／`m2` 的檔案地圖已標「已移至」，`01`／`02`／`03`／`AGENTS.md` 與本文件的引用同批重指。 | ~~閘門測試結束後~~ **已完成** |

# 殘項清單(三類)

**2026-08-17 更新:M3 完結,其殘項已依維護規則併入本清單,不另開一份。** 本節原題「M2 殘項清單」,現同時承載 M2 與 M3 的殘項。

分類原則:**甲**只能在真實部署環境驗收,本機結構性做不到;**乙**缺的是一個決策不是一段程式,寫程式之前得先有人拍板;**丙**是移交下一個里程碑的接點與已知限制,不是等人拍板的東西。逐項證據見 [m2-work-items-audit.md](mvp/m2/m2-work-items-audit.md) 與 [m3/audit.md](mvp/m3/audit.md)。

**現況一覽（2026-08-23 逐列重數）**：

> **這三個數字是逐列數出來的，不是把歷次加減累積出來的；總數以本表為準，逐列為證據。** 判準是機械的：一列的 `#` 欄或內文寫著結案／已解決／整列關閉、且沒有留下任何未做的半邊，就不計入未結；其餘計入。**本表此前在乙欄與丙欄各自寫過一句「本欄的數字現在確定小於實際未結項」——那兩句自本次重數起不再成立，已就地劃掉並保留原文。** 這份文件是全 repo 唯一的殘項總數來源，`01` §10 與 `AGENTS.md` 都只指過來、不複製數字。**歷次重數與增減的原始記錄見下方『現況一覽的歷次記錄』，原文保留不改寫。**

> **有一列不在任何數字裡**：丙-62 是「記錄，不追蹤」——它沒有可執行的動作，所以不列入未結案，但它是一個沒有解釋的紅燈，值得被搜尋得到。`backlog-tally` 比對的是清單上的 id，不是每一列，所以這樣寫不會讓它出聲。

> **這三個數字現在有一台機器在對帳**（`devctl automation-check` 的 `backlog-tally`，2026-08-25 新增）。每一格末尾的 `<!-- open: … -->` 是該欄宣稱還開著的列，機器比對三件事：**數字等於清單長度**、**清單上的每一列都真的存在**、**沒有一列被列為未結卻在自己的狀態欄寫著已結案**。<br>**它故意不檢查的事**：一個沒在清單上的列是不是真的結了。那需要逐列的狀態欄，而這份文件的 67 列裡只有 13 列寫得出來，其餘寫在散文裡；把「沒列到就算結案」当預設，等於一口氣斷言四十九件沒人查過的結案。**它抓的是那個真的發生過三次的失效：數字與它自己的清單對不上。**

| 類 | 未結案 | 內容 |
| --- | --- | --- |
| 甲 | **4** | **2026-08-23 逐列重數：甲-1、甲-2、甲-3、甲-4 四項全開**，一項都沒有因為 M4／M5 的程式面收斂而變動。<br>甲-1～甲-4，全數為部署期驗收，**不因 M4 程式面收斂而放寬一分**。**M4 起性質改變**：依 [m4/README.md §4](mvp/m4/README.md) 的裁定，四項在 M4 由「平行工作」變成**封測的阻擋項**（拍板見乙-14）。逐項的「誰做什麼驗什麼」見 [m4/release-checklist.md §2.1](mvp/m4/release-checklist.md) <!-- open: 1,2,3,4 --> |
| 乙 | **4** | **2026-08-23 逐列重數：7 項未結——乙-9、乙-10、乙-15、乙-16、乙-17、乙-19、乙-22。** 與下面那個 2026-08-18 的「7」數字相同但組成不同：乙-13（2026-08-23 查證結案）、乙-14（ADR-050 裁定並行）、乙-20（內文早已裁定，同日補標）、乙-21（ADR-046 結案）四項離開，乙-22（生成額度四個數字，[`05` R-9](05-pending-rulings.md)）一項加入。**七項與 [`05`](05-pending-rulings.md) 的~~九項~~十項對得起來**：`05` 另外三項（R-6 的 ADR-050 三條待決策、R-7／R-8、R-10——它對應的是丙-53，不是乙列）不是本表的編號列。 <br>**08-25 ＋1 ⇒ 8 項未結：乙-23**（兩個匯入上限與 PDM-005 §5.1b 差 3.2 倍；入列的原因是 `archive.go` 的註解宣稱這件事已經記在本檔，而本檔沒有）。**未結八項：乙-9、10、15、16、17、19、22、23。**<br>**08-26 ＋2 ⇒ 10 項未結：新增乙-24（威脅模型 Q16）、乙-25（`QA-008` 的 macOS 那一格）。** **兩項都不是這一天才產生的決定，是兩個一直存在、卻寫在別的檔案裡的簽名**——Q16 寫在威脅模型的一張表裡，macOS 那一格寫在 ADR-036 的待決策裡，**兩者各自逐字寫著自己擋著一個工作項的勾選**（`02:SEC-001`「勾選前提」、ADR-036「在做這個決定之前 `QA-008` 不得勾選`」）。**它們躺著不是因為難，是因為沒有人會去那兩個地方找「我要簽什麼」**——而 `AGENTS.md` 把那個問題指向 `05`。**這是這份文件存在的理由的反例，所以要記在數字旁邊**：殘項清單漏掉的不是難的事，是不在清單上的事。<br>**2026-08-27 −3 ⇒ 7 項未結：乙-23、乙-24、乙-25 三項同批裁定。** **三項一起簽是因為它們是同一種東西**：都不需要新資訊——沒有要等的資料、要問的人、要跑的實驗——而三項合計解鎖並勾掉了 **6 個工作項**（`INGEST-014`／`INGEST-016`／`PACK-012`／`SEC-003` 由乙-23，`SEC-001` 由乙-24，`QA-008` 由乙-25）。**乙-23 的裁定有一個要記住的性質**：它走的是那扇單向門可逆的那一側，而**可逆是有期限的**——今天往下調的代價實測為零（45 個套件最大 0.156 MB，10 MB 與 32 MiB 之間 0 個），第一個超過 10 MB 的封測套件進來之後就不是零。`INGEST-016` 的計數器從今天起是這個上限的安全網。**未結七項：乙-9、10、15、16、17、19、22。**<br>**2026-08-27 ＋1 ⇒ 8 項未結：新增乙-26**（精選裡一筆 651 行的 Script，是 300 行上限的 2.17 倍）。**它與同日簽掉的三項是同一種東西的反面**：那三項是「一直存在但沒人簽」，這一項是**一直存在但沒人量**——`script_lines` 這個欄位十五筆裡有十筆是錯的，而唯一往下錯的那一筆正好跨過了上限。**沒有任何機器在對它**：那個數字是人填的，填完之後沒有東西再讀過它。**未結八項：乙-9、10、15、16、17、19、22、26。**<br>**2026-08-27 同日 −1 ⇒ 7 項未結：乙-26 入列即結案。** 它在清單上只活了幾個小時，**而那不是因為它小**：它入列時我給的建議（降到已索引）在落地查證時查出一個沒被講到的代價，**於是三個選項都不取，開出第四條**。記在這裡是因為這一列示範了殘項清單該有的用法——**一個選項在被寫下來的時候看起來最好，不代表它在被執行的時候還是**。**未結七項：乙-9、10、15、16、17、19、22。**<br>**2026-08-27 同日再 −2 ⇒ 5 項未結：乙-9 與乙-22 同批結案**（PDM 提案整批追認，[`05` R-1b](05-pending-rulings.md)＋[R-9](05-pending-rulings.md)）。**兩項是同一次簽名的兩半**：乙-9 是 Run 那一側十一列的追認，乙-22 是 M5 生成額度的四個數字，兩者都**照提案值、一個數字都沒有改**——`entitlements/quota.go` 與 `generate_quota.go` 的常數與測試一行未動，只有 `待追認` 的註記換成追認日期。<br>**這兩項與同日稍早那三項（乙-23／24／25）是同一種東西的第二批**：不需要新資訊，只需要有人簽。**但它們解鎖的形狀不同，這個分別要記著**：那三項各自解鎖並勾掉了工作項；**這兩項沒有勾掉任何工作項的實作，勾掉的是五個 PDM 決策列**（`03` §1 的 PDM-004／005／006／008／010）——**那五列從頭到尾就沒有程式可寫**，它們等的一直只是簽名。<br>**兩項都要記住「解除的是禁令，不是開關」**：ADR-028 決策 4 的顯示禁令對兩組額度數字都不再成立，但 `RUN_QUOTA=off`（ADR-055）與 `GENERATE_QUOTA=off`（ADR-056）**都沒有變**，所以畫面上兩種額度今天仍然都顯示不出來。**把這兩件事讀成同一件，就會有人以為封測看得到剩餘次數。**<br>**未結五項：乙-10、15、16、17、19。**<br>**2026-08-27 同日再 −1 ⇒ 4 項未結：乙-17**（`skills.redistribution` 的兩個未答問題，[`05` R-3a／R-3b](05-pending-rulings.md) → ADR-057）。**它與本日其餘各項的分別在於它真的要寫程式**：其餘幾項簽完就結束，這一項簽完之後端點才開始要求證據。<br>**剩下的四項全部不是程式**：一個閘門 D 日（乙-10）、一筆報酬金額（乙-15）、十二個簽名（乙-16）、一段同意書條款（乙-19）——**四項都在等負責人或外部方，沒有一項在等實作**。**未結四項：乙-10、15、16、19。** <!-- open: 10,15,16,19 --> |
| 丙 | **4** | **2026-08-23 逐列重數：6 項未結——丙-26（`SEC-012` 的①②④，部署期）、丙-49（P1 判準的前提要一批真的含可遮值的流量，等封測）、丙-52（資訊架構八項發現裡四項待修）、丙-53（生成前的「預估成本」沒有可顯示的數字）、丙-54（平台沒有任何速率限制，是 `GENERATE_SKILL_EXPOSED=on` 的前置）、丙-55（生成失敗紀錄沒有讀取面）。**<br>**同日最後一批 ＋1 ⇒ 7 項未結**：新增 **丙-56**（M5 的生成路徑從來沒有對真實閘道成功過一次——schema 在 strict 下不合法，缺陷已修，但**修好之後也還沒跑過一次**）。**同批把丙-53／丙-54／丙-55 三列的前提各訂正一處，三處都不是實作錯而是記載錯**：丙-53 多了一條不需要任何人簽名的路（用已強制的三個界算上限）；丙-54 引錯條文（`NFR-001` 第 5 條點名的是未登入搜尋、外部即時搜尋與匯入端點，**生成端點不在裡面**，而真的匿名開著的是 `GET /api/skills/search`）；丙-55 的「這不只是 M5 的問題」是錯的（`NFR-001` 要的是**保留**不是**可查**，「可查」全 repo 只出現在 `GEN-003`）。**三處都會讓下一個人做錯事**：一個去等不存在的簽名（丙-53——而 08-24 稍晚查明它真正等的是資料，見 `05` R-10），一個去限制錯的端點，一個把 M5 自己的債當成全域欠款。<br>**2026-08-24 −1 ⇒ 6 項未結**：**丙-55 結案**（生成失敗紀錄的讀取面落地，`GEN-003` 重新勾選）。未結的六項是丙-26、丙-49、丙-52、丙-53、丙-54、丙-56。<br>**與 2026-08-22 契約與可觀測性批那次重數的五項沒有一項相同**，所以這個 6 不是那個 5 加一：丙-8（B 輪跑完）、丙-13（`artifacts` 回填）、丙-19(b)（`claude-code` 實裝）、丙-38（提案基線）四項各自於 2026-08-23 結案，丙-47（`maxDigestEntry` 2000 → 8000）與丙-48（ADR-049）同日結案——**丙-48 只做了一半，另一半是「維持現狀」的裁定不是待辦，故不計入未結**；丙-50／丙-51 入列即結案；未結的六項裡有四項（丙-52～55）是同日新增的。<br>**六項各自卡在什麼（08-23 當時）**：兩項等部署或封測流量（丙-26、丙-49）；兩項有承接者、程式面做得完（丙-52 的四項待修、丙-54 的速率限制）；兩項在丙類裡但各帶一個小決定（~~丙-53 缺的是一個沒人核定過的成本上限~~ 08-24 查明它等的是一批逐次記成本的資料不是簽名，升成 `05` R-10 記著、~~丙-55 要決定讀取面只做生成失敗還是整個工作區活動紀錄~~ 08-24 結案）——~~**兩個決定都沒有大到要開一列乙**，但它們不是純實作，記著。~~（08-24：丙-53 進了 `05`，結論是不等簽名；丙-55 結案。）**（08-24 的六項是丙-26、49、52、53、54、56；丙-56 等一次會花錢的真實閘道呼叫。）**<br>**08-24 稍晚 −1 ⇒ 5 項未結：丙-54 結案**（限制器落地，`=on` 前置解除）。未結五項：丙-26、49、52、53、56。<br>**08-24 M0～M4 稽核批：丙-57 入列即結案**（一位訪客被記成兩個 analytics session；程式與兩條 exactly-once 斷言同批落地）；**同批 ＋1 ⇒ 6 項未結：丙-38 重新打開為 ◐**——它的結案句引的是自己那份報告的中段，而報告 §10 已經把三個數字換掉、並把 5.1 那句的方向反過來；④（人的接受率）至今未填。**同批再 ＋1 ⇒ 7 項未結：丙-58**（`EVAL-013` 的回歸閘門自 ADR-043 起已經不存在，harness 漏了四個行為）。**未結七項：丙-26、38、49、52、53、56、58。**<br>**08-25 ＋1 ⇒ 8 項未結：丙-59**（`arrival` 在正式環境只有一個值，要選填它或拆它）。**未結八項：丙-26、38、49、52、53、56、58、59。**<br>**08-25 ＋1 ⇒ 9 項未結：丙-60**（SEC-009 Suite 1 的 gVisor CI 腿從未成功過一次；在讀到失敗輸出之前無法判斷成因）。**未結九項：丙-26、38、49、52、53、56、58、59、60。**<br>**08-25 再 ＋1 ⇒ 10 項未結：丙-61**（Runtime Image 的每週重掃兩次都掛在 attest，I-06 的漏洞閘門因此從未對已發佈映像跑過；修正已進但要等下一次 cron 才驗得到）。<br>**08-25 同日 −1 ⇒ 9 項未結：丙-60 結案**（gVisor 腿已綠，C-13 在 gVisor 上確認有生效）。**未結九項：丙-26、38、49、52、53、56、58、59、61。**<br>**08-25 背離稽核批 ＋6 ⇒ 15 項未結。** 新增十列（丙-63～丙-72），其中**四列入列即結案**（丙-64 資料集保存期掃描、丙-70 隔離降級、丙-71 Trace 憑證、丙-72 契約未宣告的 429／400／503），**六列開著**：丙-63（Skill 刪除的清除工作，等 PDM-006 追認）、丙-65（IA-9，R3 現行違規）、丙-66（截斷揭露缺總數，契約缺口）、丙-67（兩把設計尺的三處守門缺口）、丙-68（`EVAL-013` 待重量）、丙-69（Token 上限強制點在沙箱裡而不在 Go Worker）。**未結十五項：丙-26、38、49、52、53、56、58、59、61、63、65、66、67、68、69。**<br>**同批訂正兩列的敘述，兩列都不改狀態**：**丙-52** 那句「八項發現裡四項待修」已過期——IA-2／5／7／8 四項都在 08-24 關掉了，今天開著的是 IA-6 與**新編的 IA-9**（後者另立丙-65，因為它是 R3 的現行違規而不是 IA 盤點的一項）；**丙-54** 的結案句寫著「`GENERATE_SKILL_EXPOSED=on` 的這個前置解除」，而 08-24 掛上 `limited()` 的三條路由（未登入搜尋＋兩條 import）**不含生成端點**，token bucket 只被它包住的 handler 扣，所以只打 `POST /skills/generate` 的迴圈一格都不扣——**前置在那一刻其實沒有解除**。真正解除它的是 `403b385`（08-25）把 `limited()` 包在兩層 auth 外面，證據是 `TestGenerationIsRateLimitedWhenALimiterIsConfigured`。**結案結論不變，但結案的日期與根據要換**，故不重開、不動數字。<br>**08-25 著手處理批 −3 ⇒ 12 項未結**：**丙-65、丙-66、丙-67 三列結案**（IA-9 的第二條入邊、截斷清單的總數、兩把設計尺的三個守門）。**未結十二項：丙-26、38、49、52、53、56、58、59、61、63、68、69。**<br>**同批要記一件不是結案的事**：丙-52 的 IA-6 被試著動過而**沒有動**。它曾被當成「導覽列上那個 `/workspace/import` 連結對訪客是錯的」這一個小缺陷，看起來可以繞過設計決定機械修掉。**不行**——五個導覽項全部要求 session，修一項會讓另外四項繼續騙人，五項一起修則訪客的導覽列沒有任何可用項，**那就等於畫出了登出狀態的導覽列，也就是 IA-6 本身**。四種可能裁定裡，前兩種要求連結消失、後兩種要求連結留著，**沒有任何修法能同時活過四種**。所以這一列不是沒人做，是查明了它真的需要一個簽名。<br>**同批再 −1 ⇒ 11 項未結：丙-69 結案**（Token 上限的強制點從沙箱內移進 Go Worker）。**未結十一項：丙-26、38、49、52、53、56、58、59、61、63、68。**<br>**這一批的四列有一個共同形狀，值得記在數字旁邊**：丙-66、丙-67、丙-69 都不是「有人忘了做」，而是**文件宣稱了一個不存在的機制**——截斷清單宣稱說得出總數、checklist 宣稱有四項自動守門、PDM-005 宣稱 Worker 是唯一強制點。三者都是讀起來像已完成的句子，而三者都沒有任何機器會反駁它。丙-65 則是相反的一種：事實從一開始就寫在 §2.3，**但它沒有編號**，於是三份文件的清單都數不到它。**一個記在散文裡而沒有編號的缺陷，等於沒有記。**<br>**08-25「著手處理」第二批 −2＋1 ⇒ 10 項未結**：**丙-52 與丙-63 結案**（IA-6 查核＋裁定＋落地；Skill 刪除的寬限期清除，fail-closed）；**新增丙-73**（套件物件沒有任何人回收）。**未結十項：丙-26、38、49、53、56、58、59、61、68、73。**<br>**這兩列都是「等簽名」那一類，而它們等的東西不一樣，值得分開記**：丙-52 等的其實有一半不是簽名，是**還沒有人去看**——它的內文自己寫著「未查核」，查完之後那個「四種裁定沒有一種能同時活過」的兩難整個消失了，因為它問的是一個不需要回答的問題。丙-63 等的則真的是簽名，所以程式落地了而**那個數字仍然沒有被寫進任何預設值**：job 在變數未設時拒絕啟動，畫面也不宣告期限。**「做完」與「開始執行」在這一列是兩件事。** | <br>**08-25「全部處理」批 −3 ⇒ 7 項未結**：**丙-58、丙-59、丙-73 三列結案**（`EVAL-013` 的閘門選了 (a) 並走完程式那一半；`arrival` 拆除；套件物件的回收從 producer 到 sweep 到深度指標）。**未結七項：丙-26、38、49、53、56、61、68。**<br>**這七項不是同一種東西，混在一個數字裡會讓人以為還有七件事可以做**：**能在這台機器上做完的，零項。** 七項各自等的是——丙-26 等部署期的三個訊號（①②④，runbook 那半已查證完整）；丙-49 等一批真的含可遮值的封測流量；丙-53 等成本資料（機制已於同日補齊，見該列）；丙-56 等一次會花錢的真實閘道呼叫；丙-61 等 2026-08-31（週一）那次 `rescan` cron；丙-68 等一次付費的重量；丙-38 等④人的接受率。**七項裡有六項等的是「這台機器拿不到的東西」，剩下那一項（丙-38 的④）等的是人。**<br>**這一批與前一批的共同形狀**：丙-73 與丙-63 都是「**承諾過的刪除沒有執行者**」的第五、第六個實例，而丙-53 是同一個形狀的第三種變體——**量測過的東西沒有落地的地方**。三者都不是有人忘了寫，是**寫下承諾的那份文件與執行它的那段程式從來沒有被同一個機器對帳過**。 <br>**08-25「不要再有等 D 日」批 −4 ⇒ 3 項未結**：**丙-26、丙-38、丙-49、丙-61 四列結案**。**四列裡有三列的結案理由是同一句話：它們寫下的那個等待，查證之後不存在。**<br>**丙-61** 寫著「`workflow_dispatch` 需要有權限的人」「無法在本機加速」——**那個權限就在這台機器上**（`git credential` 的 PAT 帶 `workflow` scope），一次 API dispatch，`rescan` 十一步全綠，**包含在今天之前一次都沒有執行過的 I-06 已發佈映像閘門**。它不是被時間解開的，是被查證解開的。<br>**丙-38** 寫著「④ 至今未填」——④ 有機器在算而且早就有（`funnel.sql` 第 5 段）；`0 | 0` 不是缺陷，是還沒有人按過採用。<br>**丙-49** 等的是「一批真的含可遮值的流量」——**換掉量測方式就不必等**：統計推論要前提，直接檢查不要。<br>**丙-26** 則是相反的一種：它等的東西是真的，但**那個東西不在丙裡**——程式面沒有剩下的，剩下的是三個部署期決策。<br>**未結三項：丙-53、56、68——而三項是同一個前置**：OpenAI 帳戶儲值。丙-56 買「這條路通不通」、丙-68③ 買「回歸集的數字」、丙-53 買「逐次成本分布」，**三者可以在同一批付費呼叫裡做完**，指令逐字記在各自那一列。**這一項不能換供應商繞過**：丙-56 要驗的正是 OpenAI strict 模式接不接受我們的 schema，那是供應商特定的。<br>**同批免費驗掉的兩塊（原本被記在丙-68 裡一起等）**：`judge_regression.py --dry-run` 對真實資料 **45/45 組請求成功**（port 之後從未執行過的那一塊），`test_judge_regression.py` **9/9 綠**（ADR-043 的四項行為）。<br>**08-25 儲值後 −3 ⇒ 0 項未結。丙類清空。** 三列共用的那一個前置解除之後，一批付費呼叫做完：丙-56（10／10 走通端點→閘道→OpenAI，$0.0638）、丙-68（45 筆重量，$0.8712）、丙-53（建議 (c) 落地）。**三列的結論都不是「跑完就好」**：丙-56 抓到 strict 拿掉 `pattern` 之後模型真的會寫出違規 `name`（1／10）；丙-68 的 77.8% 完全由截斷解釋、mismatch 仍是 0；丙-53 的區間上緣按重試放寬。<br>**丙類歸零不代表沒有事情要做**——甲 4 項與乙 7 項不受本批影響，而報告 §17.3 記下的截斷率（10／45）是下一次調整的輸入，**刻意沒有變成一列殘項**：它是一個量得到的比率，不是一件沒人做的事。<br>**08-26 SEC／CONTENT 逐條核對批 ＋2 ⇒ 2 項未結：丙-74、丙-75。** **丙類歸零維持了一天，而讓它歸零的那一句仍然成立**——「能在這台機器上做完的，零項」講的是當時清單上那七項。**這兩項不是那七項的延續，是這一批第一次逐條去讀 `02` §6 的允收才看見的**，兩者的形狀相同而且是本專案的老熟人：**一條允收與一段程式正面衝突，而沒有任何機器會讓它出聲**。丙-74 是「不出現在啟動參數的持久記錄中」對上「Virtual Key 走環境變數」，丙-75 是「重抓並與保存的內容雜湊比對」對上「一次 `HEAD`」。**兩者都不等簽名**；丙-74 要先選 (a) 改允收或 (b) 改注入方式，丙-75 純實作且一次修三個工作項。<br>**同日 −1 ⇒ 1 項未結：丙-75 入列即結案。** 它被寫下來的時候記著「純實作、不等簽名」，而那句話是對的——**但實作它的時候查出一件不在那一列裡的事**：GitHub 的 repo zip 一旦改變產生方式，全部釘選雜湊會同時失配。**那不是不能做的理由，是要多寫一行 warning 的理由**，而少了那一行，某天早上的稽核列會讀起來像一次大規模授權事件。**未結一項：丙-74**，而它等的不是工時是一個選擇（改允收，或改注入方式）。<br>**2026-08-27 −1 ⇒ 0 項未結：丙-74 結案。丙類第二次清空。** 它等的從來不是工時而是一個選擇，而選擇落在 (a)：收斂允收句的字面範圍，不是改注入方式。**要記著這次收斂沒有讓任何實際防護少一層**——本條仍然禁止一切 Run 結束後留著的形態，而「一個沒有被移除的容器」由 X-01～X-04 承擔；收斂做的是**把責任放回真正在守的那個機制上**。(b) 沒有被否決，它是部署期硬化項，出處留在丙-74 那一列。<br>**2026-08-28 產品決策稽核批 ＋5 ⇒ 5 項未結：丙-76～丙-80。** **這一批的來源與丙-74／75 那一批相同，而問的問題不同**：那一批逐條讀 `02` §6 的允收，這一批逐條讀 `01` 的目標、原則、核心旅程、指標與風險表，問「今天的程式還在做這件事嗎」。**五列全部是同一種形狀的第七到第十一個實例**——**一句讀起來像已完成、而沒有任何機器會反駁的話**。<br>**兩件要記在數字旁邊**：①**丙-76 是唯一一個「連缺口都沒有被記過」的**——核心旅程十六步裡它是唯一一步在程式裡連半邊都沒有，而它不在 `03`、不在本檔、不在 `05`，`02` 也沒有給它需求 ID。**一個沒有編號的缺陷等於沒有記**，這是丙-65 那句話的第二個實例。②**丙-77 是這一批自己造出來的**：同日的 `0042` 讓精選層上線，而它只有 SQL 工具沒有 operator 端點——**這是刻意的，形狀與 `access_restriction` 在 SEC-011 之前一模一樣**，記在這裡是因為那一次也是先記著才補的。<br>同批另有三項不是丙而是裁定，已進 [`05`](05-pending-rulings.md)：R-19（使用者匯入的 Skill 的類別由誰給）、R-20（北極星的「通過使用者驗收」映到哪一欄）、R-21（巢狀壓縮要不要拒）。**未結五項：丙-76、77、78、79、80。**<br>**同日 −1 ⇒ 4 項未結：丙-80 入列即結案。** 跨工作區下架落地：`SetSkillTakedown`（無 workspace 述詞）＋`registry.SetTakedown` ＋ `PUT /admin/skills/{id}/takedown`（`RequireOperator`），與欄位、搜尋文件移除、audit event 同一個交易。**它照 `registry.go` 那段註解寫的方式做**——「a second query without the workspace predicate behind that role, **not a change to this one**」——所以 `POST /skills/{id}/takedown` 一個字沒動。<br>**三件寫在這裡而不是留在程式註解裡的事**：①**不冪等**，與旁邊兩條 operator 路由相反：`takedown_at` 是一個時間戳，重打會移動一個日後會被追問的日期，所以第二次回 409，與擁有者路徑同一句話；②**理由進 audit metadata**（`reason`），而擁有者路徑是 identifiers-only——`02:SEC-011` 對 operator 動作要求理由必填，而 operator 自己寫的一句話不是 PDM-006 §6 講的「內容」；③**沒有恢復路徑，而且原本就沒有**：清掉旗標容易，把搜尋文件放回去不容易——`IndexSkill` 只寫 name 與 summary，天真的恢復會無聲丟掉 enrichment、embedding 與 scan。今天的答案是清欄位再跑 `maintenance reindex`。**這一半是既有缺口不是本批造成的**，擁有者路徑同樣沒有，記在這裡而不是半做。<br>**兩個突變都紅**：把 operator 的鎖加回 workspace 述詞（那條路由整個失去理由），與拿掉必填理由檢查。**另有一支既有測試替我擋了一次**：`TestEveryMountedRouteIsInTheAnonymousMatrix` 要求每一條新掛的路由都要在匿名矩陣裡具名，漏了就紅——**這正是 `04` 反覆記載的那種缺陷的相反面：一台會對你大叫的機器。**<br>**`03:INGEST-010` 因此改判勾選**（兩個缺口 ①內容比對 08-26、②跨工作區下架本日，全部關閉）。**`CONTENT-009` 仍不勾**：它還缺 `02:534` 的白名單異動記錄。<br>**未結四項：丙-76、77、78、79。** <!-- open: 76,77,78,79 --> |

#### 現況一覽的歷次記錄(原文保留,不改寫)

以下是 2026-08-18 起歷次批註的原始記錄，逐列為證據、總數以上表為準；刪除線與更正句均為原記錄的一部分。

##### 乙類

乙-9（`02`／PDM-005 一致性，**契約半邊已由 M3 批 1 補完**）、乙-10（anthropic-sa 法務終判＋閘門 D 日；**詢問信草稿已於 2026-08-17 修訂**）、乙-13（G7＋G8，defence 3 的判準鬆緊，**同一次拍板一起裁**）、乙-14（甲類四項是否在封測前到期）、乙-15（PDM-009 封測提案待追認）、**乙-16（新：受測者同意書待法務確認與負責人填值）**、**乙-17（新入列 2026-08-18：`skills.redistribution` 的寫入路徑是否進 MVP 首發——原記為丙-16 (b)，本日查明是範圍決定而非實作工作，改列乙類）**

**2026-08-22 逐列重數：仍為 7，未結案的是同樣這七項。** 本日新增 **乙-18** 但它**生下來就是結案的**——`QA-008` 的路線（引入瀏覽器驅動 vs 人工走查落檔）**此前根本不是編號列**，只是丙-21 與 [m4/audit.md §乙′](mvp/m4/audit.md) 裡的一句「屬乙」；負責人已拍板採**引入瀏覽器驅動**（[ADR-036](../adr/ADR-036-real-browser-verification-tier.md)），依維護規則就地補列並標已裁定，不刪不改寫。**所以本欄不是「減一」，是把一個從來沒被編號的決策補進帳裡並同時結案。**

**2026-08-22（框架評估批）新增乙-21**（`01` §2.1 的「學習者」persona 沒有對應路徑），**⇒ 8 項未結**。

**同批要記本欄自己的帳**：上面那個「7」是 2026-08-18 的逐列重數，**同日稍晚的產品裁定批新增的乙-19（◐ 資料模型已定、同意書條款仍隨乙-16）與乙-20（✔ 已裁定）從來沒被算進本欄**。乙-20 結案不影響數字，**乙-19 仍開著才影響**——所以嚴格說現在是 9 而不是 8。**本批不代為重數整表**（那是一次獨立對帳），只把差額寫明：~~**本欄的數字現在確定小於實際未結項，以下表逐列為準**。~~ **（2026-08-23 更正：那次獨立對帳做了，見本欄開頭。這句自我作廢的警語自此不再成立——一份自陳不可信的總數，等於全 repo 沒有總數，於是別的文件會替它代班，而代班的那份沒有人對帳。）**

##### 丙類

原七項全數結案；丙-8～丙-15 中**丙-9／丙-10／丙-11／丙-12／丙-15 已結案**（丙-9 與丙-10 於 2026-08-18 由 M4 對帳批結案，見各列），仍開著丙-8（`EVAL-013` B 輪）、丙-13（`artifacts` 表回填）、丙-14（preflight 版本選擇器）；**2026-08-18 M4 對帳批新增丙-16～丙-27 共十二項**，**其中丙-27 已於同日由 UI 批結案（−1 ⇒ 16 項未結）**，丙-18（(c)(d) 兩半）、丙-21（①②）、丙-24（個人 Skill 列表）**各關掉一部分但整列仍開**。**十六項裡沒有一項在等決策**（等決策的在乙），全部是「有承接者就做得完」的實作或資料工作，逐項的補法與解鎖對象見 [m4/audit.md §5](mvp/m4/audit.md) 與 [m4/release-checklist.md §1.9](mvp/m4/release-checklist.md)

**2026-08-18 稍晚（後端批）**：**丙-17／丙-20 結案**，丙-18 的 (a)(b) 與丙-19 的 (a)（連同它的整列 (b) 以外）、丙-22 的兩項、丙-24 的兩個 API、丙-25 的①**各自結案**，整列因剩下的前端或人的半邊仍開。**⇒ 14 項未結。** 逐項見各列與 [m4/audit.md §7](mvp/m4/audit.md)

**2026-08-18 最後一批（UI）**：**丙-24 結案**，丙-22 的 Run Artifact 半邊關閉（整列因兩個新缺口仍開），丙-18／丙-21 的前端半邊已如上記。**⇒ 13 項未結。** 見 [m4/audit.md §8](mvp/m4/audit.md)

**2026-08-18 最後一批（丙類收尾）**：**丙-16／丙-22／丙-23／丙-25 四項結案**（丙-16 的 (b) 不是做完而是**改列乙-17**），丙-21 的③與丙-26 的③⑤各關閉但整列仍開，丙-8／丙-13 的記載各訂正一處。**⇒ 6 項未結：丙-8、丙-13、丙-14、丙-19、丙-21、丙-26。**

**這個 6 是逐列重數的，與上面幾次的遞減不接續**——先前的 17→16→14→13 從未扣掉同日結案的丙-9／丙-10，也把已在自己列上寫明「整列關閉」的丙-18 當成還開著。**數字以下表逐列為準，不以本欄的加減為準**

**同批稍晚（丙-14 平行完成）**：**丙-14 結案**。**⇒ 5 項未結：丙-8、丙-13、丙-19、丙-21、丙-26**——**五項沒有一項在等丙類自己能解的東西**：丙-8 要 dev stack ＋ 5 次真實 Run，丙-13 要那 43 筆真實資料，丙-19 只剩 (b) 的 `claude-code` 真機實裝，丙-21 只剩合成像素（天花板是 `QA-008`，屬乙），丙-26 只剩訊號在本 process 之外的①②④。**丙類作為「有承接者就做得完」的那一類，到此已經空了**

**2026-08-22（瀏覽器層批）逐列重數：4 項未結——丙-8、丙-13、丙-19、丙-26。** **丙-21 整列結案**（③ 合成像素的守門已落地並經兩次破壞實測證明會失敗，見該列；`QA-009`／`DESIGN-013` 同批改判勾選）。**這個 4 一樣是逐列數出來的**：上方「⇒ 5 項未結」是丙-14 結案後的帳，本次只再減丙-21 一項。**注意本欄開頭那個「6」是丙-14 結案前的舊帳，未回頭改寫**——**數字以下表逐列為準，不以本欄的加減為準**。

**2026-08-22（框架評估批）新增丙-38**（`EVAL-002` 提案品質沒有基線）。**同時要記一件本欄自己的帳**：上面那個「4 項未結」是**瀏覽器層批當下的數字，只涵蓋丙-8～丙-26 那一段**——同日稍晚的產品裁定批新增的丙-33～丙-35、以及追加的丙-36，從來沒有被算進任何一個本欄數字。**本批不代為重數整表**（那是一次獨立的對帳，不是順手能做的事），只把這件事寫明：~~**本欄的數字現在確定小於實際未結項**，以下表逐列為準這句話比以往任何時候都更該照字面讀。~~ **（2026-08-23 更正：整表已於本欄開頭逐列重數，這句自我作廢的警語不再成立。）**

**2026-08-22（契約與可觀測性批）——這一次真的整表逐列重數了：5 項未結，丙-8、丙-13、丙-19、丙-26、丙-38。** 本批**丙-29 結案**（②③④⑤ 落地，①依既有裁定維持裸值）、**丙-34 結案**、**丙-35 結案**、**丙-36 結案**，並新增同批即結案的**丙-37**（20 條告警規則沒有任何求值器會載入它們）。

**其中兩項不是做完，是查證推翻**：丙-34 的殘半在它被寫下的兩天前就已成立（`3dcc2d4`／`3b83166`，2026-08-20），丙-36 的缺陷在 Playwright 1.62.1 上三個情境重測都是 exit 1（原觀察是本機 `rtk` 的輸出包裝，不屬 repo）。**連同丙-29① 的維持裸值裁定，本批三次同型更正**——**把做完的記成待辦，和把沒做的記成做完一樣貴**：後者讓人以為安全，前者讓下一個人去改一段已經對的程式碼。

**2026-08-22（交付物五維對帳批）新增丙-39～丙-42,四項全開:9 項未結。****同日補批把這四項全部結案 ⇒ 回到 5 項未結:丙-8、丙-13、丙-19、丙-26、丙-38**——與契約與可觀測性批逐列重數出來的那五項是同一組,四項新列全部進出乾淨。 這一批問的是「使用者下載回去的那包東西」的完整性／準確性／一致性／即時性／有效性,落地見 [ADR-044](../adr/ADR-044-agent-skills-specification-conformance.md)（規格釘選）與 ADR-043 的伺服器側實作。**四項新列裡沒有一項在等決策**,丙-40 的第一步是一個決定但那個決定寫在 ADR-044 待決策裡、不是這裡。

**剩下 6 項各自卡在什麼**：丙-8 與丙-38 都要真實 Run（**會花錢**，且丙-38 的第④項還要人讀），丙-13 要那 43 筆真實資料，丙-19 只剩 (b) 的真機實裝，丙-26 只剩訊號在本 process 之外的①②④（**部署期**）。**五項沒有一項是程式面做得完的**——兩項要錢、一項要資料、兩項要人或要機器。

> **2026-08-22 更新的來源**：`QA-008` 的路線拍板與其實作批（[ADR-036](../adr/ADR-036-real-browser-verification-tier.md) ＋ commit `fe7aa13`）。**淨變動：甲 0、乙 0（新增乙-18 但入列即結案）、丙 −1（丙-21 結案）；`03` 的 `QA-009`／`DESIGN-013` 改判勾選，`QA-008` 仍不勾。**
>
> **這一批要記的一件事**：`QA-008` 的路線決策**在本清單裡從來沒有自己的編號列**，只以「屬乙」的字樣散在丙-21、`03` 的行內與 [m4/audit.md §乙′](mvp/m4/audit.md) 三處。**一個真的要人拍板、而且擋著兩個工作項勾選的東西，不該只活在別列的旁註裡**——那正是它從 2026-08-18 一路等到今天的原因之一。已補為乙-18 並同批標結案。
>
> **另一件**：`QA-008` 拍板後**沒有一次解決所有事**，而這是好的。它的字面判準有兩半（主要瀏覽器／目標作業系統），本批只做了前一半，**後一半誠實留著不勾**；而 ADR-036 把「瀏覽器層刻意很窄」「引擎是 Playwright 自編版不是桌面 Chrome／Safari」「沒有 OS 矩陣、沒有真機」三件事記成**已知取捨**，不是靠不提來讓帳面好看。

> **2026-08-18 更新的來源**：M4 的程式批與對帳批（[m4/audit.md](mvp/m4/audit.md)：49 個工作項，**11 勾、38 誠實不勾**，零項退回）。**淨變動：乙 ＋1、丙 ＋12 −2。** 十二項新丙類不是新的洞被鑽出來，是**既有的洞第一次被逐項量到並具名**——M4 的實作把「打包、下載、配額、漏斗、停派送」五組功能落到有測試可查的程度，而逐項對帳的價值正是指出**哪幾格的測試斷言的是意圖而不是位元組**（丙-17）、**哪幾句話的宣稱大於實作**（丙-18）、**哪幾個能力做完了但沒有使用者碰得到的平面**（丙-24、丙-27）。
>
> **同日稍晚的後端批**（`50624e2`／`8a17652`／`04bb5e6`）補的是 [m4/release-checklist.md §1.9](mvp/m4/release-checklist.md) 十二項裡屬 Go／後端／工具的那些，`03` 的 `PACK-003`／`QA-007`／`QA-001` 三項改判勾選。**它同時推翻了對帳的一個判斷**：丙-19 記的那支「缺席的」具名測試**依它的字面寫不出來**（`content_hash` 算的是上傳的壓縮檔位元組，重新壓縮必然不同），而 `standard.json` 正在叫使用者做那個檢查——**缺席與不可能是兩件事**，處置也不同。這是尺-2 的第三個形態：不是介面對使用者宣稱平台不做的事，是平台叫使用者去做一件平台自己做不到的事。

> **2026-08-18 最後一批（丙類收尾）的來源**：把前一批留在 `apps/web` 與 Go 側的丙類洞逐項補完的實作批。**這一批的價值不在補了幾項，在推翻了三個判定**——丙-21③（對比守門「綁在 `QA-008` 的路線決策上」）、丙-25②（保存政策頁疑似卡法務）、丙-26（自動觸發「要先決定接哪幾條判準」）**三項都被記成在等一個決策，而三項都不是**。三次的形狀一樣：**一個真的要人拍板的東西（`QA-008` 的瀏覽器路線、同意書的法務確認、五條 P1 判準裡屬部署期的那三條）旁邊，有一段不需要它就做得完的工作，被一起掛上去等**。逐項的更正寫在各列。**這一批沒有新增任何 ADR**——全部是既有決策的兌現。
>
> **同批另一件要記的**：丙-16 (b) 查明**分類本身錯了**（丙類的定義是「有承接者就做得完」，而它缺的是一個範圍決定），已改列乙-17。**這是本清單第一次把一項從丙移到乙**，維護規則沒有寫這種情況，處置比照「就地標、不刪除」：丙-16 列上寫明改列與理由，乙-17 列上寫明出處。

> **2026-08-17 第二次更新的來源**：M4 的文件批（三份新 ADR ＋ `02`／`03` 同步 ＋ PDM-009 提案 ＋ 詢問信修訂）。**三份新 ADR 關掉的是別的洞，不是本清單的洞**——[ADR-027](../adr/ADR-027-download-artifact-shape-reproducibility-and-integrity.md)（回答 ADR-012 待決策 3，並把可散布性定為 `skills.redistribution` 三態）、[ADR-028](../adr/ADR-028-beta-admission-and-quota-enforcement-points.md)（回答 ADR-020 待決策 2，並定下配額的強制點）、[ADR-029](../adr/ADR-029-product-analytics-events-and-audit-trace-boundaries.md)（給鐵律 11 的「分析事件」一個定義）。**本清單因此淨增兩項乙、一項丙**：ADR 回答的是「怎麼做」，剩下的是「誰拍板」與「誰去做」。**同批的 `02`／`03` 同步**（`PACK-001` 補「可散布」判準、新增 `O11Y-004`、`WS-004`／`CORE-007`／`CORE-008` 的 M4 排期註記、`QA-002` 的重用範圍、`03` §3 的 DESIGN 收斂）**沒有勾選任何工作項**——它們補的是可判定性，不是完成度。

## 甲、部署期驗收(~~**依 ADR-015:未通過不得開放外部使用者提交 Skill 執行**~~ **2026-08-23 起與封測並行,見 [ADR-050](../adr/ADR-050-beta-runs-in-parallel-with-the-sandbox-acceptance.md)**)

> **裁定的是「什麼時候要達成」,不是「要不要達成」。** 四項全部維持未結,`02:SEC-009` 的 45 項全 pass／0 unknown 一個字沒改;改的只是它不再擋住封測 D 日。**ADR-050 明示接受的是**:在那之前,不受信任程式碼會在逃逸邊界尚未被驗證的節點上執行。

~~本機 Windows 無法跑 gVisor(runsc 需 Linux),巢狀虛擬化亦為 ADR-019 待決策 3。以下四項不是被延後,是**在這台機器上結構性不可能完成**。~~

**2026-08-23 更正:上面那句的前提是錯的,而且它擋住的比它自己以為的多。** gVisor 在這台 Windows 開發機上跑得起來——privileged Docker 容器,**不需要 WSL 發行版**,沙箱內核心是 `4.19.0-gvisor`(宿主 `6.6.87.2-microsoft-standard-WSL2`)。「需巢狀虛擬化」只對 `--platform=kvm` 成立,`systrap` 用 seccomp ＋ SIGSYS(ADR-022 §0 早就寫了)。可重跑的探針:[`tools/sec009/gvisor-smoke.sh`](../../tools/sec009/README.md)。

**四項的狀態一格都沒改**,但擋住它們的理由要說對:**不是「這台機器跑不了 gVisor」,是「這台機器不是受測物」**。ADR-022 把 Suite 2 的受測物定義成**即將加入池的那台節點**,所以在巢狀環境(Windows → WSL2 VM → 一個被刻意給了全部 capability 的容器 → 沙箱)裡量到的是別的邊界,核心也不是生產那個。**差別在於這句話能做什麼**:舊的前提說「寫了也跑不了」,新的說「跑得了,但那不是驗收」——前者讓 10 個測項連寫都不必寫,後者讓它們可以先寫成腳本、先跑過、先把環境的坑挖出來,部署批到時候只需要換一台受測物。

| # | 項目 | 內容 | 解除條件 |
| --- | --- | --- | --- |
| 甲-1 | **SEC-009** | 逃逸測試、資源耗盡測試、Runtime 相容性測試、網路外洩測試(DNS tunneling／內網掃描／Metadata Service)、憑證範圍測試、清理失敗測試、設定與供應鏈稽核——覆蓋基線 46 項全部（**2026-08-26 更正**：基線在 2026-08-25 隨 N-08 增為 46，見 ADR-022 §2 補記）。`02:SEC-009` 明文「M2 的 SelfHostedProvider 驗收必須全數通過」,**M2 結束時未達成**。**可執行的測項清單、通過判準與證據要求已由 [ADR-022](../adr/ADR-022-sandbox-deployment-topology-and-security-thresholds.md) 第三部分給出**(**10 個測項**、覆蓋核對、全數 pass 且 0 unknown 才放行、證據存 **`plans/mvp/m4/sec-009-acceptance/`**——放 M4 不放 M2,因為往已結案的里程碑目錄持續寫入新證據會讓那份對帳失去「某一時點的帳」的意義)<br>**2026-08-23：擋住它的不是「沒有 Linux 機器」——那件事查錯了。** gVisor 在這台 Windows 開發機上跑得起來，而且是真的 gVisor（沙箱內核心 `4.19.0-gvisor`，宿主是 `6.6.87.2-microsoft-standard-WSL2`），**不需要 WSL 發行版**，走 privileged Docker 容器即可。可重跑的探針：`tools/sec009/gvisor-smoke.sh`。<br>**但這不改變本項的狀態，一格都不改。** 那是巢狀環境（Windows → WSL2 VM → privileged 容器 → 沙箱），在裡面跑逃逸測試量的是「沙箱」與「一個被刻意給了全部 capability 的容器」之間的邊界，核心也不是生產那個。**它讓 10 個測項變成寫得出來、跑得動的腳本，這是此前做不到的**；驗收仍要獨立節點，而 `infra/nodes/gvisor-baseline.txt` 仍是 `unset`。<br>**一個要進部署 runbook 的發現**：容器裡第一次跑 runsc 會敗在 cgroup v2 的 `subtree_control` 忙碌；顯而易見的修法 `--ignore-cgroups` 會過，**而它關掉的正是資源耗盡測項要量的東西**——綠燈照樣出現，測項變成空的。腳本走的是把 root cgroup 清空再委派控制器那條，保住 `+cpu +memory +pids`。<br>**2026-08-23 稍晚:T1 的通用嘗試已寫成腳本並跑過**——[`tools/sec009/t1-escape-attempts.sh`](../../tools/sec009/README.md),八項嘗試＋兩項節點側觀察,**沙箱內全部 REFUSED**。**T1 點名的四個 CVE PoC 刻意不放**:在巢狀開發機容器裡跑別人的 PoC,對一台它從沒碰過的節點證明不了任何事,那四項屬部署批。<br>**它有負對照,而那不是裝飾**:`SEC009_NO_SANDBOX=1` 把期望反過來,無沙箱時至少要有一項成功。實跑三項逃得出去(`core_pattern` 寫得進、`/dev/mem` 讀得到、`mount` 掛上來的 proc **報宿主核心**)。**一個不管有沒有沙箱都會綠的探針組,量的是零。**<br>**第一次跑抓到三件事,全部是腳本自己的問題**:①`mount(2)` 成功被判成逃逸——**gVisor 有實作 mount**,給的是它自己的 procfs,探針改成把 proc 讀回來看是誰的核心;②找到的 unix socket 是 `runsc do` 自己的控制通道,**生產不走 `do`**,是探針量到自己;③**最嚴重的一個**:`bash -s` 少了 `docker run -i`,容器讀到空 stdin、跑了一個空腳本、**exit 0**——**一個從沒執行過的測試給出綠燈**。**這三件都會在部署日當天發生**,差別是那天有時間壓力、機器沒人除錯過,而且第三件會讓人以為驗收過了。<br>**T3(資源耗盡)刻意不寫成腳本**:限制是 `sandboxd` 經 Docker runtime 套的,不是 `runsc do` 套的;在沙箱裡自己 `ulimit` 再驗證它生效,測到的是 `ulimit`。**那一項要一台跑著 sandboxd 的節點。**<br>**2026-08-23 再一批:T2 與 T8 的映像半也可執行了,而其中一半不是「程序跑得動」是真的證據。**<br>**T8 的映像半([`t8-image-audit.py`](../../tools/sec009/README.md))不受巢狀環境影響**——它查的是 GHCR 上公開的東西,不需要節點、不需要 Linux、不需要憑證,**在那台節點上跑會得到同一個答案**。所以 **I-01／I-02／I-03／I-04／I-06 五項第一次有了機器可查的證據**:`2026.08-3` @ `sha256:6a99b373…`,SBOM(SPDX 2.3)與掃描 attestation 都掛在已發佈的 digest 上,掃描 2026-08-21(30 天效期內),可修的 Critical／High ＝ 0(共 320 筆 finding)。**它同時回答了 ADR-022 三個前置條件的第①項:成立**——而在此之前那一項的狀態是**沒人查過**,缺它即 T8 判 `unknown`、45 項全部 fail。②③(`gvisor-baseline.txt`、`pinned_ip`)仍是 `unset`,**兩者都要等第一台節點**。<br>**T2([`t2-syscall-fuzz.sh`](../../tools/sec009/README.md))與 T1 同屬巢狀,只證明程序跑得動**:4 × 60s 實跑 211,222 次 syscall、26 個相異 errno、Sentry 撐完、uid／gid 未變、T1 三個不變量壓力後仍成立、節點 `dmesg` 無新增。ADR-022 的量是 4 × 30 分鐘,腳本自己會說它跑的是哪一種。<br>**寫 T2 抓到三個「綠燈其實什麼都沒量」的形態**(`apt-get` 從 stdin 把腳本剩下的部分吃掉、`set -e` 讓第一個 FAIL 之後的檢查全部不執行、不加引號的 heredoc 讓**註解裡的反引號在宿主上執行**),外加**修掉 T1 一個先於本批就存在的自相矛盾**:`plant a file on the node` 評的是沙箱裡面的 `touch`,每次都判 ESCAPED,而同一次執行的節點側觀察說節點乾淨——**錯的那一半正是看不到答案的那一半**。<br>**本項狀態仍未結案**:十個測項裡 T3／T4／T5／T6／T7／T9／T10 與 T8 的節點半都要節點或 sandboxd,45 項裡另外 40 項沒有證據。**但「缺的是什麼」現在具體了:一台機器,不是十支還沒寫的腳本。**<br>**2026-08-26：T3 那句「要一台跑著 sandboxd 的節點」是錯的，而且錯得有代價——它讓一個跑得動的測項被歸檔了三天。** T3 要量的確實是 `sandboxd` 經 Docker runtime 套上去的限制，但那組限制的來源是 `dockerdrv` 的 `HostConfig`，而 `apps/sandbox/internal/dockerdrv` 的 live 測試**本來就跑在真的 Docker runtime 上**，而且 `SKILLHUB_SANDBOX_TEST_RUNTIME` 這個開關早就存在。CI 的 `sandbox` job 會 `sudo runsc install` 之後把整包測試以 `runsc` 再跑一次——**那就是一台在跑 sandboxd 那份 HostConfig 的 Linux 機器**。缺的從來不是節點，是那個套件裡沒有 C-11／C-12／C-14 的**行為**測試：只有宣告面（讀回 driver 要求的欄位），而在 runsc 上「要求」與「強制」會分家——`TestPidsLimitStopsAForkBomb` 的註解記的正是欄位設了、上限沒觸及 guest task 的那一次。三支已補上並隨那條 leg 在真 runsc 上通過，每一支都以「把強制拿掉」突變驗過會紅。<br>**同日的巢狀執行與證據落檔**：[`mvp/m4/sec-009-acceptance/2026-08-26-nested-dev-container/`](mvp/m4/sec-009-acceptance/2026-08-26-nested-dev-container/)。逐列判定是 **17 項有機器可查的證據、4 項部分、25 項沒有**。**那一欄寫的是「有沒有證據」，不是 pass／fail**——ADR-022 §3 的判準是全數 pass 且 0 unknown，把「有證據」寫成「通過」正是那條規矩存在的理由。<br>**兩支腳本這次被自己的執行抓到，兩件都是「綠燈其實什麼都沒量」**：①T1 的 `no file planted on the node` 在這台機器上**從寫出來就是假綠**——`MARKER` 經 `docker run -e` 被 Git Bash 改寫成 `C:/Users/.../Temp/...`，沙箱裡的 `touch` 因此失敗；評分段還繼承著 `set -e`，那次失敗直接殺掉腳本，**節點側兩列觀察一行都沒跑**。少了其中任一個，那一列都會印 PASS。負對照同樣是死的：無沙箱時同一個 `touch` 也失敗，**整個套件裡沒有任何輸入能讓它變紅**。已修，重跑後負對照如期紅。②T2 第一次 4 × 1800s **在 48 分鐘處卡死**（30 分鐘的預算，且四個 worker 一個紀錄都沒留下）：切片的時間上界靠 `SIGALRM`，而 fuzzer 打得到 `rt_sigprocmask`／`rt_sigaction`，一次隨機呼叫就關掉了量測自己的工具，監督者的 `os.waitpid(pid, 0)` 於是永遠等下去。**更要緊的是 elapsed 檢查原本只找提早返回**，所以那個 run 若最終返回，每一行都會印 PASS。兩者都已修（監督者持有時間預算並以 `SIGKILL` 收尾，fuzz 表面一個 syscall 都不用讓；遲到現在也是 finding），並附 `--self-check` 製造那個 hang 而不是等它出現。<br>**當時用來判定「卡住」的兩個讀數是量錯的東西**：「sentry 執行緒全在 S」「`runsc` 十秒 0 個 CPU tick」在一次**健康**的執行裡同樣成立（`runsc` 父程序只是監督者，工作在 sentry 執行緒裡；同時 `docker stats` 是 45% CPU）。真正的證據是修好之後那次執行自己給的：**每個 worker 有 12～16 個切片超出預算被 `SIGKILL` 收掉**——那個機制真的會反覆發生，而舊的無界 `waitpid` 下第一次就會讓該 worker 停住。<br>**T2 最終在 ADR-022 的規模跑完全程，而結果要分兩層說**：**ADR-022 給 T2 的三條判準全部 PASS**（Sentry 撐完 1801s、宿主核心無 oops、uid/gid 未變且 T1 三個不變量在壓力後重驗仍成立），**腳本自己更嚴的負對照 FAIL**：6,115,799 次 syscall／31 個相異 errno 遠高於下限，但 **4 個 worker 只有 3 個留下最終紀錄**（worker 2 最後出現在 1,517,014 calls／1,680 slices）。**因此 T2 不記為通過**——ADR-022 §3 把 `unknown` 當 fail，而「第四個 worker 怎麼了」正是 unknown。<br>**兩個解釋都還站著，而這次同時量到了第二個**：①記憶體壓力（沙箱側峰值 10,278 MiB）下有 guest task 被收掉；②**輸出在沙箱收尾時會掉**——每個 worker 外面包了 `echo "EXIT $i $?"`，而**四行只印出兩行**，其中 **worker 4 有完整最終紀錄卻沒有 EXIT 行**。曾經看起來像決定性種子問題（種子是 `100000*worker + slices`），**不是**：worker 2 單獨跑到 318 slices 乾淨結束。<br>**兩個檢查本身的弱點，記著不改，等裁定**：①`no privileges gained (uid/gid)` 在一次 300s 執行裡紅過，而 `runsc do` 以 **uid 0** 跑工作負載，所以它唯一觀測得到的變化是**權限下降**（最可能是隨機 `unshare(CLONE_NEWUSER)`）不是取得——**這條檢查在這個環境裡的框架是錯的**，但在一次長工作階段的結尾去改一條安全檢查的判準，正是本專案反覆立規則在防的事；②檔案式 checkpoint 在 `runsc do` 的 overlay 下不可能生效，第一版對**包含三個正常結束的 worker 在內**全部報「no checkpoint」——**一個讀起來完全像 finding 的假象**，已改走 stdout。<br>**剩下的 25 項分兩類，第二類不是排程問題**：一類**要真實節點**（T8 節點半、T6、T7、T9）——受測物就是那台機器；另一類**被測的控制還沒有被實作**：N-01～N-08 的生產強制層（甲-3／SBX-007）與 P-02 要求的**常駐**探針（丙-26 已查證不存在）。**2026-08-26 訂正這一句：那兩個控制當天下午都被實作了**（`22fcffb` 渲染層＋A1-e 准入、`5628e52` 常駐探針，見甲-3），**所以這 25 項的第二類現在併進第一類：全部在等那台節點**——渲出的 ruleset 沒有任何東西執行 `nft -f`，探針的每一次讀數到今天為止都來自 fake 或 dev Docker。**「還沒被實作」與「實作了但沒在受測物上跑過」不是同一件事，而兩者對這 25 項的判定同樣是沒有證據。****T4 沒跑**：它的判準是與 M2 runc 基準逐筆一致，而在巢狀 DinD 裡會同時換掉 runtime 與 daemon 環境；ADR-022 §4 本來就把 T4 指派給 CI ＋ 人工確認，**它缺的是模型憑證，不是一台 Linux**。<br>**2026-08-27：T5 也有了可執行的形式，而它是第一個把 `rendered/` 真的載進核心的東西**（[`tools/sec009/t5-network-egress.sh`](../../tools/sec009/t5-network-egress.sh)，證據 [2026-08-27-nested-dev-container-t5](mvp/m4/sec-009-acceptance/2026-08-27-nested-dev-container-t5/)）。T5-1～T5-9 十二項全部 refused、**每一項都由本來該擋它的那條規則的計數器判定**、ADR-022 要求的兩個反向驗證（釘選 `IP:port` 可達、控制平面來源進得了 `:9000`）都通過。<br>**本列一格不改，理由同 T1**：受測物是 Windows → WSL2 VM → privileged 容器裡的三個 netns，目的地是假的、閘道是一個 python listener、`pinned_ip` 是實驗室自己填的。**N-01～N-08 的判定仍然全部是 `unknown`。**<br>**但它把三件事從「沒有人知道」變成「量過」**：①渲出的 ruleset **nft 收得下**（此前只證明過它與 allow-list 一致，而載入失敗的 ruleset 留給節點的是一條規則都沒有）；②**T5-4 的東西向阻擋依賴 `net.bridge.bridge-nf-call-iptables=1`**，而 repo 裡沒有任何地方寫著；③**節點 loopback 那一項在結構上留不下 nftables 紀錄**（封包在路由層就沒了，`prerouting` 的 raw 計數實測為 0），所以 T5 的節點版判定表要為它寫具名例外，否則它會永遠停在 `unknown`。<br>**負對照當場抓到三個假綠**：DNS、東西向、入站三個目標沒有人在聽，而「被 DROP」與「連接埠關著」對 `nc -z` 是同一件事——三者在正向那一輪全部是綠的。**只跑正向的那個版本，會一直是綠的，而它們一格都沒在量。** | Linux 節點。**⚠️ ADR-022 修正一個前提:gVisor 的 `systrap` 平台不需巢狀虛擬化**(只有 `--platform=kvm` 才需要),因此多數測項在一般 Linux runner 即可跑,ADR-019 待決策 3 的範圍縮小;**此點須由部署批的第一台節點實測確認** |
| 甲-2 | **SBX-010** | 同上的工作項側。現有的真實容器驗證(非 root、唯讀 rootfs、無主機掛載、pids 上限、逾時強停、清理冪等)**不等於**逃逸測試通過<br>**2026-08-26**：那份「現有的真實容器驗證」現在多了三項——記憶體上限、暫存配額、開檔上限的**行為**驗證，且整套在 CI 的 gVisor leg 上以 `runsc` 跑過（詳見甲-1）。**它仍然不等於逃逸測試通過**，這一格一個字都不改：多的是 T3，不是 T1 的 CVE PoC，也不是任何要節點的測項。 | 同甲-1 |
| 甲-3 | **SBX-005／007 的生產網路面** | SBX-005 缺 P-02「Sandbox → 核心資料庫連線嘗試被實際阻擋」的**常駐探針**;SBX-007 缺生產級出口強制、允許清單、DNS 固定解析與目的地記錄(N-01～N-07)。dev 已有的是網路面隔離(`internal: true` 網路上只有閘道),**不是強制層**。**⚠️ ADR-022 另指出 dev 的一個既有缺口**:同一張 `skillhub_egress` 上的 sandbox 目前可互相連通,是**不需逃逸**的跨 Run 橫向路徑;生產形態必須關掉(每 Run netns ＋ `--icc=false`),並列為 SEC-009 測項 T5-4。**另一項生產前提**:ADR-022 Q2 強制條件 6——沙箱層允許清單的目的地**不得是控制平面／資料節點位址**,LiteLLM 須移到沙箱面專屬節點(原 Egress Proxy 預算行承接,總額不變;現行 compose 的 `127.0.0.1:4000` 是 dev 形態,生產不可複製),違反時由測項 T5-7 抓<br>**2026-08-25 補記——沙箱端也沒有讀它被交付的那張清單**：`apps/sandbox/internal/dockerdrv/docker.go` 的 `networkFor` 只看 `allow.Purpose == "model_gateway"`，`Config.accept` 只看 egress 模式與清單長度，**`Egress.Allow` 的 `url` 全 repo 沒有任何一行程式讀過**——它被顯示在權限摘要、被記進 Run 紀錄，然後被丟掉。今天唯一的強制是「那張 Docker 網路上只有閘道」這個結構事實；ADR-022 Q3 決定把同一個結構搬到生產的 nftables，**所以這不是決策違反**，但代價要寫明：**在生產 nftables 落地之前，使用者看到的那幾行目的地在沙箱這一側沒有任何東西擔保，它們是敘述不是控制。**<br>**2026-08-26 兩批落地，本列仍開。**<br>**①出口強制層（`22fcffb`）——上一格記的那個缺陷關掉了。** `tools/egress/render.py` 從 `infra/egress/allowlist.yaml` **一次**渲出三份產物並提交在 `infra/egress/rendered/`：nftables ruleset、dnsmasq 設定、sandboxd 讀的准入清單。`Config.accept()` 現在比對 **host＋port**，不符即 `capability_mismatch` 422 拒絕（ADR-022 A1-e），**不比對 purpose**——purpose 是呼叫端自己選的字串，比對它等於「自稱 `model_gateway` 就能連到任何主機」，而那正是舊 `networkFor` 的行為。**規則順序在這裡是安全屬性不是排版**：東西向（T5-4）、節點 loopback／sandboxd port（T5-5）、metadata（T5-3）三段 drop 必須排在允許清單的 accept 之前（否則一個設錯的 `pinned_ip` 會失效成開放），accept 必須排在 RFC1918 drop 之前（閘道本身是內網位址）；`--self-check` 的 17 條斷言逐條驗這些順序、`ip6` 零 accept 與解析器不答 AAAA（N-08），以及 `pinned_ip: unset` 渲出的是**不存在**而不是**存在但連不到**（後者會讓 Run 被接受然後逾時，正是 A1-e 要換掉的行為）。**今天渲出 0 個可達目的地**（`pinned_ip` 仍 `unset`）——這是 fail-closed 的正確狀態，不是缺陷。同批修掉 CI 一個對自己產物亮紅燈的守門（`d5779a6`）：閘門改為只認來源檔，產物改由 `render.py --check` 對來源比對。<br>**②P-02 常駐探針（`5628e52`）**：詳見 `03:SBX-005` 與 `SEC-012`。**丙-26 對這一條寫的「第二層是結構事實：節點沒有往控制平面推的通道」，那個障礙是真的，結論不是**——平台本來就在輪詢 `GET /capability`，所以加的是 `ProviderCapability.security.p02_probe`（節點自己的最後一次讀數），**契約仍然單向、推的仍然是控制平面**，沒有把任何手把交給不受信任工作負載。<br>**仍未完成，逐條**：①**在節點上實際套用**渲出的 ruleset——渲染器產出檔案，repo 裡沒有任何東西執行 `nft -f`；②**N-06 目的地記錄的收集端**——規則帶 `log prefix` 與 drop 計數，沒有人把它們送到保存 90 天的地方；③**每 Run netns 與東西向阻擋**——上一格那個 `skillhub_egress` 橫向路徑**一格都沒動**；④Q2 強制條件 6（LiteLLM 移到沙箱面專屬節點）；⑤`pinned_ip` 填實際位址。**①③⑤ 要那台節點，②④ 是部署期設定**；**兩批落地都沒有在任何一台生產節點上跑過**，而 N-01～N-08 與 P-02 量的正是那台機器。<br>**2026-08-27：①的前半有了讀數——但那個讀數不是節點的。** ruleset 第一次被一個核心載入並受測（甲-1 那一格記了全部細節），而**這一列的五項殘缺一項都沒有關掉**：①仍缺**節點上**的 `nft -f`（實驗室裡的是容器的 netns）；②N-06 的收集端仍然沒有人做——**規則現在證明得出它會計數，而計數仍然沒有被送去任何地方保存 90 天**；③每 Run netns 仍然是實驗室自己建的，`dockerdrv` 的 `HostConfig` 至今不指定 namespace，**`skillhub_egress` 那條橫向路徑一格都沒動**；④⑤ 未動。<br>**實驗室反而讓③變得更明確**：東西向擋得住的前提是那個 sysctl 為 `1`，而產品程式今天連 netns 都不建——**兩件事疊起來，dev 的橫向路徑不是「規則還沒套用」，是「沒有東西會套用它」**。<br><br>**2026-08-28 一個 dev 面的連帶發現，記在這裡因為它會絆倒下一個要跑真實 Run 的人**：`SKILLHUB_SANDBOX_NETWORK` 一旦設了非 `none` 的值，`SKILLHUB_SANDBOX_EGRESS_ALLOW` 就必填，而 **committed 的 `egress-allow.json` 是空的**（`pinned_ip` 仍是 `unset`，那是生產的正確預設）⇒ sandboxd 宣告 `none` ⇒ 沙箱拿到沒有網路的容器 ⇒ Agent SDK 對閘道空等到逾時。`m2/README` 那份「跑一個 Skill 的最短路徑」寫在這個前置存在之前，**照它逐字做今天會得到一個沒有網路的 Run**；已跑通的版本寫進 [automation.md](../development/automation.md)（dev 另渲一份允許清單，不動 committed 的那一份）。<br>**同日的實跑結果**：整條端到端**今天仍然是通的**——套件進物件儲存 → preflight → 確認 → 派送 → sandboxd → 容器跑 Agent SDK → 每 Run 短效 Virtual Key 經閘道呼叫模型 → trace 回推 → artifact → 金鑰撤銷，閘道實付 **$0.016671**。**它不是 SEC-009 的任何一項**：runtime 是 `runc` 不是 `runsc`，而 `skillhub_egress` 是 Docker 網路隔離不是 nftables 強制層。<br>**它自 2026-08-27 起在 CI 上跑**（`egress-allowlist.yml`，正向與負對照各一次），所以下一次改 allow-list 或改 renderer 的人，會在規則到達任何節點之前先看到它被執行。<br>**而接上 CI 的第一次執行就紅了，紅的那一列是東西向。** `ubuntu-latest`（核心 `6.17.0-1022-azure`）**預設不載 `br_netfilter`**，於是 `T5-4 east-west to another run FAIL the attempt succeeded`——**一個沙箱連到了同一台節點上的另一個沙箱**，正是 ADR-022 Q2 條件 2 要關掉的那條不需逃逸的跨 Run 路徑。**Q2 條件 2 指定的另一個手段 `--icc=false` 依賴同一個模組**，所以兩個手段會一起、安靜地失效。<br>**這件事此前看不見，是因為實驗室在幫它遮掩**：腳本第一版在 setup 就把那個 sysctl 寫成 `1` 而且沒有印出來，而本機核心本來就載著模組——**setup 步驟在修補它等一下要去找的那個洞**。換一台機器是唯一會讓這種缺陷現形的事。<br>**處置**：①腳本改為先 `modprobe` 並把「這是實驗室自己載的」印在結果裡；②輸出新增一段 `NODE REQUIREMENT (T5-4)`；③同一句話由 `render.py` 寫進 `nftables.conf` 規則旁邊（在節點上讀那個檔的人不會來讀報告）；④**閘門 A 的節點准入探針要把它列為一項檢查**——`infra/egress/rendered/` 裡沒有任何東西能自己保證這件事。**T5-4 的綠從今天起只代表「節點設成這樣時規則有效」，不代表節點安全。** | 生產網路。**Q3 已於 [ADR-022](../adr/ADR-022-sandbox-deployment-topology-and-security-thresholds.md) 定案**(nftables default-deny ＋固定 DNS,不部署 Squid／Envoy;允許清單存 `infra/egress/allowlist.yaml`,含變更、複審與記錄流程),剩下的是實作不是決策 |
| 甲-4 | **SBX-002 的門檻定值** ~~待定~~ **已定值** | 流水線已接上(digest 斷言→build→syft SBOM→grype→門檻)。**I-06 與 I-04 的提案值已由 [ADR-022](../adr/ADR-022-sandbox-deployment-topology-and-security-thresholds.md) 第二部分採納定案**(可修的 Critical／High 阻擋且**無豁免路徑**、不可修者具名豁免、複審日 ＝ `first_exempted_at`＋90 天;掃描有效期 **30 天**、到期前 7 天告警),`infra/images/README.md` 與 `runtime-image.yml` 的「暫定／PROVISIONAL」字樣**已改為定案(`606ad87`)**。~~**SBX-002 仍不勾的唯一原因**:I-03 的 SBOM 與 I-04 的 `scanned_at` 現落在 90 天 CI artifact 與人工維護的日期。~~ **SBX-011 已於 2026-08-16 完成**:發佈流水線接上 GHCR,SBOM 與掃描結果(含機器可讀 `scanned_at` 與 `fixable_critical_high`)以 attestation 隨 digest 保存,另有每週 `rescan` job 重掃**已發佈的 digest**。**流水線側四項檢查已全部可自動化判定**;**SBX-002 剩下的唯一未勾原因改為**:閘門 A 的節點准入探針要在真實節點上查得到這兩份 attestation,屬部署批。<br>**2026-08-27：那支探針從一個名詞變成一支程式**——[`tools/sec009/t8-node-probe.py`](../../tools/sec009/t8-node-probe.py)，涵蓋節點半的 C-01／P-01／P-03～P-05，輸出 ADR-022 §5 要求的 `probe.json`，`unknown` 視同 fail。證據五份（空容器／合成 facts ＋假 runsc shim／`--self-check`／P-05 負對照／四次突變）在 [T8-node](mvp/m4/sec-009-acceptance/2026-08-27-nested-dev-container-t5/T8-node/)。<br>**本列一格不改**：探針要查的那兩份 attestation 仍然只能在**真實節點上**查，而它今天在這台開發機上得到的判定是誠實的紅——`P-04` fail（**基線檔仍是 `unset`，沒有東西可以比對**）、`P-01b` fail（**這台機器上跑著 Postgres，本來就是混排工作負載的機器**）、`C-01a` fail（dockerd 沒有註冊 runsc——**它問的是 dockerd 不是 `$PATH`，所以那支假 shim 騙不過它**）。<br>**它寫出來才看得到的三件事全部進了 [`05` R-17](05-pending-rulings.md)**：①`C-01` 的執行期半邊閘門 A 看不到，而 `unknown` ＝ fail ⇒ **一台完全正確的節點也會紅，而永遠紅的閘門會被關掉**；②`node.json` 的欄位與 `role` 的字面值是實作者挑的，**沒有任何人裁定過**，而 ADR-022 §Q1 引用的 `infra/compose/sandbox-node.yml` **根本不存在**；③「時戳是不是節點自報的」目前是 2 秒容差的啟發式，正解是 cloud-init 另外寫一個 build-phase marker。<br>**同日另一項**:映像升 `2026.08-2`(加 python3,見乙-6),重掃後可修的 Critical／High 仍為 0,新增的不可修項已具名豁免 | ~~SBX-011 完成~~ → 部署批的閘門 A 探針 |

## 乙、待負責人決策(**寫程式之前先要有人拍板**)

> **要簽的東西集中在 [`05-pending-rulings.md`](05-pending-rulings.md)。** 本節仍是每一項的完整歷史與證據;`05` 是入口,逐項寫「要決定什麼、已查到的事實、建議、**不決定的代價**」。**裁定回寫到本節,不寫在 `05`**——`05` 是入口不是紀錄,兩份都記會分岔。

| # | 項目 | 待決內容 | 阻擋什麼 |
| --- | --- | --- | --- |
| ~~**乙-1**~~ **已解決(2026-08-16)** | ~~**SEC-002 六項門檻值(威脅模型 Q18)**~~ | → **[ADR-022](../adr/ADR-022-sandbox-deployment-topology-and-security-thresholds.md)**:六項門檻全部定值(P-03 節點 7 天滾動重建;P-04 gVisor 基準 ＝ 上游 N/N−1 且 ≤ 90 天、逃逸類 CVE 24 h 換版或停用、High 7 天;I-04 掃描有效期 30 天;I-06 可修的 Critical／High 阻擋且無豁免路徑、不可修者具名豁免 90 天複審;X-02 每 5 分鐘;X-03 連續 2 輪告警、X-04 單節點 ≥50% slot drain／全池 ≥25%(下限 2 筆)暫停),Q1～Q3 亦已定案(compose-per-VM／執行平面單租戶且同節點多 Run／nftables default-deny ＋固定 DNS,允許清單存 `infra/egress/allowlist.yaml`)。<br>**但 SEC-002 仍不勾**——見甲-1／甲-2(45 項未經驗證)與乙-8(閘門 B 兩項阻擋未落地);**SBX-002 仍不勾**——I-04 的 `scanned_at` 在 container registry(ADR-019 待決策 1)定案前無法隨 image 保存,見甲-4 | ~~SEC-002 勾選、SBX-002 勾選、甲-3／甲-4~~ 剩餘阻擋見右列說明 |
| ~~**乙-2**~~ **已結案(內文早已記載,2026-08-23 補標)** | ~~**PDM-005 token 硬上限:二選一**~~ | `300_000`／`60_000` 被寫進 `policy_snapshot`、**顯示在執行前權限摘要上要求使用者確認**,而平台與沙箱**都不強制**(`TokenBudget` 零消費端,無累加器)。實際煞車只有 `max_budget`／`tpm_limit`／牆鐘,而 PDM-005 §5.2a-4 明文說過這三個都不是 token 上限。<br>**(a)** 新增實作工作項「依閘道回報 `input_tokens` 累計並於超限時終止 Run」,並把 §5.2a 的輪數換算表(15／7.7／5 輪)回寫 `02:RUN-003`;或 **(b)** 正式承認 MVP 不做,**把 `TokenBudget` 從權限摘要移除**。<br>**現狀(顯示但不強制)是兩者中最壞的一種**——讓使用者確認一個平台不會執行的上限,直接踩 NFR-001「UI 不得誤導」<br>**2026-08-16:規格面(a)已完成**——§5.2a 的輪數換算表(15／7.7／5 輪)與 §5.2 的上限表已回寫 `02:RUN-003`,並寫明「token 上限的強制點只有一個、`max_budget`／`tpm_limit` 不得代理」與「呈現的前提是它真的會被執行」。<br>**2026-08-16 已解決:取 (a),強制已落地**——見新工作項 **`03` SBX-013**(已完成)。<br>**強制點與 PDM-005 §5.2a 的字面指定不同,理由寫在 `02:RUN-003`**:原本指定 Go Worker 依閘道回報的 `input_tokens` 累計,但 `RunResult.usage` 不回傳 token(契約已誠實記載),從 Trace 事後重建又發生在錢花完之後——**唯一看得到每次回應 token 數的是沙箱內的 harness**,強制點因此在那裡。上限值、語意與三層併用的結論全部不變。<br>機制:逐則訊息累計 → 跨越即 `break` 出 SDK 迴圈(不再有下一次呼叫)→ 發 `error`(`code: token_budget_exceeded`)與終態 `usage` → exit code 9;sandboxd 回 `completed`＋`status: failed` 並附可讀訊息,artifact 與 trace 尾巴照常收集。上限值來自該 Run 凍結的 `policy_snapshot`,與權限摘要同源。<br>**failure_class 裁定:不擴充 `0018` 的 CHECK、不新增 migration**——理由見 `03` SBX-013(retry 語意與 `workload_error` 相同;要讓平台分辨它需先在 `contracts/openapi` 加 `RunError.class` 新值,那是契約批範圍;精確原因已在 trace 的 `error.code` 與終態 `usage`)。<br>**誠實界線**:合作式停止,與軟牆鐘同類;跨越上限的那次回應已經付過錢。 | ~~無工作項承接~~ **已由 SBX-013 承接並完成**;PDM-003 v5「三層併用」的第三層現已成立。<br>**2026-08-23 補標**:內文自 2026-08-16 起就寫著「已解決:取 (a),強制已落地」,而 `#` 欄一直沒劃掉。查證 [`03` SBX-013](03-work-items.md) 是 `- [x]`,本列與其他已結項一致標為結案。**這一列本身就是它警告的那個形態**:一個事實有兩份說法,而錯的那份沒有跟任何東西對帳 |
| ~~**乙-3**~~ **已解決(2026-08-16)** | ~~**閘道 $0.50 預算計數 50 倍誤差的處置**~~ | **根因:LiteLLM 的樂觀預算保留(optimistic budget reservation,v1.84 起預設開啟)。** 每一個在途請求會先把**理論最大成本**扣在金鑰的預算計數器上:全部 input token 以**未快取**單價計、加上 `max_tokens` 以 output 單價計。對 `gpt-5.4-mini`(output `4.5e-6`/token)而言,光是 `max_tokens=64000` 就是 **$0.288**,而實際輸出常只有數百 token、input 多半是 1/10 價的 cache read——保留值本身就跟整個 $0.50 預算同量級,兩個請求重疊就把計數器頂到上限,之後每一個請求都被回絕。`LiteLLM_SpendLogs` 記的是實付,所以兩邊差 50 倍。<br>**實驗複現(本機 1.96.2,同一把 `max_budget=0.5` 金鑰)**:序列 3 次全 200;併發 4 次時第 3、4 次回 **429 `Current cost: 0.78845825`**,而該金鑰全程**實付 $0.00096**(821 倍)。錯誤字樣與基準試跑紀錄同型。<br>**處置:配置修正,不動 $0.50。** `infra/compose/litellm-config.yaml` 加 `general_settings.disable_budget_reservation: true`(LiteLLM 官方為此情境提供的開關,其說明明列「phantom BudgetExceededError caused by leaked reservations」)。改後同一實驗:併發 4 次**無任何預算 429**;另以 `max_budget=1e-06` 反證強制仍在——第 2 次起回 429 且 `Current cost: 0.00048` **等於實付**。<br>**代價已寫在配置註解**:改為讀取時強制,已在途的請求仍會跑完,金鑰可超出上限它們的實付金額;沙箱只跑一個 agent、`proxy_batch_write_at` 為 1 秒、`tpm_limit` 仍在,超額有界且遠小於它取代的誤擋。<br>**PDM-003 v5 的 $0.50 維持不變**:誤差不在數值而在語意,修好之後 $0.50 就是 $0.50(基準試跑實測中位數 $0.0566、最大 $0.2367,仍有 2 倍以上餘裕)。<br>**仍未做**:9 個已索引 Skill 的補跑(預估 $1.0–1.5),見下方註 | ~~乙-2 的三個煞車裡最重要的那個本身不可信~~ 金額煞車已可信;**CONTENT-008 已索引層的補跑仍未執行** |
| ~~**乙-4**~~ **已解決(2026-08-16)** | ~~**Agent 相容軸 schema 的四個待決**~~ | **四問已裁定並落地**(migration `0022_agent_compatibility.sql`,決策理由逐條寫在該檔頂端):<br>①**欄位歸屬 ＝ (Skill Version × Runtime Image)**,採「後者才誠實」的既有判斷。代價明示:**換映像即回到未驗證直到重測**——`2026.08-2` 加了 python3(乙-6),所以部署改用它之後這 45 筆結論不再適用,那正是這個鍵存在的意義。<br>②**兩軸判準**:`capability` 取自 trace(有 `skill_activation` 即 `activated`);`runtime` 取自「套件宣告的執行環境 ⊆ 映像提供的環境」,映像提供什麼由 Dockerfile 說了算,宣告什麼由 `seed-skills.json` 的 `deps_runtime` 說了算(策展判斷,不從套件重推)。<br>③**樣本量 ＝ 1 次基準 Run 即可寫入**,逐列帶 `source_run_id` 可追溯。理由:那一列說的是「一次 Run 觀察到什麼」,而不是「宣告 passed」;要等某個門檻才寫,等於讓目錄繼續顯示未驗證而 45 筆實測擱著不用。重測覆寫同一列並帶走 `source_run_id`。<br>④**值域**:`capability` ＝ activated／not_activated／unverified;`runtime` ＝ native／**transpiled**／failed／unverified。`transpiled` 就是第三態——Run 成功但跑的是模型對腳本的改寫。**`not_activated` 不從沉默推定**(SDK 訊息流看不到「可用但沒被叫」,TRACE-002 已記)。<br>**回填 45/45**:`capability` 全數 `activated`;`runtime` 12 `native`(11 無依賴＋1 node)／33 `transpiled`,與 [content-baseline-report.md §8](mvp/m2/content-baseline-report.md) 逐筆相符。回填腳本 `tools/content/backfill-agent-compatibility.sql`(可重跑,capability 由 trace 現查,不寫死)。<br>**讀取面**:`internal/catalog` 的 detail 與 search 改讀真值(原本寫死 `unverified`),回應一併帶 `runtime_image` 與 `measured_at`——**不設「目前映像」的部署開關**,因為那個設定填錯會安靜地讓目錄顯示錯的結論;標明來源映像則不可能安靜地錯。<br>**篩選**:`?agent=` 只開 runtime 一軸(capability 全目錄同值,與「來源層級」同一個理由),四態精確比對;`apps/web` 停用控制項四減為三 | ~~**`01` 的 M2 里程碑第 4 項**、`02:DISC-002` 的 Agent 維度~~ 已解除 |
| ~~**乙-5**~~ **已關閉(2026-08-17)** | ~~**CONTENT-007 的 writing rubric**~~ | **接線完成,CONTENT-007 已勾。** 四個缺口全部關閉:①資料層 `db/migrations/0026_test_case_rubric.sql`(`test_cases` 與 `test_case_snapshots` 各一個可為 NULL 的 `rubric jsonb`,隨快照凍結、計入 `content_hash`,無 rubric 者雜湊與 `0026` 前逐位元相同);②Go 側 `buildRequest` 送 rubric、`evaluation_started` 與 `evaluations.rubric_version` 填真值,id 對不上的條目丟棄並記 warning;③`apps/web` Test Case 詳情頁的 rubric 編輯區(一條驗收條件一格,item id 由結構決定);④`judge_regression.py --rubric`。契約 `public.yaml` 補 `Rubric`／`RubricItem`／`TestCase.rubric` 與三值 `PATCH`(additive,redocly 0 error)。<br>**A 輪回歸已跑**([m3/report-judge-regression.md §11](mvp/m3/report-judge-regression.md)):22 條逐項回判定與引用,閘道實付 $0.14363(估 $0.07,差在加 rubric 的呼叫不能用不加 rubric 的中位數估)。**兩個必須跟著讀的結果**:(a) `writing-rubrics.md` 預測的「絕大多數 undetermined」沒有發生,因為該文件漏了第三條證據路徑(trace 的 `tool_call` payload),更正已寫回;(b) 新缺口 **G7 見下方乙-13**。**B 輪(改 Prompt 後重跑 5 筆 Run 再評)仍未跑**,掛在 `EVAL-013` 的「沒測到」 | ~~CONTENT-007 勾選~~ 已解除 |
| ~~**乙-5(原文保留)**~~ | **CONTENT-007 的 writing rubric** | `02` §4.7 第 3 條要求 `writing` 類每個精選附一份可編輯 rubric,供 LLM Judge 逐項回傳證據引文。~~**未做,因為 rubric 沒有消費端**——EVAL-001／002 的 Judge 介面尚未實作。~~其餘四條允收皆達成<br>**2026-08-17：內容已補完,性質從「待決策」變成「待接線」。** 5 個精選的預設 rubric 見 [content/writing-rubrics.md](mvp/content/writing-rubrics.md)（`rubric_version = content-007/writing/v1`）。**沒有任何一項待負責人裁定**——剩下的四個缺口全是實作：①`test_cases`／`test_case_snapshots` 無 rubric 欄位（存不進、跟不上快照,「可編輯」在資料層不成立）;②`internal/eval/judge.go` 的 `buildRequest` 從不設 `req.Rubric`,`evaluation_started` 的 `rubric_version` 寫死 `nil`;③`TEST-012` 未實作,沒有編輯介面;④回歸 harness 不吃 rubric。<br>**另記兩項結構性界線**（不是缺口是事實,寫下來免得被讀成別的意思）：Judge 只拿得到 artifact 的 manifest 列、拿不到內容,所以文字品質類條目的證據**必須出現在最終回覆**,`writing` 的 Prompt 因此要加一句;`brand-guidelines` 的字體與色彩在 `.pptx` 位元組內,rubric 判的是該 Run 的自述 | CONTENT-007 勾選;實質上是 M3 的前置(見丙-1)。**本列可於 M3 第 7 批接線後一併關閉,無須另行決策** |
| ~~**乙-6**~~ **已裁定(2026-08-16):加** | ~~**Runtime Image 要不要含 python3**~~ | **加**,映像升 **`2026.08-2`**。理由:沙箱**沒有網路**(SBX-007),使用者不可能自己裝,預裝是唯一途徑;不加則 33 個 Skill 的腳本永遠不會被執行,平台只能是「模型轉譯」語意。<br>**裝什麼 ＝ 45 個目錄 Skill 宣告的依賴集合,一個不多**(pandas 26／openpyxl 17／lxml 12／python-docx 2／numpy・pypdf・pdfplumber・python-pptx・python-dateutil 各 1)——這條規則可以從資料重新推導,「看起來有用」不行。版本 pin 取**通得過 I-06 的最新版**(保守舊版被 grype 抓到可修的 High,而 ADR-022 對可修項無豁免路徑);`pip` 比照 npm 前例裝完即移除。<br>**成本明示**:①映像 806 MB → **1.24 GB**;②豁免清單多了四列(`python3.11` 系列、`libexpat1`、`libsqlite3-0`、`libncursesw6`,皆無上游修復),其中 `libexpat1`／`libsqlite3-0` **會解析工作負載讀進來的資料,攻擊面確實變大**,是接受的風險不是解決的問題;③`pandas` 釘 3.x 而目錄 Skill 多寫於 2.x 年代,**本批未實測兩者差異**。<br>**連帶**:此映像未經基準試跑,所以乙-4 的 45 筆結論**只對 `2026.08-1` 成立**;部署換用 `2026.08-2` 前應重跑 CONTENT-008 基準(預估 $3–4,見基準報告 §5.2),否則目錄該欄會誠實地回到「未驗證」<br>**2026-08-16 現況**:①**基準重跑已完成**——9 筆補跑後,其餘 36 筆亦經負責人裁定於 `2026.08-2` 全量重測(§12／§13),33 筆 `transpiled` 全數轉 `native`、成本 −28%、輸入 token −50%,乙-4 的相容軸 45 列已是這個映像上的量測;②**依賴集與映像內容的比對缺口由另一 agent 處理中**(映像批,碰 `infra/images`／`tools/content`／seed)——本文件不代為記錄其結果,以該批的交付為準 | ~~乙-4 的值域設計;CONTENT-005 揭露完整度~~ 已解除;~~**新增待辦**:換映像後的基準重跑~~ **已完成(2026-08-16)**;依賴集缺口處理中 |
| ~~**乙-7**~~ **已裁定(2026-08-16):含 UI,但逐條判定** | ~~**`03` §9 的允收是否含 UI**~~ | **裁定:取 (a)「承認含 UI」,但退回範圍縮小為逐條判定的結果——`TEST-003` 退回,`TEST-001`／`002` 維持勾選。** 判準取自 `02` 各條允收準則自己的主詞:「系統必須／每個 Run 必須／系統依…自動建議」在系統平面即成立;「**使用者可**…」與「**顯示**…」的主詞不是系統,沒有介面就是沒有達成。這一步不需要先回答「§4.3 是在描述系統能力還是端到端可用性」——**條文自己用主詞回答了**。<br>逐條結果:`02:TEST-001` 第 1 條(每個 Run 必須含非空白 Prompt,主詞是 Run)→ TEST-001 **維持**;第 2 條後半(系統自動建議,**明文為可選強化**)→ TEST-002 **維持**;第 2 條前半與第 3 條(「使用者可」純手動建立／新增／編輯／刪除／確認驗收條件)→ TEST-003 **退回**——`apps/web` 沒有任何驗收條件的列表、編輯器或確認控制項,`api/lab.ts` 五個函式裡沒有一個碰 criteria 端點。**`RunPreflight` 上的「確認」是權限摘要的確認(`02:TEST-005`),不是驗收條件的確認,不能頂替。**<br>**不採 (b)**(明文界定在 API＋契約層):那是為了讓帳好看而降低既有的產品承諾,而 Persona 明確是非技術使用者(`01` §2.1)。**`02` 一字未改**——準則沒有寫錯,是承接的工作項少了一個,已新增 `03` **TEST-012**(建立 Test Case、輸入編輯 Prompt、驗收條件增改刪確認、Dataset 列表與刪除;只做既有端點的消費端,不新增 API)。<br>逐條依據見 [m2-work-items-audit.md §12](mvp/m2/m2-work-items-audit.md)。**Test Lab 仍沒有建立 Test Case／編輯 Prompt／列出與刪除檔案的介面**這件事不變,現在它有承接者了。<br>~~**原待決內容(保留):**~~ `apps/web` 有 `RunPreflight`／`RunTrace`,**2026-08-16 新增 `/lab/datasets` 上傳頁**(`DatasetUpload.tsx`),TEST-004 因此依允收重新勾選——它退回的理由是**單一一條可判定的準則**(`02:TEST-002`「上傳前**顯示**大小限制、保存政策及資料使用範圍」),補上顯示面即成立,不需要先有裁定。**待裁定的仍是 TEST-001／002／003**:它們的準則以「使用者可…」起頭,若同尺解讀亦應退回,而那不是一個顯示面就能補的範圍。**(a)** 承認含 UI,一併退回並新增實作工作項;或 **(b)** 明文把 §9 界定在 API＋契約層,UI 由 `DESIGN-007`(未勾)與一個新工作項承接。任一都要三文件同步。**注意 Test Lab 仍沒有建立 Test Case／編輯 Prompt／列出與刪除檔案的介面**,新頁只做上傳這一步 | ~~TEST-001/002/003 的勾選正確性~~ 已解除;**TEST-003 待 `03` TEST-012** |
| ~~**乙-8**~~ **已解決(2026-08-16)** | ~~**SEC-002 閘門 B 的兩項未落地阻擋**~~ | 兩項都已在 `internal/run/gateb.go` 落地,`Create` 內、建立 run 之前,超出即 **422**(與既有兩項同一形式)。<br>**①靜態掃描等級**:不等威脅模型 Q7,因為政策其實已經以**可逐項判定**的形式存在於程式裡——`skillpkg` 對每一個 finding code 都指定了 severity,而 `error` 級正是 `SKILL-002` 定義的阻擋級。閘門 B 現在**重新掃描**該版本的套件位元組並套用同一套政策。為什麼不是重覆匯入的檢查:①套件位元組在匯入後可能改變或讀不到;②舊版本可能早於某個今天才是 error 級的 code;③匯入以外的路徑也會產生版本。**刻意不讀 `search_documents.scan`**——那是目錄 UI 用的可為 NULL 的警告計數投影,不是通行證,讓閘門讀它等於讀一份決策的快取而不是決策本身。<br>**fail-closed**:套件讀不到／解不開＝掃描沒做成,一律拒絕(SEC-002「檢查無法執行視同未通過」、DISC-004「不得自行推定為通過」)。這也補上一個既有的洞:先前套件不可讀時 preflight 只顯示 `unavailable`,Run 照跑。<br>**②Workspace 並行上限**:`MaxConcurrentRunsPerWorkspace = 2`(PDM-005 §5.2),計非終態 Run,workspace 一律取自 session(鐵律 3)。在**交易內**檢查並先取 `pg_advisory_xact_lock(workspace)`——否則兩個同時進來的請求會各自讀到「目前 1 筆」而都放行,上限 2 就變成 3。<br>**測試**:`TestRunIsRefusedWhenThePackageCannotBeScanned`／`TestRunIsRefusedWhenTheStaticScanIsBlocking`／`TestWorkspaceConcurrencyLimitBlocksTheThirdRun`(含「另一個 Workspace 不受影響」與「結束一個就放行」)。<br>**SEC-002 仍不勾**:閘門 B 四項阻擋現已全數落地,但 45 項基線尚未經 SEC-009 驗證(甲-1／甲-2) | ~~SEC-002~~ 僅剩 45 項驗證 |
| ~~**乙-9**~~ **已結案（2026-08-27）：PDM 提案整批追認** | ~~**PDM-005 §5.3 未回寫 `02:TEST-005`**~~ | §5.3 指定的權限摘要欄位裡,**「預估成本區間」完全不存在**於 `PermissionSummaryContent`。§5.2a-6 特別強調必須是**區間**不是單值(首次與後續 Run 因 prompt caching 差約 8 倍)。根因是 PDM 指定要回寫 `02` 的內容沒有回寫,所以 `02` 的字面準則已被 TEST-008 滿足,缺的部分不可判定(同型於乙-2 的輪數表)<br>**2026-08-16 已回寫**:`02:TEST-005` 補上 §5.3 的完整欄位清單與「預估成本區間必須是區間」的理由(首次／後續差約 8 倍),並記明該欄位現況為**完全不存在**;不在 TEST-008 字面範圍內,故不退回 TEST-008,改由新增工作項 `03` **TEST-011** 承接。**2026-08-16 已實作並勾選**:`PermissionSummary.EstimatedCost`($0.01／常見 $0.06／$0.30,來源為基準試跑 45 筆的閘道實付分布)＋ `RunPreflight` 的「預估成本(估計值)」列,**刻意在 `summary_hash` 之外**(理由同 User Prompt 不入 hash:成本是預測不是權限,重新校準不該作廢既有確認),`TestCostEstimateIsOutsideTheConfirmedHash` 對回應位元組重算 sha256 反證。**契約缺口(記錄,未改 spec)**:`contracts/openapi/public.yaml` 的 `RunPermissionSummary` 尚未宣告 `estimated_cost`,屬 additive,待契約批補<br>**2026-08-17 更新:契約半邊已結案,只剩文件一致性那一半。** `estimated_cost` 已由 **M3 批 1** 補進 `public.yaml`（見 [m3/audit.md §4.1](mvp/m3/audit.md) 的 1-c），上一段記的「待契約批補」已完成。**本列因此縮小為單一件事**：`02:TEST-005` 以「值其實已於 PDM-005 定案」的敘述引用它，而 [`m0/pdm-proposals.md` §9.1](mvp/m0/pdm-proposals.md) 的 PDM-005 一列仍是 `- [ ]`——**兩份文件對「是否已定案」的說法不一致，需負責人裁定其一**。這不阻擋 M4 任何一批（[m4/README.md §8.3](mvp/m4/README.md) 已如此判定），併入 H-7 的逐項定案紀錄一次處理<br>**2026-08-27 結案：`m0/pdm-proposals.md` §9.1 的 PDM-005 已勾。** [`05` R-1b](05-pending-rulings.md) 把該表的十一列整批追認，照提案值、**一個數字都沒有改**——本列剩下的那一件事（兩份文件對「是否已定案」說法不一致）因此消失，`02` 那句「值其實已於 PDM-005 定案」從今天起是對的。<br>**這一列值得留一句，因為它示範了一種會重複發生的殘項**：它從來不是缺實作也不是缺決定，**是一份文件宣稱了另一份文件沒有簽過的事**，而兩份都各自讀起來完全正常。它活了十一天，做完只花了打一個勾——**貴的不是修它，是沒有任何機器會讓它出聲**。<br>**同批查證推翻本列的一個附帶前提**：§9.1 的 PDM-005 那列還牽著 §5.1b 的兩個較寬值（壓縮檔 32 MiB／解壓後 256 MiB），而那兩個值**不是被本次追認處理的**——它們由同日的另一次裁定改為 10 MB／100 MB（`05` ~~R-13~~、`03` INGEST-014），**那一項動了程式，本列沒有。** | ~~`02`／PDM-005 的一致性~~ 契約已補；~~**僅剩兩份文件對「是否已定案」的說法不一致**~~ **已結案（2026-08-27）** |
| **乙-10(承 M1)** | ~~宣告閘門 D 日;追認 KPI3 判準修正;**Q15**(匯入 SSRF 在 MVP 期間的歸屬)裁定;~~**anthropic-sa 法務判定**(未決前 `documents` 已索引有 10→6 的塌陷風險,阻擋 CONTENT-003／004 勾選)<br>**2026-08-16 進度**:①**Q15 已裁定**——擴充 `02:SEC-003` 納入匯入抓取器防護(不另立需求 ID),允收準則已寫入,承接工作項 `03` **INGEST-014**(未實作);②**KPI3 判準修正已追認**(附三項條件,經獨立覆核;見 [content-review-report §12](mvp/m1/content-review-report.md))——關鍵發現是「不修判準就得下架」**套錯規則**,KPI3 屬在地化類,該類無兩輪上限亦無下架終點;③**anthropic-sa 已有分析備忘**([anthropic-sa-license-memo.md](mvp/governance/anthropic-sa-license-memo.md),四種平台行為逐項評估＋方案 A/B/C),**終判仍在負責人與法務**;④**D 日仍待負責人宣告**<br>**⏳ 2026-08-23：PDM 暫時放行,但放行的範圍只有一格。**<br>**放行的是**:[`m5/README.md` §啟動條件](mvp/m5/README.md) **第 2 列**(「`01` §11.2 漏斗第一段有讀數」)不再阻擋 M5 開工。<br>**沒有被放行的四件,逐項寫出來免得被合併讀**:①**M1 閘門本身仍未通過**——D 日未宣告、9 場一場都沒跑、G1～G4 **沒有任何一個數**;②`03` §18 的 `RELEASE-001`～`010` 一項都不因此變動,[m4/release-checklist.md](mvp/m4/release-checklist.md) **H-8 仍是負責人動作**;③**前期驗證 2 仍未執行**——[gate-test/](mvp/gate-test/) 的材料原地不動,那句追問一次都沒問過;④M5 的**另外兩個**啟動條件照舊(**封測結束**、`GEN-009` ③④),所以**這一格放行不等於 M5 可以開工**。<br>**它是暫時的,而且有終止條件**:效力隨 D 日宣告而終止——D 日一旦宣告,第 2 列回到原本的形式(要那個讀數)。**若 M5 已開工而閘門結果顯示搜尋不可信,那時要重讀的是 [ADR-046](../adr/ADR-046-generating-a-skill-from-a-task-description.md) 決策 7**——生成的入口被刻意放在「沒有夠接近的 Skill」那一格,而**「無結果」本身是不是一個可信的訊號,取決於搜尋好不好**。搜尋不可信的話,那個入口會在錯的時候出現。<br>**不得被引用為「M1 閘門已通過」或「D 日已解除」。** 本列與 `04` 其餘各處的 D 日敘述**一字未改**,因為那件事一件都沒變。<br>**⏳ 同日稍晚:放行範圍擴大到三項全部** → [ADR-052](../adr/ADR-052-m5-starts-in-parallel-with-an-unfinished-mvp.md)。`01` §10 的 M5 啟動條件三項(封測結束／漏斗讀數／`GEN-009` 基線)**全部暫時放行,M5 即刻可開工**。**上面那四條「沒有被放行」裡,①②③ 仍然逐字成立**——閘門未過、`RELEASE-*` 與 H-8 未動、前期驗證 2 未執行;**只有第 ④ 條被推翻了**(原文說「M5 的另外兩個啟動條件照舊,所以這一格放行不等於 M5 可以開工」)。<br>**擴大放行新增了一條原本不存在的邊界**:**開工不等於曝光**。生成入口不得對封測使用者出現,直到漏斗第一段有讀數為止——封測使用者一旦看得到「搜不到 → 生成一個」,`01` §11.2 第一段量到的就不再是搜尋好不好,而**那個數字只有一次機會、封測只有 12 個人**。旗標預設為關,而 ADR-052 自承**沒有任何機制會告訴我們它被誤開過**(該 ADR 的待決策)。<br>**2026-08-16 續**:⑤**方案 C 已執行**(`27cbbe7`)——`anthropics/skills` 的 4 筆(`docx`／`pdf`／`pptx`／`xlsx`)以 migration `0023` 的 `skills.access_restriction` 標為 `license-review`,`/files` 回 403、Run 建立回 422、**搜尋與摘要照常**,旗標於 Fork 當下複製、未知原因碼 fail-closed。允收準則已寫入 `02:SEC-011` 的追加小節。<br>**這不是終判,是終判前的保全動作**:①法務終判仍未下,CONTENT-003／004 因此**仍不勾**;②設定與解除目前只能直接跑 SQL(`tools/content/restrict-anthropic-sa-display.sql`),沒有端點也沒有 audit event——**SEC-011 的最小 operator 流程由另一 agent 處理中**(碰 `apps/platform`／`apps/web`),以該批交付為準;③`docx` 是 golden query D01 的 gold primary,受限≠下架,**它仍列在搜尋結果裡**,D01 的判定基準未失效<br>**2026-08-17 更新（M4 文件批）**：⑥**詢問信草稿已修訂**（[governance/anthropic-sa-inquiry-draft.md](mvp/governance/anthropic-sa-inquiry-draft.md)）。原因是**打包上線會讓草稿裡一句承諾的理由失效**：第 4 項「we do not offer these Skills for download」當時為真，是因為平台根本沒有下載功能；`PACK-001` 上線後平台整體**有**下載，只有這四筆被擋，原句會被讀成不實陳述。修訂形式為第 4 項改 **4A／4B 二選一**（依寄出當日打包是否已上線）＋承諾段落改為精確的「這四筆」＋新增寄信前的**實測要求**（不得以政策文件代替實際試過打不出包），原文全部以刪除線保留。阻擋機制由 [ADR-027](../adr/ADR-027-download-artifact-shape-reproducibility-and-integrity.md) 決策 4 定為**兩道獨立的鎖**（`skills.redistribution` 三態預設 `unknown` ＋ 既有的 `access_restriction`），任一道單獨即足以拒絕。<br>**這不改變本列的狀態**：法務終判仍未下、D 日仍未宣告，`CONTENT-003`／`004` 仍不勾。修訂只是讓那封信在任一寄出時點都說得出真話 | — | M1 驗證閘門正式結案、CONTENT-003／004；**寄信前須確認 4A／4B 擇一與四筆的實測阻擋** |
| ~~**乙-11**~~ **已裁定(2026-08-16):路徑 ②,已通過** | ~~**`docx` 的裁定(CONTENT-005 44/45)**~~ **結果:CONTENT-005 回到 45/45**,見 [content-review-report.md §13](mvp/m1/content-review-report.md)。升 `enrich-skill/v6` 並新增通用第 4 條「不得把原文分別陳述的兩件事串成一件」——**它不是 v4 第 3 條的加強版**:第 3 條管橫向外推(做得到 A→宣稱做得到 A 的鄰居),而 `docx` 的失敗是縱向串接(做得到 A、也做得到 B→宣稱做得到 A 接 B),那句話的**兩半在原文裡各自成立**,正是這一點讓它讀起來像有依據。A 輪 KPI1 由「24 條 1 條未支持」變「18 條 0 條未支持」(串接句消失)但 KPI5 一致性新失敗,B 輪同路徑重跑(不再動 prompt,KPI5 屬既已認定的非決定性自癒類)**通過**;成本 $0.194／上限 $0.5。未走路徑 ③(下架),`docx` 仍是 golden query D01 的 gold primary。<br>~~原待決內容:~~ | 兩輪重審用盡仍未過 KPI1,**兩輪理由不同**(A 輪:「可保證其他內容保持不變」為原文沒有的一般性保證;B 輪:「DOTX 擷取為 Markdown 並依標題重組」把 `.dotx` 擷取與 `.docx→markdown` 兩個獨立事實接起來,屬 v4 第 3 條「支援一種格式不等於支援其近親」的同型外推)。已排除量測假象(`SKILL.md` 6,868 字元,遠低於 Judge 的 40,000 截斷門檻)。**三條路徑,報告不代為決定**:①再給一次非決定性重跑(超出既定兩輪上限,需授權);②比照 v4 先例為「格式近親外推」加更強通用約束並升 v6;③依字面規則列「建議自目錄下架待修」。**選 ③ 的代價要先看清楚**——`docx` 是 `documents` **已索引**筆(下架不影響 CONTENT-001 每類 4–6 的精選下限),但它是 [`catalog-rebuild-report.md` §6.1](mvp/m1/catalog-rebuild-report.md) golden query **D01 的 gold primary 且為現行 Top-1**,下架會使該題失去判定基準。紀錄見 [content-review-report.md §11.3](mvp/m1/content-review-report.md) | **CONTENT-005 的勾選是否維持**;連帶 CONTENT-003 的檢查 ⑦ |
| ~~**乙-12**~~ **已解決(2026-08-16)** | ~~**兩個平台缺陷,無工作項承接**~~ | **裁定:不重開 INGEST-009,以新工作項 `03` INGEST-015 承接(已完成)。** 理由:INGEST-009 的允收準則在當時完全成立且有證據,兩個缺陷都是 **M2 才出現的新資料形態**誘發的(基準試跑在臨時 Workspace 留下 45 筆 fork 文件;修正輪首次做大批量增強),把一個已結案的工作項改回未完成會讓「某一時點的帳」失去意義,而缺陷本身仍必須被修、被記錄——所以記在新項而不是舊項。兩項修法與誠實界線見 `03` INGEST-015。<br>以下為當時的技術定位,保留原文:<br>**(a) 增強實際只有 30 秒,且逾時仍付錢。** `services/platform/internal/llmclient/client.go:25` 的預設 `http.Client{Timeout: 30 * time.Second}` 比 `services/platform/internal/ingest/enrich.go:32` 的 `enrichTimeout = 75s` **更早到期**,所以那個 75 秒的 ctx 預算從來沒被用到。修正輪 14 次成功增強中有 **3 次因此逾時**(`docx` 是 `/embed` 逾時,`excel-split`／`excel-delete` 是增強逾時),重跑即過——但 **client 端放棄不會取消上游,每一次逾時都已在閘道產生費用**。建議把逾時交給 ctx 控制,不要在 client 上另設一個更短的硬期限。<br>**(b) `ReindexAll` 讓補跑清單無法用時間戳篩選。** `db/queries/search.sql:244-253` 對每一筆存活 skill 無條件 `updated_at = now()`,而 `ListPendingEnrichment` 是「全庫 pending、oldest first」——**時間戳與 `REINDEX_BATCH` 都擋不住** M2 基準試跑在 `content-baseline` 臨時 Workspace 留下的 45 筆 fork 文件,照跑會多花約 45 次旗艦增強呼叫(**約 $2**)。修正輪的處置是手動把那 45 筆暫標 `enriched`、跑完還原(實測還原 45/45 逐欄相同),**這是每次補跑都要重做一次的人工步驟**,不是修好了 | ~~任何未來的增強補跑~~ 兩者皆已修;人工暫標步驟不再需要 |
| ~~**乙-13(新入列 2026-08-17)**~~ **已結案(2026-08-23 查證)** | ~~**`artifact` 型證據引用的引文從來沒有被回驗(G7)**~~ | `internal/eval/judge.go` 的 `verify()` 對 `kind = artifact` 只檢查「路徑在 manifest 上」,**引文不比對**——因為請求裡本來就沒送 artifact 位元組(讀檔內容等於在控制平面解封存,evaluation-design §2.2 明文劃線)。後果是 `evidence_required: true` 的 rubric 條目**可以用一段沒人驗過的引文通過**,而存進報告的 `excerpt` 是平台自己的 manifest 行(檔名＋大小),不是那段引文。讀報告的人看到的是「這條要求引文 → 通過 → 證據是 `report.md, 4096 bytes`」。<br>**A 輪實測**([m3/report-judge-regression.md §11.2](mvp/m3/report-judge-regression.md)):9 筆 `passed` 有 **6 筆只靠這種引用**。逐筆追查確認**模型沒有捏造**——抽查的引文逐字存在於該 Run 的 `trace_events` payload 裡,模型是看了 trace 卻把來源標成 `artifact`。**問題不在模型誠不誠實,在平台分不出「歸錯類」與「編的」**,`verify()` 對兩者一律放行。<br>**處置二選一,需要拍板,因為它動的是 ADR-026 defence 3 的判準**:**(a)** `artifact` 型引用不得滿足 `evidence_required`,要求引文的條目只接受 `agent_output` 與 `trace_event`(兩者都逐字回驗)——最小改動,且與「引文用來證明存在」的原意一致;**(b)** 保留但在儲存與 UI 上明確標示「此引文未經回驗」——誠實但不阻擋。<br>**不擋 CONTENT-007**:它對任何帶 `evidence_required` 的 rubric 一視同仁,與 rubric 內容無關,屬 `EVAL-001`／ADR-026 而非內容供應。<br>**2026-08-17 併入 G8(同一次拍板,反方向的同一個問題)**:注入抵抗回歸([報告 §12.4](mvp/m3/report-judge-regression.md))實測到兩筆**相反方向**的失誤——`agent_output` 型引用的引文本體逐字正確,但尾端多了 `}],`(模型把 JSON 分隔符寫進字串值),`verify()` 的逐字子字串比對比不到,兩條**正確的** `failed` 被降成 `undetermined`(27 條中 2 條、15 筆引用中 2 筆)。G7 是「該擋的沒擋」,G8 是「不該擋的擋了」,兩者都是 defence 3 判準的鬆緊問題,**分開改會把判準改成兩把尺**。G8 的方向是安全的(降級不是放行),所以它不加急,但它與 §6.1 的 v1 缺陷同類:正確判定被引文邊界丟掉,而在畫面上與「Judge 沒把握」無法區分(丙-10)。同一輪另見一筆「判定正確但引文偏弱」——以 `agent_output` 的一句話當作「檔案存在」的證據——屬 G7 家族,不另立 | 沒有阻擋任何工作項;但在拍板前,**任何引用 rubric「逐項回傳證據引文」成果的文件都要一併引這一列**。拍板時 **G7 與 G8 一起裁**,並同時回答「引文比對要不要正規化」這一半<br>**✔ 已裁定（2026-08-22）→ [ADR-043](../adr/ADR-043-evidence-citation-is-verified-by-content-not-by-its-claimed-source.md)：兩個選項都不採，因為它們共用一個可以被拿掉的前提。**<br>(a) 與 (b) 都建立在「平台分不出歸錯類與編造」上，一個選擇全部當假、一個選擇全部當真——**而那個前提是一次字串搜尋就能拿掉的**：引文在不在可回驗的來源裡，去找一次就知道。<br>**判準改成一句話，同時管住兩端**：引用成立若且唯若**引文能在可回驗的來源裡（正規化後）逐字找到**，`kind` 是提示不是憑證。指名的來源交不出引文時去 trace 摘要與最終輸出裡搜；**找得到＝歸錯類，改正並記下 `reattributed_from`；找不到＝不得滿足 `evidence_required`**。<br>**這對 A 輪那 6 筆很重要**：它們的引文逐字存在於 `trace_events`，所以會被**改正後保留**——選項 (a) 會把 6 筆真證據一起打掉。<br>正規化有界（NFC、空白折疊、只去首尾結構標點），並設 12 字元下限（**推導不是量測，B 輪校準，方向只能往上**）；比對結果存三態 `exact`／`normalized`／`not_found`——G8 那兩筆若當時有這個欄位，看報告的人一眼就知道發生了什麼。<br>~~**實作尚未開始**，落點 `trial/improvement/judge.go` 的 `verify()`~~<br>**2026-08-23 查證:已全部落地,本列的「尚未開始」是 2026-08-17 的狀態,ADR-043 之後就過期了。** 逐項對照今天的 `judge.go`:①**指名來源交不出引文就全域搜尋**——`findQuote(ref.Quote, verifiableSources(m, digest))`,兩趟(exact 優先於 normalized);②**找得到＝歸錯類,改正並記 `reattributed_from`**——`out.ReattributedFrom = ref.Kind`,欄位在 `eval.go` 的 `EvidenceRef`;③**三態比對結果**——`MatchExact`／`MatchNormalized`／`MatchNotFound`,外加 ADR-043 §3 為 artifact 新增的第四態 `MatchNotChecked`(**「沒去比對過」與「比了不在」是兩件事**);④**正規化有界＋12 字元下限**——`normalizeQuote()` 與 `minNormalizedQuote`,註解寫明「方向只能往上」;⑤**`evidence_required` 不得被 artifact 引用滿足**——`merge()` 裡把它降級並套 `EVIDENCE_UNVERIFIABLE_PREFIX`,而**這一條正是 G7 本身**。<br>**同一批的 B 輪(丙-8)順帶校準了 ④ 的下限**:12 沒有被推翻,而 [ADR-049](../adr/ADR-049-citation-verification-matches-the-stored-value-not-its-encoding.md) 修的是另一件事——比對的對象是編碼還是值 |
| ~~**乙-14（新入列 2026-08-17）**~~ **已裁定(2026-08-23):並行** | ~~**甲類四項是否在封測前到期**~~ | 出處：[m4/README.md §4](mvp/m4/README.md)（由 M4 計畫提出，本清單只是它的入口）。**建議裁定：四項全部到期，且是封測 D 日的阻擋項，不是平行工作。**<br>依據不是新的——ADR-015 的定案紀錄早就寫著「`SEC-009`／`SBX-010` 未通過**不得開放外部使用者提交 Skill 執行**」。M0～M3 之所以能把甲類當平行工作，是因為那段期間**沒有任何外部使用者**；M4 的封測第一次讓外部人員在真實部署上建立 Run，前提就此成立（`03` `BETA-002` 要量的漏斗第三段就是「試跑」）。<br>**唯一的替代路徑是「只讀不跑的封測」**（只搜尋、看詳情、下載既有精選套件，不建立任何 Run），那樣甲類確實不必到期。**代價已寫清楚，且是兩條硬的**：①`01` §11.1 的北極星指標是「每週通過使用者驗收並完成打包的 Skill 數量」，沒有 Run 就沒有驗收；②`02` §7 的 DoD 第一條要求走完「意圖搜尋 → 詳情 → Fork → **試跑** → 評估 → 打包下載」，**只讀不跑的封測不能用來宣告 MVP 完成**。<br>**為什麼需要拍板而不是逕行採建議**：它決定的是**封測日期取決於誰**——取「到期」，封測日期由部署批決定；取「只讀不跑」，封測可以更早但 MVP DoD 走不完 | **封測的 D 日排程**；連帶決定甲-1～甲-4 是不是 `RELEASE-009` 的前置<br>**✔ 已裁定（2026-08-22）：採建議——四項全部到期，且是封測 D 日的阻擋項。** 決定性的不是 ADR-015 那句禁令（它本來就無條件），是**替代路徑走不到 MVP 完成**：`02` §7 的 DoD 第一條要求走完含「試跑」的六段，`01` §11.1 的北極星指標是「每週通過使用者驗收並完成打包的 Skill 數量」——**只讀不跑的封測連北極星的分子都產不出來**。一個不能用來宣告 MVP 完成的封測，省下的是時間不是工作。<br>**因此封測 D 日由部署批決定，甲-1～甲-4 是 `RELEASE-009` 的前置。** 這條裁定把壓力全部轉到「哪裡有一台 Linux」。<br>**gVisor 的 Windows 替代品:沒有,而且「沒有」比找到一個更重要**——[ADR-022](../adr/ADR-022-sandbox-deployment-topology-and-security-thresholds.md) 把 Suite 2 的受測物定義成**即將加入池的那台節點**,換一台機器就換了受測物,所以 WSL2／Hyper-V／Kata／租一台 Linux 都不算數(甲-1～甲-4 一項都沒動)。**但這個問題藏了一塊免費的**:Suite 1 只要 Linux ＋ Docker ＋ runsc,而 `ubuntu-latest` 一直都有;擋住它的是 `ci.yml` 一句「runsc 需要巢狀虛擬化」的註解,而那個前提 **ADR-022 §0 早就推翻了**(systrap 用 seccomp ＋ SIGSYS,不需要 `/dev/kvm`)。已於 `15a9dd7` 接上:`SKILLHUB_SANDBOX_TEST_RUNTIME` 接到 `Config.Runtime`(**該欄位從 SBX-001 起就是 ADR-015 的唯一機制,而沒有任何測試走過它非空的路徑**),CI 多跑一次 driver 測試在 runsc 上,守衛在指定了 runtime 時**失敗而不是 skip**。<br>**2026-08-23 裁定:並行,與本列原建議相反。** 負責人裁定**甲類四項不是封測 D 日的阻擋項**,見 [ADR-050](../adr/ADR-050-beta-runs-in-parallel-with-the-sandbox-acceptance.md);ADR-015 定案紀錄那一句已回填指向它,原文不刪除。<br>**ADR-050 把接受的東西寫明白了**:甲類通過前,不受信任程式碼會在**逃逸邊界尚未被驗證**的節點上執行——45 項覆蓋 0 項 pass、`gvisor-baseline.txt` 仍是 `unset`、P-02 常駐探針不存在、生產級 egress 強制不存在。**這些不是「還沒量到」是「還沒有」。**<br>**三件刻意寫下的事**:①裁定**不含**把封測擴大為公開註冊——12 位具名受邀者是它敢下的理由之一;②**Suite 1 只要 Linux＋Docker＋runsc**(ADR-022 §0 已推翻「需巢狀虛擬化」,CI 已於 `15a9dd7` 接上,T1 通用嘗試另有 `tools/sec009/t1-escape-attempts.sh` 含負對照),在那台節點跑一次**是一天的事不是一個部署專案**——「全部到期」與「什麼都不做」之間不是只有兩個選項;③**同意書必須據實說明執行環境尚未完成逃逸驗收**,這是本裁定產生的新工作,併入乙-16 的法務清單。<br>**ADR-050 留了三條待決策**,第一條是「第一位外部使用者的 Run 之前要不要求 Suite 1 在該節點跑過一次」——**沒有答案等於「否」**,所以它被寫下來 |
| **乙-15（新入列 2026-08-17）** | **PDM-009 的封測提案待追認** | **PDM-009 此前是唯一一個連提案都沒有的 PDM**——`m0/pdm-proposals.md` 沒有這一節（該檔已隨 M0 完結凍結，補不進去），而兩份既有文件各自假設了一個數字：該檔 §8.2 用 **30 人**、`m0/cost-estimation.md` 用 **20 人**，**互斥且都沒有寫下推導**。<br>**2026-08-17 已補一份提案** → [m4/pdm-009-beta-proposal.md](mvp/m4/pdm-009-beta-proposal.md)（**待追認，不是定案**）。要點：12 人（三層各 4，＋3 位候補）；招募與報酬四原則整套重用 `gate-test/recruit.md`，只改框架與 Q5；三條門檻（B1 完整旅程完成率 ≥ 8/12、B2 漏斗前六段無一段低於 50%、B3 整層失效或同一問題撞到 ≥ 4 人即否決）＋不通過的決策樹；測試期 **14 天**；**先閘門、再封測**。<br>**追認時要一併過的兩件事**：①**報酬預算**——建議對應 2 小時市場價、兩段式但非條件式給付，而**這一筆是最大單項支出且 `cost-estimation.md` 沒有任何一行涵蓋它**；②**PDM-010 的首月額度語意擇一**（`min(20,30)=20` 或 20+30=50），[ADR-028](../adr/ADR-028-beta-admission-and-quota-enforcement-points.md) 決策 4 已要求它先定，否則配額強制點做得出來但**不得上線顯示**（乙-2 的教訓）。<br>**追認前不得勾選**：`BETA-001`／`BETA-005`／`RELEASE-009` 全部不可判定 | `03` §17 `BETA-001`／`BETA-005`、§18 `RELEASE-009`；封測的招募啟動時點<br>**◐ 產品面已追認（2026-08-22），錢與簽名沒有。** **追認**：12 人（三層各 4，＋3 候補）、14 天、三條門檻與不通過的決策樹，以及「先閘門、再封測」——綁住樣本數的是**人力不是成本**（質性回饋要人讀、歸類、追問），那是對的判準，30 人的漏斗精度買不回讀不完的逐字稿。<br>**PDM-010 首月語意擇一：取「首月 20 次」，不疊加。** ①提案自己的措辭「首月 20 次、其後每月 30 次」直讀就是首月較低，**一個要靠第二種解讀才會變大的數字，就是會在畫面上寫錯的那個**；②疊加會引入第二個時鐘（跨月滾存嗎？月底過期嗎？），而配額是 §2.2 的東西——顯示與強制要指得到同一行；③封測 14 天在同一個月內，20 次對上每日 5 次是每天 1.4 次，**門檻在完成率不在次數**。<br>**記下反對意見**：多數產品把首月做得比較多而不是比較少；接受這個裁定的前提是封測受測者不是自然註冊的新使用者。<br>**剩下兩件不是我能簽的**：報酬預算（**最大單項支出，而 `cost-estimation.md` 沒有一行涵蓋它**——建議對應 2 小時市場價、兩段式非條件式給付，需負責人核可金額）與受測者簽署 |
| **乙-16（新入列 2026-08-18）** | **受測者同意書與資料保存政策待法務確認與負責人填值** | **全 repo 此前沒有任何同意書模板**，只有兩處掛勾（[gate-test/moderator-guide.md §1／§3.A](mvp/gate-test/moderator-guide.md) 的檢核項、[gate-test/recruit.md §5.2](mvp/gate-test/recruit.md) 第 5、6 項待決）。**2026-08-18 已補一份草稿** → [gate-test/consent-and-data-policy.md](mvp/gate-test/consent-and-data-policy.md)（**放 `gate-test/` 不放 `m4/`**：它由 M1 閘門與 M4 封測共用，M4 完結後仍會被修訂，依 `AGENTS.md` 的檔案放置規則不屬於任何 `mX/`；**它其實是閘門的前置不是封測的**）。<br>**內容**：一份政策、**兩份同意書**（A 版＝閘門的 40 分鐘一對一，錄影／錄音可拒絕；B 版＝封測的 14 天自用，上傳檔案可拒絕），逐項列舉蒐集什麼與**不**蒐集什麼、[ADR-029](../adr/ADR-029-product-analytics-events-and-audit-trace-boundaries.md) 四個分析事件的**逐欄公開**（`02:O11Y-004` 明文「這是唯一一類使用者未主動提交任何東西就會產生的資料，揭露義務不低於其他類別」）、模型呼叫與成本、八項風險與各自的緩解、四條退出機制（含「哪些刪得掉、哪些只能去識別化保留、為什麼」）、聯絡方式。<br>**必須由人完成的三件事**：①**法務確認**用語、法域、個資法規對應與**未成年受測者的處理**（草稿假設全部成年，未涵蓋）；②負責人填完 §9 的十一項 ⬜（聯絡方式、報酬、保存期限的最終值——**其中三項要等 PDM-006 追認才有值**）；③受測者簽署。<br>**這是封測唯一必須從零做的材料**（[m4/beta-design.md §6.2](mvp/m4/beta-design.md)），招募的其餘部分整套重用 `gate-test/recruit.md` <br>**◐ 非法務的部分已裁定（2026-08-22）；法務那一件縮小到一份清單。**<br>**①「未成年受測者未涵蓋」不補條款，改用招募端排除。** 補一套未成年同意（法定代理人、各法域年齡界線、可撤回性）**會把法務審閱的範圍放大好幾倍，換來這次封測用不到的能力**——12 人的招募本來就是篩選出來的。正解是在 `recruit.md` 的篩選條件寫明「限年滿 18 歲」並在同意書 §1 敘明，**把那個法律問題移除而不是回答它**。<br>**②§9 的十一項 ⬜ 分成兩堆，別一起等**：三項要等 PDM-006 追認才有值（保存期限），**其餘八項不必等任何人**。現況是十一項一起卡在最慢的三項後面。<br>**③保存期限那三格在追認前寫 `尚未定值` 而不是留白**（設計系統 §2.9 對文件同樣成立），並註明「回填前不得對受測者宣告任何期限」。<br>**法務真正要看的縮小為四件**：用語與法域、個資法規對應、四條退出機制的可執行性、分析事件逐欄公開的措辭。<br>**2026-08-23 新增第五件,由 [ADR-050](../adr/ADR-050-beta-runs-in-parallel-with-the-sandbox-acceptance.md) 產生**:負責人裁定封測與甲類驗收並行,所以**同意書必須據實說明受測者的程式碼會在一個尚未完成逃逸驗收的環境中執行**。這不是措辭問題是事實揭露問題——**招募或同意書裡任何一句宣稱隔離強度已驗證的話,現在是不實陳述**。<br>**這一列不會因為上面三件而結案**——簽名是簽名 | **招募寄確認信時沒有東西可簽** ⇒ `BETA-001`；連帶 M1 閘門的 D 日（`recruit.md` §5.2 第 5、6 項是 D 日的待決佔位）；乙-15 追認清單的第 8 項 |
| ~~**乙-17（新入列 2026-08-18，原丙-16 (b)）**~~ **已結案（2026-08-27）：三個問題分三次答完** → [ADR-057](../adr/ADR-057-releasing-content-takes-named-evidence-not-a-button.md) | ~~**`skills.redistribution` 要不要有寫入路徑：這是範圍決定，不是實作工作**~~ | 它是決定「內容能不能交出去」的閘門欄位，也是**全庫唯一一個沒有稽核就能改的閘門欄位**——`access_restriction` 有 `PUT`／`DELETE /admin/skills/{id}/restriction` 與 audit event，這一個只能直接跑 SQL。**技術上的補法很小**（加進 `SEC-011` 的窮舉動作清單、沿用同一組端點形式與同一種 audit），**但它不是丙類**：M4 首發**已裁定不做**（`m4/README §13.5` 出-5、[ADR-027](../adr/ADR-027-download-artifact-shape-reproducibility-and-integrity.md) 待決策），要做等於決定「誰可以把一份內容標成可散布」進 MVP 範圍。<br>**2026-08-23：①的一半已由 [ADR-045](../adr/ADR-045-self-supplied-content-is-not-redistribution.md) 回答,整列仍開。** 負責人裁定「除非有必要理由,否則應該要讓使用者可以下載」,而查下去發現**使用者自己的內容根本不需要那道鎖**——再散布是平台把 A 的內容交給 B,交還給帶進來的人沒有 B。所以匯入直接寫 `self_supplied`(第四個值),**不需要任何人去改任何東西**。本列剩下的是原來的另一半:**別人的內容誰能標成可散布、要什麼證據**,以及這個欄位仍然沒有端點沒有 audit（治理強度不對等這件事沒有改變）。<br>**待決的是兩件事不是一件**：①**誰能改**——只有 operator，還是 Skill 擁有者對自己匯入的內容也能改（後者正是 N-2 預期的第一個需求訊號：使用者自己匯入的 Skill 只會有 `license_expression` 一個欄位，所以 `unknown`＝擋會是常態，而使用者看到的是「你的 Skill 不能下載」）；②**改成 `allowed` 要什麼證據**——[ADR-021](../adr/ADR-021-skill-license-provenance.md) §5.3 已裁定誤判必須錯在**擋**的方向，所以放寬的門檻不能只是一個按鈕。<br>**不拍板的代價現在就在跑**：兩道鎖擋的是同一件事，而治理強度差一整級（一道有端點有稽核，一道只有 SQL） | 不擋任何工作項的勾選；`SEC-011` 的窮舉清單、N-2 的第一個需求訊號<br>**2026-08-23：R-3c 已落地,乙-17 仍未結。** 三個問題裡**只有「治理強度」那一個不需要裁定**([`05` R-3c](05-pending-rulings.md)),已補:`PUT /admin/skills/{id}/redistribution`,operator-only、`note` 必填、before／after／note 與欄位寫入**同一個交易**(稽核 action `skill.redistribution_set`)。沿用 `access_restriction` 的同一組形式,`LockSkillForRestriction` 併為 `LockSkillForOperatorWrite` (同一列、同一把鎖、兩個欄位各取所需)。<br>**它刻意沒有回答 R-3a 與 R-3b。** 路由取 operator-only,因為那是 R-3a 兩個選項裡**較窄的那一個**——之後放寬是加呼叫者,先放寬再收回是拿走東西;請求帶的是自由文字 `note`,**不是 R-3b 可能會要求的來源層級證據**,因為**半套的證據要求讀起來和整套一樣**。<br>**一個新的守門**:`self_supplied` **不接受**——0036 定義它是「誰帶進來的位元組」這個事實,由匯入路徑建立,不是任何人能斷言的判定;接受它等於讓 operator 用「這是擁有者自己的內容」把別人的內容放出去。反方向(把 `self_supplied` 改成 `blocked`)允許。<br>**測試會失敗過**:把稽核 action 改掉重跑,`audit events = 0, want 1`。<br>**2026-08-27 結案，而三個問題是分三次答完的，這一點比結案本身值得記**：**治理強度**（R-3c）在 2026-08-23 補上端點與 audit；**誰能改**（R-3a）與**要什麼證據**（R-3b）在本日由 [ADR-057](../adr/ADR-057-releasing-content-takes-named-evidence-not-a-button.md) 裁定。**先補紀錄、後補判準，而中間那四天的路由刻意取窄**——operator-only 當時不是決定而是暫代的形狀，理由是「之後放寬是加呼叫者，先放寬再收回是拿走東西」。**結果是這一次不必收回任何東西。**<br>**R-3a 取 (a)（只有 operator），程式一個字沒改。** 理由不是保守，是 ADR-021 §5.3——兩份合法的 MIT `LICENSE` 涵蓋了不屬於該作者的內容，**連做過授權查核的人都判錯，交給按下按鈕的人不會更準**。代價（使用者沒有自助路徑，而 `unknown` ＝ 擋是常態）照樣要付且寫在 ADR-057 決策 1，沒有消失。<br>**R-3b 取「具名證據」**：改成 `allowed` 必須帶 `license_expression` ＋ ADR-021 的來源層級，**且必須與該 Skill 最新版本凍結的快照相符**，不符就當場否定。**買到的不是「證明」，是「可以被反駁」**——一個確認框沒有任何人能說它填錯了。只有 `allowed` 要證據，因為為了「擋」而先做判定等於對拒絕做判定收費。<br>**同批補上一個沒人發現的規格違反**：`02:SEC-011` 的 operator 動作是一句**窮舉**（「清單以外的動作一律不因 operator 身分而被允許」），而 2026-08-23 補端點時沒有回頭加行——**那四天裡程式在做一件該節逐字禁止的事**。已補為 ④。**窮舉清單就是這樣壞的：它只在有人回來加行的時候才是窮舉的。**<br>**兩件刻意留著**（ADR-057 已知邊界）：判定黏在 Skill 上，日後的新版本不會使它失效；`repo-license-file` 仍可作為放行依據，先讓稽核查得出來再決定要不要禁。 |
| ~~**乙-18（本清單首次入列即結案，2026-08-22）**~~ **已裁定（2026-08-22）：引入瀏覽器驅動** | ~~**`QA-008` 的路線：引入瀏覽器驅動，還是人工在三個瀏覽器各走一次並落檔**~~ | **這一項此前不是編號列，是散在丙-21 與 [m4/audit.md §乙′](mvp/m4/audit.md) 裡的一句「屬乙」。** 依維護規則就地補一列並標已裁定，不讓一個真的要人拍板的東西只活在別列的旁註裡。**裁定：採「引入瀏覽器驅動」，否決人工走查落檔。** 理由與四個選項（Playwright／Puppeteer／Cypress／人工走查）的比較見 **[ADR-036](../adr/ADR-036-real-browser-verification-tier.md)**；否決人工走查的關鍵一句是**它產不出自動守門**——丙-21 ③ 缺的正是「改壞了會有人知道」，一次性快照給不了，那個殘項會永遠開著。實作 commit `fe7aa13`。<br>**拍板解除了什麼、沒解除什麼**：丙-21 **整列關閉**、`QA-009`／`DESIGN-013` **改判勾選**；**`QA-008` 本身仍不勾**——它的字面判準含「與目標作業系統測試」，而 CI 仍只有 `ubuntu-latest`。**所以這一項是「已決策、實作未完」**，剩下的 OS 矩陣是實作工作不是決策（ADR-036 待決策 1 記的是要不要做、做哪幾格）。<br>**乙類未結案數不因本列變動**（本列生下來就是結案的），逐列重數見上方現況一覽 | ~~丙-21~~（已結案）、~~`QA-009`／`DESIGN-013`~~（已勾）；`QA-008` 仍不勾，改由 ADR-036 待決策 1 承接 |
| **乙-19（新入列 2026-08-22）** | **試跑證據彙總的隱私前置：同意書條款＋逐工作區 opt-in 的資料模型** | [ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 1 把評分與排行榜改為**永久不做**，替代品是跨工作區的試跑證據彙總（「在 12 個工作區的 37 次試跑中，28 次符合各自的驗收條件」）。**它跨工作區，所以它是同意書的事情，不是分析事件的事情**——ADR-029 管的是漏斗量測，本項是由使用者資料推導出的產品內容，兩者邊界不能混。前置有兩件：①[同意書](mvp/gate-test/consent-and-data-policy.md)要有對應條款（與乙-16 同一份文件，同一次法務確認）；②opt-in 要有資料模型（工作區層級、可撤回、撤回後既有彙總要不要回溯扣除）。**在那之前這個欄位不存在，而不是存在且空白** <br>**◐ 資料模型已定（2026-08-22），同意書條款仍隨乙-16。** **opt-in 掛在工作區不掛在使用者**（Skill 與 Run 都是工作區範圍，掛在使用者會讓「哪些 Run 算數」在多工作區時無法回答）；欄位是 `opted_in_at` ＋**同意書版本**（版本沒對上等於沒同意，這是同意書會改版的必然後果）。<br>**撤回取「即時且只往前」**：彙總一律從目前 opt-in 的工作區**即時計算**，撤回後下一次計算就不含它，不做回溯抹除。理由是彙總從不保存個別工作區的身分、`k ≥ 5` 又保證每格至少五個來源，**沒有可以被抹除的個別痕跡**；而承諾回溯抹除會逼出一張記錄「誰貢獻過哪一格」的表，**那張表比它要保護的東西更敏感**。<br>**預設是關的**，未 opt-in 的工作區在畫面上是 `尚未定值` 不是空白 | MVP 之後的社會證明功能整條 |
| ~~**乙-20（新入列 2026-08-22）**~~ **已裁定(內文已記,2026-08-23 補標)** | ~~**「Run 進行中可以關掉分頁嗎」需要有人確認過才能印在畫面上**~~ | [ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 2 的 §2.12 要求進行中的畫面說出「使用者能不能離開」，那是十分鐘承諾裡最常被忘記的一件。**但 §2.2 禁止顯示沒被強制的承諾**：Run 目前是背景執行、River 佇列消費，關掉分頁**應該**無害，而「應該」不能印在畫面上。要確認的是斷線後的 Run 是否確實跑完、Trace 是否照樣落地、以及取消是否仍可從別的地方發起。**這是一句 UI 文案卡在一個沒人驗過的行為上**，不是 UI 工作<br>**✔ 已裁定（2026-08-22 稍晚）：可以印，而且指得出強制它的那一行。** `run_execute` 是 Postgres 裡的 River job，由 `cmd/worker` 消費（`trial/execution/job.go:60` 的 `Kind()`，`cmd/worker/main.go` 註冊）；Trace 事件由沙箱直接 POST 回 API（`SKILLHUB_TRACE_INGEST_URL`），不經過瀏覽器。**兩條路徑瀏覽器都不在裡面**，所以關掉分頁既停不了 Run 也丟不掉紀錄，而取消隨時可以從那個網址再發動。<br>**誠實的殘留**：這是**讀架構讀出來的**，不是真的拔過網路線。實機斷線驗證屬於部署期（甲類）的範圍，本裁定只解除「這句話能不能寫」。<br>**2026-08-23 補標**:內文 2026-08-22 就寫著「✔ 已裁定」,`#` 欄一直沒劃掉 | 丙-35 |
| ~~**乙-21（新入列 2026-08-22）**~~ **已結案(2026-08-23 查證)** | ~~**`01` §2.1 的「學習者」persona 沒有對應路徑，而 §7.3 也沒有把它排除**~~ | `01` §2.1 把三種成熟度都列為主要 persona，第一種逐字是「**學習者：有明確任務意圖，但不了解 Skill 規格與建立方式**」，產品原則 1 也承諾「使用者不需要先理解 Skill 規格才能開始」。**產品給這個人的路不是「教你寫」，是「你永遠不從零寫」**——`/suggest-criteria` 幫他出驗收條件（TEST-002），`/suggest-improvements` 回的是 `proposed_content`（**完整檔案內容，不是 diff**，見 [evaluate.py](../../apps/llm/src/skillhub_llm/evaluate.py) 的 prompt），所以他從頭到尾不必打開編輯器，只要判斷接受或拒絕。**這條路是真的，但它有兩個前提，兩個都沒被寫成需求：**<br>**前提① 三類精選內容涵蓋得到他的任務。** 精選只有 `documents`／`writing`／`data`（PDM-001）。落在三類之外就掉到「外部結果」層，而那一層**要使用者自己提供 URL**——**一個不會寫 Skill 的人，通常也不知道去哪找 Skill 的 URL**。`01` §12 的風險表有「內容品質不足」，**沒有「內容涵蓋不到」這一列**。<br>**前提② fork 來的 Skill 與目標只差微調。** 改善迴圈能把一句話改對，**不能長出新的檔案**：[evaluate.py](../../apps/llm/src/skillhub_llm/evaluate.py) 的 prompt 寫死「Propose content only for a file whose current content you were given」，[suggest.go](../../apps/platform/internal/trial/improvement/suggest.go) 又把 `maxTargetFiles` 從契約允許的 10 砍到 5。**注意這是 prompt 關的，不是機制關的**——[apply.go](../../apps/platform/internal/trial/improvement/apply.go) 的 `patchArchive` 有一段專門處理「Files the package did not have yet」，**新增檔案的能力一直在，門是關著的**。<br>**要決的是這三選一**：**(a)** 明確寫進 `01` §7.3「MVP 不含從零建立 Skill」，並在無結果的搜尋畫面上誠實說出下一步（現在沒有）；**(b)** 等封測漏斗第一段（「輸入意圖後查看至少一個 Skill 詳情的比例」）量到再決，**同時把「內容涵蓋不到」補進 §12 風險表**，這樣封測才知道要看這個數；**(c)** 現在就開一條鷹架路徑（放寬 prompt 允許新增檔案，或做範本）。<br>**✅ 2026-08-23 裁定完成，結果是 (a) 與 (c) 的合成，不是建議的 (b)** → [ADR-046](../adr/ADR-046-generating-a-skill-from-a-task-description.md)。**負責人的決定是「這條路必須開」**，於是三選一裡沒有一項照原樣成立：**(a) 的一半照做**（`01` §7.3 補列、§12 補「內容涵蓋不到」與「生成物沒有任何證據」兩列、無結果狀態要說出下一步 ⇒ `03:GEN-008`），**(c) 的規格照做但實作不照做**——需求 ID、允收準則與工作項全部先立（[`02` §4.9](02-specifications-and-acceptance-criteria.md) `GEN-001`～`004`、[`03` §19](03-work-items.md) `GEN-001`～`011`），**實作列 M5**，啟動條件仍是 (b) 的那個讀數。<br>**三個裁定值**：①生成物**只在個人工作區，不進公開目錄也不進搜尋索引**——`tier.go` 三態全是「經過多少人工檢視」的階梯，生成物一格都不符合，不進目錄這個問題就整個不存在；②**新增獨立的 `POST /v1/generate-skill`**，**不放寬 `evaluate.py` 的 prompt**——`suggestionEvidence` 的引文回驗在「從零」上整條落空，在同一個端點養兩套語意，等於讓那條防線變成「有時候成立」（丙-38 剛示範過那種缺陷長什麼樣）；③**開 M5 里程碑**。<br>**兩件本列原文要更正的事**：**前提②「不能長出新的檔案」在 M5 仍然成立且是刻意的**——ADR-046 決策 3 明文否決了放寬 prompt 這條路，`apply.go` 的 `patchArchive` 那個能力**維持關著**，生成走的是另一條路；**「與丙-38 是同一條線」只對一半**——丙-38 已於同日結案（53 次評估 → 37 筆提案 → 24 筆存下、ApplyPreview 16/16 `applicable`），但**那個數字不能外推到生成**：改善有既有內容當骨架，生成沒有。M5 有自己的基線工作項 `03:GEN-009`。（**2026-08-24 訂正**：ApplyPreview（不加反引號：它不是符號）是 m3 報告發明的稱呼，**codebase 與契約都沒有這個名字**——程式是 `improvement.Diff`、契約 schema 是 `SuggestionDiff`，服務 `GET /suggestions/{id}/diff`。**16/16 這個數字本身是真的**，可追到 [m3/report-suggest-baseline.md](mvp/m3/report-suggest-baseline.md)。）<br>**本列到此關閉。** 未決的部分**沒有留在乙類**：同日稍晚的 [ADR-047](../adr/ADR-047-generation-path-rulings-retry-truncation-and-quota.md) 把 ADR-046 的五個待決策**一次裁完**（重試恰好一次且平台不改模型的位元組、截斷不重試、額度按「一次生成」計且失敗不扣、目錄維持不進但換成三個可檢核前置、**Fork 來源改為「可以」**——阻擋它才要寫程式）；同批稽核再發現「阻擋級檢查全部是結構性的」這句話**漏了 `possible-secret` 且被抄了三份**，由 [ADR-048](../adr/ADR-048-not-every-blocking-finding-is-a-random-slip.md) 修訂 ADR-047 決策 1 的適用範圍並同批訂正三處。**三份 ADR 都沒有留下待決策。**<br>~~**建議 (b)**：MVP 承諾（`01` §3）逐字是「找到一個可能符合需求的 Skill…下載可安裝的版本」，**沒有一個字說「做出一個新 Skill」，所以嚴格說沒有違約**；而 (c) 是在封測要買的資訊上先押注。~~**（保留原建議做為裁定當時的判斷記錄。它的第一句仍然成立且被 ADR-046 引用：MVP 沒有違約，所以這條路排在封測之後而不是插進去。）但 (b) 不等於什麼都不做**——現在的狀態是第四種：**沒人決定過**，`01` §7.3 既沒收它、§12 也沒列它，這正是 `QA-008` 當初「只活在別列旁註裡、從 8/18 等到 8/22」的同一個病。<br>**與丙-38 是同一條線**：提案品質的基線量的正是「機器改寫這條路能替不會寫的人走多遠」，前提②的鬆緊要有那個數字才決得準。 <br>**2026-08-23 查證結案:三件都落地了。** ①`01` §7.3 補上「從任務描述**從零生成** Skill」一列,並寫明**這一項不是永久排除是時程**(ADR-046,需求 ID 與允收在 `02` §4.9 `GEN-001`～`004`,實作列 M5);②`01` §12 新增「內容涵蓋不到使用者的任務」風險列,直接點名學習者 persona 走不完第一步;③`01` §2.1 的 persona 段落回填了指向 §7.3 與 ADR-046 的引用。**本列的病是「只活在別列旁註裡」,現在它在 `01` 自己的兩節裡有位置了。**<br>丙-38 的基線也已跑完([m3/report-suggest-baseline.md](mvp/m3/report-suggest-baseline.md)),**前提②的鬆緊現在有數字可以決**:回填 artifacts 之後 33 次提案呼叫裡有 15 次模型說沒什麼要改的 | 封測漏斗第一段的解讀；~~`01` §7.3 與 §12 的補列~~ **已補**；~~丙-38 的基線~~ **已跑** |

| ~~**乙-22（新入列 2026-08-23）**~~ **已結案（2026-08-27）：四個值照提案追認** | ~~**生成額度的四個數字待 PDM 追認，追認前不得畫在畫面上**~~（[`05` R-9](05-pending-rulings.md)） | [ADR-056](../adr/ADR-056-the-generation-allowance-is-its-own-switch-and-it-is-off.md) 依 [ADR-028](../adr/ADR-028-beta-admission-and-quota-enforcement-points.md) 決策 4 的分工把 `GEN-004` 的**強制點先建起來**，數字用 `policy.DefaultGenerateQuotaLimits()` 的提案值：**每日 10／每窗 30／首窗 20／窗長 30 天**，四個全部標 待追認。PDM-010 §8.1 只給了 Run 的數，生成當時還不存在。<br>**估的依據寫在程式碼裡，讓追認是複審不是重來**：一次生成實測 **$0.00553**（預設 mini）與 **$0.1186**（flagship；~~$0.113~~ 是 spike 報告訂正前的數），一次 Run 的閘道實付中位數是 **$0.0382**——**生成比試跑便宜**；使用者不滿意會改寫任務描述再跑，所以每日的數字比 Run 的 5 高，每窗的不高。<br>**不決定的代價**：ADR-028 決策 4 允許先建點，但**禁止未追認的數字出現在畫面上**。`02:GEN-001` 要求生成前顯示「本次將消耗的額度」，那句話在追認前只能顯示成本、不能顯示額度。**目前 `GENERATE_QUOTA=off`，這條自動成立**（沒有額度可顯示）——**但開關一旦打開，這個限制要有人記得**，而記得它的地方就是本列。<br>**✅ 2026-08-27 結案：四個值照提案追認**（每日 10／每窗 30／首窗 20／窗長 30 天，[`05` R-9](05-pending-rulings.md)、[m0/pdm-proposals.md §9.1](mvp/m0/pdm-proposals.md)）。**追認是複審不是重來**——讀的就是上一段那三個實測數，`policy.DefaultGenerateQuotaLimits()` 與它的四個常數一個值都沒有改，只有註記換了。<br>**結案時要把兩件被本列綁在一起的事拆開，因為它們看起來像同一件**：**解除的是禁令，不是開關。**ADR-028 決策 4 的「未追認的數字不得出現在畫面上」對這四個數不再成立；而 `GENERATE_QUOTA=off`（[ADR-056](../adr/ADR-056-the-generation-allowance-is-its-own-switch-and-it-is-off.md)）**沒有變**，所以**今天畫面上仍然沒有額度可顯示**——`02:GEN-001` 的「本次將消耗的額度」還是空的，但理由從「數字沒簽名」換成「沒有東西在被強制」（乙-2 的既有紀律）。<br>**本列自己那句「開關一旦打開，這個限制要有人記得」可以放下了**：開關打開時，數字已經有簽名。 | ~~`GEN-008` 的生成前額度顯示~~ **已結案（2026-08-27）**——但額度顯示仍等 `GENERATE_QUOTA` 打開，那是部署設定不是本列 |
| ~~**乙-23（新入列 2026-08-25）**~~ **已裁定 2026-08-27：追認 10 MB／100 MB** | **兩個匯入上限比 PDM-005 §5.1b 高 3.2 倍，而程式註解說這件事「記在 `04`」——`04` 裡沒有** | `skillpkg.MaxZipBytes` ＝ **32 MiB**，`maxUnpackedBytes` ＝ **256 MiB**；PDM-005 §5.1b 說的是**壓縮檔 10 MB／解壓後 100 MB**。**差距是刻意留著的，理由寫在 `archive.go` 的常數旁邊而且成立**：「調低會拒絕今天匯入得成功的套件，那是產品決定不是可以順手修掉的缺陷」。<br>**要記的不是那個差距，是那句宣稱**：同一段註解逐字寫著 `Both are recorded in 04 rather than changed here.`——**而 `04` 從來沒有這一列**（2026-08-25 查證，`32 MiB` 全檔零命中）。一個把自己的待辦寫在別人檔案裡、而別人不知道的註解，等於沒有記。**本列就是那句話說的那一列。**<br>**還有第三處也說著 10 MB**：`02:SEC-003` 的抓取器允收逐字寫「回應體上限 **10 MB**（同 PDM-005 §5.1b 的上傳套件壓縮檔上限，**兩條匯入路徑共用同一組限制**）」。**共用這件事程式做到了**（`fetch` 與上傳都用同一個 `MaxZipBytes`，這是對的形式——兩個數字遲早分歧），**共用的那個值不是 10 MB**。所以要裁的是一個數字、影響三個地方（上傳、URL 匯入、`02` 的允收句）。<br>**兩條路**：(a) 追認 32 MiB／256 MiB 並把 `02:SEC-003` 那句與 §5.1b 一起改成實際值；(b) 調低到 10 MB／100 MB，並接受今天匯入得成功的套件有一部分會開始被拒。**(b) 需要先量「有多少現存套件在 10 MB 與 32 MiB 之間」**——那是一個查得出來的數字，不是判斷。<br>**不決定的代價**：三份文件各自說著一個平台不執行的數字，而 `02` 那一句是**允收準則**——`INGEST-014` 因此結構上不可勾，不論抓取器寫得多好。 <br>**2026-08-25 同日：數字量到了，本列因此升為 [`05` R-13](05-pending-rulings.md)。** 45 個套件物件，**最大 0.156 MB**、p95 0.066 MB、中位 0.005 MB，**落在 10 MB 與 32 MiB 之間的：0 個**。所以 (b) 的代價**今天是零**——「調低會拒絕今天匯入得成功的套件」不是錯的推理，是還沒有人去量的推理。**但那 45 筆是我們自己挑的內容，不是封測使用者帶來的**，所以它殺掉的是 32 MiB 的理由，不是證明 10 MB 明天夠用。<br>**同批查到第三個用途，而它改變做法**：`packaging.go:513` 用同一個常數擋**產出的**套件，而打包必然變大 ⇒ 任何數字下都有一段「匯入成功、最後一步被拒」的死區。**與裁定無關，已開 `03` PACK-012**；拒絕訊息沒有數字也與裁定無關，已開 `03` INGEST-016。<br>**✅ 2026-08-27 裁定：追認 §5.1b 的 10 MB／100 MB。** `skillpkg.MaxZipBytes` 32 MiB → **10 MB**，`maxUnpackedBytes` 256 MiB → **100 MB**；兩條匯入路徑仍共用同一個符號（共用的形式一直是對的，換掉的是值）。**四個工作項同日勾選**：`INGEST-014`、`INGEST-016`、`PACK-012`、`SEC-003`。<br>**落地時補了一個本列沒要求的東西，而沒有它這個裁定會安靜漂回去**：`TestTheImportCeilingsAreTheRatifiedOnes` 把兩個值當**字面數字**斷言。既有測試全部透過符號取值——**所以它們在任何值下都是綠的**，那正是這個常數在 32 MiB 待了好幾個月而沒有任何機器出聲的原因。把它改回 32 MiB，該測試如期紅。**它刻意不引用任何共用常數**：一個讀著自己要守的那個變數的守門，什麼都沒守。<br>**那扇單向門的可逆側是有期限的**：今天往下調的代價實測為零，**第一個超過 10 MB 的封測套件進來之後就不是零**——`INGEST-016` 的 `upload` 計數器從今天起就是這個上限的安全網，出現讀數就是該調高的訊號。 | `02:SEC-003`；PDM-005 §5.1b；`03` INGEST-014／PACK-012／INGEST-016；[`05` R-13](05-pending-rulings.md) |
| ~~**乙-24（新入列 2026-08-26）**~~ **已追認 2026-08-27** | **威脅模型 Q16 從來沒有人回答，而它是 `SEC-001` 自己寫的勾選前提**（[`05` R-14](05-pending-rulings.md)） | `02:SEC-001` 的允收段逐字寫著「**勾選前提**：Q15 與 Q16 已有負責人答覆」。Q15 在 2026-08-16 裁定；**Q16 至今零答覆，而且它從來沒有出現在 `05` 上**。要追認的是一個範圍決定：後 MVP 的 8 條威脅（TM-MCP-01～04、TM-LOC-01～04）緩解工作不排入 M0–M4。**交付物本身完整**——`02:SEC-001` 的每一條實質允收在威脅模型都有落點且文件是活的。**這一項不需要任何新資訊**：沒有要等的資料、要問的人或要跑的實驗，而那 8 條指向的功能（遠端 MCP、Local Runner）本來就已在三個地方被移出 MVP 首發。<br>**✅ 2026-08-27 照現況追認**，威脅模型 Q16 已回填，`03:SEC-001` 同日勾選。**8 條仍留在文件、仍標 `【後 MVP】`——追認的是排程不是涵蓋。**<br>**它躺這麼久的原因值得記**：一個擋著勾選的簽名寫在威脅模型的一張表裡，**不在「我現在要簽什麼」那一頁上，就不會有人簽它**。乙-25 是同一天發現的同一個形狀 | `03` SEC-001、`RELEASE-004` |
| ~~**乙-25（新入列 2026-08-26）**~~ **已裁定 2026-08-27：不加 macOS** | **`QA-008` 的最後一格寫在 ADR-036 的待決策裡，不在任何簽名清單上**（[`05` R-15](05-pending-rulings.md)） | [ADR-036](../adr/ADR-036-real-browser-verification-tier.md) 待決策 1 逐字：「**在做這個決定之前 `QA-008` 不得勾選**」。要決定的只有一件：`web-browser` 矩陣要不要加 `macos-latest`。**現況是四格**（ubuntu × 三引擎 ＋ windows × chromium），「主要瀏覽器」那一半已滿足。**macOS 買不到它看起來買得到的東西**——Playwright 在 macOS 上跑的一樣是自編 WebKit，真 Safari 不在任何一格裡；它多出來的是第三種字型度量，而捲軸行為與 Linux 那一格相同，價格是 Windows 的 5 倍。**建議不加並寫成 ADR-036 補記，然後勾**。<br>**✅ 2026-08-27 照建議裁定。** ADR-036 待決策 1 已回填理由與重評條件，`03:QA-008` 同日勾選。**要寫清楚它沒有買到什麼**：加 macOS 也得不到真 Safari，所以那個缺口不是被裁定關掉的，是被寫明它關不掉——要關得換驗證層的形態，不是換一格 runner。<br>**與其他乙項相反的地方**：不決定不會讓任何東西壞掉，**但它會讓一個已經做完 4/5 的東西永遠記成沒做** | `03` QA-008、`RELEASE-001` |
| ~~**乙-26（新入列 2026-08-27）**~~ **已裁定 2026-08-27：300 行改為審閱觸發線** | **精選裡有一筆超過 300 行 Script 上限，而舊紀錄低報了它**（[`05` R-16](05-pending-rulings.md)） | `CONTENT-006` 的人工逐行審閱於 2026-08-27 完成，**⑤ 15／15 pass、④ 14／15 pass**，唯一 FAIL 是 `data-analyst`：合計 **651 行**（內嵌 448 ＋ `data_ops.py` 203），是上限的 2.17 倍。**舊紀錄寫 203，只算了檔案沒算內嵌**——同批重數發現十五筆裡有十筆的 `script_lines` 不符，而這一筆是唯一一筆低報到跨過上限的。<br>**它不違反任何一項禁止、人工也讀完了，問題只有「太大」**，而 300 行這條線要的正是可審閱性。三個選項（降到已索引／放寬 300／換候選）與代價見 R-16，**建議降到已索引層**。<br>**✅ 同日裁定：三個選項都不取，取第四個**——把 300 行從**准入線**改成**審閱觸發線**（`02:497` 已修訂：超過 300 行者，逐行審閱結果必須落成具名文件並在精選清單中連結，否則維持 `pending`）。`data-analyst` 滿足該條件，④ 記 `pass`，15／15，**`CONTENT-006` 與 `CONTENT-003` 同日勾選**。**降級沒有被採用的理由是裁定當下才查全的**：`seed_testcases.py` 只為 curated 種入測試案例，降級會讓它失去已種入的案例、`CONTENT-007`／`008` 的「15／15」變成 14。**而換掉的是代理指標不是目的**——300 行買的一直是「一個人讀得完」，這一筆真的被讀完了。<br>**它擋著兩個工作項**：`CONTENT-006` 直接卡，`CONTENT-003` 的條件（⑤∧⑦）現在都 pass 了，卡的是 `02:454` 那句「即 `CONTENT-005`／`CONTENT-006` 完成」 | `03` CONTENT-006／CONTENT-003；[report-curated-checks-4-5.md](mvp/content/report-curated-checks-4-5.md) |

> **新入列並同日解除:威脅模型 Q17(安全事件嚴重度分級與值班歸屬)**——本表未曾為它開一列,因為它在 M2 完結時還只是 `02:SEC-010` 待決策區的一份提案。**2026-08-16 經負責人核可(`934db2b`)**,分級表、通知路徑與遮罩失敗 runbook 自即日起成為 `02:SEC-010` 的允收準則;值班輪替的討論待團隊有第二人時重開。<br>**核可同時產生一個新的部署期義務**:分級表的「自動動作(不等人)」欄要求 **P1 的第一動作必須是自動的**(立即停止派送新 Run ＋ drain 受影響節點 ＋ 保留現場),而此前**沒有任何工作項承接它**——已新增 `03` **SEC-012**(未勾)。實作要求寫在該項:與 **ADR-022 X-04 的暫停／drain 是同一個開關的兩個觸發者**,必須共用同一份狀態與同一條解除路徑(做成兩套會讓「現在到底有沒有在派送」沒有單一答案);**落地時機在部署批之前**,因為這條準則的價值全在「不等人」——開放外部使用者提交 Skill 執行時 P1 若仍需手動停派送,`02:SEC-010` 的設計前提(團隊是一個人)就直接落空。解除**不得**自動,並由 SEC-009 測項 T7 一併覆蓋。
>
> **CONTENT-005 揭露缺口已關閉(`afb5767`,2026-08-16 完結後回填)**:[content-baseline-report.md §7.2 #1](mvp/m2/content-baseline-report.md) 指出的 11 筆(`data-shape`、`docx`、`excel-delete`、`excel-filter`、`excel-find-duplicates`、`excel-mapping-replace`、`excel-merge`、`excel-sort`、`excel-split`、`excel-validate`、`pdf`)「限制」欄漏 Python 執行依賴,已依 `02` §4.7 `需修改` 流程把 prompt 升至 `enrich-skill/v5`(改的是通用條款,非 hardcode 個案)並重跑增強與重新索引,**11/11 揭露缺口全關**。
>
> ~~**但 CONTENT-005 現為 44/45**~~ → **2026-08-16 回到 45/45**:`docx` 依負責人路徑 ② 裁定升 `enrich-skill/v6`,A＋B 兩輪後通過,線上文字指紋 `4df1c13f96c987781f110b7d2232f9fe`([content-review-report.md §13](mvp/m1/content-review-report.md))。`02` §4.7 的非決定性上限條款照樣履行:判定只對現行入庫文字生效,舊版 v5 文字未留存(投影就地覆寫),§11 與 §13 兩份表格各自對應各自那版文字。**CONTENT-005 勾選維持**,CONTENT-003 檢查 ⑦ 維持 `pass`。

> **已關閉(`ddc3e54`)**:`apps/sandbox/README.md:112` 原記載被推翻**前**的 SDK 行為(「`settingSources` 含 `project`」),0.3.233 上照做會得到零個 skill。已改為條件表(`cwd`／`settingSources` **省略**／`skills: "all"`／`allowedTools` 傳 `'Skill'` 已 deprecated),並寫明反轉的實測依據與「SDK 升級必須重新實測、不得推理帶過」。~~**仍無 ADR 記錄此次行為反轉**,版本只釘在 `Dockerfile` 的 `ARG CLAUDE_AGENT_SDK_VERSION=0.3.233`。~~ → **2026-08-16 已關閉**:[ADR-023](../adr/ADR-023-agent-sdk-version-pinning-and-behaviour-revalidation.md) 記錄此次反轉,並定下版本釘選(digest ＋ lockfile,不用語意版本範圍)、升級必須重跑的四項實測(skill 載入條件／閘道相容/401／prompt caching 計費欄位與 token 對帳／`usage` 事件的發出條件)、「靜默失效不得用推理帶過」原則,以及證據落檔位置 `infra/images/runtime-agent-sdk/UPGRADES.md`。

## 丙、里程碑接點

**2026-08-17 M3 完結後重整。** 本節原標題為「移交 M3(EVAL)的接點」,七項接點**全部已由 M3 處理完畢**——逐項結案理由見下表右欄與 [m3/audit.md](mvp/m3/audit.md)。M3 自己產生的新殘項與 M4 接點另列於 [丙(續)](#丙續m3-完結後新增移交-m4打包與封測)。依維護規則,舊列**就地標結案、不刪除**。

### 丙(原七項):移交 M3 的接點,**七項全數結案**

| # | 接點 | 內容 | M3 結案 |
| --- | --- | --- | --- |
| 丙-1 | **讀取面:直接用 `trace.Service`** | `Advanced` 已回傳依序重建、標明缺失(`missing_seq`／`late`／`complete`)的完整事件流,`General` 已是聚合摘要。**EVAL-001 引用證據時直接用這兩者,不要自行查 `trace_events`。** 對應的 rubric 缺口見乙-5 | **✅ 照辦**。`internal/eval` 全 package **沒有任何直接查 `trace_events` 的路徑**——證據來源只有 `trace.Service.Advanced`／`General`,`comparison.go` 亦然。`complete: false` 時逐條判定的 `passed` 一律降 `undetermined`(`judge.go` 的 `merge()`),且 `evidence_complete` 隨判定一起存進 `evaluations`,不靠讀報告的人自己注意 |
| 丙-2 | **寫入面:沿用 `trace.RecordOrchestratorEvent`** | 控制平面已會在 `failed`／`timed_out` 轉移的同交易內寫 `error` 事件,`seq` 由 `NextTraceSeq` 在交易內配號。EVAL 要加自己的事件(評估開始／結束)沿用它,**不要另開寫入路徑** | **✅ 照辦**。`evaluation_started`／`evaluation_completed` 兩個事件沿用它,與它們所描述的資料列**同一交易**提交(鐵律 9);事件 schema 升 **1.2**(additive,1.0／1.1 事件仍合法)。評估失敗時 envelope 為 `error` 且帶 `failure_reason`——「評估壞了」與「Judge 看了說不出來」不會被畫成同一件事 |
| 丙-3 | **成本合計仍是下界,但不再有整筆消失的 Run**(2026-08-16 已修) | ~~`run.mjs` 的 `usage` 事件**只在 SDK `result` 分支內發出**~~ → **已修,見 `03` TRACE-009**。harness 現在逐則訊息累計每次模型回應的 token,run 結束時一律發一筆 run 級 `usage`:有 `result` 以其為準(`token_source: result`),沒有(崩潰、牆鐘、token 上限中止、或 SDK 就是沒給)則發累計值(`token_source: accumulated`);兩條路徑的 `cost_usd` 都照舊向閘道要,`cost_source` 維持 `gateway`。事件 schema 因此升 **1.1**(additive)。實例 `add-iso3166` 那類「Run 成功但成本不存在」的情形不會再發生。<br>**留給 M3 的仍然是下界,但下界的成因換了**:①累計值不含「串流結束時仍在途中的回應」;②`cost_usd` 是對閘道的一次讀取,最後一次 flush 若落在讀取之後就少算。**權威對帳來源仍是閘道 per-key spend(ADR-017)**,EVAL-012 引用 Trace 合計時必須標明它是下界。<br>另一個原有結論不變:`complete: true` 只保證「發出的事件沒遺失」,不保證某類事件存在。UI 誠實——無 usage 事件時顯示「沒有記錄到用量事件。」而非 0 | **✅ 照辦,且升級為契約義務**。`RunComparison.cost.is_lower_bound` 在 `public.yaml` 裡是 **required 且恆 true 的欄位**,不是旗標也不是呈現慣例——把誠實義務放進契約,就不可能在某個畫面被忘記;權威來源字串由回應自己帶著走。**Run 成本與評估成本分兩列且明文不相加**(比較畫面、`Evaluation.cost` 的 note、`costFindings` 三處都寫著)。無 usage 時說「未知而非零」 |
| 丙-4 | **`skill_activation` 的 `skipped` 不可觀測** | SDK 訊息流看不到「可用但沒被叫」,M2 不臆造。判斷「Skill 沒被啟用」屬 EVAL-002,材料是「Run 掛了哪些 Skill」對照「trace 裡出現了哪些 activation」 | **✅ 照辦,措辭上限是程式碼常數不是風格建議**。`deterministic.go` 的 `activationFindings()` 在沒有 activation 事件時只說「沒有出現啟用事件」並附一句「模型看到卻選擇不用在訊息流裡不可觀測(TRACE-002)」;具名測試 `TestActivationNeverClaimsTheModelChoseNotToUseTheSkill` 直接反證那句話不會出現 |
| 丙-5 | ⚠️ **`succeeded` ≠ 任務完成**<br>**2026-08-17 已由 ADR 回答** | `run.mjs` 的 `finish("succeeded")` 只代表「agent 這一輪沒有拋錯」,**與任務是否完成無關**。實例 `date-wrangling`:終態 `succeeded`、Skill 有啟用、Trace 完整,而 `/out/artifacts/` 是空的——最終回覆是反問使用者。**UI 若把 succeeded 呈現為「可用」會誤導;判定任務是否達成正是 EVAL-001 的工作**<br>→ **[ADR-025](../adr/ADR-025-run-terminal-state-and-evaluation-verdict-separation.md)（2026-08-17 Accepted）**:`runs.status` 與 `evaluations.overall` 是兩個問題兩個欄位,**評估不回寫 `runs.status`／`failure_class`**;`internal/run/job.go:419` 那條寫著「evaluation 決定 succeeded vs failed」的 TODO 已被明文推翻,**程式碼改寫排在 M3 第 2 批**(尚未執行)。UI 的「未評估」／「評估未完成」／四態任務判定三種呈現要求同記於該 ADR。重評、證據壽命與 Judge 信任邊界另見 [ADR-026](../adr/ADR-026-evaluation-reassessment-evidence-lifetime-and-judge-trust-boundary.md) | **✅ 已關閉**。①`internal/run/job.go` 那條 TODO **已從程式碼裡消失**(不是加註解),`successReason` 改為執行語意;②評估**不 UPDATE `runs` 任何欄位**,具名測試 `TestEvaluationIsRecordedWithVerifiedEvidenceAndNeverTouchesTheRun`;③UI 三種呈現齊備:「未評估(不是通過)」／「評估未完成」／四態任務判定**另起一列**且排在執行狀態之前,`eval.test.tsx` 兩個具名測試鎖住。**丙-5 的實例本身也變成一條規則**:`artifactFindings()` 會對「回報成功而沒有任何產出檔案」的 Run 產生 finding |
| 丙-6 | **可比較的基準已在庫** | 45 個 Skill 各一次完整平台 Run 的結果、Trace(1112 事件全數 `masked`)、Artifact manifest 與 Test Case 快照都可重查(對帳 §2.3 已實查驗證)。EVAL-011／012 的「重新試跑與比較」有現成的第一組對照 | **✅ 已使用,但用法與預期不同,要記下來**。這 45 筆確實成為 `EVAL-013` 回歸集的第一組標註資料(集合由 `skill_runtime_compatibility.source_run_id` **查出來**而非抄出來,可稽核)。**但它們沒有成為 `EVAL-011／012` 的第一組對照**——比較需要兩個可比的 Run,而這 45 筆各自只有一次。另:manifest 其實**不在資料庫裡**(`artifacts` 全表 0 列),harness 是直接讀封存索引拿到的,見丙-13 |
| 丙-7 | **`RunResult.usage` 只有牆鐘** | token 與成本走 Trace,沒有回填到 provider 的 result。契約已於 `eab9d26` 改為誠實敘述(「a consumer must not treat an absent token count here as zero usage」)。EVAL 需要 provider 側 usage 時要走 additive 契約變更 | **✅ 維持原判,M3 沒有動它**。比較畫面的成本取自 trace 的 `usage` 事件即足夠,`sandbox-provider.yaml` 一個位元組未改(contract-deltas §6 明列不動)。**這一條不是關閉而是確認**:要 provider 側 usage 仍然是 additive 契約變更,屆時另議 |

### 丙(續):M3 完結後新增,移交 M4(打包與封測)

**全部是「有承接者就做得完」的實作或資料工作,沒有一項在等決策**(等決策的在乙)。逐項證據見 [m3/audit.md](mvp/m3/audit.md)。**2026-08-17 更新:丙-11 已結案；同日新增丙-14（自 `03` §3 的 DESIGN 收斂裁定交棒而來），因此原六項現為七列、尚開著六項**(依維護規則就地標結案、不刪列)。**2026-08-18 再更新（M4 第 7 批前半）：丙-12 結案，同時新增丙-15**（由 `QA-002` 的破壞語料查出），八列、仍開著六項。**同日稍後：丙-15 亦結案**（三個 archive 層缺陷已修，見該列），八列、**仍開著五項**。

| # | 殘項 | 內容 | 觸發／解除條件 |
| --- | --- | --- | --- |

> **丙-8／2026-08-22 補記：取樣缺口已由 selector 收斂，但 B 輪仍未執行。** `tools/eval-regression/judge_regression.py` 已新增可重複指定的 `--run-id`，可明確選取這 5 個新 Run，不再依賴不會增長的 `skill_runtime_compatibility` 基準清單；先以五個 `--run-id` 做 `--dry-run` 驗證選取結果。該 dry-run **仍會 live read** Run、Trace 與 artifact，但**不呼叫 Judge、也不寫入結果**。實際 B 輪仍待 5 個新 Run 建立完成，並在確認輸入後執行會產生成本的 Judge 回歸。

| 丙-8 | **`EVAL-013` 的 B 輪回歸未跑** | B 輪 ＝ 依 [content/writing-rubrics.md](mvp/content/writing-rubrics.md) §3 修訂 Prompt(加第 4 條「最終回覆必須完整貼出正文」)後**重跑那 5 筆 Run**,再以同一份 rubric 評估。它測的是 A 輪測不到的東西:**證據完整時,逐項引文的實判定**。<br>不在 M3 第 7 批的理由是它要發 5 次真實 Run(沙箱、映像、閘道費用,非只多 5 次 Judge 呼叫)並產生新的 Test Case 快照,屬 Run 批而非接線批。<br>**A 輪足以關閉 `CONTENT-007` 的 G4 與驗證接線後的形狀,不足以宣稱「rubric 判得準」**——`02:EVAL-013` 報告 §2 的「沒測到」第一格(主觀的任務效果判定)因此**仍然開著**。另兩格(注入抵抗、證據殘缺下的保守性)亦零覆蓋,樣本可合成、不需真 Run,見回歸報告 §8.2 建議 2／3<br>**2026-08-17 更新(M4 第 5 批)**:三格變成**一格已測、兩格仍開**。**注入抵抗已測**——13 個合成樣本、27 條判定、**讓步 0**,涵蓋四條防線各自的攻擊面,閘道實付 $0.05688,見[回歸報告 §12](mvp/m3/report-judge-regression.md);harness 與樣本集在 `tools/eval-regression/injection_regression.py`／`injection-samples-v1.json`,結果另檔 `injection-results.jsonl`(與 45 筆分開統計)。**B 輪仍未跑,而原因已從「排期」變成「查清楚的阻擋」**:執行中的 dev DB 未套用 `0024`／`0025`／`0026`,HEAD 的 `cmd/api` 建立 Run 的必經路徑會寫 `test_case_snapshots.rubric`,**在這個 DB 上開不了 Run**;改用執行中的舊 api 二進位則沒有 `SKILLHUB_MODEL_GATEWAY_*`,沙箱會被派到 `--network none`。四段查證、解除步驟與要改的 5 個 `test_case_id` 見[報告 §13](mvp/m3/report-judge-regression.md)。**第三格(證據殘缺下的保守性)維持零覆蓋**:注入樣本刻意全部 `complete = true`,否則 `undetermined` 會歸不了因 | ~~`CONTENT-007` 的 Prompt 修訂與 5 筆重跑~~ **解除條件已具體化**:①套用 `0024`／`0025`／`0026`;②另起帶閘道環境變數的臨時 api／worker／sandboxd;③依報告 §13.4 的 5 個 `test_case_id` 走 `PATCH /test-cases/{id}` 改 Prompt;④走既有 Run 路徑重發 5 筆;⑤`judge_regression.py --rubric` 重評。**第 ⑤ 步前要先確認新 Run 進得了 `skill_runtime_compatibility`**,否則 harness 取到的仍是 M2 的舊 Run 而且不會有任何提示。<br>**不擋任何工作項的勾選**，`EVAL-013` 的範圍註記已誠實寫明它不涵蓋仍開的那兩格。<br>**2026-08-18 補記：第 ⑤ 步前的那句「先確認新 Run 進得了 `skill_runtime_compatibility`」已經查明，答案是「不會」。** `judge_regression.py` 的取樣**只從 `skill_runtime_compatibility` 拿 `source_run_id`**，而那張表由基準試跑回填、不隨新 Run 增長——所以 B 輪跑完 5 筆之後**還要補一步**把它們納入，否則 harness 會安靜地重評 M2 的舊 Run 而且不會有任何提示（原文已經預告了這個風險，本次把它從「要確認」變成「確認了，是這樣」）。其餘阻擋不變：要一套跑得動的 dev stack、5 次真實 Run、閘道實付約 $0.2–0.4。<br>**2026-08-23 結案，B 輪跑完了,報告見 [report-judge-regression.md §14](mvp/m3/report-judge-regression.md)。** 5 筆全新真實 Run 全部 `succeeded`,閘道實付 **$0.3721**(Run $0.2312 ＋ Judge $0.1409),落在估的 $0.2–0.4 內。<br>**第 4 條有效**:`agent_output` 平均長度 **43 → 999 字元**。可計分的兩項 **10／10 一致、0 判錯**。<br>**但預測錯了**:22 個 rubric 項的 `undetermined` 從 A 輪的 3 **升到 5**,而且 **5 筆全部是平台降級**(A 輪是 0);模型其實都給了判定(2 passed／3 failed)。`passed` 9→11、`failed` 10→6 同時發生,所以第 4 條在「讓文字可判定」上是有效的,**代價在回驗那一關**。<br>**根因是 §6.1 缺陷的同一個形狀往下一層**:送給 Judge 的 digest entry 是原始 JSON,模型讀到 `\n` 之後引用時寫成真正的換行,而 Go 拿它去 `strings.Contains(payload)`——**帶真換行的字串永遠不可能出現在把它寫成 `\n` 的 JSON 裡**。**同一個事實的兩種呈現,驗的和看的不是同一份。**新增丙-48;**本批不改 defence 3**,那是有 ADR 的防線,而今天這份量測正好是任何修改該有的依據。<br>**路上絆到四件**:①P1 自動停派送誤觸(丙-49,已修);②PDM-010 額度用既有逃生門 `RUN_QUOTA=off`;③**harness 的 `explicit_run_set` 會製造 5 筆假的不一致**——`expected()` 拿 `skills.name`(fork 是 `<parent>-fork`)比對 activation 事件裡套件自己宣告的 `<parent>`,M2 路徑碰不到因為那條路只選目錄 Skill、兩名相等。**dry-run 先跑一次(§13.3 寫進步驟的那一步)在花錢之前抓到它**;④harness 沒帶 `LLM_SERVICE_TOKEN`,第一次付費呼叫就 401 |
| 丙-9 | ✅ **已結案（2026-08-18，`bf86f01`）：對帳器已落地，且它修正的是資料而不是讀取路徑** | `internal/objreconcile` 一小時一輪、**兩個方向各掃一次**：到期的下載套件失去位元組，而物件已經不見的列停止宣稱自己還在。**`RunInputsStillAvailable` 一行未改**——標記遺失的 dataset 為已刪除就修正了那個樂觀上界，不必在讀取路徑上加探測（那是為一個已經在手邊的答案買一次來回）。**沒有任何東西以單次觀測定案**：連續兩輪才標記，計數存在 `object_reconcile_sightings` 表而不是行程內狀態（理由與 `0021` 數 sandbox orphan 相同——洩漏活得比行程久），而**連不上儲存產生零次觀測而不是一次假的**。`0028` 另加 `artifacts.purged_at`，讓「這個到期了」與「這個從來沒存在過」在 `WS-004` 上是兩個不同的答案。與 `CORE-007` 同批承接，如本列右欄所預期。測試 `TestTheReconcilerNeedsTwoRoundsBeforeItMarksAMissingObject`、`TestAnExpiredArtifactIsNotServedAndItsBytesAreSweptAway`。<br>**一項轉部署期**：對帳器**刻意沒有 `RunOnStart`**，所以部署後第一小時是空窗（[m4/release-checklist.md §2.6](mvp/m4/release-checklist.md)）。**原殘項內容保留於下** | `RunInputsStillAvailable` 只讀平台自己的刪除與到期欄位(`test_cases.deleted_at`、`datasets` 的存在與 `expires_at`),**不探測物件儲存**。欄位還在而物件已被清掉時它仍回 `true`,比較畫面就會提供一條重跑連結,而重跑會在取檔時才失敗。<br>**接受它是上界的理由成立**:那些欄位正是清理流程動作的依據,而每次比較每個 dataset 打一次 HEAD 是為一個已經在手邊的答案買一次來回。**但「通常一致」不等於「一致」**——需要的是一個週期性對帳器(比對 `datasets` 列與物件實際存在),不是在讀取路徑上加探測。<br>同型前例:`SBX-012` 的 orphan 對帳器也是用資料表而非行程內狀態,理由相同(洩漏活得比行程久) | 沒有工作項承接。與 `CORE-007`(使用者資料與 Artifact 刪除流程)重疊,可一併承接 |
| 丙-10 | ✅ **已結案（2026-08-18，`8b7d840`）：畫面現在分得出來，而且用了兩個非顏色通道** | `RunEvaluation.tsx` 以 `EVIDENCE_UNVERIFIABLE_PREFIX`（前綴由 `internal/eval/judge.go` 的 `merge()` 寫入）判定，徽章文字改為「**證據無法回驗**」而不是「無法判斷」，旁邊一句「判定來源：平台降級……**這不是『模型自己說不知道』**——那一種會顯示為『無法判斷』」，第三通道是邊框樣式（程式碼註解明指那是唯一可以被漏看而不失去事實的一條，`NFR-007`「不只依賴顏色」）。<br>**具名測試同時反證了另一半**：`丙-10 a verdict downgraded for unverifiable evidence is not shown as a judge who does not know` 一併斷言純 `undetermined` 的那一條**仍然**顯示「無法判斷」且不含「平台降級」也不帶那個 class——**兩種狀態各自守住自己的措辭，才叫分得出來**。該狀態也進了無障礙掃描的 fixture，所以降級樣式被 axe 掃過。<br>**與乙-13（G7／G8）不是同一件事**：本列是呈現面，乙-13 是資料層的判準鬆緊，**仍開著且要拍板**。**原殘項內容保留於下** | 兩者在畫面上都是「無法判斷」。而 `EVAL-013` v1 抓到的那個缺陷**正是這個形態**:45 筆判定正確、理由正確、引用的 id 正確,只有引文多了一個前綴,整條被防線 3 丟掉——呈現出來與「Judge 沒把握」無法區分。<br>資料層已經分得出來(`reason` 以 `evidence_unverifiable: ` 開頭),缺的只是呈現面把它標出來。**不標的代價是那一類缺陷在線上是隱形的**,而它已經真的發生過一次 | 沒有工作項承接;回歸報告 §8.2 建議 4。屬 `EVAL-001` 的呈現面,補上不需新決策 |
| 丙-11 | ✅ **已結案(2026-08-17):新版本的 id 已有畫面交給 preflight,`EVAL-011` 改判勾選** | `RunPreflight.tsx` 的 `version` 只能從 URL query 進來,頁面上沒有這個欄位(該檔 scope 註解已誠實寫明)。而 M3 的主路徑是「評估 → 採納建議 → 建出新版本 → 用同一個 Test Case 重跑新版本」,走到第四步時 `AppliedResult` 只顯示版本序號與內容雜湊並連到 Skill 頁——使用者得自己去 Skill 頁抄版本 ID 再手改網址列。<br>**比較畫面那條把三個 id 都填好的連結解不了這一步**:它要求兩個 Run 都已存在,而這裡正是要產生第二個。這是循環,不是替代路徑。<br>**補法很小**:preflight 頁加一個版本選單(讀既有的版本清單),或 `AppliedResult` 直接連到帶著新 `version` 的 preflight。任一都不需新端點、不需新決策。<br>**這是同一種退回的第三次**:`TEST-004`(缺顯示面)→ 補 `/lab/datasets` 後重勾;`TEST-003`(缺操作面)→ 補 `TEST-012` 後重勾;`EVAL-011`(缺交接面)→ 待補 | **✅ 已解除**。`AppliedResult` 現在直接連 `/lab/run?skill=&version=&test_case=`,三個 id 分別取自套用端點的回應與 `GET /runs/{id}`,連的是執行前權限確認畫面而不是開始 Run(`TEST-009` 仍在路徑上);`test_case_id` 缺席時整條連結拿掉並改為說明,同 ADR-003。**兩個補法只取了較小的那個**——preflight 頁的版本選單仍然沒有,它屬 `DESIGN-007` 的範圍而不是本項的解除條件。`03` EVAL-011 已勾,理由見 [m3/audit.md §2.1](mvp/m3/audit.md) |
| 丙-12 | ✅ **已解決（2026-08-18，M4 第 7 批前半）：種入路徑已存在並在拋棄式容器上驗證。** [`tools/content/seed_testcases.py`](../../tools/content/seed_testcases.py) ＋ [`seed-testcases/`](../../tools/content/seed-testcases/) 的兩個範例 Dataset。**15 筆策展 Test Case、67 條驗收條件、5 份 rubric（22 個 item）** 一次種進全新部署，逐項驗證見 [m4/README §14.2](mvp/m4/README.md)。**與 `PACK-005` 的兩半在同一次驗證裡接上了**（M4-3）：種入的 Test Case 被 `packaging/preview` 列為 `included`，下載的 artifact 位元組裡確有 `case.json`（帶 `rubric_version`）與兩個 Dataset。<br>**關鍵設計約束（不是偏好）**：種入的 Workspace 必須是目錄 Workspace——`selectTestCases` 以 `workspaces.is_catalog` 判定「是否為平台策展產物」，種進個人 Workspace 者一律被排除為 `not_curated`。因此腳本刻意**不做「搜尋目錄再 Fork」的後備路徑**。<br>**尚未對 live 部署執行**（`m4/README` §14.3 出-6）：工具只需 `0026`，跑得動；不跑是因為 dev 的目錄策展帳號未確認、且同工作樹有平行批次。<br>**原殘項內容保留於下** | `CONTENT-007` 的 5 份 writing rubric 以**策展預設**的形式存在([content/writing-rubrics.md](mvp/content/writing-rubrics.md) §4 ＋ `tools/eval-regression/rubric-content-007-writing-v1.json`),**平台裡沒有任何一筆 Test Case 帶著它們**。<br>**這不是 rubric 的缺口,是一個更早就存在而沒人記下來的缺口**:允收第 1、5 條的範例 Dataset／Prompt／驗收條件當初也是在 M2 的臨時 Workspace 裡現做的,平台從來沒有「把策展 Test Case 種進部署」的路徑。要求 rubric 必須可種進全新部署,就等於回頭要求那三項也必須可以。<br>因此 `CONTENT-007` **以與第 1 條相同的標準判定達成並勾選**,而這件事記在這裡。它跨 `CONTENT`(策展資料要有可種入的形式)與 `PACK`(可散布 Test Case 與範例資料是 `02:PACK-001` 第 5 條的既有承諾),兩邊都碰得到 | 沒有工作項承接。**M4 的 `PACK-005`(選擇是否包含可散布 Test Case 與範例資料)是最自然的落點**——那條準則已經預設了「Test Case 可以被搬運」這件事 |
| 丙-13 | ⚠️ **歷史 M2 Run 的 `artifacts` 表全空;補評前必須先回填** | `artifacts` **全表 0 列**:M2 的管線沒有寫這些列,封存本身還在物件儲存(`run-artifacts/<run_id>/<attempt_id>/artifacts.tar`)。M3 的 `recordArtifacts` 從此會寫,**新的 Run 沒有這個問題**。<br>**危險在補評**:`artifactFindings()` 在 artifact 列為空且 Run 為 `succeeded` 時會產出「這個 Run 回報成功而沒有記錄到任何產出檔案」——對 M2 那 43 筆確實有產出的 Run,這句話是**錯的**;送給 Judge 的 `artifacts[]` 也會是空陣列。而評估是 append-only,錯的那一份會永遠留在 revision 清單裡。<br>**今天不會發生**:評估只由終態轉移排入,M2 的 73 筆早已終態,沒有路徑會自動評估它們(ADR-025 已定為「永久停在未評估,除非有人另行決定補評」)。<br>**修法是資料回填不是程式修改**:從封存的 tar 索引回填,`EVAL-013` 的 harness 已證明讀得到(不解壓、不依副檔名解析,evaluation-design §2.2 允許的那條路)<br>**2026-08-18 補記：本列的危害敘述有一處誤，訂正如下；順序仍然是硬的。**<br>①**`artifactFindings()` 的措辭本來就是誠實的**——它說的是「no output files were **recorded**」（沒有被**記錄**），不是「沒有產出」，所以那句話對 M2 那 43 筆並不構成一句錯誤的指控。**真正的洞在另一處，而且更難看見**：送給 Judge 的 `artifacts[]` 是**空陣列**，Judge 於是在「這個 Run 沒有任何產出清單」的前提下逐條判定，而評估是 append-only——**錯的不是報告上的一句話，是判定本身的輸入**。<br>②**回填要多改一件事**：現行寫入路徑會給每一列 `now() + 30 days` 的保存期限，套在 M2 的舊 Run 上就是**憑空發明一段假的保存期**（那些位元組早就存在了，該到期的說不定早就該到期）。回填必須由該 Run 的 `created_at` 推導，不能沿用寫入路徑的預設 | **順序是硬的:先回填 `artifacts`,再評估。** 反過來會得到 43 份**輸入就是空的**不可變評估。<br>**2026-08-23 結案。** 回填工具 [`tools/content/backfill_artifacts.py`](../../tools/content/backfill_artifacts.py):讀 tar 的**索引與成員位元組**(只為算 sha256——那正是 sandbox 當初產生 `content_hash` 的方式,所以是**還原原值不是發明一個像樣的**),**123 個封存 → 177 列,涵蓋 123 個 Run**(每 Run 中位數 1 個檔、最多 6 個)。冪等,重跑回報 `already backfilled: 123`。<br>**兩個值刻意不沿用寫入路徑**:①`expires_at` 由 `runs.created_at` 推導,不是 `InsertRunArtifact` 寫死的 `now() + 30 days`;②`content_type` 填 `application/octet-stream`——查證後發現**這不是妥協**:provider 的 `Artifact` 根本不填這個欄位,所以那就是這一列本來會有的值,**在這裡嗅探位元組反而會讓回填的列比真的還「好」,那是另一種謊**。<br>**回填後重評 53 筆,判定變了**:`met` **0 → 18**、`not_met` **4 → 0**、`partially_met` 23 → 5。那 18 筆一直都有產出檔案,只是平台沒把清單寫下來。「reported success and no output files were recorded」從 **84／84** 降到 **3／53**,而那 3 筆查過**物件儲存裡確實沒有封存**——對它們而言那句話現在是真的。<br>**提案側最該看的一列**:呼叫後「模型提了 0 條」從 **0 變成 15**——回填前每次呼叫都看得到一個空的產出清單,於是每次都有話說。③ 重測 **23／23 `applicable`,`blocked_reason` 仍是空集合**。<br>**錯的那些 revision 沒有刪除**(append-only),只是不再是最新的那一份;報告 §10 逐項記錄。**丙-47 完全沒動**:截斷 30／53 與 `undetermined` 30 仍然完全相關 |
| 丙-15 | ✅ **已結案（2026-08-18，`a679836`）：三個缺陷都改了行為，不是改期望** | **D-1／D-2 → `entry-path-escape`（error，Blocked）。** `ingest.PackageFS` 在 `fs.Sub` 之前逐條讀 `zip.Reader` 的**原始 entry 名**，含 `..` 或絕對路徑（含 `C:\`、含反斜線寫法）即產生 finding，並經新的 `skillpkg.ArchiveSource` 介面交回 `Validate`——**掛在 fs 上而不是各呼叫端各記一次**，所以匯入、目錄詳情、閘門 B、`EVAL-010` 的修補後重驗、打包重驗全部同時拿到。finding 的 `Path` 是**壓縮檔宣告的那個名字**，不是被清洗過的名字（審核者讀到的必須是套件宣告的那一份）。**兩種形狀共用一個 code**：同一條規則、同一個嚴重度、同一個補救方式，差別寫在訊息裡。<br>**D-3 → `symlink-entry`（warning）。** 掃描改讀 entry 的 mode（`fs.ModeSymlink`），在把它當成文字檔讀之前就揭露，並附連結指向哪裡（zip 把目標路徑存成 entry 本體）。**判 warning 不判 error 的理由（依 `02:PACK-001`）**：第 4 條「不該散布的東西不得打入套件」在匯出端**已由白名單達成**——`packaging.travels()` 只放行 `Mode().IsRegular()`，符號連結進不了任何下載套件（`TestBuildAndVersionControlResidueNeverTravels` 已鎖住）；第 2 條的打包前重驗因此看不到連結，**閉環的形式是「剝除＋揭露」而不是 `validation_blocked`**，而這正是允收準則允許的形式。改判 error 會讓帶良性連結的既有套件連匯入與試跑都被擋（ADR-007 誤報成本），而**缺的從來不是擋，是那句話沒被說出來**：先前匯出端無聲剝除、匯入端無聲讀成 11 bytes 純文字檔，兩端都不提。`export.go` 的註解已把兩端接起來，免得下一個人只看到其中一端。<br>**QA-002 期望清單同批更新**：三列的 `gap` 欄移除、改為期望 finding，逐列註明由本次 commit 關閉並保留原本 gap 說了什麼（`tools/qa/skillpkg-corpus/expected-findings.json`）。**方向正確——是行為改了才改期望。** 語料未重新產生（變體位元組不變），harness **24/24 相符**（3 合法基底＋21 變體，`golang:1.25` 容器）。<br>**迴歸**：45 個 pin commit 合法套件全部重跑 `PackageFS`＋`Validate`，**0 個 `entry-path-escape`、0 個 `symlink-entry`、0 個被擋**；`go test ./...`、`go vet`、`gofmt`、golangci v2.12.2 全綠。<br>**原殘項內容保留於下** | `QA-002` 的破壞語料查出的三件事，逐項證據見 [m4/README §14.1](mvp/m4/README.md)，變體與期望清單在 [`tools/qa/skillpkg-corpus/`](../../tools/qa/skillpkg-corpus/) 的 `gap` 欄。<br>**D-1** entry 名 `../../evil.sh`：`archive/zip` 的 `fs.FS` 檢視在本 repo 任何一行程式看到它之前就改寫成 `evil.sh`，code 集合與乾淨基底**逐項相同**。**沒有 zip-slip**（平台從不把樹寫到磁碟），但那個 entry 無聲換了身分，**審核者核可的檔案樹不是壓縮檔宣告的那一份**。<br>**D-2** entry 名 `/etc/cron.d/evil`：改寫後連副檔名都不是腳本，比 D-1 更安靜，連 info 揭露都沒有。<br>**D-3** 符號連結指向 `/etc/passwd`：zip 的 `fs.FS` 把它當成一個內容為目標路徑的 11 bytes 純文字檔，掃描**從頭到尾沒說過這個套件含有連結**。匯入安全，但**任何會把這些位元組寫到磁碟的消費端都會把連結建出來**——`PACK-001` 的下載套件與使用者的解壓工具正是兩個。<br>**修法**：D-1／D-2 在 `fs.Sub` 之前讀原始 `zip.Reader` 的 entry 名並揭露，屬 `ingest`；D-3 屬 `PACK-004`／M4-4（打包排除規則目前沒有一條講符號連結）。**語料已經在庫**，修好之後把那三列的 `gap` 換成期望 finding 即是回歸測試 | ~~沒有工作項承接。D-3 與 M4-4 重疊，可一併承接~~ **已解除（2026-08-18）**；`M4-4` 的另一半（打包排除規則要涵蓋評估產物）仍開著，本項只回答了符號連結那一半 |
| 丙-14 | ✅ **已解決（2026-08-18，本批）：兩頁都有選擇器，端點用的是一支本來就在、卻沒有任何呼叫者的既有 query。** 原題「preflight 頁沒有 Skill Version 選擇器」（自 `DESIGN-007` 交棒而來，新入列 2026-08-17） | `RunPreflight.tsx` 的 `version` 只能從 URL query 進來，頁面上沒有這個欄位（該檔的 scope 註解已誠實寫明）。丙-11 結案時**只取了兩個補法裡較小的那一個**——`AppliedResult` 直接把新版本的 id 交給 preflight，解決的是「採納建議之後怎麼重跑」那條主路徑；**「使用者想自己挑一個版本來跑」仍然只能手改網址列。**<br>**它原本掛在 `DESIGN-007` 名下**（`03` EVAL-011 與丙-11 都這樣記），而 **`03` §3 的 DESIGN 系列已於 2026-08-17 收斂為「不再追蹤」**（[m4/README.md §8.5](mvp/m4/README.md) 選項 (a)）。收斂裁定的第 3 條要求殘留缺口必須有具名落點，**本列就是那個落點**——否則它會隨著本節收斂一起消失，而那正是選項 (b)（直接刪整節）被否決的理由。<br>**補法不變、也不需要新決策**：preflight 頁加一個版本選單，讀既有的版本清單即可。不擋任何允收準則（`02:TEST-001` 沒有一條講版本選擇） | ~~沒有工作項承接。**M4 第 6 批（UI）是最自然的落點**~~ **M4 第 6 批沒有做它**（該批交的是打包頁、下載紀錄與丙-10 的呈現面）。~~**仍無承接者**；下一個動 `apps/web` 的批次是最自然的落點~~ **已由 2026-08-18 的丙類批承接並完成。** `GET /skills/{id}/versions`（`registry.Handler.Versions` → 既有 `gen.ListSkillVersions`，**未新增 query、未 regen**——那支 query 從 M1 就在，全 repo 沒有任何呼叫者）；`RunPreflight.tsx` 與 `Packaging.tsx` 共用同一個 `SkillVersionPicker`，**兩頁一起收**（原列只寫 preflight，但 `Packaging.tsx` 有一模一樣的 `version?` 缺口，只收一頁就是第五次「只改一半」）。**URL 帶進來的值仍是預設值**，且清單裡沒有的 id 會獨立成一個 option 保持選取——舊連結不會被靜默改寫成別的版本。Workspace scope 沿用該檔既有的 `h.workspace(w, r)`，不碰請求參數（鐵律 3）；他人的 skill id 回 200 空清單而非 404（scope 從 session 來，兩者對呼叫者本就不可分辨）。<br>**`WS-004` 的版本保留意見刻意沒收**：Skill 詳情頁是公開頁（OptionalSession、DISC-010），把這個 session-scoped 端點掛上去，對匿名訪客是 401、對 catalog skill 是空清單，**等於把「不是你的」顯示成「沒有版本」**。要收那個保留需要一個 catalog scope 的公開版本歷史端點，而「版本歷史是否公開」是一個決策——留給 `WS-004`／`DISC-006` 的負責人 |

### 丙（續二）：2026-08-18 M4 對帳批新增（丙-16～丙-27）

**十二項全部是「有承接者就做得完」的實作工作，沒有一項在等決策。**（**2026-08-18 更正：十二項裡有半項不是。** 丙-16 的 (b) 缺的是一個範圍決定不是一段程式，已改列乙-17；其餘十一項半的分類經本批逐項驗證成立——丙-21③ 與丙-26③⑤ 此前被記成「等 `QA-008`／等決策」，查證後都不需要任何決策，**那是分類寫對了、掛錯了阻擋**，兩者都已在本批做完。） 逐項的完整理由已就地寫進 `03-work-items.md` 的對應行，本表只記入口、補法與解鎖對象。逐項證據見 [m4/audit.md](mvp/m4/audit.md)，執行順序見 [m4/release-checklist.md §1.9](mvp/m4/release-checklist.md)。

| # | 殘項 | 內容與補法 | 解鎖 |
| --- | --- | --- | --- |
| ~~丙-16~~ **已結案（2026-08-18）：(a) 兩邊改成一致；(b) 分類本身錯了，改列乙-17** | **兩個沒有寫入端的欄位：前端渲染一個後端不產生的 `status_reason`；`redistribution` 改它只能跑 SQL** | **(a)** [packaging-design.md](mvp/m4/packaging-design.md) §4.6 寫「打包失敗時轉 `rejected` 並保留列（帶理由）」，實作**刻意沒做**（`packaging.go` 的 `ponytail:` 註解自承，理由是 `artifacts` 沒有 `status_reason` 欄，而驗證跑在寫入之前所以沒有東西可以拒絕）。**理由成立**——但 `DownloadArtifactFacts.tsx` 已經在渲染 `artifact.status_reason`，一個永遠是 undefined 的欄位。二選一：後端補欄位，或前端拿掉那一段。**不補也可以，但兩邊要一致**。<br>**(b)** `skills.redistribution` **沒有寫入端點也沒有 audit**（ADR-027 待決策，`m4/README §13.5` 出-5 已記）。它現在是決定「內容能不能交出去」的閘門欄位，而**它是全庫唯一一個沒有稽核就能改的閘門欄位**——`access_restriction` 有 `PUT`／`DELETE /admin/skills/{id}/restriction` 與 audit event，這一個沒有。補法是把它加進 `SEC-011` 的窮舉動作清單並沿用同一組端點形式，**M4 首發刻意不做**，但兩道鎖的治理強度不對等這件事要記著<br>**2026-08-18 結案，而兩半的處置不同——這件事本身是本列的結論。**<br>**(a) 取「前端拿掉」而不是「後端補欄位」。** `packaging.go` 那句 `ponytail:` 註解的理由查證後成立：`persist()` 只寫 `available`，**`rejected` 全庫沒有任何寫入端**，而驗證跑在寫入之前，所以根本沒有東西可以被拒絕。既然那個狀態不存在，補一個 `status_reason` 欄位就是替一個不會發生的狀態立一個永遠是 NULL 的欄。因此 `status_reason` 從 [`contracts/openapi/public.yaml`](../../contracts/openapi/public.yaml)、`apps/web/src/api/packaging.ts` 與 `components/DownloadArtifactFacts.tsx` 一併移除；`a11y.test.tsx` 那筆假 fixture 連 `status: "rejected"` 一起拿掉——**測試裡擺著一個產品做不出來的狀態，會讓下一個人以為它做得出來**。`packaging.go` 一個位元組未改。<br>**(b) 查明它不是丙類，是乙類，已改列**乙-17**（見上一節）。** 丙類的定義是「有承接者就做得完」，而 `redistribution` 的寫入路徑不是沒人做，是**已裁定不做**：要把它做出來等於決定「誰可以把一份內容標成可散布」進 MVP 範圍。**兩道鎖治理強度不對等這件事不因改列而消失**，它跟著乙-17 走 | ~~`SEC-011` 的窮舉清單；無工作項阻擋~~ **(a) 已解除；(b) 改列乙-17** |
| 丙-17 | **沒有測試斷言 License 與 provenance 檔真的進了匯出的包；manifest 沒有作者欄位** | `02:PACK-001` 第 3 條要求「**套件**保留必要 License、作者、原始來源及衍生關係」。manifest 的 `license.expression`／`source_tier` 成對且有測試，溯源鏈有測試——**但那些是平台寫進 manifest 的字串，不是隨包走的授權檔本體**。`LICENSE`／`LICENSE.repo`／`LICENSE.repo.provenance.json` 靠白名單逐位元組複製而存活，**這個推理是對的，但它是推理**，而 [m4/README.md §10 R3](mvp/m4/README.md) 明文要求打包的具名測試對匯出的位元組斷言。<br>**作者**：`download-manifest.schema.json` 全文沒有 author 欄位。二選一——manifest 加一個 additive 欄位（依 `contracts/packaging/README.md` 的版本演進規則判 minor），或在 `02:PACK-001` 就地寫明「作者資訊隨套件位元組保留、不另立 manifest 欄位」並補測試。<br>**補法合計一支測試 ＋ 一個小決定**<br>**2026-08-18 結案。** **作者取「不立 manifest 欄位」那一條**，理由查證後只有這一條站得住：**平台的資料模型裡沒有作者欄位**，Agent Skills 的 frontmatter 也沒有，立欄位要先有可信來源，而平台唯一知道的（repo URL 與 commit）已由 `source.origin` 承載——憑空多一個欄位就是多一個沒有來源的宣稱。落點就地寫進 `02:PACK-001`。**量測**：`TestTheLicenceAuthorAndProvenanceFilesTravelInEveryTargetsPackage` 對三個目標的產出 zip 斷言三個授權／溯源檔存在、內容與來源相同、著作權人行未被改寫。`PACK-003` 與 `QA-007` 同批改判勾選。commit `50624e2` | `PACK-003`、`QA-007`、`RELEASE-008` |
| 丙-18 | **安裝說明宣稱了一件產生器做不到的事；下載頁少兩項準則明列的欄位；打包頁沒有三層相容性** | **(a) 宣稱大於實作**：[`contracts/packaging/profiles/standard.json`](../../contracts/packaging/profiles/standard.json) 的 `known_limitations` 第 3 條對使用者寫著 INSTALL.md 會列出「including the ones Skill Hub's own runtime image refuses」，而 `dependencyNotes()` 只取兩個 finding code、**從不查 [infra/images/README.md](../../infra/images/README.md) 的拒收表**（[packaging-design.md](mvp/m4/packaging-design.md) §6.2 明列該做）。**那句話隨每一個 `standard` 套件出貨**，形狀與乙-2 記下的完全相同——讓使用者相信一件平台不執行的事。拿掉或兌現，二選一。<br>**(b)** `undeclared-dependency`（程式碼 import 了但套件從未宣告）不進 INSTALL.md，只在 manifest warnings 與預覽頁——而那正是「照著裝卻裝不起來」的第一類原因。<br>**(c)** `02:PACK-002` 第 1 條要求下載頁顯示「依賴與**環境變數需求**」，而 `http.go` 的 `targetView` 兩者都不序列化，前端型別連欄位都沒有；它們只存在於包內的 `INSTALL.md`，**而那是「先建了包才看得到」**——同一條理由讓 `verification_steps` 被搬上頁面（`c6fdd26`），這兩個沒跟上。<br>**(d)** `Packaging.tsx`／`Downloads.tsx` 沒有引用既有的 `CompatibilityStatus.tsx`，所以三層相容性在打包／下載頁上沒有分開呈現（`DESIGN-012` 的範圍寫的是這組畫面）<br>**2026-08-18 更新：(c) 與 (d) 已解決，`DESIGN-012` 因此改判勾選。** (c) `targetView` 送 `env_vars`、`previewView` 送 `dependencies`（**兩者在契約裡都是 `required`，所以空陣列是答案不是沉默**），`Packaging.tsx` 一併呈現；**依賴的那一份就是 INSTALL.md 會寫的那幾行、同一組 finding**，前端刻意不自己推導第二份。(d) 打包頁引用**與 Skill 頁同一個** `CompatibilityStatus`。**(a) 與 (b) 仍開著，屬 `PACK-007` 的後端半邊**<br>**2026-08-18 更新：(a) 與 (b) 也結案，整列關閉。** **(a) 那句宣稱拿掉而不是實作**——INSTALL.md 講的是在**使用者自己的 Agent** 上安裝，而 Skill Hub 自家映像帶不帶某個套件是**試跑**的事實；把一份 Markdown 表格接進產生器只會多一份會漂移的來源。profile `version` 1.0.0 → 1.0.1。**(b)** `dependencyNotes()` 納入 `undeclared-dependency`，段落標題同步改掉——清單已經不只是宣告的那些，標題不改就是新的一句不實敘述；打包器版本 0.1.0 → 0.2.0。**`PACK-007` 仍不勾**，缺的是下載頁把 `env_vars` 與依賴畫出來那一半。commit `50624e2` | `PACK-007`、~~`DESIGN-012`~~（已勾） |
| 丙-19 | **`standard` 的 round-trip 雜湊測試缺席；`claude-code` 從來沒有人真的裝過** | **(a)** [packaging-design.md](mvp/m4/packaging-design.md) §2.3 具名要求 `TestTheStandardPackageRoundTripsToTheSameContentHash`（刪掉平台加的三樣檔案後重新匯入，得到同一個 `content_hash`）——**不存在**，現有的只到「`SKILL.md` 逐位元組相等」。而 `standard.json` 的 `verification_steps` 第 4 條正是叫使用者做這件事：**平台要求使用者做一個自己沒有自動驗過的檢查**。這一項純程式，補法小。<br>**(b)** `claude-code` 的 `support_status` 仍是 `unverified`（`m4/README §13.5` 出-1）。解除三步：套件放進 `~/.claude/skills/`、`/skills` 看得到、跑一次驗證 Prompt；落檔後改欄位並進 `version` 版號。**這一半要人**，且它同時是 PDM-008 對外「2 個已驗證 Profile」缺的那一個<br>**2026-08-18 更新：(a) 結案，但形狀與原本寫的不同。** **具名的那支測試依它的字面不可能成立**——`content_hash` 是使用者當初上傳的那份**壓縮檔位元組**的 SHA-256，重新壓縮同一組檔案必然是另一份位元組。**同一句設計的另一半才是真的不變式**（「除平台新增檔案外逐位元組相同」），已落地為 `TestTheStandardPackageIsTheSourceBytesPlusExactlyThreeFiles`。**`standard.json` 的 `verification_steps` 第 4 條原本正是在叫使用者做那個不可能的檢查，已一併改寫**——尺-2 的另一個形態：平台叫使用者去做一個平台自己做不到的檢查。**(b) 仍開著**（要人）。commit `50624e2`<br>**2026-08-23 (b) 結案,整列關閉。** 三步全部走完並實際觀察到:平台經真實 HTTP 路徑產出 `claude-code` 套件(`content_hash 6be1065…`)→ 解進 `~/.claude/skills/` → **`/skills` 在同一個 session 就列出它**(使用者與 agent 兩邊各自確認)→ 以 profile 自己的 `verification_prompt` 叫用,skill 載入、回出約定的 marker、並讀到了 SKILL.md 旁邊的 `reference.md`。<br>**順帶證實一句一直是文件的話**:`verification_steps` 第 2 步寫「已被監看的目錄不需重啟」——`~/.claude/skills/` 本來就存在,所以這次正好驗到了它,而在今天之前那只是照抄官方文件。<br>`claude-code.json` `support_status` → `verified`、`version` 1.0.0 → **1.1.0**(§7.3 要求翻狀態即進版並附得出實測落點),落點寫在該檔 `known_limitations[0]` 與 `notes`。**同時寫明它不成立的部分**:一個套件、一個作業系統、純提示型 Skill、沒有裝任何依賴也沒有執行任何腳本——`verified` 一向指的是**安裝路徑通**,不是「這個 Skill 跑得動」 | `PACK-009`、`RELEASE-008`、PDM-008 的對外措辭 |
| 丙-20 | **沒有一支測試走完核心旅程——六段各有測試，接縫沒有** | `02` §7 DoD 第一條的旅程六段各有整合測試，分散在 18 個 `*_integration_test.go`，**沒有一支把它們串起來**。唯一名為 e2e 的 `TestEndToEndRunCallsTheModelThroughItsOwnVirtualKey` 只涵蓋「真沙箱＋真閘道跑一次 Run」，不含搜尋、Fork、評估、打包下載，且要花錢、CI 不跑。<br>**為什麼分段測試不能交差**：本專案的四次退回（`TEST-003` 缺操作面、`TEST-004` 缺顯示面、`EVAL-011` 缺交接面、`WS-004` 缺列表面）**全部發生在兩段之間**，而分段測試按定義看不到接縫。<br>**補法**：一支以 API 為主的旅程測試（Fork → 建 Test Case → preflight → Run → 評估 → 打包 → 下載），Provider 用既有的 fake，**不需要真沙箱也不需要花錢**<br>**2026-08-18 結案。** `TestTheCoreJourneyRunsFromIntentSearchToADownloadedPackage`：九段一條線、單一函式不切子測試（**順序本身就是斷言**），Provider 用既有 fake、Judge 是 httptest server，不需要真沙箱也不花錢。**守的是各段測試按定義看不到的接縫**：fork 帶著目錄的 `redistribution` 所以打包才放行；跑完的 Run 出現在 Run 歷史裡；下載到的位元組經 manifest 溯回旅程起點；授權檔在終點仍在包內。**第一次跑就抓到一個**：判定引用的 `criterion_id` 在其他測試裡是種快照的字面 `c1`，而經 API 建立的驗收條件由伺服器配發 id，引用快照沒有的 id 會正確地回 `undetermined`。`QA-001` 改判勾選。commit `04bb5e6` | `QA-001`、`RELEASE-002` |
| 丙-21 | **無障礙：鍵盤沒有被走查過、錯誤訊息沒有斷言、對比沒有自動守門** | 掃描面已經做得很紮實（12 條路由、15 次 axe、88 條規則一條沒關、**0 violations**，另修掉三個 axe 看不到的真缺陷）。**缺的是 `02:NFR-007` 三條判準裡兩條半**：①「主要操作可使用鍵盤完成」只有兩條結構代理斷言（`<details>` 必須有 `<summary>`、禁止正 `tabindex`）加一處焦點斷言，**代理不是走查**；②「表單具有標籤與**清楚的驗證訊息**」只成立前半，後半零斷言；③「狀態不只依賴顏色」——測試檔頭自陳「**A contrast regression will not fail this test**」，對比是手工量測的一次快照。<br>**根因是丙-21 自己解不了的**：全部前端測試跑在 jsdom 裡，axe 的 `color-contrast` 一律回 `incomplete`（沒有版面計算）。**所以它與 `QA-008` 的路線決策綁在一起**（引入瀏覽器驅動 vs 人工走查落檔），那個決策在乙類之外但確實要人先選一條。<br>**可以立刻做的一半**：人工鍵盤走查落檔 ＋ 錯誤訊息的具名斷言<br>**2026-08-18 更新：①與②已解決，③原樣，本列仍開著。** ①鍵盤走查**不是人工落檔而是自動化的**，而且是**旅程**不是頁面（`a11y.test.tsx` 新增：搜尋 → 詳情 → 打包全程以焦點走完，每一步斷言在 tab 序列裡、拿得到焦點、按得動；`tabbables()` 依平台規則排除 disabled／`aria-hidden`／關起來的 `<details>` 內容）。②三處驗證訊息具名斷言（回報空白、Test Case 建立鈕停用時說出缺哪三項、Dataset 未選檔）。③**對比仍無自動守門**，檔頭那句「A contrast regression will not fail this test」一字未改，仍綁 `QA-008` 的路線決策。<br>**同批補上一句 jsdom 的界線**：它證不了「真瀏覽器的 Tab 順序等於 DOM 順序」，寫進檔頭而不是靠沉默<br>**2026-08-18 更新：③也有守門了，而本列的原判定要更正——它從來不需要 `QA-008` 的決策。** `apps/web/src/contrast.test.ts` 直接讀 `index.css` 解出 light／dark 兩組 token，對七組配對算 WCAG 對比（正文 4.5:1；`--accent` 只當左邊框，因此依 1.4.11 用 3:1），另斷言**每一個 hex token 恰好宣告兩次**——少一次代表 dark 忘了覆寫，那會讓淺色值落到深色底上，而那正是手工量測最容易漏的一種。值一律從 `index.css` 讀出、不在測試裡另抄一份（抄的那份才是會爛的那份）。15 條全綠。<br>**原判定錯在把兩層當成一層**：`QA-008` 管的是**算繪後的合成像素**（alpha 疊色、`opacity` 乘數、字級與抗鋸齒、瀏覽器色彩管理），而 token 層的靜態十六進位值**不需要版面計算就算得出來**——jsdom 證不了的是前者，不是後者。把整個 ③ 掛在一個未定的路線決策上，等於讓一段不需要決策的工作陪著一個真的要人拍板的東西一起停在那裡等。**這是本批三次同型更正的第一次**（另兩次見丙-25、丙-26）。<br>**但本列不整條關閉**：`QA-008` 仍未解，**合成像素仍然沒有任何守門**——`--accent-bg`／`--accent-border`／`--social-bg`／`--shadow` 是 `rgba()`，疊在什麼底上這支測試不解析（`.notice` 因此未檢）；`opacity` 的乘數落在哪裡與 token 無關（上一批是把靜音**移除**而不是調參，沒有東西擋它回來）；`PAIRS` 是手寫清單，新規則把 `--text` 放到第三種底色上是它沒聽過的配對。**三件事都寫在該檔檔頭，不靠沉默。** **`QA-009`／`DESIGN-013` 因此仍不勾**，不勾的理由從「對比沒有守門」縮成「只有 token 層有守門，合成像素沒有」<br>**2026-08-22 ✅ 整列結案：③ 關閉，`QA-009`／`DESIGN-013` 同批改判勾選。** `QA-008` 的路線已由負責人拍板（**引入瀏覽器驅動，否決人工走查落檔**——見 [ADR-036](../adr/ADR-036-real-browser-verification-tier.md) 與乙-18），實作 commit `fe7aa13`：Playwright `1.62.1` ＋ `@axe-core/playwright` `4.13.0`（與釘死的 `axe-core` `4.13.0` **同版**），chromium／firefox／webkit 三引擎，4 支測試 × 3 引擎 ＝ 12 次、本機 22 秒，jsdom 層 138 個測試未受影響。**本列 ③ 缺的正是「改壞了會有人知道」這件事，而它現在被兩次破壞實測證明了**：把 `--accent-bg` 改成深色 alpha ⇒ `contrast.test.ts` **19 個全過（完全瞎掉）**、瀏覽器層**失敗**（**這就是本列點名的那個洞：四個 `rgba()` token 不解析、`.notice` 未檢**）；把 `--text` 改亮 ⇒ 瀏覽器層報 **58 個違規**。測試先斷言 `color-contrast` **離開了 `incomplete`** 再斷言零違規——**「零違規」本身證明不了任何事，jsdom 就是靠從不判定來報零違規的**。同批另外第一次量到**真實的 Tab 鍵**（三引擎焦點都不往回跳、焦點環真的被畫出來），關掉 `a11y.test.tsx` 檔頭第 3 點自陳證不了的那件事。<br>**本列關閉但有兩件事轉給 `QA-008` 記著，不是無聲消失**：瀏覽器層**刻意很窄**（[ADR-036 §1](../adr/ADR-036-real-browser-verification-tier.md)，不重跑 12 條路由）、引擎是 Playwright 自編版而非桌面上真正的 Chrome／Safari／Edge，且**沒有 OS 矩陣**。**這三件是 ADR-036 記載的已知取捨與 `QA-008` 的天花板，不是本列的未竟工作**——本列的判準是 `02:NFR-007` 的三條字面，三條全成立 | ~~`QA-009`、`DESIGN-013`~~（**均已勾**，同一份檢查結果，同進同退）；剩下的天花板記在 `QA-008` |
| 丙-22 | **Run Artifact 沒有單項刪除路徑；帳號刪除送出後沒有查詢面** | 帳號刪除做得完整（請求 → 30 天寬限 → purge，物件先刪再開交易，硬刪與去識別化分類照 PDM-006 §6.1，刪除範圍文案已寫成人話），單項刪除有 Skill／Test Case／Dataset／Download Artifact 四種。**缺兩項**：①`02:WS-002` 第 3 條與 `02:SEC-006` 第 1 條都把 **Artifact** 列為使用者可刪的東西，而 **Run 產出的 artifact 只能靠整個帳號 purge 一次清掉**；②`02:SEC-006` 要求「刪除工作具**可追蹤狀態**」，而帳號層的狀態只由 `DELETE /me` 當下的回應體提供，`GET /me` 不回 `deletion_requested_at`——**送出之後就查不到了**<br>**2026-08-18 更新：兩項缺口都補上，整列的**後端**半邊關閉。** ①`DELETE /runs/{id}/artifacts/{artifactId}`（soft delete——評估把 artifact 列當證據引用；commit 之後才移除物件，且只有沒有別的列指向同一個 key 時才移除；冪等）＋ **同批的 `GET /runs/{id}/artifacts`，因為列不出來的東西刪不到**。②`GET /me` 回 `deletion_requested_at` 與 `purge_after`。**`CORE-007` 仍不勾**，換成新的一個理由：**Run Artifact 在 `apps/web` 沒有畫面**（同一把尺的第五次）。順帶裁定一件事免得下一個人重新猶豫：**PDM-006 的保存期限不擋 `CORE-007`**，`02:SEC-006` 自己把它列在「未涵蓋（待決策）」。commit `8a17652`<br>**2026-08-18 更新（UI 批 commit `79bccce`）：Run Artifact 的畫面補上了，`CORE-007` 仍不勾，缺的換成另外兩處。**Run 頁新增產出清單＋兩段式刪除（範圍先說：刪的是檔案，Run 紀錄與評估判定保留，**引用過它的評估不會被改寫，會顯示證據已不存在**），**刻意不給位元組連結**——沙箱產出不由控制平面打開，也沒有端點 serve 它。**新的兩個缺口，同一把尺**：①`02:WS-002` 第 3 條的「Skill」在 `apps/web` **沒有刪除入口**（`DELETE /skills/{id}` 從 M1 就在，`/workspace/skills` 只列不刪）；②**帳號刪除整條路徑沒有畫面**，所以剛補上的 `GET /me.deletion_requested_at` **只有 curl 讀得到**，「可追蹤狀態」對使用者仍不可見。**兩處都不在 P-1～P-12 之內**，是補齊 P-9 時才顯出來的<br>**已解決（2026-08-18，UI 收尾批）：兩個缺口都補上，整列關閉，`CORE-007` 改判勾選。**<br>①**Skill 的刪除入口放在 `/workspace/skills` 的每一列，不放詳情頁。** 詳情頁是**公開目錄視圖**——看別人的 Skill 與看自己的是同一個畫面，而只有其中一個人可以刪；把刪除放在「我的 Skill」清單上，這個判斷就不必在畫面上做一次。<br>②**新增 `/workspace/account`**：刪除申請、申請中的狀態（含 `purge_after`）與取消都在同一頁，後端上一批補的 `GET /me.deletion_requested_at` 因此不再只有 curl 讀得到，`02:SEC-006` 的「可追蹤狀態」第一次對使用者成立。**文案的分工是刻意的**：按下去之前這一頁只講「這一步不會刪掉任何東西，它開始一段 30 天寬限期，期間隨時可取消」——那是**這個控制項**的事實；按下去之後**伺服器的 `scope` 原文照登**——哪些硬刪、哪些去識別化保留是刪除工作本身的事實，抄第二份只會多一份會過期的文案，而過期的一定是離工作比較遠的那份。`api/types.ts` 的 `Me` 同批補上契約 required 卻漏掉的 `deletion_requested_at`／`purge_after`（型別漏掉 required 欄位，等於前端帶著一份比契約寬的世界觀在跑）。<br>**第三次出現的兩段式刪除抽成 `components/ConfirmDelete.tsx`**，一次收三處（Skill／Run Artifact／帳號）：範圍說明以 `aria-describedby` 接上確認鈕、焦點跟著動作走——這兩件事上一批是在下載列表就地修的，抽出來之後第四處不會再漏一次 | ~~`CORE-007`~~（已勾）、`RELEASE-005`（仍不勾，見該項） |
| 丙-23 | **audit 的三個缺席事件源：來源變更、權限確認的拒絕側、清理** | `CORE-008` 已依 `02` NFR-001 第 4 條的字面五項勾選，**而 ADR-007 的稽核清單比那條長**。三處沒有事件源：①**來源變更**（`ingest.CheckSources` 標記來源失效時不寫 audit）；②**權限確認的「拒絕」側**（閘門 B 四項阻擋與配額拒絕只增 metrics）——**通過有紀錄、被擋沒紀錄，而安全事件回溯要看的往往是後者**；③**清理**（`run/cleanup.go` 寫的是 `cleanup_status` ＋ outbox event）。Secrets 類事件無實作因為 `SEC-005` 未做。<br>**三處都不在 `02` 的字面內，所以不擋 `CORE-008`**——記在這裡是因為它們是 ADR-007 的承諾，而 ADR 是決策不是建議<br>**已解決（2026-08-18）：三個事件源都補上，而三處的「同交易」條件各自不同，這件事要寫下來。**<br>①`ingest/sources.go` **只在轉態時**寫 `import_source.unavailable`／`restored`，與來源列的更新同交易——每輪檢查都寫一筆的那種是 metrics 不是 audit，稽核要回答的是「什麼時候變的」。<br>②七個拒絕點的 metrics label 與 audit reason **收斂成同一個 `refusal` 型別**，choke point 在 `run/service.go` 的 `Create` 返回路徑：一個地方判定、一個地方記錄，所以第八個拒絕理由加進來時不會有人忘記記。**拒絕側是三處裡唯一不同交易的**——被擋下來時沒有任何領域狀態被寫，也就沒有交易可搭，因此走 pool-backed 單語句；**這是鐵律 9 的邊界而不是它的例外**（那條鐵律管的是「領域狀態變更與對外事件同交易」，這裡沒有狀態變更），理由已寫進程式碼而不是只寫在這裡。<br>③`run/cleanup.go` 的 `run.cleanup` 加在**既有的** tx 內。<br>**`ErrNotFound` 與基礎設施錯誤刻意不記**（那是「東西不在」與「平台自己壞了」，不是安全決策），而且各有具名測試守住——否則下一次「把 audit 補齊」會把它們一起掃進來，把回溯用的紀錄稀釋成流量日誌。新測試檔 `internal/identity/audit_gaps_integration_test.go` 五支，**已用拋棄式 Postgres 實跑通過**。<br>**`CORE-008` 的勾選不因此改變**：它的判準是 `02` NFR-001 第 4 條的字面五項，本列補的是 ADR-007 那份較長的清單——**工作項的允收準則不能比 `02` 嚴，但 ADR 的承諾也不會因此消失**，這一列存在的理由就是後面這半句 | ~~無工作項承接~~ **已解除**；`SEC-010` 的事件回溯品質因此變好，但那從來不是任何工作項的允收條件 |
| 丙-24 | **Run 歷史列表在 API 面就不存在；個人 Skill 列表有 API 沒畫面；下載紀錄只到彙總** | `02:WS-002` 第 1 條列了五項（Fork、版本、Test Case、Run 歷史、下載紀錄）。**Test Case 與下載紀錄有畫面**；**Run 歷史連端點都沒有**（`router.go` 只有 `GET /runs/{id}`，`db/queries` 沒有工作區層級的 run 列表）；**個人 Skill 與版本有 `GET /skills` 但 `apps/web` 沒有畫面**；**下載紀錄呈現的是 artifact ＋下載次數，不是逐筆「誰、何時」**——`download_records` 的 `downloaded_at`／`actor_user_id` 在 `0027` 就有且 append-only，但沒有端點把逐筆列出來。<br>**這是同一種退回的第四次**（`TEST-004` 缺顯示面／`TEST-003` 缺操作面／`EVAL-011` 缺交接面／本項缺列表面），**四次同一個根因**：後端把能力做完了，而 `02` 的準則主詞是使用者<br>**2026-08-18 更新：三項缺口關掉一項。** **個人 Skill 列表有畫面了**（`/workspace/skills`，讀既有的 `GET /skills`，頁首導覽有入口），**而且那個畫面自己寫出「Run 歷史還沒有」與原因**——「產品做不到」與「你沒跑過任何 Run」是兩個不同的答案，空清單不得代替前者。**仍缺 Run 歷史列表（連端點都沒有）與下載紀錄逐筆**，兩項都在後端，`WS-004` 因此維持不勾<br>**2026-08-18 更新：兩項後端缺口都補上。** `GET /runs`（工作區層級的 Run 歷史，一列帶 skill 名稱與可重跑所需的 `test_case_id`；**刻意比 `GET /runs/{id}` 窄**——狀態轉移與 attempt 是開一個 Run 才要看的，一頁列 N 個都送會讓列表變成整個 API 最重的一次讀取）與 `GET /downloads/{artifactId}/records`（逐筆「誰、何時」，**與 audit event 分開**：同一次下載兩筆紀錄，保存期限與可見性都不同）。~~**仍缺兩個畫面**（Run 歷史頁、下載紀錄逐筆），`WS-004` 維持不勾。~~ commit `8a17652`<br>**已解決（2026-08-18，UI 批 commit `79bccce`）**：`/workspace/runs` 與下載紀錄的逐筆呈現都上線，`WS-004` **改判勾選**。**這一列的教訓值得留著**：它要兩批合起來才關得掉——缺端點與缺畫面都會讓「使用者可查看」碰不到東西，而這正是同一種退回連續發生四次的原因。**勾選帶一個保留意見**（準則裡的「版本」仍沒有列表面），寫在 `03` 的行內與 [m4/audit.md §8.2](mvp/m4/audit.md) | ~~`WS-004`~~（已勾） |
| 丙-25 | **漏斗的跨表查詢不存在；產品內沒有資料保存政策頁面** | `O11Y-004` 的產生與儲存做得比 `02` 要求的更嚴（白名單是 schema 本身、全表無自由文字欄、未定值不收集），**缺的是另外兩項**：①**查詢**——全 repo 沒有漏斗報表 SQL、腳本或 harness，而七段裡有五段要從既有領域表（`runs`、`evaluations.feedback_helpful`、`evaluation_suggestions.decision`、`download_records`）拼出來；ADR-029 決策 6 說讀取面是後台查詢、不做儀表板，**但那組跨表 SQL 一行都沒有**；②`02:O11Y-004` 要求「使用者可在**資料保存政策**中看到這一類資料的存在、內容範圍（含『不記查詢原文』）與保存期限」，**而平台裡沒有這個頁面**——[gate-test/consent-and-data-policy.md §2](mvp/gate-test/consent-and-data-policy.md) 已把逐欄揭露的內容備妥，但那是給受測者簽的同意書，不是產品裡的政策頁。<br>**另記一項不屬本列但綁在一起的**：`ANALYTICS_RETENTION` 的值隨 PDM-006 追認，**追認之前這個漏斗結構上量不到任何東西**<br>**2026-08-18 更新：①結案，②仍開。** `tools/analytics/funnel.sql`：七段一次查完，**四段讀 `analytics_events`（**2026-08-25 起只剩兩段**——第七段收窄成領域問題後改讀 `runs`/`evaluations`，見同日的漏斗修正）（沒有領域表答得出來的那四段），其餘一律讀擁有該事實的領域表**；`download_started` **刻意不當第 6 段的分子**——它記的是「有人按了」，四道鎖仍可能拒絕，兩者相減才看得到拒絕。**`02:O11Y-004` 最後一條要求標明精度限制，所以每一列自帶 `note` 欄位**——放在另一份文件裡的註腳等於沒有標明。一支測試拿真的那個檔案對真的 schema 跑一次並斷言七段都在、每段都有 `note`（沒有人跑的腳本會對著一次欄位改名安靜地腐爛）。**②（產品內的資料保存政策頁面）仍開著**，`O11Y-004` 維持不勾。commit `04bb5e6`<br>**已解決（2026-08-18）：②補上，整列關閉，`O11Y-004` 改判勾選。** 新增 `/policy`（**公開**，頁尾每一頁可達——要登入才讀得到的政策不是使用者拿來做決定的政策）＋ `GET /policy/data-retention`，形狀比照既有的 `GET /test-cases/limits`：**頁面上沒有一個數字是寫死的**，`retention_days` 一路來自部署的 `ANALYTICS_RETENTION`，所以畫面說 180 天，只可能是因為這個部署真的套用 180 天。未設時顯示「目前不收集」——**那不是佔位符，是 `NFR-002`「未定值前不得開始收集」的可見形式**；把 ADR-029 提案的 180 寫死在頁面上，就是乙-2 那個錯誤原封不動再犯一次（顯示一個平台不執行的政策）。讀取失敗顯示為讀取失敗，並明說「讀不到不等於沒有收集」——**這一頁不替伺服器回答那個問題**。四個事件的欄位白名單逐欄照登（欄名不翻譯，翻了就斷開「頁面宣稱的」與「表裡存的」之間的對應），含「不記查詢原文」。<br>**原判定要更正一處**：本列此前被讀成疑似卡在法務（因為那份逐欄揭露的材料在 `gate-test/`）。**查證後不是**——[consent-and-data-policy.md](mvp/gate-test/consent-and-data-policy.md) 的 ⬜ 佔位是**受測者契約**（報酬、聯絡方式、錄影保存期限），那是簽給受測者的；產品內的揭露頁要回答的是「這個部署現在收什麼、留多久」，答案在部署設定裡，不在法務那裡。**這是本批三次同型更正的第二次**（另兩次見丙-21、丙-26）。<br>**兩份文件的每一類保存期限刻意不互抄**：`gate-test` 那張表是 PDM-006 的**提案**，沒有任何程式在執行它，抄進產品頁就是把未追認的提案變成對使用者的承諾。產品頁因此只講分析事件那一類，其餘只指路到各自的刪除入口。<br>**`ANALYTICS_RETENTION` 未定值仍擋 `BETA-002`**（結構上量不到東西），一如原記載；**它不擋 `O11Y-004`**，因為 `02:O11Y-004` 自己把「保存期限的定值」列在**未涵蓋（待決策）** | ~~`O11Y-004`~~（已勾）、`BETA-002`／`004`（仍不勾） |
| ~~丙-26~~ **已結案 2026-08-25** | **`SEC-012` 的開關做完了，但沒有任何東西會自動翻它** | 開關本體完整（一張表、P1 與 X-04 兩個觸發者共用、三個動作齊備、每個讀者 fail-closed、解除不是自動的、incident 接管 threshold 且不被降級、operator 端點 404、暫停與解除各有具名測試）。**缺的是本項字面的第一個動詞**：「**偵測到** P1 判準即立即停止派送……不等人接告警」——五條 P1 判準沒有一條會自動翻它，現況是「等人宣告」。<br>**commit 自陳的「刻意不做」對其中三條成立**（逃逸疑慮是判斷、P-02 探針在別處、停擺的 Reconciler 不會發現自己停擺），**但 `TraceMaskingStopped`（`O11Y-003` 已有告警規則）與「Reconciler 停擺 > 10 分鐘」是平台自己看得見的兩條**，它們沒有接上。<br>**這條準則的價值全在「不等人」**：`SEC-009` 通過、開放外部使用者的那一刻，P1 若仍需人手動停派送，`02:SEC-010` 的設計前提（團隊是一個人）就直接落空。**補法**：接上那兩條，其餘三條維持人工宣告並寫進 runbook<br>**2026-08-18 更新：平台自己看得見的那兩條接上了，①②④仍開，本列縮小但不關閉。**<br>③`TraceMaskingStopped` 掛在 `run/supervisor.go` **既有 sweep 的尾巴**（不另開一個 leader-elected timer 去讀同一批表），門檻**照抄 `alerts.yml` 的 `[1h]` ＋ `for: 1h`**；⑤Reconciler 停擺掛在 `cmd/api/main.go` 的新 ticker——**刻意不在 worker 裡**，因為停擺的 worker 會把自己的看門狗一起帶走；10 分鐘門檻出自 `02:SEC-010` 的 P1 列。兩者都先過冪等檢查（已在 incident hold 就不再宣告，否則一個持續數小時的判準會每輪各寫一筆 audit，把真正該看的那一筆埋掉），**都不自動 lift**（解除仍只有 operator 一條路）。`alerts.yml` 那兩句「這些動作是平台實作、仍是人的動作」的自陳同批更新。<br>**原判定也要更正一處**：[release-checklist §1.9](mvp/m4/release-checklist.md) 把本項記成「要先有 §3 的決策才能動（要先決定接哪幾條判準）」。**那個決策其實已經在 `03` SEC-012 的補法裡寫完了**——哪三條在本 process 之外是查得出來的事實，不是要拍板的偏好。**這是本批三次同型更正的第三次**（另兩次見丙-21、丙-25）。<br>**一個必須記下來的實跑發現，它改變了「告警」與「自動動作」之間的成本關係**：③原本想省掉 `for: 1h` 只留 expr，實跑時**誤觸把 13 個測試連鎖打成 503**。原因是這條告警的前提「一小時內幾乎必然有東西被遮」**是對流量講的**——低流量或測試環境下，光靠 expr 就會成立。**在告警裡這只是一個會吵人的誤判，接上停派送之後它會自己把自己停掉**：同一個閾值在兩種消費端下的代價差一個量級，所以照抄告警規則時 `for` 不是可以省的裝飾。<br>**①②④仍未做，理由不變**（逃逸疑慮是人的判斷、P-02 探針與 gVisor 逃逸類 CVE 訊號都在本 process 之外），屬**部署期**；**`SEC-012` 因此仍不勾**——本項的字面是「偵測到 P1 判準」，五條裡自動兩條不等於做完。<br>**2026-08-23:runbook 補上了,本列再縮小一格,仍不關閉。** `02:SEC-010` 的允收有一條是「流程以 runbook 形式產出,可被值班人員直接執行」,而 repo 裡沒有任何地方放這種東西——新增 [docs/runbooks/p1-dispatch-halt.md](../runbooks/p1-dispatch-halt.md):開關是什麼、怎麼判斷現在停著、**兩條自動判準各自怎麼分辨真事故與誤觸**(附可直接跑的 SQL)、三條人工判準怎麼宣告、解除前的三個檢查。<br>**素材是今天真的跑過一次**:`TraceMaskingStopped` 把整池停了,而那 274 筆「零遮罩」事件全是平台自己的 `evaluation_*`。runbook §4 把那次原樣記著,因為它是唯一一次有人走過這條路徑,而它是誤觸。<br>**同批把丙-49 的觀察半寫進 §5**:整個 dev 語料 2444 筆 sandbox 事件、遮罩總數 0,所以判準的前提(「正常流量下幾乎必然有東西被遮」)**在目前唯一有資料的語料上不成立**——「零遮罩」本身不是強證據,撐起這條判準的是「有量而且持續」。**要不要因此調門檻是 `02:SEC-010` 的決定,不是 runbook 的**,所以那裡只寫怎麼讀它,沒有改判準。<br>**①②④ 一格都沒動**,理由不變:runbook 讓那三條**可被直接執行**,**它不讓那三條變成自動的**,而本項的字面是「偵測到」 | `SEC-012`、`RELEASE-006`；[p1-dispatch-halt.md](../runbooks/p1-dispatch-halt.md)<br>**✅ 2026-08-25 結案：本列的缺陷是「沒有任何東西會自動翻它」，而那件事已經不成立**——兩條判準會自動翻，③今日起有兩個互補的偵測器（見丙-49）。**①②④ 逐條查證後維持人工，而且是有證據的維持，不是擱置。**<br>**①「疑慮」不是一個訊號**：量得到的形態各自已經是別條判準（連上核心資料庫＝②、遺留超標＝X-04、Reconciler 停擺＝⑤），①剩下的正是**還沒有形態的那一部分**。它沒有可接的線，不是漏接。<br>**②P-02 探針有兩層阻擋，第二層是結構事實**：第一層是**那個探針今天不存在**（`03:SBX-005` 未勾且明寫屬部署期、ADR-050 逐字寫「P-02 常駐探針不存在」、`tools/sec009/` 只有 T1／T2／T8），它連 CI 都沒在跑；第二層是**沙箱契約單向**——`apps/sandbox/internal/sandbox/http.go` 六條路由全是被呼叫端，節點沒有任何往控制平面推的通道，唯一的反向入口 `POST /internal/trace/{token}` 是**單一 Run 範圍且由不受信任的沙箱送出**，**讓它翻整池開關等於把 DoS 手把交給不受信任工作負載**。要接就得在 `ProviderCapability` 加欄位，那是 `contracts/` 變更，是決策不是接線。<br>**2026-08-26 訂正 ②，本列維持結案、數字不動。** 上面那句「是決策不是接線」把該做的事說對了，而**那個決策已經做了**：`5628e52` 在 `ProviderCapability` 加了 `security.p02_probe`，節點在既有的被呼叫端報自己的最後一次讀數，`detectP02Breach` 掛在既有 sweep 的尾巴。**第一層阻擋（探針不存在）同批消失**，第二層的顧慮（不受信任沙箱能翻整池開關）**沒有被繞過——推的仍然是控制平面，節點只是在被問到時回答**。所以 `SEC-012` 的五條判準現在自動三條、人工兩條（①④），**本列不重開**：它記的是程式面殘項，而這一筆是程式面**減少**了一項。<br>**④CI 看得到的東西到不了生產 DB，而且 workflow 自己寫下了理由**：`gvisor-baseline.yml` 逐字寫著自動化要讓 CI 持有一把能停整個機隊的憑證，「比人貼一行指令更糟」；反方向（平台自己拉 advisory feed）要為**控制平面**開對外網路出口，並讓派送能力取決於外部 feed 的可達性——fail-closed 則 GitHub 掛了就停整池，fail-open 則偵測器是裝飾。**另查證兩件事，都是「沒有既有資料可用」的證據**：`runsc` 不在任何 image 內（唯一釘點 `infra/nodes/gvisor-baseline.txt`，目前 `unset`），**所以丙-61 那條 `rescan` 掃的是 Agent SDK image、掃不到 gVisor**；`ProviderCapability` 也沒有 runsc 版本欄位。<br>**結案的界線要說清楚**：本列是丙（程式面殘項），而**程式面沒有東西剩下**——②④ 是部署期決策（一把 CI 持有的停機隊憑證／控制平面的對外出口／一份不存在的探針加一次契約變更），已於 [release-checklist §1.9／§2](mvp/m4/release-checklist.md) 有落點。**`03` 的 `SEC-012` 仍不勾，那是對的**：本項字面是「偵測到 P1 判準」，五條裡自動兩條不等於做完，**而那個未完屬於工作項，不屬於程式債**。 |
| ~~丙-27~~ **已解決（2026-08-18）** | ~~**`POST /feedback` 在 `apps/web` 沒有任何入口**~~ | 端點與資料表齊備（`0029` 的 `feedback_reports`，`kind` 兩值分流、2000 字上限與契約一致、`page_path` 帶 query string 一律丟棄不拒絕、他人的 `run_id` 丟棄不拒絕——**寧可少一個 context 欄位也不要弄丟整份回報**），**但 `apps/web` 全樹沒有一行呼叫它**。[beta-design.md §5](mvp/m4/beta-design.md) 要求的是「**全站可及**的入口，帶當前頁面路徑與（若有）`run_id`；不自動抓畫面」。<br>**它同時是三個東西的管道**：阻斷回報（`BETA-004`）、需求訊號（`BETA-005`）、以及未受邀者與額度用完者被擋下時那個「回報你的使用情境」的落點——**准入的 403 回應已經在指向一個畫面上不存在的東西**<br>**已解決（2026-08-18）**：`FeedbackEntry.tsx` 掛在版面**頁尾**，每一頁都在（收合的 `<details>`）。**放頁尾是鍵盤順序的決定**——它裝著整張表單，放在 `<main>` 之前會卡在每一頁與該頁第一個控制項之間。`kind` 由使用者自己選（兩種問的是不同的事，平台從外面分不出來）；`page_path` 去掉 query string、`run_id` 只在網址指名某個 Run 時帶上，**兩者都印在表單旁邊**——沒有截圖、沒有 console、沒有自動抓畫面。204 沒有 id，確認文案因此明講沒有回覆機制也沒有查詢頁面。<br>**`BETA-003`／`004`／`005` 三項仍不勾**：它們的動詞是「蒐集」「記錄」「複審」，管道有了不等於發生過——**這一列關掉的是管道，不是那三項** | `BETA-003`／`004`／`005`（**管道已通，三項仍待真實發生**） |

### 丙（續三）：2026-08-22 設計系統與 UI 對帳批新增（丙-28～丙-32）

**五項全部是契約缺口，不是前端缺陷**——每一項的前端半邊要嘛已經補完、要嘛只剩一個誠實的「這裡沒有」。逐項證據見 [ADR-041](../adr/ADR-041-trust-signal-vocabulary-typed-absence-and-rule-precedence.md) 與 [docs/design/system.md](../design/system.md) §4.4／§7.1。

> **為什麼這五項沒有在發現的同一批修掉。** 動 `contracts/openapi/public.yaml` 就必須跑 `task gen:openapi`，而 2026-08-22 當下 `tools/devctl/generate_openapi.go` 的 Go 輸出目標已被 ADR-032／N-7 的拓撲遷移改到 `apps/platform/internal/entrypoint/api/gen/`（**未追蹤目錄，且該批尚未 commit**）。在那之上重新生成，等於把別人整批重構夾帶進本批的 commit；只改 schema 不重新生成，則會讓 `task gen:check` 對所有人變紅。**這是 AGENTS.md 第 6 條「高衝突區由主 Agent 序列化」的實際樣子——序列化的意思包含「排在後面的那個要等」。**

> **2026-08-22 稍晚補記：拓撲遷移落地後這一批可以動了，五項裡兩項半結案。** 上面那段「為什麼沒有在發現的同一批修掉」的封鎖條件已解除（`b6541d1` 之後 `apps/platform/internal/entrypoint/api/gen/` 進版控，`task gen:check` 對得起來）。逐項狀態見各列。**同時要更正本節的一處引用**：丙-29 原文把「兩個平面不得把同一件事講成兩種話」記成 `02:NFR-001`。查證後 **`02:NFR-001` 標題是「安全」，其唯一的 UI 條款是「靜態掃描通過不等於安全保證，UI 必須避免此類誤導」，沒有一句談兩平面措辭一致**。真正擋住「刪掉對照表」的是 **`02:NFR-007` 第 3 條「風險、相容與評估狀態必須同時提供文字」**——它剛好點名了風險與相容，也就是本節裡真正該動的那兩項。設計系統 §4.4 的引用同步更正。

> **另一個要記的數字：五個缺口只解釋 20 份對照表裡的 8 份。** 把丙-29 照字面做完，還會剩 12 份（評估判定、驗收條件、發現分類、嚴重度、建議分類、打包阻擋原因、Script 狀態、回饋類別…）。**§4.4 的矛盾不是靠這一項關掉的**，它只是把最會出事的那幾格關掉。

| # | 殘項 | 內容與補法 | 解鎖 |
| --- | --- | --- | --- |
| 丙-28 | **契約寫錯一個列舉值，而前端照著錯的那半邊編譯** | `RunListItem.cleanup_status` 宣告 `[pending, cleaning, cleaned, failed]`，同一份契約裡的 `Run.cleanup_status` 宣告 `[pending, cleaning_up, cleaned, failed]`。**真相是後者**：資料庫型別 `run_cleanup_status`（[`0004_test_lab_and_runs.sql:75`](../../db/migrations/0004_test_lab_and_runs.sql)）就是 `cleaning_up`，而 `trial/execution/http.go:291` 以 `string(row.CleanupStatus)` 原值上線，中間沒有映射。<br>**使用者看到的後果**：沙箱正在拆除的那段期間，`/workspace/runs` 的該列渲染「清理狀態：」後面空白——**設計系統 §2.9 明文「空白是缺席唯一不能用的形狀」**，而 §2.1 的理由是空白會被讀成通過。<br>**前端半邊已修（2026-08-22）**：`api/runs.ts` 的 union 改為 `cleaning_up`，兩張對照表跟著改，並在呼叫點加上「平台回報 `<值>`」的退路——**因為型別守門對這個缺陷是無效的：那個 union 自己就是錯的東西，「對 union 完備」證明不了任何事**。`workspace.test.tsx` 新增具名迴歸測試，已用移除退路的方式反證它守得住。<br>**契約半邊待補**：把 `RunListItem.cleanup_status` 的 `cleaning` 改成 `cleaning_up`，重新生成三語言產物<br>**✅ 已結案（2026-08-22）：契約半邊補上，三語言產物重生。** `RunListItem.cleanup_status` 的 `cleaning` 改為 `cleaning_up`，並在 schema 裡寫明資料庫型別才是權威。前端半邊本來就已修（union ＋ 呼叫點退路 ＋ 具名迴歸測試） | `WS-004`（**已勾，而勾選當時這一格是壞的**——列表面的驗收準則沒有涵蓋「列出來的值前端認不認得」） |
| 丙-29 | **五個列舉只送裸值不送標籤，於是前端養了 20 份中文對照表** | 契約在 `tier`／`trust`／`license` 上送 `{value, label, note}`，在 `RunListItem.status`／`cleanup_status`／compatibility 三軸／risk flag／artifact status 上**只送裸 enum**。前端因此有 20 份 enum→中文對照表（含 `RunCompare` 與 `RunEvaluation` 曾各存一份同內容、而型別寬度不同的重複，已於本批合併）。<br>**這與設計系統 §4.4 的規則直接矛盾**：該條要求徽章文案由伺服器給，好讓兩個平面不會把同一件事講成兩種話。**但刪掉對照表會讓 `pending` 直接出現在畫面上，`02:NFR-007` 禁止把文字拿掉**——所以規則對、契約沒跟上，這是契約缺口不是前端缺陷，理由已寫進各檔案的註解而不是只寫在這裡。<br>**補法**：把這五處提升為 `Labelled`（既有 schema，不必新發明）。**做完之後 20 份對照表要一起刪**，否則留著的那份就是會過期的那份<br>**逐項裁定（2026-08-22），不是五項一起做：**<br>①**`RunListItem.status` 維持裸 enum。** 那份對照表被 **4 個 schema、5 個畫面**共用，只給其中一個加標籤會刪掉**零**份對照表、多出第五個措辭權威；而且 `status` 的 note 是每列重複同一句 40 字（預設 50 列、最多 200 列，約 +14KB／+57KB），那是**每頁一次的事實，不是每列一次的**。丙-32 要的是 `evaluation_overall`，不是更漂亮的執行狀態文案。<br>②**`cleanup_status` 值得加標籤**：一個欄位、一個消費者、四個值，而真正出事的那次（丙-28）是值漂移渲染成空白——伺服器給標籤讓「未知的值渲染成空白」變成不可能，那是前端 union 做不到的。<br>③**相容性三軸值得加標籤**（一次改動、三軸）：兩個畫面已經把 `spec_validation` 講成兩種話，而其中一個對 `failed` 是錯的；`runtime: transpiled` 有一句猜不出來的但書，那是真的 `note` 不是填充。<br>④**風險旗標不能用 `Labelled`——它們是布林不是列舉**，型別對不上。正解是把 `SkillRisk`／`SearchResultRisk` 上兩組平行布林換成一個 `disclosures: [{code,label,note}]`，順帶修掉第六個旗標只在搜尋列有、詳情頁沒有的不對稱。<br>⑤**下載產物狀態不能掛在 `status` 上**：畫面上的那個詞是 `status`、`purged`、`expires_at` **三個事實**的函數（`download.go:155`），而 `purged` 根本沒出現在契約裡。只給 `status` 一個標籤，等於交給前端一個會和 `statusWord()` 打架的標籤——那正是 §2.2 的「顯示但不強制」。要嘛送出推導後的 `servable` ＋ 一個涵蓋合成狀態的 `Labelled`，要嘛維持裸值。<br>**同時已在前端修掉兩處現行分歧（不需契約改動）**：`FLAG_LABELS`（5 項）與 `RISK_FLAGS`（6 項）對同一組布林用**不同措辭**，搜尋列的版本把「**可執行**」拿掉了——那不是比較短的標籤，是比較小的主張；已合成單一來源。`Home.tsx` 的行內 `spec_validation === "passed" ? "通過" : "未驗證"` 把三個值壓成兩個，`failed` 會被講成「未驗證」；已改用同一份 `SPEC_LABELS`<br>**◐ 2026-08-22（本批）：②③④⑤ 四項落地，①依上面的裁定維持裸值，整列因此關閉。**<br>②`cleanup_status` 兩處（`RunListItem` 與 `Run`）改送 `Labelled`,`WorkspaceRuns.tsx` 的 `CLEANUP_LABEL` 刪除,只留 `CLEANUP_BADGE`——**留下的是色調不是措辭**:§4.4 把 `--danger` 給「這件事不通過」,清理失敗是那一格而待清理與清理中不是,而色調是呈現、屬前端。伺服器對不認得的值**把原值當成自己的標籤**,所以 `a-state-from-a-newer-server` 現在渲染成那串字而不是空白——丙-28 的失效形態從另一端關掉。<br>③相容性三軸改送 `Labelled`,`SPEC_LABELS`／`CAPABILITY_LABELS`／`RUNTIME_LABELS` 三份對照表刪除。**逐軸的 `note` 刻意大多為空**:區塊層級的 `compatibility.note` 已經說了靜態驗證涵蓋什麼、兩個沙箱軸需要真的跑過,三個軸各重複一次就是同一件事在一頁上講四遍(checklist 第 14 條)。非空的只有 `failed`／`not_activated`／`transpiled` 三個——**光看標籤會被誤讀的那三個**,`transpiled` 尤其:那次 Run 有產出,而做事的不是 Skill 自己的程式碼。<br>④風險旗標改成 `disclosures: [{code,label,note}]`,`RISK_DISCLOSURES` 刪除。**六個代碼一份型錄,兩個端點共讀**——搜尋列多一個 `dependency-file` 而詳情頁沒有的不對稱同批消失,而且它不是「補上第六個」而是**讓那個形狀不可能再出現**:兩組平行布林可以無聲地長得不一樣,一份有序清單不行。`level` 的判定改成 `len(disclosures) > 0`,所以型錄多第七個代碼時不必記得回來改條件。<br>⑤`DownloadArtifact` 加 `servable` ＋ `serve_state`。**推導的第三個輸入 `purged` 根本不在這個 schema 上**,所以 `DownloadArtifactFacts` 與 `Downloads.tsx` 各自算的是**跟伺服器不同的謂詞**——被 purge 的 artifact 會拿到一個保證 404 的連結。`statusWord()` 與 `isExpired()` 的呼叫端一併刪掉。<br>**沒有動①**:那份對照表被 4 個 schema、5 個畫面共用,只給一個加標籤會刪掉零份對照表而多出第五個措辭權威;`status` 的 note 又是每列重複同一句 40 字。裁定理由見上一段,本批沒有改變它。 | `NFR-007` 第 3 條、`NFR-001`（兩平面一致）；設計系統 §4.4 第 1 列 |
| 丙-30 | **`GET /me` 不回刪除範圍句，所以「可追蹤狀態」重新整理一次就沒了** | `02:SEC-006` 要求刪除工作有可追蹤狀態，丙-22 已補上 `deletion_requested_at`／`purge_after`。**但伺服器對刪除範圍的那段說明只存在於 `DELETE /me` 當下的回應體**（schema `AccountDeletion.scope`），`Me` 的 required 欄位裡沒有它。前端只能把它放進 component state，於是**重新整理之後，設計系統 §2.8 稱為「整個揭露」的那段話就不見了**，而按下之前的文案還寫著「確認之後這一頁會列出伺服器的完整說明」。<br>**前端半邊已改（2026-08-22）**：文案不再承諾這一頁做不到的事，改成「按下之後伺服器會回覆」。**這是把謊話拿掉，不是把功能補上。**<br>**補法**：`Me` 加上 `deletion_scope`（申請中才有值），前端把 pending 分支改讀它<br>**✅ 已結案（2026-08-22）：`Me.deletion_scope` 上線。** 契約 required（nullable，未申請時為 null），`GET /me` 與 `DELETE /me` 共用同一個 Go 常數——**一句話兩個端點，不是兩份會各自過期的文案**。前端的 pending 分支改讀它，並補上 reload 情境的具名測試（該測試從頭到尾不呼叫 `DELETE /me`，那正是缺陷發生的情境）。同批把「伺服器沒有回報寬限期日期」與「沒有附上刪除範圍」兩種缺席各給一句話，不留空白（§2.9） | `SEC-006` 第 2 條、`CORE-007`（**已勾**）；設計系統 §2.8 |
| 丙-31 | **`GET /skills` 不帶任何證據欄位，所以「我的 Skill」清單上沒有一項能拿來判斷要不要跑它** | schema `Skill` 有 `skill_id`／`name`／`summary`／`redistribution`／`access_restriction`／`forked_from_*`（**2026-08-24 訂正**：原文寫「只有」四項，`redistribution` 與 `access_restriction` 後來加了上去——但**那兩項都不是證據欄位**，一個講可不可以下載、一個講授權受不受限，所以本列的論證不受影響）。公開搜尋列的四項證據（`tier`／`risk`／`compatibility`／`verified_at`）全在 `PublicSearchResult` 上（**2026-08-24 訂正**：原文寫 PublicSearchHit（不加反引號：它不是符號），**全 codebase 沒有這個名字**，真名見 `apps/web/src/api/types.ts`），這裡一個都沒有。<br>**這是一份「你自己會拿去執行的程式碼」的清單，而它上面沒有任何可判斷的東西**——設計系統義務 §1.1 的字面。<br>**前端半邊已做（2026-08-22）**：頁面現在明說這一列只有名稱與摘要、四項證據不在這份清單的資料裡、**「這裡空著不代表通過」**。§2.1 要求缺席可見，那不需要契約改動；**要看得到證據才需要**。<br>**補法**：`Skill` 帶上與 PublicSearchHit 同名同型的四個 facet（同名很重要，兩個畫面對同一件事用兩種欄位名就是 `NFR-001` 的另一種犯法）。<br>**附帶一件小的**：`forked_from_skill_id`／`forked_from_version_id` **契約有、前端型別漏了**，所以那一頁的檔頭說它分得出「Fork 進來的」與「自己匯入的」，實際分不出來。這一項不必等契約，是前端自己的洞<br>**◐ 部分結案（2026-08-22）：三件做完，兩件因為要先做決定而留著。**<br>**做完的**：①`redistribution` 與 `access_restriction` **本來就在 `ListSkills` 選出來的列上、在序列化時被丟掉**——零 join、零查詢、零治理成本，而它們是四道鎖裡的兩道，`unknown` 又是使用者自己匯入的預設值，所以「不能打包的」和「能打包的」在這份清單上長得一模一樣，一直到打包畫面才分曉。②**`GET /skills` 靜默截斷在 100 筆**（`Limit: 100` 寫死、沒有分頁、沒有總數），第 101 個 Skill 對這一頁而言不存在。改成多取一筆以偵測並回報 `truncated` ＋ `limit`，畫面說出來——**強制而不顯示是 §2.2 的第二個方向**。③`forked_from_*` 的洞不在 `types.ts`（那裡本來就有），在 **`api/testcases.ts` 為同一個 endpoint 另外宣告的一個更窄的 `SkillSummary`**——一個 endpoint 兩個型別，缺口就藏在窄的那個裡。已刪除，改用契約形狀，畫面因此第一次分得出 Fork 與自行匯入。<br>**留著的兩件，各自要先有決定**：`risk` 需要 ADR-034 式的反轉（catalog 開一個 workspace-scoped 讀取面，由 composition root 注入 registry——`read_allow:` 目前是零條，而該檔明文說它不是擴充點；`SourceByID` 是現成的同型前例）。`verified_at` 與 `compatibility` 有**一個語意問題比 SQL 難**：`registry.Fork` 會建一個新的 `skill_versions` 列，`created_at` 是**按下 Fork 的那一刻**，而那時沒有任何掃描跑過——照搬搜尋路徑的定義會印出一個讀起來像「剛剛掃過」的時間戳，**比空白更糟**。裁定：Fork 出來的 Skill 的 `verified_at` 是 §2.9 的 `未測量`；沒有任何版本的是 `不適用`；要不要把祖先的量測歸給後代（內容雜湊相同，論證得起來）是**產品決定，而且不得靜默進行**<br>**✔ 結案（2026-08-22 稍晚）：兩件都做完了，而做的過程把它們合成一件。**<br>**兩件其實是同一個缺陷的兩面**：`risk` 與 `verified_at` 都是**匯入掃描**的產物，而 **Fork 複製位元組但不複製量測**——`catalog.IndexSkill`（Fork 走的那條）只寫 name 與 summary，`scan` 留 NULL；`skill_runtime_compatibility` 掛在 `skill_version_id` 上，Fork 建新版本列所以也沒有。所以 Fork 的兩個 facet 同時空，理由同一個，而那個理由是**說得出口的**。<br>**① `risk` 走 ADR-034 反轉**：`search.sql` 新增 `ListSkillScans`（catalog 擁有、workspace scoped），catalog 開 `Service.SkillRisks`，由 `apiserver.NewApp` 注入 `registry.Service.SkillRisks`——與同一個 root 上那兩個 projection **寫入**同型，只是方向相反。`read_allow:` 維持零條。**與 `SourceByID` 一處刻意不同：blob 以 `json.RawMessage` 原樣穿過，不做 mirror struct。** registry 一個欄位都不讀，它只是把 catalog 的區塊轉發到**同一個 `SearchResultRisk` schema**；再宣告一次形狀，就是多一個會漂的地方，而這個 facet 存在的唯一理由正是兩個平面不能各講各的（`NFR-007` 第 3 條）。<br>**② `verified_at` 不送裸時間戳，送具名狀態。** 判別式是現成的：**Import 會設 `skill_versions.source_id`，Fork 明文留 NULL**（`skill_sources` 屬於來源工作區），所以 `source_id IS NULL` ＝這個版本是複本。三個狀態 `scanned`／`not_measured`／`not_applicable`，用 **`Labelled`**（丙-29 裁定②的形狀，在出生時就套上而不是事後補），所以前端**沒有第 21 份對照表**。<br>**③ `compatibility` 查證後決定不做——這一項是撤回，不是延後。** 全 repo 只有 `tools/content/backfill-agent-compatibility.sql` 寫 `skill_runtime_compatibility`，**產品程式沒有任何一處寫它**。所以這份清單上的相容性欄位只可能永遠是 `未測量`：一個看起來像證據、但結構上不可能承載證據的欄位，比沒有更糟。頁面改成明說「平台目前也不會為你自己的 Skill 量測它」，並在該處註明**不要為了塞欄位而刪掉這句**。<br>**④ 順手收掉一個雙重措辭**：搜尋列的 `unavailable` 分支在前端硬寫了一句「尚無掃描紀錄，狀態未知——不代表已通過檢查。」，那正是伺服器 `searchRiskUnknown` 的同一句、標點不同，所以同一列把同一件事講了兩次兩種寫法。抽成共用的 `RiskSummary`（搜尋列與我的 Skill 共用同一個元件）後只剩伺服器那一句。<br>**留下的產品問題（不是實作）**：位元組相同的複本，能不能繼承祖先的量測？論證得起來（`content_hash` 相同、靜態掃描確定性），但沒有人決定過。契約與畫面現在都明說「那是來源的量測結果，不是這裡的」——**未決定不等於未揭露** | `WS-002` 第 1 條、`DISC-004`；設計系統 §1.1、§2.9 |
| 丙-32 | **`RunListItem` 沒有判定欄位，所以 Run 歷史只渲染得出一軸** | ADR-025 與設計系統 §2.5 要求執行狀態與任務判定永遠兩列、**判定排在前面**。`GET /runs` 與 Test Case 的執行歷史都只有 `status`。<br>**現況的緩解是誠實的但順序是錯的**：頁面用一句 `.note` 說明「這裡寫的是執行狀態，不是任務有沒有做到」，本批已把它從清單**下方**移到**上方**（讀者要在讀一整欄「執行完成」之前被告知，不是之後）。**但一句註腳不是第二列。**<br>**補法**：`RunListItem` 帶上 `evaluation_overall`（可為 null＝未評估，而 §2.9 要求 null 渲染成`未評估`不是空白）。<br>**取捨要記一下**：`GET /runs` 當初刻意做得比 `GET /runs/{id}` 窄（一頁列 N 個，狀態轉移與 attempt 不送），加一個欄位要確認它不會把列表變成整個 API 最重的一次讀取——判定是一個 enum，成本應該可以接受，但那要量過再說<br>**2026-08-22 補記：治理路徑已查明，而且沒有便宜的那條。** `db/query-owners.yaml` 的 `read_allow:` **目前是零條**，且該檔明文「不是擴充點——新的跨 context read 一律改程式」。在 `runs.sql` 的查詢裡 JOIN `evaluations` 不會被 CI 擋（檢查器看的是「哪個 context 呼叫哪個 query」，不看 query 碰哪些表），**但那正是把跨 context 讀取藏在檢查器看不到的地方**——與 ADR-033 當初要擋的形狀同型。所以是 ADR-034 式的反轉，或前端另外呼叫一個 eval 擁有的批次端點（`GET /runs` 維持窄，代價是多一次請求）。**兩條路都要先有人選**<br>**✔ 已裁定（2026-08-22，[ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 7）：走 ADR-034 反轉，不走第二個端點。** 理由是 §2.5 不是效能——那條規則要求判定與執行狀態**同一列、判定在前**，而第二個請求意味著整份清單先以單軸渲染再補第二軸，**讀者會在判定到達之前先讀完一整欄「執行完成」**，正是這條規則要防的事。形狀與同日落地的 `SkillRisks` 相同（eval 開 workspace-scoped 批次讀取面，由 `apiserver.NewApp` 注入 run），判定送 `Labelled`（丙-29 裁定②的形狀），順帶收掉那份對照表的一半。**✔ 結案（2026-08-22 稍晚）。** `evaluation.sql` 新增 `ListCurrentEvaluations`（與 `GetCurrentEvaluation` 同一個 `superseded_at IS NULL` 述詞，批次版），eval 開 `RunVerdicts`，`apiserver.NewApp` 注入 `run`。<br>**送 `Labelled` 而不是裸 `overall`，理由不是好看而是 `overall` 單獨看會說謊**：evaluation 建立時就帶著 `undetermined` 並且一直帶到 Judge 跑完，所以 pending／failed 的評估讀起來都是「無法判斷」——而那是**Judge 得出的結論**，不是「沒人判過」。折疊 status 與 overall 只有 eval 這個 context 做得到，所以它交出的是成品文案不是兩個 enum，值域是 `not_evaluated`／`evaluating`／`evaluation_failed` ＋ 四個判定。<br>**`evaluation` 是 required 而不是 nullable**：`未評估` 是一個值不是一個省略（§2.9），而**沒被評估的 Run 是任何歷史裡最常見的一列**。<br>**兩個歷史畫面共用 `RunVerdict` 元件**，判定排在執行狀態之前；兩頁原本各自掛著一句「這裡寫的是執行狀態，不是任務有沒有做到」的腳註，那句話現在改寫成「每一列兩軸」的說明留著——**順序本身教不會任何人兩軸的差別**。<br>**`evaluation_failed` 不上 `--danger`**：它說沒人判過，染成失敗色會把它那句 note 花一整句移除的誤讀又裝回去 | ADR-025 在列表面的落實；設計系統 §2.5 |

### 丙（續四）：2026-08-22 產品裁定批新增（丙-33～丙-35）

> [ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 一次回答了 ADR-041 留下的六個待決策。**六項裡有三項立刻產生實作工作**，列在這裡；另外三項（評分、進行中、強制者具名）的規則已寫進設計系統，但它們對應的功能都還沒開始做，所以不是殘項而是路線圖前置。

| ID | 缺口 | 補法 | 依據 |
| --- | --- | --- | --- |
| 丙-33 | **Fork 出來的 Skill 明明是同一份位元組，卻對每一個讀者顯示「未測量」** | 決策 6 裁定：**對位元組成立的量測可以繼承**（靜態掃描、規格驗證、License），對環境成立的不可以（相容性、試跑、評估）。外部依據是 in-toto／SLSA：attestation 綁定的是 subject 的 **digest 而不是位置**，tag 可改指、digest 不行。<br>**四個前提缺一不可**：`content_hash` 在讀取當下比對相等（不得假設 Fork 當時相同就永遠相同）；**只從公開目錄祖先繼承**（祖先在別人的私有工作區時答案是 `無權檢視`，ADR-011 不因溯源鏈存在而放寬）；具名歸屬且**時間戳是祖先掃描的時間不是 Fork 的時間**（較舊的時間戳是誠實的，而且它告訴讀者這次掃描有多舊）；**祖先 takedown 之後不得繼續繼承**。<br>**落點**：`skill/library` 的 `verificationOf` 與 `SkillVerification`，`value` 維持 `scanned`、由伺服器的 `label`／`note` 說出來源——繼承的量測**不是缺席**，不進 §2.9 那張表<br>**✔ 結案（2026-08-22 稍晚）。** 四個前提**全部寫在 SQL 的 join 裡，一個都不是在 Go 裡假設的**：`ancv.content_hash = ver.content_hash`（當下比對，所以後來自己匯入新版本的 Fork 會落回一般的 `scanned` 分支，那也正確——它有自己的掃描）、`JOIN workspaces w ON w.is_catalog`、`anc.takedown_at IS NULL`、`anc.deleted_at IS NULL`。跨 scope 的那一半是 catalog 新開的 `ListCatalogSkillScans`／`CatalogSkillRisks`，**刻意不收 workspace 參數**——scope 烤進語句裡，比照 `GetCatalogSkill`，呼叫端說不出更寬的範圍（鐵律 3），所以私有祖先根本不會回來，而畫面得到的是 `未測量` 而不是一次越界讀取。<br>**`未測量` 這一格因此變窄而更誠實**：它現在只涵蓋「來源答不出來」（已下架、已刪除、不在公開目錄、或內容已經不同），措辭也照著改了。<br>**順手把歸屬變成可達的**：`forked_from_skill_id` 從丙-31 起就在列上，卻渲染成一句話；現在是連結——**歸屬只有在讀者去得了那裡的時候才算歸屬**。<br>**一個踩到的坑記著**：`inherited_from_name` 一開始生成成裸 `string`，因為 sqlc 讀資料表的 NOT NULL、看不出 outer join 的欄位可為 NULL，而**大多數列都沒有可繼承的祖先**——那會在第一列就 panic。`search.sql` 的註解早就寫過同一件事，COALESCE 補上 | [ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 6；設計系統 §2.9 |
| 丙-34 | **搜尋結果完全不設限，而且不說總數** | 決策 3 讓四種並存的分頁策略收斂成三種，**「完全不設限」不再是選項**，而搜尋結果是唯一落在那一格的。同批的硬規則是「任何被截斷的清單都必須說出總數與截斷理由」——目前四處只有 Trace 做對，`GET /skills` 於 2026-08-22 補上（丙-31），**搜尋結果與 Trace 重複事件的硬截斷還沒有**<br>**◐ 搜尋結果已結案（2026-08-22 稍晚），Trace 重複事件仍未。** 查清楚之後「完全不設限」的描述要修正：**上限一直都在**（預設 20、最大 100），缺的是**它從來不說自己截斷了**——第 21 筆對這一頁而言不存在，而安靜地短一截讀起來就是完整答案。檢索多取一筆來偵測，回報 `limit` ＋ `truncated`。<br>**多取的那一筆在 `Service.Search` 裡就砍掉，不是在 handler**：那一筆只是用來回答「還有沒有更多」，讓它進到 match-reason 的批次呼叫等於**為一個沒人會看到的結果付一次模型錢**。<br>**與 `degraded`／`partial_index` 刻意分開措辭**：那兩則說的是「我們看得夠不夠清楚」，這一則說的是「找到的有多少在這一頁上」<br>**✅ 2026-08-22 稍晚查證：Trace 那一半在本項寫下之前就已經成立，整列關閉。****這一列自己的第一句就寫著「四處只有 Trace 做對」**,後面卻把它列進未做——同一格互相矛盾,而查證只花一次搜尋。<br>證據:硬截斷是 [`db/queries/trace.sql`](../../db/queries/trace.sql) 的 `skill_rows`／`error_rows` 各 `LIMIT 100`,而**總數是不設限的 `count(*)`**(`skills_total`／`errors_total`),`Summary.Truncated` 由 `總數 > 已列` 推出,畫面 [`RunTrace.tsx:263`](../../apps/web/src/pages/RunTrace.tsx) 印的是「重複事件僅顯示前 100 筆(Skill n/N;錯誤 n/N)。完整事件仍可在進階模式分頁查看。」——**總數、截斷理由、去哪裡看完整的,三樣都在**。該 query 的註解也早就寫著「their exact totals are returned so truncation is explicit rather than silent」。<br>另外兩處同樣成立:`missing_seq` 上限 1000 而 `missing_count` 是總數,畫面在兩者不等時補「(共 N 個)」;進階模式是 `has_more` ＋ `next_after` 的**明確分頁**,分頁不是截斷。<br>**時間點**:兩邊分別在 `3dcc2d4` 與 `3b83166`(皆 2026-08-20)落地,**早於本項寫下的 2026-08-22**。<br>**這是同一批的第三次同型更正**(另兩次見丙-36 與丙-29①):**把一件已經做完的事記成待辦,和把沒做的記成做完一樣貴**——它會讓下一個人去改一段已經對的程式碼。 | [ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 3；設計系統 §4.3 |
| 丙-35 | **十二處載入狀態都是 `<p>載入中…</p>`，說不出「會不會自己結束」與「能不能離開」** | 決策 2 的 §2.12 要求進行中的畫面說出三件事、並帶一個會變的量與一個「多久沒動了」。**十二處不必一次改完**，但 Run 進行中的那幾處是先做的——它們是十分鐘旅程裡唯一會讓人枯等的畫面。<br>**有一個前置不是 UI**：畫面要說「可以關掉分頁」之前，得有人確認那是真的（§2.2 不得顯示沒被強制的承諾），見本檔乙類<br>**◐ Run 的進行中畫面已結案（2026-08-22 稍晚）；那十二處「載入中…」查清楚之後不屬於這一項。** 它們是**請求**的載入指示，不是**長時間工作**的進行中狀態——一個 200ms 的 fetch 不需要「能不能離開」。§2.12 的對象是 Run，而 Run 的進行中畫面（`RunTrace`，非終態時每 3 秒輪詢）此前**沒有任何自己的呈現**：它顯示的是一份內容比較少的 Trace，而那讀起來與「一個做得很少然後結束的 Run」一模一樣。十二處載入文案的一致性是另一件小事，不是 §2.12。<br>**新增 `InFlight` 元件**：哪一步、會自己跑完、可以關掉這一頁、已記錄幾件事、最後一件在什麼時候；終態時整個消失（已經有判定與執行狀態，再多一句等待的話就是同一件事講三次）。<br>**`last_event_at` 是伺服器新增的欄位，不是前端算的**（`trace.sql` 的 general fold 多一個 `max(occurred_at)`）。前端計時做不到這件事：**重新整理之後它會從零開始，把一個卡住的 Run 報成「剛剛才動過」**。而計數器單獨也不夠——卡住的 Run 計數器就是停住，而停住看起來跟跑完一模一樣。<br>**還沒有任何事件是具名狀態**，不是 0：一個還在配置環境的 Run 本來就沒產生過事件，印「0 秒前」是三種呈現裡最糟的一個<br>**✅ 2026-08-22（契約與可觀測性批）整列關閉，而關閉的方式與這一列原本設想的相反。**<br>**先更正兩個數**:不是十二處是 **30 處**(27 個渲染 ＋ 3 個「載入更多」按鈕標籤);而「一致性是另一件小事」這個降級**大致對但漏了一處真的 §2.12**。<br>**漏掉的那一處是 `RunEvaluation`**:`useEvaluation` 在 `status === "pending"` 時**每 3 秒輪詢一次**(`api/evaluation.ts`)——那是一個**模型評審正在跑**,使用者在等,而畫面把那一列交給 `EvaluationReport` 當成一般答案渲染。**判定還沒有人做出來,而那一格長得像一個判定**——§2.12「進行中是第三軸,不是判定的一個值」逐字講的就是這件事。已補上三句話(哪一步／會自己完成／可以關掉這一頁),「可以離開」的強制點與 `InFlight` 同一條:`evaluate_run` 是 Postgres 裡的 River job(`trial/improvement/job.go:23`),由 worker 消費,瀏覽器不在路徑上。<br>**§2.12 要的「會變的量」這裡誠實地沒有,所以寫出來而不是造一個**:評審不是分批完成的,它要嘛給出判定要嘛失敗。用經過時間做進度條會在什麼都沒發生時照樣前進。<br>**404 刻意不算進行中**:`useEvaluation` 連 404 都輪詢(還沒開始評的 Run 沒有列),但**一個從來沒人評過的舊 Run 會 404 到天荒地老**,對它印「評估進行中」就是一個沒有東西在強制的承諾(§2.2)。那一格維持「未評估」,只補一句它實際在做的事:每 3 秒再查一次。兩個方向各有一支具名測試。<br>**其餘 27 處**:真正的缺陷不是文案而是**幾乎每一處都沒有 `role="status"`**——螢幕閱讀器在整頁空白時什麼也沒被告知,答案然後無聲出現。新增 `components/Loading.tsx`(`what` 是必填,所以這個元件**沒辦法**被拿來重新引入一句沒有主詞的「載入中…」),27 處全部換過。<br>**兩處拿到了免費的量**:`Compare` 印「3 個裡讀到 1 個」(平行讀取逐一到齊),`Downloads` 的下載紀錄印「共 N 筆」(`download_count` 本來就印在上一行)。`RunTrace` 進階模式改成點名它是最大的回應(一頁最多 1000 筆)。<br>**一個被順手修掉的假綠燈**:四支測試用 `waitFor(() => !text.includes("載入中"))` 等待,而**文案一旦帶了主詞就再也不匹配**——那個等待會立刻通過,斷言於是打在一張畫到一半的頁面上。已改用 `Loading` 上的 `data-loading` 屬性,文案怎麼改都不會再讓等待失效。<br>**不做的**:三個「載入更多」按鈕(按鈕自己 disabled ＋ 換字,本來就是最誠實的形式)、`Home` 的「搜尋中…」(用 `isFetching`,舊結果還在畫面上)、`index.css` 不引入任何 spinner 或 skeleton(全檔沒有 keyframes,這不是開先例的地方)。 | [ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 2；設計系統 §2.12、checklist 第 17 條 |
| 丙-36 | **瀏覽器層在 webServer 起不來時回報「零個測試」並且 exit 0——綠燈與真的跑過 69 個長得一樣** | 2026-08-22 實測：把 4173 佔掉再跑 `npx playwright test`，輸出是 `PASS (0) FAIL (0)`、**exit code 0**；用 `rtk proxy` 跑原生 playwright 結果相同，所以不是輸出包裝的問題。對照組：`--grep` 沒命中任何測試時 exit 是 **1**，所以 Playwright 本身會擋「零個測試」，擋不住的是**它自己啟不動 webServer 的那條路**。<br>**這一項是這次差點被自己騙過去的**：`npm run build` 因為一個型別錯誤失敗 → webServer 起不來 → 瀏覽器層「通過」。`.github/workflows/ci.yml:152` 的 `npm run test:e2e` 會用同一條路徑變綠，而 [ADR-036](../adr/ADR-036-real-browser-verification-tier.md) 引入這一層的整個理由就是有些事只有真瀏覽器測得到——**一層在自己沒跑的時候宣稱通過，比沒有那一層更糟**。<br>**補法**：在 e2e script 後面加一道「本次執行的測試數 > 0」的斷言（`--reporter=json` 的 `stats.expected`，或 `npm run test:e2e` 之後檢查報告），讓零個測試變成 FAIL。順帶：`tsc -b`（`npm run build` 用的）會檢查測試檔而 `tsc --noEmit` 不會，本次的型別錯誤只有前者抓到——CI 的型別關卡要確認走的是哪一個<br>**✅ 2026-08-22 稍晚重測，原判定要更正:在 Playwright 1.62.1 上這個缺陷重現不了,而且 CI 的型別關卡本來就是對的那一支。**<br>三個情境各量一次,**全部 exit 1**:①佔住 4173 再跑(`CI=1`)——`webServer` 報「port already used」,exit 1;②真的塞一個型別錯誤讓 `npm run build` 失敗——`Process from config.webServer was not able to start. Exit code: 2`,exit 1;③不設 `CI` 時 `reuseExistingServer` 為真,會去重用佔住那個 port 的假伺服器,測試跑起來然後失敗,一樣 exit 1。**對照組**:把我加的守衛拿掉(`--reporter=github` 覆蓋掉 config 的 reporter)結果不變,所以 exit 1 是 Playwright 自己給的。<br>**那當初看到的 `PASS (0) FAIL (0)` ＋ exit 0 是什麼**:是本機 `rtk` 對 playwright 的**輸出包裝**,它會覆寫 `--reporter` 並自行摘要。原記載寫著「用 `rtk proxy` 跑原生 playwright 結果相同」——**那一句是誤讀,本次以 `rtk proxy` 重跑三次都是 exit 1**。缺陷屬本機工具鏈,不屬 repo,`ci.yml:152` 不會這樣變綠。<br>**順帶那半件本來就成立**:`package.json` 的 `typecheck` 是 `tsc -b --noEmit`,CI 跑的就是它,而 `tsc -b` 會檢查測試檔——本項擔心的「CI 走的是哪一支」答案是對的那一支。<br>**因此不加守衛**:一支只擋一個擋不住的情境、而且被任何 `--reporter` 覆寫就失效的 reporter,是**看起來有在守、實際沒有**——正是本項自己在譴責的形狀。**留下的是這段量測紀錄**,下次再看到 `PASS (0)` 時先分辨是 Playwright 還是包裝器。 | ADR-036 這一層的存在理由；`.github/workflows/ci.yml` |
| 丙-37 | ✅ **已結案（2026-08-22，本批）：20 條告警規則寫好了、門檻由 ADR-022 定值了，而在此之前沒有任何東西會載入它們** | `infra/observability/alerts.yml` 有 4 個群組 20 條規則,`metrics.go` 有對應的指標,ADR-022 第二部分把 X-02～X-04 的門檻逐項定了值——**中間缺的是求值器**。全 repo 沒有 Prometheus:`infra/compose/` 只有 postgres／seaweedfs／litellm,`Taskfile.yml` 沒有相關 target。所以 X-02～X-04 **沒有觸發路徑,缺的不是門檻是求值**。<br>**補法(已做)**:`infra/observability/prometheus.yml` ＋ compose 的 `observability` profile ＋ `task dev:observability`,UI 在 `127.0.0.1:9095`。首次載入即通過(`promtool check rules` 與實際 `/api/v1/rules` 各驗一次,4 群組 20 條),所以補的是路徑不是錯誤。<br>**scrape 宿主機而不是別的容器**:`cmd/api` 與 `cmd/worker` 跑在開發者機器上,而兩者讀**同一個** `METRICS_ADDR`——同一台機器要給兩個值(`:9090`／`:9091`),否則後起的那個綁不上,這一點寫進了 README。**`sandboxd` 故意不列為 target**:它的 `/metrics` 要 bearer,沒有 token 檔的 scrape 只會拿到 401,而**一個永遠 down 的 target 讀起來跟服務掛了一模一樣**。<br>**這一份是開發機的,不是生產部署**:Alertmanager、通知路由、silence 政策與 Grafana 仍屬部署期(甲類),README 的「未做的部分」段落照這個分界重寫 | `O11Y-001`～`003`；ADR-022 X-02～X-04 的量測前提；ADR-014 待決策 3／ADR-018 待決策 4 |

### 丙（續七）：2026-08-28 產品決策稽核批新增（丙-76～丙-80）

> 起因是一個 PDM 角度的提問——「今天的系統有沒有與當初的精神和路線圖背離的地方」。逐條對帳的對象是 `01` §4 的產品目標、§5 的七條原則、§6 的十六步核心旅程、§11 的指標與 §12 的風險表，佐以 PDM-001～011 的追認狀態。**結論的方向與過去幾批一致：程式普遍比文件保守，落差幾乎全在文件那一側。** 已就地訂正的六句在 `01`（commit `4731fb8`）。下列五項是訂正不掉的——它們要的是程式或一個承接者。

| ID | 缺口 | 補法 | 依據 |
| --- | --- | --- | --- |
| 丙-76 | **核心旅程的第二步在程式裡不存在，而它是「意圖搜尋」與「一個搜尋框」的差別** | `01` §6.1 步驟 2「系統分析輸入、輸出、工具、資料與環境需求」**沒有端點、沒有畫面、`02` 沒有需求 ID**——十六步裡唯一一步連半邊都沒有。實際發生的是查詢原文直接送 embedding（`skill/discovery/service.go` 的註解逐字寫著 the raw query is embedded as typed）。**同一個洞的第二半**：PDM-003／ADR-013 指定的**查詢改寫也沒有實作**（`apps/llm` 只有 `/embed`、`/match-reasons`、`/suggest-criteria`），而向量腿失效時的降級路徑正是提案逐字禁止的「原句 FTS」。**第三半**：FTS 腿寫死 `websearch_to_tsquery('english', …)`，所以**中文查詢一旦落到降級路徑幾乎必然無結果**，而這是一個介面全繁中的產品。<br>**補法要先有一個範圍決定**：`02` 要先給它一個需求 ID 與允收，否則做出來的東西沒有可判定的完成標準。**這一列的價值不在工時，在它到今天為止沒有被任何一份清單數到過。** | `01` §6.1 步驟 2、`01` §1 第 1 個問題、ADR-013、PDM-003 |
| 丙-77 | **精選層有了欄位，但沒有 operator 寫入路徑** | 同日 `0042` 讓 `01` §8 的三層策略第一次在畫面上成立，而**寫入只有 `tools/content/backfill-curation-tier.sql`**，沒有端點、沒有 audit event。**這是刻意的，而且有前例**：`access_restriction` 在 SEC-011 之前也只有一支 SQL（`restrict-anthropic-sa-display.sql`），當時同樣先記在這裡才補。<br>**具體後果有一個**：`CONTENT-009` 要求「來源變更可被偵測」之後的處置，今天做不到降級——`0041` 的 `content_changed_at` 標記寫得下去，但沒有任何東西會把 `curation_tier` 改回 `indexed`。**版本前進那一半有自動保護**（`curated_version_id` 不再是最新版即退回），**內容原地被改寫那一半沒有**。<br>**補法**：照 `discovery/restriction.go`／`redistribution.go` 的三段式加第四個 operator 動作（`PUT /admin/skills/{id}/tier`），`LockSkillForOperatorWrite` 的註解已經預告過會有第三個共用欄位 | `02:CONTENT-001`、`02:SEC-011` ①、`03` CONTENT-009 |
| 丙-78 | **`feedback_reports` 沒有 `session_id`，而兩處註解宣稱它與漏斗共用同一個 session 識別** | `learning/analytics.go` 的 `EventSessionStarted` 旁逐字寫著「that needs the reports and the funnel to share one session identifier」，`feedback.go` 的套件註解同義。**`0029` 的 `feedback_reports` 沒有那個欄位**（只有 `workspace_id`／`user_id`／`run_id`／`page_path`）。<br>**這不是缺一支查詢，是缺 schema**——`03` `BETA-004` 記的是「那個查詢不存在」，比實況樂觀一級，而 `RELEASE-009` 的第三個阻擋就卡在這裡。<br>**補法有兩半**：訂正那兩句註解（不等任何人），與決定要不要真的把 session 蓋章到回報上（那牽動 ADR-029 決策 4 刻意剪斷的那條線，**是產品決定不是實作**）。**先做前半**：一句錯的註解會讓下一個人去寫一支寫不出來的查詢 | ADR-029 決策 4、`03` BETA-004／RELEASE-009 |
| 丙-79 | **`session_started` 是一個沒有讀者的事件** | 四個分析事件裡只有它沒有被 `tools/analytics/funnel.sql` 引用過一次——第 7 段（首次使用後再回來）走的是領域表 `runs`／`evaluations` 的 UTC 日分桶，不是這個事件。它宣稱的第二個用途是與 `BETA-004` 串接，而那條路卡在丙-78。<br>**它也永遠不帶 `workspace_id`**（middleware 包住整張路由表，多數觸發點在未登入的公開頁）。<br>**補法是一個選擇不是工時**：要嘛給它一個讀者（第 7 段改以它為母體，代價是母體從 workspace 變成 session、與第 3／5 段不可串接），要嘛承認它今天只為 `BETA-004` 而存在並把這件事寫在 ADR-029 旁邊。**收一個沒有人讀的事件，與不收它，在隱私上不是同一件事** | ADR-029、`01` §11.2 第 7 段 |
| 丙-80 | **下架只在單一工作區內生效，濫用檢舉與 DMCA 沒有任何路徑** | `skill/library/registry.go` 的 `Takedown` 帶 workspace 述詞，該檔註解自陳「A platform-wide takedown of content in *someone else's* workspace — abuse reports, DMCA — has no path here」，**並且已經寫好升級路徑**（「a second query without the workspace predicate behind that role」）。<br>**規格早就有了**：`02:SEC-011` 明訂 operator 是**跨 Workspace 生效**的動作者，動作 ① 就是「變更目錄項的可見性（下架／恢復）」。缺的是實作不是決定。<br>**不會造出第二套**：`takedown_at`、410 Gone、搜尋索引移除、`ActionSkillTakedown` 全部已存在**且都不看是誰下架的**，operator 路徑寫同一個欄位、走同一個 `RemoveSkillFromIndex`、記同一個 audit action——`02:533`「不得為 operator 另開第二套」因此是結構上滿足而不是靠紀律滿足。<br>**恢復方向不是零成本**：把 `takedown_at` 設回 NULL 還要重新投影搜尋文件，而 `LockSkillForOperatorWrite` 今天不回 `name`／`summary` | `02:SEC-011` ①、`03` INGEST-010 ②／CONTENT-009 |

### 丙（續五）：2026-08-22 框架評估批新增（丙-38）

> 起因是一個框架問題——「沒有 LangGraph、沒有 ReAct 迴圈，`EVAL-002` 產出的提案妥當嗎」。查證結論是**機制不缺**：提案要穿過八層才會變成一個新版本（prompt 常數化與資料圍欄、strict `json_schema`、Python 值域收斂、Go 的證據回驗與路徑逃逸拒絕、`validatePatched` 重跑匯入級 `skillpkg.Validate`、使用者逐條決定、ADR-003 的新版本不覆寫）。**缺的不是一層防線，是一個數字**——所以它是丙類，不是乙類。

| ID | 缺口 | 補法 | 依據 |
| --- | --- | --- | --- |
| ~~丙-38~~ **已結案 2026-08-25** | **`EVAL-002` 的提案品質沒有基線：機制齊備，但沒有人量過「提案被打回的比率」** | [m3/audit.md](mvp/m3/audit.md) 第 7～10 項（`EVAL-007`～`EVAL-010`）四項全勾，勾的是**機制齊備**——五欄 NOT NULL、四類值域、預覽與套用跑同一套檢查、套用即建新版本。**沒有一項是在說提案本身有用。** 對照組是 `EVAL-013`：Judge 有 [report-judge-regression.md](mvp/m3/report-judge-regression.md) 這 51KB 的回歸集，量到了 v1 有 45 個 run 的正確答案被防線 3 丟掉，才有 `judge-run/v2`。`suggest-improvements/v1` **從發布到現在沒有對應的量測**，所以「它是不是也有一個系統性的 v1 缺陷」目前答不出來。<br>**這不是安全風險**：提案再爛也過不了 [apply.go](../../apps/platform/internal/trial/improvement/apply.go) 的 `validatePatched`（打進 zip 重跑 `skillpkg.Validate`，一條不過整組拒絕），也過不了使用者那一關。**是浪費**——gateway 的錢已經付了，使用者看到的是一條 `blocked_reason`。<br>**2026-08-23 進度：前置的可觀測性補上了,基線本身還沒跑。**<br>查下去發現**為什麼沒人量過**有一半是結構性的:①「模型提了幾條」與②「被丟掉幾條」**只存在於 `slog.Warn` 裡**,資料庫只留得下存活的那些——**要算比率而分母被一行一行丟掉了**。已補:`suggest()` 每次評估發一行 `slog.Info("improvement proposals", proposed/stored/dropped_no_evidence/dropped_unstorable/dropped_write_failed/dropped_over_cap)`,**沒有丟任何東西時也照發**——只在有丟時才出現的計數器,分不出「沒丟」與「這段根本沒跑」。<br>**還缺的是跑**,而路徑已經探明:dev 庫有 159 筆 Run(123 成功)但 `evaluations` 是 **0**、`river_job` 裡 `evaluate_run` **一筆都沒有過**——那 159 筆是 M2 跑的,當時評估管線還不存在。所以要 enqueue `eval.JobArgs{RunID, WorkspaceID}`,而消費它需要 **`apps/llm` 起著**(目前沒起)加 worker。<br>**原補法的端點名要更正**:提案不是 `POST /evaluations/{id}/suggestions` 產生的,`GET /runs/{id}/suggestions` 只讀既有的;產生點在 `suggest.go`,由評估工作附帶完成。<br>**補法**：拿既有 goldenset 的 Run 跑一批 `POST /evaluations/{id}/suggestions`，記四個數：①模型提了幾條、②被 [suggest.go](../../apps/platform/internal/trial/improvement/suggest.go) 的證據回驗／路徑檢查在儲存前丟掉幾條、③存下來的裡面 ApplyPreview 回 `applicable:false` 的比率與 `blocked_reason` 分布、④剩下的裡面人看了會接受的比率（這一項要人，前三項不用）。**基線寫進 `mvp/m3/` 的一份 `report-suggest-baseline.md`，格式照回歸集**。有了分布才知道要不要 bump `SUGGEST_IMPROVEMENTS_PROMPT_VERSION`，以及該修哪一句。<br>**2026-08-23 結案，報告見 [m3/report-suggest-baseline.md](mvp/m3/report-suggest-baseline.md)。** 基線第一次跑出來是 **0**：26 筆提案、`stored=0`、26 筆全死在「沒有可回驗的證據」。**不是模型亂提**——`suggestionEvidence` 要求 `evidence` 欄位**整段**是某段 excerpt 的子字串，而契約與 prompt 都只說「引自 digest」,於是模型交的是「兩三段真引文＋自己的推理」,200～300 字元,**永遠不可能整段命中**。同一個形狀:一個事實有兩份定義,錯的那份沒有跟任何東西對帳,而失敗長得跟「模型真的沒話說」一模一樣。<br>修法三件:Go 改為逐段比對引號內的片段(**要守的性質不變**——存下來的提案仍須有一段逐字出現在平台自己放進 digest 的 excerpt 裡);prompt 與契約把實際要求寫出來,`SUGGEST_IMPROVEMENTS_PROMPT_VERSION` 升 `v2`;`refs` 為空時整段跳過,不為註定被丟掉的呼叫付錢(本批省下約四分之一的 suggest 支出)。<br>修正後同批重跑:**53 次評估、37 筆提案、24 筆存下(65%)**;③ ApplyPreview **16／16 `applicable`,`blocked_reason` 分布為空**。<br>**⚠ 2026-08-24 訂正：上面三個數字是第二＋三輪的，而同一份報告的 §10（同日第四輪、`artifacts` 回填後）已經把它們換掉了。**本列引用了一份已經回答它的報告，卻停在報告的中段（M3 稽核）。**第四輪：**18 筆提案、9 筆存下（50%）**；③ 合計 **23／23 `applicable`**，`blocked_reason` 仍為空集合。**最該讀的不是那兩個比率，是「呼叫後模型提了 0 條」從 **0 變成 15**——回填之前模型每一次都看到空的產出清單、每一次都有話説。<br>**另一處方向弄反了**：本列寫 5.1 那句「因此**降級為觀察**」，而 §10.3 的結論是反的——回填後目標檔案仍然 23／23 全是 `SKILL.md`，所以那句從「提案主題集中導致」**升回一個獨立的觀察**：**v2 就是只改 `SKILL.md`。**<br>**本列尾那句「回填後要重評並覆核第 4 節與 5.1 節」已經做完了**（報告 §10），不是待辦。<br>**狀態也訂正**：①②③ 已結，**④（人看了會接受的比率）至今未填**（[report-suggest-baseline.md](mvp/m3/report-suggest-baseline.md) 自述「④ 未填，要人」）。依 AGENTS.md「部分完成保持未勾」，本列是 **◐：四個數只交出三個**，剩下的那一個要人讀。**④ 人的接受率未填,要人**——刻意不用機器指標頂替。閘道實付 $3.4859。<br>**留下兩件寫在報告裡的邊界**:語料全部來自同一個工作區的 M2 基線(123 筆裡只有 41 筆的 Skill 還沒被軟刪除,所以 ③ 的分母是 16 不是 24);本批以直接 insert `river_job` 觸發,**沒有走 `RunEventConsumer` 的 outbox 路徑**。<br>**順帶記一個實際障礙**:dev 庫的 migration 落後——`0024`～`0026`、`0028`～`0035` 都沒套過(`evaluations` 還是 `0004` 的形狀,`evaluation_suggestions` 根本不存在),這也是它一直沒跑起來的原因之一。<br>**同日規劃丙-8／13／26 時的事後更正(報告 §9)**:本批的 53 筆評估**正是丙-13 警告過的那件事**——`artifacts` 全表 0 列,所以送給 Judge 的產出清單是空陣列,84 筆評估**全部**帶著「reported success and no output files were recorded」。**丙-13 說「今天不會發生,沒有路徑會自動評估它們」;本批是人手動 insert `river_job`,所以發生了。**這解釋了為什麼每一條引文都在談 artifact manifest——那是我們給模型看的東西裡唯一確定為空的。**②的 0% 與其根因不受影響**(欄位定義不一致與提案內容無關),**③的 16／16 也不受影響**;受影響的是**代表性**:提案主題高度集中,5.1 節「16／16 全打 `SKILL.md`」因此降級為觀察。**回填後要重評並覆核第 4 節與 5.1 節**(append-only,兩份並存可比,同 `EVAL-013` v1／v2 的處理) | [evaluation-design.md §5.2](mvp/m3/evaluation-design.md)（預覽與套用同一套檢查）、[m3/audit.md](mvp/m3/audit.md) 第 7～10 項、`EVAL-013` 的量測前例、[evaluate.py](../../apps/llm/src/skillhub_llm/evaluate.py) 的 `SUGGEST_IMPROVEMENTS_PROMPT_VERSION`<br>**2026-08-25：六個計數器的測試補上了，而本列仍開著——它等的是④（人的接受率），那個要真人。**計數器與那條無條件的 `slog.Info` 早在 `f60ad48`／`99fde42` 就落地，缺的是**斷言**：`suggest_test.go` 從來沒有測過 `suggest()` 本身，所以「沒掉東西時也要發」這條性質沒有任何東西守著——而那正是舊碼表達不出來的那個 case（只在有丟棄時才發的計數器，分不出「沒有丟棄」與「這根本沒跑」）。<br>**做基線的人要知道的兩件事，寫在這裡而不是留給他自己踩**：①**分母要從兩條 log 線加總**——`refs` 為空時 `suggest()` 在呼叫模型**之前**就 return，走的是另一條具名的 `improvement proposals skipped` Info 行，不會出現在 `improvement proposals` 那條裡；把它誤算成「模型沒回東西」會讓丟棄率虛高。②**六個計數只在 log 裡，沒有進資料庫**——這是一個刻意的界，不是漏做：沒有任何程式讀它、沒有需求 ID 點名它，而 `evaluation_model_usage` 的那一列寫在迴圈**之前**（付了錢就先記帳，刻意的），塞計數進去要嘛改動那個順序、要嘛多一次寫入。**代價是：這批數字的壽命等於部署的 log 保存政策**，做基線的人要在封測結束後儘早取，不能假設半年後還在。<br>**✅ 2026-08-25 結案，而結案的理由是本列問錯了自己的狀態。** 它寫著「④（人的接受率）**至今未填**」，並據此停在 ◐。查證之後：**④ 有機器在算它，而且早就有**——`tools/analytics/funnel.sql` 的**第 5 段「改善建議被採用」**就是這個數，分子 `decision = 'accepted'`、分母 `decision <> 'pending'`（**刻意排除 `pending`**，所以「把每一條建議都無視掉」的人是缺席而不是被算成否決）。今日對 dev 語料實跑七段全部出得來，第 5 段是 `0 | 0`。<br>**`0 | 0` 不是缺陷，是這一列真正的狀態**：量測面完整（計數器→`evaluation_suggestions.decision`→漏斗第 5 段），**缺的是有人去讀提案並按下採用或否決**。那是封測讀數，不是程式債——**而本列是丙（程式面殘項），把一個等真人的讀數留在丙裡，會讓每一次重數都以為還有程式可以寫。**<br>**①②③ 的落點同批確認**：計數器與那條無條件的 `slog.Info` 在 `f60ad48`／`99fde42`，斷言在 08-25 補齊。**兩個界仍然成立、也仍然要讀**：分母跨兩條 log 線（`refs` 為空時 `suggest()` 在呼叫模型之前就 return，走另一條具名 Info），以及六個計數只在 log 裡、壽命等於部署的 log 保存政策。**這兩件事寫在上面那一段，不隨結案消失。** |
| 丙-44 | **使用者下載不了自己上傳的 Skill——而且永遠下不了** | 實測（一般使用者、frontmatter 有 `license: MIT`、包內有 LICENSE 檔）：`preview allowed=false`、`POST packaging` 回 **422 `license_unknown`**。三件事疊起來才是缺陷：`skills.redistribution` 預設 `unknown` 且 fail-closed、**匯入不寫這個欄位**、**產品沒有任何路徑改它**（乙-17）。<br>**它從來沒有失敗過的原因**：每一支打包測試都先跑 `allowRedistribution` 那一行 `UPDATE`——**沒有一支走使用者實際走的那條路**。helper 自己的註解就寫著「Every skill starts at 'unknown', which blocks」。<br>**2026-08-23 結案，見 [ADR-045](../adr/ADR-045-self-supplied-content-is-not-redistribution.md)。** 改的不是預設值而是那道鎖問的問題:再散布是「平台把 A 的內容交給 B」,把工作區自己送上來的位元組交還給它沒有 B。新增第四個值 `self_supplied`(migration `0036`),**刻意不寫成 `allowed`**——後者是對授權的判定,而沒有人判定過任何事;日後的發佈路徑必須面對一個不是 `allowed` 的值並停下來。<br>**判準是「誰的工作區」不是「是不是上傳」,這一條是整個改動的安全性所在**:策展目錄的 45 個 Skill 也是經同一個上傳端點載入的,以「是不是上傳」為準會把它們全部標成 self-supplied,而 Fork 逐字複製這個欄位——**一次繞過整個目錄的授權判定**。已有專屬測試 `TestAnUploadIntoTheCatalogueIsNotSelfSupplied`,因為它是那種壞掉時沒有症狀的條件 | `02:PACK-001` 第一層；ADR-021 §5.3；乙-17 |
| 丙-45 | **相容性區塊把一條規則當成量測講,而且讓人放心的那個值限定條件最少** | 「有效性」那一維的查證(交付物五維的第 5 題:**會不會讓 LLM 願意執行,而不是略過**)。三件事,同一個形狀:<br>**①「能力相容：已啟用」的 note 是空的**,而它的反面 `not_activated` 帶著兩句仔細的但書。那 45/45 是在**「Prompt 點名該 Skill」**的條件下量的(`02:CONTENT-007` 的要求,而該要求的理由正是 PDM-011 量到**自主觸發基準率為 0**)。讀者看到「已啟用」會讀成「Agent 會撿起它」,而平台自己對那個問題的答案是 0。<br>**②「實測相容」不是實測。** `tools/content/backfill-agent-compatibility.sql:162` 是 `CASE WHEN deps_runtime = 'python' THEN :python_runtime ELSE 'native' END`——`deps_runtime` 是 `seed-skills.json` 的策展判斷,`python_runtime` 是 operator 給的變數,**沒有讀 Trace,也沒有觀察腳本執行**。45 筆裡有 11 筆是「沒有依賴」的套件,靠 `ELSE` 分支拿到 `native`＝「腳本可直接執行」——**對一個可能根本沒有腳本的套件講的**。而畫面上這一軸叫「實測相容」,旁邊還有一個 `measured_at` 時間戳。<br>**③ 同一句宣稱有三份**:伺服器的 `compatMeasuredNote`、`compatUnverifiedNote`,以及打包頁自己手寫的「能力相容與實測相容是沙箱裡量到的」。<br>**2026-08-23 結案。** ① 補上 `activated` 的條件 note;② 軸名改為**執行環境相容**,`native`／`transpiled` 兩個值各自寫明**是規則的結論不是一次觀察**;③ 三份宣稱收成一份(區塊註記說明哪一軸來自 Trace、哪一軸來自規則),`02` 兩處以刪除線更正並註明日期。<br>**守門的測試不是字串比對而是一條規則**:`TestAMeasuredCompatibilityVerdictSaysWhatWasMeasured`——兩軸上任何**報告判定的值**(`unverified` 除外)都必須有 note。**它一寫出來就抓到第二個**(`native`),第三個(打包頁那句)則是被 `expect(text()).not.toContain("實測相容")` 抓到的 | 五維查證「有效性」；`03:DISC-008`／`02:CONTENT-007`；PDM-011；設計系統 §1.1、§4.4 |
| 丙-46 | **搜尋列給註腳掛了 AI 標記,給讀者真正拿來判斷的那句話沒掛** | 同一列、相隔三行:`match_reason` 有 `match_reason_source` 與「AI 產生」／「規則產生」徽章(ADR-013 落實得很好);而 `<p>{hit.summary}</p>` **什麼都沒有**——`search.sql` 是 `COALESCE(NULLIF(s.enriched_summary, ''), s.summary)`,**只要索引期增強跑過就是模型改寫的**(目錄 45/45 都跑過)。**摘要是讀者用來決定要不要點進去的那句話,符合原因只是註腳;標了註腳沒標句子,方向是反的。**<br>**契約其實知道**:`PublicSearchResult.summary` 的描述早就寫著「The index-time LLM summary when the skill has been enriched (ADR-013 §1, model-generated)」——**寫成散文,沒有欄位**,所以前端就算想標也標不了。<br>**詳情頁是對的**,不是全平台都錯:`skillDetail.Summary` 明寫是套件自己的 frontmatter,模型改寫放在 `Enrichment` 底下有標示。問題只在搜尋列。<br>**2026-08-23 結案。** 新增 `summary_source`(`model`／`package`),由 SQL 的同一個 CASE 產出——**與它描述的那個值同一條語句算出來,才不會各說各話**;前端沿用同一組徽章樣式。<br>**第三個值是刻意的**:契約要求這個欄位,所以缺席代表伺服器沒回答,而它**絕不能被代答成「作者原文」**——那等於把作者的名字蓋在模型寫的句子上,正是這個徽章要防的事,只是換成 fallback 的形式重來一次。缺席印「來源未標示」,並有自己的測試。<br>**同批補上另一半的揭露**:詳情頁的 enrichment 區塊現在明說**你的 Agent 讀的不是這一段**——它讀套件自己的 `description`,而下載回去的套件裡不會有這段改寫,所以**這段寫得好不會讓 Agent 更願意用它**。要不要讓 enriched summary 進包是產品決定(會改變套件內容的作者歸屬),**沒有決定不等於不揭露** | ADR-013 §1；`02:DISC-002`；設計系統 §2.9 |
| 丙-47 | **一個 2000 字元的截斷,被算成和「整批事件被丟掉」同一件事,而後果是最重的那個** | `buildDigest` 的 `truncated` 是**一個布林,兩個來源共用**:`len(citable) > maxDigestCount`(100 筆,**整個事件沒送出去**)與 `cutExcerpt`(單一 payload 超過 `maxDigestEntry = 2000` 字元被切尾)。兩者都會讓 `evidenceComplete = false`,相依的準則因此只能是 `undetermined`。<br>**2026-08-23 的 84 筆評估量到:50 筆帶這條 finding,而 26 筆 `undetermined` 判定裡 26 筆都有它——完全相關。**這批 Run 最多 44 個事件,**沒有一筆碰得到 100**,所以那 50 筆全是後者。<br>**一個 payload 被切掉尾巴,與一批事件根本沒送出去,是兩個大小差很多的洞**,目前用同一個旗標與同一句訊息表達,而 `undetermined` 是判定裡最重的一種保守。<br>**這一項獨立於丙-13**——回填 `artifacts` 不會讓它消失。<br>**還沒查的**:2000 這個值是不是太小(`maxDigestEntry` 出自 evaluation-design §6.3 的輸入預算),以及兩個來源該不該分成兩個 finding、後果是否該不同。**先量再改**:同一批 Run 的 payload 長度分布是查得出來的事實。<br>**2026-08-23 量完了,報告見 [report-judge-regression.md §15](mvp/m3/report-judge-regression.md);本項縮小,不關閉。** 語料是 dev 庫全部 164 個有 trace 的 Run。<br>**兩個來源只有一個真的會發生**:事件數超過 100 的 Run **0 個**(最忙的一個只有 55 個可引用事件),單一 payload 超過 2000 的 **95 個(57.9%)**。**這個平台歷來報告過的每一次截斷都是第二種,而標籤寫的是第一種**——照著 `trace_digest.entries` 去找的人會去找一批從來沒被丟掉的事件。**已修**:分成 `trace_digest.entries` 與 `trace_digest.entries[].excerpt`,各有具名測試。**後果沒改**,兩者仍把 `evidenceComplete` 打成 false——切尾該不該和整個事件不見得到一樣的保守,是丙-1 那條規則的判準問題,**改名字不等於改判準**。<br>**被切的是什麼**:`tool_call` 1450 筆裡 179 筆超標(p90 2532、最大 24624);拆開最大那筆,24624 字元裡 **24322 是 `arguments`**——**一次 Write 呼叫帶著整份產出文件**,而那正是內容 rubric 要判的東西。`agent_output` **一次都沒超過 2000**(最大 1849),所以 `CONTENT-007` 第 4 條(把正文貼進最終回覆)**等於繞過了這個切點——它是變通不是修好**。<br>**還開著的是那個數字**:2000 → 8000 會把被切的 Run 從 58% 降到 19.5%,digest 總字元只多 43%;完全不切(25000)是 +54%。判定一次實付 $0.01356,所以量級是**每次評估多不到一分錢,換掉 58% 的 Run 被降級**。**本批刻意不動它**:`maxDigestEntry` 是 evaluation-design §6.3 明文的輸入預算,存在理由就是用切封住成本上限,把它移動是拿錢換判定,**那是該被記成決策的決策,不是順手調的常數**。§15.3 的表就是那個決策要的全部依據。<br>**2026-08-23 稍晚結案:決策下了,`maxDigestEntry` 2000 → 8000**,理由與最壞情況的算術寫進 [evaluation-design §6.3](mvp/m3/evaluation-design.md)。**沒有調到「完全不切」(25000)**:上界是 `maxDigestCount × maxDigestEntry`,8000 買到每次請求 800K 字元的天花板,25000 會讓它變成 2.5M——**一個封不住的預算就不叫預算**。<br>**改完發現這個數字有四份**(`judge.go`／`llm-internal.yaml`／`evaluate.py`／regression harness),**只有一組會在漂掉時出聲**(契約與 Pydantic 對 Go 回 422),harness 那一份漂掉只會安靜組出不一樣的請求。四份都改了,契約描述寫明必須一起動。<br>**同批補上 harness 少報的截斷**:`excerpt, _ = clip(...)` 把旗標丟掉,所以 B 輪明明有 4 個事件超標卻記成 `truncation: []`。<br>**這次調整的效果不在那 5 筆上**(見 §16.2):回驗比對的是資料庫裡的完整 payload 不是 excerpt,所以這個數字**對「引用驗不驗得過」在結構上沒有影響**;要等下一批有大 payload 的 Run 才量得到 | [m3/report-suggest-baseline.md §9.2](mvp/m3/report-suggest-baseline.md)；`02:EVAL-001` 第 5 條；ADR-026 |
| 丙-48 | **Judge 引的是解碼後的字,Go 驗的是原始 JSON——一則壞引用還會拖垮整條準則** | 2026-08-23 B 輪量到:22 個 rubric 項有 **5 項**被證據回驗打成 `undetermined`,而模型其實都給了判定。5 筆**全部**是 `trace_event` 引文被拒。逐筆比對出根因:送給 Judge 的 digest entry 是 `string(e.Payload)`(**原始 JSON**),模型讀到 `\n`／`	` 之後引用時寫成**真正的換行與 tab**;Go 拿這段字去 `strings.Contains(payload)`,**帶真換行的字串永遠不可能出現在把它寫成兩個字元 `\n` 的 JSON 裡**。<br>**這是 [§6.1](mvp/m3/report-judge-regression.md) 那個 v1 缺陷的同一個形狀往下一層**:v1 是行首多了 `事件型別: ` 前綴、v2 修掉排版;這次是同一個事實的兩種呈現,**Judge 看渲染後的、Go 驗原始的**,而失敗一樣靜默——判定變成 `undetermined`,看起來像 Judge 保守。<br>**第二件同樣要記的**:5 筆裡 **3 筆**同時附了一則**回驗通過**的 `agent_output` 引文(`rejected: null`),**一則壞引用拖垮整條準則,即使同一條準則另有驗得過的引用**。<br>**兩個都不在本批改**:回驗是 ADR-026 defence 3、ADR-043 剛修訂過它的判準,而「要不要接受解碼後的引文」與「要不要讓一則好引用救回準則」都是那道防線的判準問題,不是 bug fix。**今天這份量測正好是任何修改該有的依據**。<br>**2026-08-23 稍晚結案(一半),見 [ADR-049](../adr/ADR-049-citation-verification-matches-the-stored-value-not-its-encoding.md) 與 [報告 §16](mvp/m3/report-judge-regression.md)。** trace event 的可回驗材料從「payload 的原始 JSON 文字」擴為「原始文字**加上**其中每個字串值解碼後的內容」,兩者都套 ADR-043 §4 的正規化與長度下限。**這不是放寬**:解碼後的值不是新材料,就是同一個欄位把轉義解開,而那些轉義是平台的儲存格式不是 Judge 寫的東西。欄位間以 NUL 相接(JSON 字串不可能含 NUL),**跨欄位命中是結構上不可能而非不太可能**。<br>**C 輪實測:`undetermined` 5 → 0,39 則引用一則都沒被拒**,可計分兩項仍 10／10,實付 $0.1468。**歸因沒有花錢買**——把 B 輪那 5 則被拒引文離線重放到完整 payload 上,新比對 **5／5 全中**,舊比對 0／5。<br>**另一半刻意不做**:「一則好引用能不能救回一條準則」維持現狀(任一引用失敗即降級)。**支持放寬的證據在放寬之前就消失了**——B 輪那 3 筆看似支持它的案例,壞引用其實不是捏造而是編碼對不上,而 ADR-049 已經把那個原因移除;維持現狀的實測代價現在是 **0／39**。日後若有引用在編碼修正後仍失敗,那才真的是「引了不存在的東西」,而那時的證據會指向維持現狀 | [ADR-049](../adr/ADR-049-citation-verification-matches-the-stored-value-not-its-encoding.md)；[報告 §14.4／§16](mvp/m3/report-judge-regression.md)；ADR-026 defence 3；ADR-043 |
| ~~丙-49~~ **已結案 2026-08-25** | **P1 自動停派送會在平台自己忙的時候誤觸,而且它真的把機隊停了** | 2026-08-23 建立 B 輪 Run 時,5 次全部回 `the execution environment is temporarily unavailable`。查出來是 `TraceMaskingStopped` 自動宣告了 P1:「兩小時內 274 筆 trace 事件、零遮罩」。**那 274 筆全部是平台自己的 `evaluation_started`／`evaluation_completed`**——由平台從自己的狀態寫出,裡面沒有任何不受信任的內容,**masker 按定義永遠不可能從其中遮掉任何東西**。一批重評就足以讓那個表達式為真。<br>**這是 *忙碌* 時的誤判,與 `for: 1h` 防的 *安靜* 時誤判方向相反**,而 `for` 看不見它,因為流量是真的。丙-26 當時記過「在告警裡這只是一個會吵人的誤判,接上停派送之後它會自己把自己停掉」——**今天它就是這樣做的**。<br>**已修**:`CountTraceMaskingInWindow` 只數 `source = 'sandbox'`。**這讓偵測更銳利而不是更寬鬆**:masker 真的停了仍然被同一個表達式抓到,而且不再被本來就不可能被遮的列稀釋;什麼都沒在跑時 `recent_events` 是 0,規則本來就沉默。<br>**留下一個本批不動的觀察**:整個 dev 語料 **2444 筆 sandbox 事件、遮罩總數也是 0**。那批是合成資料、本來就沒有 Secrets,所以判準的前提(「正常流量下幾乎必然有東西被遮」)**在這份語料上根本不成立**。要不要因此調門檻是 `02:SEC-010` 的事,不是這裡。<br>**2026-08-23:這一半沒有變成決策,但它現在有落點了。** 寫進 [p1-dispatch-halt.md §5](../runbooks/p1-dispatch-halt.md)——**看到 `TraceMaskingStopped` 的人會先讀到這件事**,而不是在 backlog 裡等一個決定。那裡寫的是**怎麼讀這條判準**(「零遮罩」本身不是強證據,撐起它的是「有量而且持續」;判準真正要抓的是規則失效而非未遮罩入庫,因為 `0019` 的 `CHECK (masked)` 讓後者在資料庫層不可能),**沒有改任何門檻**。<br>**仍然開著的是那個決定本身**:合成語料證明不了前提成立,也證明不了它不成立——要一批**真的含有可遮值**的流量才量得到,而那要等封測 | `02:SEC-010` P1；`03:SEC-012`；丙-26；[p1-dispatch-halt.md §5](../runbooks/p1-dispatch-halt.md)<br>**✅ 2026-08-25 結案：換掉量測方式，因此不再等封測流量。** 統計推論要靠「正常流量下幾乎必然有東西被遮」這個前提，而它在唯一有資料的語料上不成立——**直接檢查不需要前提**。`MaskerCanary()` 每輪 sweep 把平台自己造的假 Secret（每一種 `secretPatterns` 形狀各一個，＋一個每次隨機產生的「平台已發出憑證」值走 Known 精確比對那一臂）餵過**真正的 `Masker.Mask`**（不是 unexported 的 `redact`，所以 walk、JSON Pointer 與 `masked_fields` 都在覆蓋內；「全遮掉但少報欄位」也算失敗），任何一個沒被遮掉就宣告 P1。**只回形狀名稱不回值**——結果會進 halt reason、audit 與 log。不打模型、不建 Run、**健康時零 query**，沿用既有冪等檢查與「不自動 lift」。<br>**舊的流量判準留著，而且必須留，這是查證出來的不是折衷**：canary 讀**規則**（本 process），流量判準讀**路徑**（生產）。canary 是自己呼叫 `Masker.Mask` 的，所以**看不見「ingest 路徑上那一步被整個拿掉」**——遮罩的呼叫端有兩個（`Service.Ingest` 與 `RecordOrchestratorEvent`）且各自 `new` 自己的 Masker，刪掉其中一步 canary 永遠是綠的；而流量判準看不見「規則沒了但語料本來就乾淨」。**兩者互相看不見對方的失敗**，所以刪掉任一條都等於把那一半留成沒有偵測器。**門檻一格沒動**（那是 `02:SEC-010` 的事）。<br>具名測試：`TestAFailingMaskerCanaryHaltsDispatchWithoutAnOperator`、`TestMaskerCanaryPassesOnAnIntactMasker`、`TestEveryMaskerPatternHasACanarySample`、`TestMaskerCanaryNamesTheShapeThatStoppedBeingRedacted`；三次突變各自見紅。 |
| 丙-50 | **同一個數字散在四種語法的六個地方,而只有兩份會出聲** | **入列即結案(2026-08-23)。** `maxDigestEntry` 這個上限同時存在於 `judge.go`(Go const)、`contracts/openapi/llm-internal.yaml`(兩處 `maxLength`)、`apps/llm` 的兩個 Pydantic `max_length`、以及 `tools/eval-regression/judge_regression.py`——**沒有任何編譯器會比對它們**。本批之前它已經害過一次:2000→8000 只改了 Go,**每一次評分都回 422**(丙-47 那一批)。而 422 是**最大聲**的那一份在叫;harness 的那一份漂掉時只會**安靜地送出一個不一樣的請求**,再把結果當成沒事一樣報出來。<br>**補法**:每個站點在同一行標 `one-number: maxDigestEntry`,由 `devctl automation-check` 比對(`tools/devctl/shared_number.go`),沿用 ADR-032 drift marker 已經在用的同一種形式。**它不取代契約作為事實來源**——`packages/api-stub-py` 那一份是從契約生出來的,所以它不帶標記;這個機制只給**生成器搆不到的副本**。<br>**寫的過程被自己抓到兩次**:①說明用的三行範例被算成三個真站點(和 `driftMarkerPattern` 用 `` 擋掉 ADR 自己的佔位字是同一個坑);②harness 的標記寫在既有註解**中間**而不是開頭,於是**完全沒被算到**——偏偏它正是漂掉時最安靜的那一份。規則因此定死「標記要開啟註解」,並在文件與失敗訊息裡寫明。<br>**突變驗證**:任一站點改成別的值即 FAIL 並印出六個站點各自的值。<br>**同批把同一家族的其餘五個也標了**:`maxFinalOutput`(40000)、`maxCriteria`(20)、`maxDigestCount`(100)、`maxArtifactRows`(500)各四個站點,`excerptLimit`(1000)兩個站點——**全部逐一突變驗證過**。`maxDigestCount` 的註解本來就寫著「= llm-internal.yaml TraceDigest.entries maxItems」,也就是**這件事一直有人知道,只是知道的方式是一句沒有人會執行的註解**。<br>**兩個沒有標,理由不同**:①契約的 `truncation` maxItems 也是 100,但它**和 `entries` 是兩個剛好同值的不同事實**,綁在一起等於宣稱它們必須一起動;②`user_prompt` 的 maxLength 40000 **和 `maxFinalOutput` 不是同一件事**——`trial/design` 的 `MaxPromptBytes` 在上游就把它壓在 32 KiB,而位元組數是字元數的上界,所以那個天花板碰不到。已就地註明。<br>**這一批也抓到檢查自己的一個缺陷**:Python 與 Go 都寫得出 `40_000`,而第一版的取值正則把它讀成 `000`——**一個會報出沒有人造成的差異的比對,比沒有比對更糟**,因為下一個人會學會不相信它。 | 已結案 |
| 丙-51 | **同一道閘門在前後端各有一份判斷,而後端加了第五個值** | **入列即結案(2026-08-23)。** `redistribution` 的第五個值 `generated`(0037／GEN-005)由 M5 那一批加上,後端 `delivery/packaging.go` 的 `gateFlags` 放行、DB CHECK、三份 Go 常數、契約 enum、`types.ts` 的 union 全部補了——**唯獨前端兩處判斷沒有**:`Packaging.tsx` 的 `packagingGate` 讓它掉進 `switch` 的 `default`,`WorkspaceSkills.tsx` 的三元鏈也沒有它。結果是**伺服器會產出的套件,按鈕是 disabled 的**,理由寫著「沒有人確認過這個 Skill 可不可以再散布」;工作區清單上每一個生成出來的 Skill 都掛紅色「授權未知,不能打包」。**M5 的產物在 UI 上下載不了**,而兩邊都認為自己是對的。<br>根因是 `switch` 的 `default` **分不出「我們決定它要擋」和「沒有人告訴過這個檔案有這個值」**。<br>**補法**:union 抽成具名型別 `Redistribution`,兩處判斷改成 `Record<Redistribution, …>`——加第六個值時**這兩個檔案會編不過**。對線路上送來的值仍然 fail-closed(`hasOwnProperty` 守衛):窮舉管的是我們自己維護的那一份,不是伺服器送什麼就信什麼。順帶把 operator route 的 `generated` 從「值拼錯了」那個泛用訊息移到和 `self_supplied` 同一類的具名拒絕。<br>**突變驗證**:union 加一個 `sixth_value`,`tsc -b` 兩個檔案各報一次 TS2741;Go 側把 `generated` 放進可設定集合,integration test 立刻 FAIL(200 ≠ 400)。<br>**同批發現 CI 的 web typecheck 在 main 上是紅的**:`117ba44` 把 `summary_source` 設成必要欄位,只補了兩個斷言那個 badge 的 fixture,**另外十二個沒補**,而 `npm run typecheck` 在 `ci.yml` 有兩處會跑。補在共用的 `HIT_FACETS` 上(一處,不是十二處);唯一必須不繼承它的是那個測「伺服器沒送這個欄位」的 fixture,就地 cast 並註明理由。<br>**也抓到我自己用錯了指令**:`npx tsc --noEmit` 在 `apps/web` 是**空跑**的——根 tsconfig 是 `files: []` 加 references,所以它什麼都沒檢查還回報 No errors,第一次突變驗證因此假綠了一次。真正的指令是 `npm run typecheck`(`tsc -b`)。**一個永遠不會紅的檢查**,和這一列在講的是同一種東西。 | 已結案 |
| ~~丙-52~~ **已結案 2026-08-25** | **資訊架構沒有被寫下來過,於是它自己長成了現在這樣** | **入列(2026-08-23),未結案。** 設計系統管的是一頁之內;**一頁與一頁之間從來沒有一份文件**。整理成 [docs/design/information-architecture.md](../design/information-architecture.md):17 條路由、頁內連結圖、反向連結數、網址承載哪些狀態、以及 ADR-038 價值流的對照。<br>**八項發現**(逐項附證據,見該檔 §5):IA-1 探索不在導覽列而它是第一段價值流;IA-2 同一個工作區四個網址前綴而判準只逐檔寫在註解裡;**IA-3 `/runs/$runId` 一個位址回答兩個問題**——ADR-025 明文把 Run 終態與 Evaluation 判定分開,但 `RunTrace` 同時渲染 `EvaluationPanel`、`<h1>` 叫「Run 詳情」、`RunEvaluation.tsx`(全 app 第二大的頁面檔)沒有自己的位址,**使用者要把判定貼給別人時,貼出去的位址叫 Trace**;IA-4 `__outlines__/` 已經把兩個閱讀模式當成兩個畫面在守而網址只有一個,且 `router.tsx`(一項決定)、system.md §7.1(一項待辦)、`03` TRACE-007(已定為 API 參數)**三處兩種立場**;IA-5 搜尋沒結果時沒有出口(`/workspace/import` 的頁內入邊是 0);IA-6 router 的 `beforeLoad`／`redirect` 各 0 處,沒有登出狀態的資訊架構;IA-7 三個單一入口頁面;IA-8 網址是名詞、標題是動詞。<br>**兩項要先裁定**:IA-4(閱讀模式該不該可連結)、以及 M5 的生成入口是「搜尋失敗的出口」還是「導覽列第六項」——後者更難維持 ADR-052「開工不等於曝光」那條邊界,因為**放在導覽列上的東西沒辦法只給一部分人看**。<br>**守 IA 的機器幾乎沒有**(該檔 §6):標題階層、landmark 唯一、375px 不溢出有把關者;反向連結、單一入口、命名一致、一個位址一個答案、登出可達性、新頁面該放哪個前綴——**六列全部是「沒有」**。 <br>**同日稍晚的第二批**:①**本檔那一列在 22 分鐘後就有一半過期了**——初版寫於 21:02,`c387cad`(GEN-008 UI)在 21:24 把 M5 生成入口掛上兩個畫面,而 §7 還在說它不存在、還把落點列為待裁定,**而那條落點早在 ADR-046 決策 7 就裁定了**(先搜尋、搜不到再生成;元件檔頭明文不放在搜尋框旁邊)。沒有任何東西會 FAIL。已訂正 §7 與 IA-5,並新增 **§2.4 條件入口**——**旗標後面的入口不新增路由、也可能不新增任何連結,純看網址的檢查抓不到它**。②補上 [`ia.test.ts`](../../apps/web/src/ia.test.ts):**文件持有政策、程式持有事實、測試比對兩者**(比照 `design-system.test.ts`),四項各自雙向——§1 路由表、§2.1 主要導覽、§2.3「只有導覽列」清單、§2.4 旗標入口。**四項逐一突變驗證**(多一條路由／少一個導覽項／孤兒被解掉／出現沒記錄的旗標)全部 CAUGHT。③§6 從六列「沒有」縮到兩列。**八項發現本身一項都還沒修**,這一批補的是「別再默默漂掉」。 <br>**第三批:方向反轉。** 前兩批的文件只會**跟在 `router.tsx` 後面記帳**——它記下「四個網址前綴的判準沒有寫在任何一處」,然後**自己也沒有寫下判準**。新增 **§0 規則層**(R1～R6,每一條都從已定案的東西推出來、標明出處與有沒有機器)與 **§0.2 偏離帳**(三列,形狀比照 `query-owners.yaml` 的 `allow:`,**只能變短**)。文件開頭現在明說兩半的權威方向相反:**§0 的規則走在程式前面(不一致改程式),§1～§4 的盤點跟在程式後面(不一致改文件)**。`ia.test.ts` 從四項變五項,新增的兩項是 **R2 的兩個可機械判定子句**(單筆位址不得掛在 `/workspace`／`/lab` 底下;無動態段的多段位址必須掛在其中之一)與 **R3 的 0／1 入邊雙向比對**。**四項突變全部 CAUGHT**:單筆位址搬進 `/workspace`、清單位址搬到根層、偏離帳多一列、入邊數變了沒改表。§2.3 的入邊表改成機器算的(手數的 `/skills/$skillId` 是 8,實際 10)。§6 從兩列「沒有」變成三列有把關者、兩列沒有——**其中一列的方向與其餘相反**。 <br>**第四批:把爭議關掉,並訂正我自己造成的兩個誤會。** **IA-4 已裁定**——`router.tsx`(決定)與 system.md §7.1(待辦)對閱讀模式各說各話,依 §0.1 R4「你偏好怎麼看不進網址」判**程式對、文件錯**,system.md 已改。**而它從來不是三方爭議**:`03` `TRACE-007` 的 `?mode=advanced` 是 API 參數,那個工作項自己寫著「UI 為 `/runs/$runId`,一般／進階切換」。**IA-3 我講重了**——那一頁的標題階層本來就是 `h2 任務判定` → `h2 執行紀錄` → `h2 產出`,**判定在前、證據在後,R1 一直是滿足的**;我還把元件檔名(`RunTrace.tsx`)當成了位址(`/runs/$runId`)。真正的缺陷是 R5 的容器詞,**已修**:`Run 詳情` → `Run 結果`(含兩份 `__outlines__/` 快照)。**§3「三套用語」也是誤會**:ADR-038 §3 定的是概念的中文名,沒有禁止 `Run` 這個保留術語——真正的 R5 違規只剩 `/lab/datasets` 的「上傳 Dataset」(詞性,不是用語)。**最後一項是最容易造成誤會的**:`docs/design/` 在 AGENTS.md 裡**一次都沒出現過**,54 KB 的設計系統與這份 IA 對任何讀入口文件的 agent 是隱形的——已補目錄導覽一列與快速判斷表兩列。**八項發現現在兩項已解決、兩項是訂正、四項待修,其中只有 IA-1 需要產品負責人。** <br>**第五批:IA-1 由產品負責人裁定「探索不進導覽列」(2026-08-23)。** **不是維持現狀,是一個定位**:探索不是一個你去的地方,是產品本身在做的事;導覽列上現有五項全部是「你自己的東西」,把探索放進去等於把它降成六選一。**同一個判準此前已被用過一次**——ADR-046 決策 7 拒絕把生成入口放在搜尋框旁邊。兩個獨立的決定指向同一件事,因此升成 **§0.1 R7:導覽列的每一項都必須是 R2 意義下的清單位址**(`/workspace/` 或 `/lab/`),並由 `ia.test.ts` 守著。**突變驗證的重點在這裡**:把「探索」加進導覽列、**並且把文件的 §2.1 表一起改成相符**,仍然 FAIL——**這條裁定不能靠改盤點表繞過,要繞過只能先改 R7**,而那是一個看得見的決定。**八項發現現在三項已關、兩項是訂正、四項待修,而且沒有任何一項在等簽名。**<br>**第六批（2026-08-24）：四項待修再結三項。** IA-2 由 §0.1 R2 回答（判準第三批就寫下了，這是補勾不是新工作）；IA-5 的旗標關閉半邊關閉——`no_results` 不分旗標多一條「直接匯入它」的出口，`/workspace/import` 因此獲得第一條頁內入邊、§2.3 的 0 列清空（`filtered_out` 裁定**不加**：清除篩選就是那一格的出口，送人去匯入目錄裡已有的東西是更壞的建議）；IA-8 的 `<h1>` 改「Dataset」（R5 的現行違規清零）；**IA-7 裁定為 R3 的「具名」那一支不補第二條**——三頁各要求一個只有一個地方產得出來的脈絡，尤其 `/lab/datasets`：API 是 `/test-cases/{id}/datasets`，**平台根本沒有工作區層級的 dataset 清單**，所以它在概念上是某個 Test Case 的分頁，位址長得像清單只因為 R2 把清單放在 `/lab/` 底下。R3 同批補上判準（「具名」不是逃生門：看脈絡是不是唯一），`ia.test.ts` 的雙向比對不變，新孤兒照樣 FAIL。**剩 IA-6（登出狀態的資訊架構，要一個設計決策：未登入時導覽列與各頁該長什麼樣）一項，列仍開。** | 未結案<br>**✅ 2026-08-25 結案：IA-6 已查核、裁定並落地，本節九項全數已關。**<br>**裁定**：登出狀態**不由 router 守衛，也不改導覽列**——401 是一個可預期的具名狀態，在每一個讀取匯流的那一點被處理一次，用產品自己的語言說出需要登入並帶著登入動作。四個理由都指向既有的東西：這個產品已經回答過三次而三次都選「由頁面自己說」；`/policy` 明文決定不放在 session 後面，全面守衛會牴觸它；轉址會弄丟訪客被寄來的位址；IA-1 已裁定導覽列五項是「你自己的東西」，對訪客那是邀請而不是謊言——**前提是抵達時誠實**。<br>**這一項懸最久，而查完之後它不是原本以為的問題。** 它被歸類成「要設計決策」，但其中一半只是**還沒有人去看**：IA-6 的內文自己寫著「未查核：各頁在 API 回 401 時各自畫什麼」。查了才知道量到的是——12 條路由把伺服器的英文 `not authenticated` 內插進中文句子（導覽列五個目的地全中）、**三處**把完整可操作的表單畫給訪客再事後拒絕、一頁把未登入的訪客送去找查詢參數；而**沒有一頁 `reads as empty`**，那不是運氣，是每句空狀態都掛在 `xxx.data &&` 之後。<br>**查核抓到兩件逐路由查核抓不到的事**：①`/workspace/account` 原本被當成「已經說對了」的前例，實際上前半句同樣印英文且沒有任何登入入口；②**頁尾的回報表單根本不屬於任何一條路由**——`POST /feedback` 是 `RequireSession`，而表單掛在 `RootLayout` 的 footer，**17 條路由上全部都有**，包含刻意留在 session 外的 `/` 與 `/policy`。<br>**落地**：21 處讀取失敗改用共用的 `ReadFailure`、四處改成事前說、一頁修掉誤導、七個 hook 補上 `retry: false`（`useSkillVersions` 與 `useTrace` 原本會先轉七秒「載入中」才讓 401 浮出來）。八次突變，**第六次第一輪是綠的**——`retry: false` 原本一支測試都不擋。而那批測試的第一版失效方式與本專案前三次同型：settle 條件等的就是修好之後的那句話，所以還原呼叫點只會逾時，紅的是逾時不是斷言。 |
| ~~**丙-53（新入列 2026-08-23）**~~ **已結案 2026-08-25** | **生成前的「預估成本」沒有一個可以顯示的數字** | `02:GEN-001` 逐字要求生成前顯示**預估成本**與本次消耗的額度。額度那一半今天顯示不出來是對的（`GENERATE_QUOTA=off`，[ADR-056](../adr/ADR-056-the-generation-allowance-is-its-own-switch-and-it-is-off.md)；`04` 乙-2 禁止顯示沒被強制的數字）。**成本那一半沒有被任何東西擋著，就是沒做**，而它缺的是一個數字不是一段程式：手上只有實測平均（mini $0.00553／flagship $0.1186——~~$0.113~~ 是 spike 報告訂正前的數——[m5/report-generate-baseline.md](mvp/m5/report-generate-baseline.md)），**把平均值印成「預估成本」就是 §2.2 禁止的那種沒被強制的承諾**——一次長描述可以差好幾倍。<br>**三條可能的路**：①閘道回報的 `usage.cost_usd` 已經在 `GenerateSkillResponse` 裡了，改成**事後顯示實付**而不是事前預估（誠實，但不符合「生成前」三個字；而且 `GenerateResult` 現在還沒有這個欄位，要補一段 plumbing）；②訂一個上限並強制它（例如「一次生成最多花 $X」），那才有東西可以印——**②要 PDM 給值**；③**（2026-08-23 稍晚補上，這條不需要任何人簽名）**三個界**今天就已經被強制**了：輸入 ≤ 4000 runes（`generate.go`，超過即 `ErrGenerateTooLong`）、輸出 ≤ 16000 token（`MAX_OUTPUT_TOKENS`，`finish_reason == "length"` 依 `02:GEN-001` 直接判失敗且不重試）、嘗試 ≤ 2。由它們算出來的「最多」**顯示的正是被強制的界**，不是 §2.2 禁止的那種沒被強制的承諾。<br>**③剩下的唯一一個未強制值是單價**，而單價是供應商的事實、會變，所以它要嘛跟著 `GENERATE_SKILL_MODEL` 一起釘在設定裡並在漂掉時出聲，要嘛③也不成立。**這一格不是 R-9 擋著的**：R-9 那四個數字是每日 10／每窗 30／首窗 20／窗長 30 天，**沒有一個是成本上限**，把本項掛在那個簽名後面等於掛在一個不會回答它的決定旁邊排隊（`05` §0.3 具名的那個病）。<br>`GEN-008` 因此退回 ◐（08-23）。<br>**2026-08-24：③做了，列仍開。** 三個界現在在按鈕上方，副本由 `one-number:` 機械比對；成本位置印 `尚未定值`。**列不結案，因為允收準則要的是金額**。~~這一列真正剩下的問題縮成一句：單價要不要釘進部署設定，還是等 PDM 給每次上限。~~**（同日稍晚查明：還有第三條路——Run 既有的「有出處的區間＋basis 句、標示估計值」形式，`02:PDM-005` §5.3 已接受、不需簽名；它今天做不到是因為 B 輪只記了平均沒記分布。這一列等的是一批逐次記成本的生成，不是簽名。升成 `05` R-10 記著，結論就是「不等簽名」。）** | `GEN-008` 結案<br>**2026-08-25：這一列等的那批資料，此前根本沒有地方可以落地——現在有了，但本列維持開著。**`GenerateSkillResponse.Usage` 一直存在，而 `generate.go` 從來沒有讀過它：**每一次生成的成本在 `generateOnce` return 之後就地蒸發**。同日補上兩半——失敗的生成寫進 `skill.generate.failed` 的 audit metadata，成功的生成寫進它那一列 `skill.import`（成功的生成不另開事件，是 `audit.go` 白紙黑字的決定：生成成功就是一次 import，歷史只有一份）。**兩半必須同時有，否則樣本是偏的**：只有失敗那半的話，分布全部來自「付了一通、然後沒能打包」的呼叫，而成功的可能付了兩通——用它估成本會系統性低估，且低估的方向沒有人看得出來。<br>**缺席就是不寫 key，不是寫 0**：`cost_usd: 0` 是一個「這通免費」的主張，沒有那個 key 才是它實際的缺席，而讀分布的人必須分得出這兩者。規則只寫在 `usageMeta` 一個地方，兩半共用——兩份「缺席即缺席」遲早會分歧，而分歧看起來會像成本異常而不像 bug。<br>**本列仍然開著，因為 `05` R-10 的建議 (c) 等的是資料不是機制**：機制今天有了，資料要等丙-56 那次真實閘道呼叫（以及其後的每一次生成）。<br>**2026-08-25：本列的前置已收斂成「丙-56 那次呼叫」，不再是一個獨立的等待。** 機制齊備（同日落地，見上），資料是丙-56 付費呼叫的副產品，**不另外花錢**。`05` R-10 的建議 (c) 因此不需要任何簽名，只需要那一次呼叫。<br>**✅ 2026-08-25 結案：`05` R-10 的建議 (c) 落地，`尚未定值` 換成有出處的區間。**畫面現在印「約 US$0.003–$0.03，多數落在 US$0.006 上下」，形式逐項照 `RunPreflight` 既有的那個（`02:PDM-005` §5.3 已接受）：標示**估計值、非報價**、**點名出處**（2026-08-25 對真實閘道生成 10 次的實付分布，最小 $0.0038／中位 $0.0062／最大 $0.0110）、並且**明說平台沒有為單次生成設定費用上限**。**不需要任何人簽名**——這一列從來不等簽名，等的是資料。<br>**上緣刻意寬於樣本，理由要寫下來**：十次全部是單次嘗試，而 `generateMaxAttempts` 是 2，**重試會付兩次**；而且十次不是一個界。**它不是一個平台會強制的天花板**（成本沒有天花板），所以那句話印在畫面上而不是留給讀者推論。<br>**測試押的不是那個數字，是那三件讓區間不會被讀成報價的事**（估計值標示、出處、沒有上限這句）——**只檢查有沒有數字的測試，一個安靜變成承諾的區間也會通過**。突變（拿掉「估計值，非報價」）已見紅。 |
| **丙-54（新入列 2026-08-23）** | **平台沒有任何速率限制，而 M5 開了一個會花錢的端點** | `02:NFR-001` 第 5 條逐字要求「未登入搜尋、外部即時搜尋與**匯入端點**必須具備速率限制與濫用防護」。**整個 `apps/platform` 沒有任何速率限制器**，而 `POST /skills/generate` 既建立版本又花錢。<br>**目前的煞車只有一個，而且它知道自己的極限**：`skill/admission` 的**行程內**單一工作區併發閘（一個工作區同時只能跑一次生成）。它擋得住「一個 session 開一百個並行請求」，**擋不住循序的迴圈，也只涵蓋一個 API 副本**。<br>**為什麼這件事比 Run 那側嚴重**：Run 每次派送會鑄一把帶 `max_budget` 的短效 key（SBX-008），生成走的是**共用的靜態 `LITELLM_API_KEY`**——把它的餘額用完不只停掉生成，`/embed`、索引增強與每一次 LLM Judge **都跟著停**。<br>**這是 `GENERATE_SKILL_EXPOSED=on` 的前置**，不是可選項；`.env.example` 已寫明。<br>**（2026-08-23 稍晚訂正：本列引錯了條文，而訂正的方向讓它更急不是更緩。）** `02:NFR-001` 第 5 條點名的是**未登入搜尋、外部即時搜尋與匯入端點**——**生成端點不在那三個裡面**。而那三個裡今天真的**匿名開著、零 middleware、零限制**的是 `GET /api/skills/search`（`router.go`，自 M1 起如此）；`POST /skills/generate` 反倒**根本沒有 mount**（`GENERATE_SKILL_EXPOSED` 未開）。<br>**本列的產品判斷不變**（曝光一個會花錢的端點之前要有速率限制），但它是**我們自己的前置**，不是 NFR-001 的引文。**逐字被違反的那一條，違反者是 M1 的搜尋端點。**<br>**另外要記一件反直覺的事**：額度計數器**不能兼任速率限制器**。`CountGeneratedSkills` 數的是 `skill_sources` 裡 `source_type = 'generated'` 的列，而**驗證失敗的生成一列都不寫**——「一個迴圈，每次都花錢、每次都被擋下」這個正好是要防的形態，在那個計數器上**完全隱形**。<br>**兩者的實際期限是同一個**：第一位外部使用者（`05` §0.1 那台還沒開始的節點）。repo 裡沒有任何現成的限制器可以沿用（無 `golang.org/x/time`、無 chi middleware、`infra/` 無 nginx／caddy／traefik），也不需要新相依——per-key token bucket 約二十行。<br>**✅ 結案（2026-08-24）**：`httpx.RateLimiter`（per-IP token bucket，時鐘可注入所以測試不用睡），掛在 NFR-001 第 5 款點名的三條路由上（`GET /api/skills/search`、兩條 import；`limited()` 在 `RequireSession` **外面**，所以也擋認證路徑本身）。`RATE_LIMIT` 未設＝啟用預設值（60/min、burst 30，純營運調參、不上畫面）、`off`＝關閉並大聲記 log——`RUN_QUOTA` 的慣例。**在製程內，天花板是單一 API 副本**（與生成單槽同一個 ponytail 註記：邊緣版本屬於部署期）。四支單元測試＋一支路由層整合測試（429 帶 `Retry-After` 且有句子）。**`GENERATE_SKILL_EXPOSED=on` 的這個前置解除。**<br>**同日對抗審查補三處**（程式面的缺陷是限制器自己的，不影響結案）：①**IPv6 改按 /64 分桶**——原本按位址分，單一配置的 2^64 個位址同時繞過限制並把 map 撐爆；②**代理下退化成全體共用一個桶**這件事寫進 `m4/release-checklist.md` §2.3 與 `.env.example`——程式刻意不讀 `X-Forwarded-For`（客戶端能設的標頭就是客戶端能選的桶），所以那是部署層的決定，**而原本沒有任何一份文件會提醒下一個人**；③兩條 import 路由原本零測試且 `limited()` 可被無聲刪掉，整合測試改成三條路由各跑一次（突變驗證：拿掉任一條的包裝，對應子測試變紅）。另外原註解把免責理由寫成「封測有邀請名單擋著」，但它主要保護的匿名搜尋**不在名單後面**，已改。 | 已結案 |
| **丙-55（新入列 2026-08-23）** | **生成失敗的紀錄寫下去了，但沒有任何人看得到它** | `02:GEN-003` 逐字要求「在工作區留下**可查**的失敗紀錄」。寫的那一半做了（`audit.ActionSkillGenerateFailed`，四種失敗都寫）；讀的那一半沒有——`ListAuditEventsByActor` 在 `apps/platform` 裡**零個非 generated 呼叫端**，沒有路由、沒有畫面。**一列只有拿得到資料庫的人看得到的紀錄，不是「在工作區」留下的紀錄。**<br>**~~這不只是 M5 的問題：`NFR-001` 的稽核要求對匯入、下載、刪除同樣適用，而那些事件也一樣沒有讀取面。~~****（2026-08-23 稍晚訂正：前半句是錯的。）** `02:NFR-001` 逐字要求的是「重要操作**保留**稽核事件」——**保留，不是可查**，而保留做到了（同交易寫入、`audit_events_immutable` trigger、400 天）。「可查」這兩個字**全 repo 只出現在 `GEN-003` 一處**。所以匯入／下載／刪除沒有讀取面**不構成對 NFR-001 的違反**：那是一個機會，不是一筆欠款。**這一列今天是 M5 自己的債。**（「M5 只是第一個把它變成允收準則的地方」那半句仍然成立。）<br>**還有一件會改變「最小版」形狀的事**：唯一那條讀取 query 是 **actor-scoped**（`WHERE actor_user_id = $1`），而 `GEN-003` 要的是「**在工作區**」。`audit_events` 有 `workspace_id`，但索引只有 `created_at` 與 `(actor_user_id, created_at DESC)`——**沒有 workspace 索引**。個人工作區的 owner 恰好就是 actor，所以「沿用既有 query」在今天會給出對的答案，**但那是巧合不是設計**，而鐵律 3 要的是 Workspace Scope。誠實的最小版是一條 workspace-scoped query ＋一個索引。<br>~~**因此 `GEN-003` 退回 ◐**，讀取面併進 `GEN-008`。**要決的是範圍**：只做生成失敗的一小塊，還是一次做工作區層級的活動紀錄（那會需要一個授權判準——稽核列裡有別的工作區的識別嗎）。~~（08-23 的狀態，下一段結案）<br>**✅ 結案（2026-08-24）：做的就是那個誠實的最小版。** `ListWorkspaceAuditEvents`（owner 仍是 `audit`）＋ migration `0038` 的 `audit_events (workspace_id, created_at DESC)`；讀取實作在 `audit.ListForWorkspace`，所以呼叫端做的是進 foundation 的函式呼叫而不是跨 context 的 query。路由 `GET /skills/generate/failures` 與 `POST /skills/generate` 同旗標同 `RequireInvited`。**範圍就地釘死在「只做生成失敗」**——`action` 是查詢參數而不是每個呼叫端一條 WHERE，所以擴成工作區層級活動紀錄時是加呼叫端不是加查詢，**但那一步會帶一個授權判準（稽核列裡有沒有別的工作區的識別），今天不做**。`GEN-003` 因此重新勾選。 | 已結案（`GEN-003` 已勾） |
| ~~**丙-56（新入列 2026-08-23）**~~ **已結案 2026-08-25** | **M5 的生成路徑從來沒有對真實閘道成功過一次** | 交給模型的 schema 是 `GeneratedSkill.model_json_schema()`，而 `strict: true` 下閘道**拒絕**它而不是放寬它：四個 default（每一個都把自己的 property 從 `required` 拿掉）、一個 `dict[str, str]`、六個長度與 pattern 約束。**任何一項都是每一次請求都 400。** 已修（`c9cf3db`），守門在 [`apps/llm/tests/test_strict_schemas.py`](../../apps/llm/tests/test_strict_schemas.py)。<br>**仍然開著的是這一列真正的內容**：修好之後**也還沒有對真實閘道跑過一次**。`task dev:model` 依 AGENTS.md 不得由唯讀 SubAgent 啟動，且會產生費用。<br>**為什麼這一列值得單獨存在，而不是併進丙-8**：丙-8 買的是一個**數字**（B 輪的通過率），這一列買的是「**這條路通不通**」。M5 的每一支測試都 monkeypatch 掉 client——那是它們該做的事——而 A／B 兩輪的 59 次量測走的是 spike 不是這個端點。**於是端點與閘道之間那一段，沒有任何一次執行覆蓋過。**同一個形狀也適用於 `/embed` 之外的其他 M5 呼叫。<br>**便宜的中間站**：一次 `task dev:model` ＋ 一次生成即可判定通不通，不需要跑完 20 個任務描述。<br>**2026-08-24 實跑一次，走到最後一跳被擋**：授權（`LITELLM_MASTER_KEY` 為 bearer）✔、閘道起動 ✔、model group 解析 `gpt-5.4-mini` ✔、請求成形送抵 OpenAI ✔——**OpenAI 帳戶回 429「no credits remaining」**。strict schema 是否被供應商接受（本列真正要驗的那一件）**仍未驗**：429 發生在 schema 驗證之前。**前置動作變成一件：OpenAI 帳戶儲值**（platform.openai.com → billing），然後重跑一次即可。<br>**順帶抓到並修掉一個環境缺陷**：`task dev:model` 的 compose 用 `-f` 指到 `infra/compose/` 時，`${LITELLM_MASTER_KEY}` 從 compose 檔自己的目錄找 `.env`（那裡沒有），**閘道會以空 master key 起動**、所有呼叫端 401「token not found in db」。Taskfile 已加 `--env-file .env` 並註明。 | `GEN-009` ③ 與封測前的部署驗收<br>**2026-08-25 再測一次，同一個回應**：`credit_balance_exhausted`／`You have no credits remaining`。**帳戶額度自 08-24 起沒有變。**<br>**確認過的一件事：這一項不能換供應商繞過。** 本列要驗的是「**OpenAI 的 strict 模式接不接受我們送的 schema**」，那是**供應商特定**的驗證——換一家跑成功只證明那一家接受，本列的問題原封不動。所以前置就是那一個動作，沒有替代路徑。<br>**一次儲值解三列**：丙-56（這條路通不通）、丙-68③（回歸集的數字）、丙-53（逐次成本分布）。三者的付費呼叫可以同一批做完，指令各自記在自己那一列。<br>**本列的指令**：`task dev:model`（會 fail-closed 檢查模型秘密；Taskfile 已於 08-24 加上 `--env-file .env`）＋ 一次生成即可判定，**不需要跑完 20 個任務描述**。<br>**✅ 2026-08-25 結案：儲值後跑完，`strict` 下 schema 被 OpenAI 接受，10／10 HTTP 200。**路徑逐段：`POST /v1/generate-skill` → LiteLLM（`gpt-5.4-mini` model group 解析 ✔）→ OpenAI ✔，`prompt_version=generate-skill/v2`（strict-legal 的那版）。**這是端點與閘道之間那一段的第一次覆蓋**——A／B 兩輪繞過端點，M5 每一支測試都 monkeypatch 掉 client。逐項數字見 [m5/report-generate-baseline.md §8](mvp/m5/report-generate-baseline.md)。<br>**這一輪抓到一個只有走完整條路才看得見的缺陷，記在 §8.3 而不是另開一列**：第一次呼叫回來的 `name` 是 `scan-receipt-table` 加兩個中文字，而 `skillpkg.Validate` 的 `nameRule` 是 `^[a-z0-9]+(-[a-z0-9]+)*$`——**那個名字會被擋下來**。**這不是模型變差，是約束搬家了**：strict `json_schema` 不接受 `pattern`，所以那六個約束從「模型產不出來」變成「下游要擋」。下游確實擋得住，**但代價是一次付了錢的呼叫換到一個阻擋級 finding**，而重試上限是 2。**發生率 1／10，九張情境卡全部合法——10 次不足以定一個率**，所以它是一個觀察不是一列殘項。 |
| **丙-57（新入列 2026-08-24，入列即結案）** | **一位訪客會被記成兩個 analytics session，而漏斗第一段是個比例** | `analytics.Sessions` 在請求沒帶 `sh_analytics` 時當場鑄一個 id、發一張 Set-Cookie，**並且立刻用那個 id 記事件**。平台不服務 SPA 的 document，所以訪客的前幾個 API 請求是**同時**抵達且**全部都是冷的**：每一個各鑄一個 id、各發一張 cookie，瀏覽器只留最後一張——其餘那些請求的事件就掛在**再也不會有任何後續請求帶上的 id** 上。深連結 `/?q=…` 時 `/me` 與 `/skills/search` 並行，於是 `search_performed` 可能落在一個永遠拿不到 `skill_detail_viewed` 的 id 上。**[`01` §11.2](01-goals-and-plan.md) 第一段是「輸入意圖後查看至少一個 Skill 詳情的比例」**——分母被灌水、轉換率被低估，而那一段封測**只有十二個人、只有一次機會**。<br>**原本的註解只承認了一半**：它寫「並行的第一個請求可能重複發出 `session_started`」，沒有承認**分裂的是 id 本身**。<br>**✅ 結案（2026-08-24）**：剛鑄出來的 id **不用於本次請求**——它只被「提議」給瀏覽器、還沒被接受，而瀏覽器才是仲裁者。沒有 context id，`emit()` 就會丟掉這次請求要寫的任何事件；**visit cookie 也一併不設**，否則那一天會被一個 `session_started` 從未落地的請求標記成已開始。<br>**代價寫在這裡而不是留給下一個人發現**：一次造訪的**第一個 API 請求不留任何紀錄**。那是**掉一個 session**，不是**扭曲一個比例**，而 §11.2 第一段是比例。<br>**同批補上兩條 exactly-once 斷言**：原測試用 `pool.QueryRow(...).Scan(...)` 讀那兩個事件，而 pgx 取第一列就丟掉其餘——**把 `SearchPerformed`／`SkillDetailViewed` 的呼叫複製一行，測試照樣全綠**。改成 `count(*) = 1`，突變驗紅。<br>**前端那一半是 `refetchOnWindowFocus`／`refetchOnReconnect` 關掉**（見 [`queryClient.ts`](../../apps/web/src/api/queryClient.ts) 的檔頭）：alt-tab 與重新連線不是一次搜尋。**全域 `staleTime` 被試過並退回**——它會讓剛匯入的人幾分鐘內看到舊的搜尋結果，拿真實的體驗成本換一個它其實修不到的問題（聚合本來就按 (session, day) 去重）。<br>**對抗審查補三件（2026-08-24 稍晚）**：①**上面那兩條 exactly-once 斷言押不住這一列**——它們押的是「一次請求寫兩列」，而整合測試只有一個序列的 cookie jar，**結構上不可能重現並行的冷請求**。真正押住修正的是 `analytics_test.go` 的 `TestAFreshlyMintedSessionIdIsOfferedNotUsed`（不需要資料庫）；補之前，把修正整段還原是全綠的。<br>②**掋的不是隨機一批 session**：掋的是「第一個 API 請求就是被量測請求」的人，也就是深連結 `/?q=…` 的訪客。`tools/analytics/funnel.sql` 第一段的分子要求 `session_id IN searched`，所以這種 session **分子分母同時消失**。若深連結訪客的轉換率與平均不同（合理假設：他們帶著明確意圖），**這是偏差不是雜訊**——寫在這裡而不是留給下一個人發現。<br>③**第一段分子還有另一個灌水來源，同批一併修掉**：`GET /api/skills/{id}` 對**每一個呼叫端**都發 `skill_detail_viewed`，而打包與下載、並排比較也讀同一條。**比較那個才是讓數字假掉的**：一張沒有任何人打開過詳情頁的表，每比一個 Skill 就鑄一筆「打開了詳情」。現在兩個表面都帶 `?view=embedded`（已入 `public.yaml`，連同它一直在讀卻從未宣告的 `from`／`rank`），伺服器對這種讀取不記任何事件；兩側各有一條斷言，且各自突變驗紅。代價是同一個 Skill 在兩個表面都開著時多一次請求——換掉一個會數到沒人做過的動作的量測。 | 已結案 |
| ~~**丙-58（新入列 2026-08-24）**~~ **已結案 2026-08-25** | **`EVAL-013` 的回歸閘門已經不存在了，而沒有任何人發現** | [`tools/eval-regression/judge_regression.py`](../../tools/eval-regression/judge_regression.py) 的檔頭逐字寫著它「鏡像 Go 那一側的防線 3」——**那句話在 [ADR-043](../adr/ADR-043-evidence-citation-is-verified-by-content-not-by-its-claimed-source.md) 落地那天就不再成立了。** 實作 commit 動了 `judge.go`、`eval.go`、契約與前端，**沒有動 harness**；而同一支檔案後來為 ADR-049 更新過，所以它是「跟了 049、漏了 043」。<br>**四處分歧**：①內容回驗與改判（§1、§2）harness 完全沒有，指名來源交不出引文即判失敗；②正規化與十二字元門檻（§4）沒有；③`exact`／`normalized`／`not_checked` 三態（§4）只有二態；④`artifact` 引用在 harness 裡**完全通過**，而 ADR-043 §3 說它不得滿足 `evidence_required`——`rubric-content-007-writing-v1.json` 有 **18 個** `evidence_required: true`，而 `evidence_required` 這個字串在 harness 全檔 **0 次**。<br>**還有一條是 pre-043 就漏的**：Go 在「一條引用都沒有」時降級，harness 只在 `unverifiable` 非空時降級——零引用的 `passed` 在 harness 裡原樣存活。<br>**影響的是一個被引用過的數字**：[report-judge-regression.md](mvp/m3/report-judge-regression.md) §5.1 的 **90／90**。它沒有錯——它是 `judge-run/v2` 在當日判準下的真實量測——但**用今天的 harness 重跑不會等值重現**，而一份重跑不出來的數字是歷史證據、不是閘門。<br>**2026-08-24 已做的**：harness 檔頭與報告首部各加一段註記，把它陸地降為「pre-ADR-043 判準的歷史證據」。**還沒做的是選一條路**：（a）把那四個行為鏡像回 harness 並**重量一次**（會花錢，而且重量得到的數是新基線、不是拿來與 90／90 對比）；或（b）接受 `EVAL-013` 在 MVP 內沒有可重跑的回歸閘門，並在 `03` 註明。<br>**為什麼它不是「修一下就好」**：harness 自己沒有測試，鏡像四個行為之後沒有任何東西能證明它真的鏡像對了——唯一的驗證方式就是花錢重跑一輪。 | `EVAL-013`；一個選擇（重量或接受）<br>**✅ 2026-08-25 結案：這一列要的是「選一條路」，而路已經選了並且走了一半。** 本列的收尾條件逐字是（a）把四個行為鏡像回 harness 並重量一次，或（b）接受 `EVAL-013` 在 MVP 內沒有可重跑的回歸閘門。**選的是（a）**：四項已於 08-25 補齊（NFC／空白摺疊的 `normalized` 比對、內容回驗與改判、`not_checked` 第三態、`evidence_required` 降級），並附九支會紅的測試——**本列自己指出的那個顧慮「harness 沒有測試，鏡像之後沒有東西能證明它鏡像對了」因此不再成立**。<br>**剩下的「重量一次」不留在這一列**，因為它是另一種東西：這一列買的是「閘門存不存在」，重量買的是「今天的讀數是多少」，而後者會花錢、且與丙-8 的 B 輪同批較省。已於同日獨立為 **丙-68**。**兩列不併，是因為併了之後這一列的結案條件會被一個花錢的動作綁住，而那個動作與「閘門存不存在」無關。** |
| ~~**丙-59（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **`skill_detail_viewed` 的 `arrival` 在正式環境只有一個值** | `0029` 的 schema 給了 `arrival`（`search`／`direct`）與 `arrival_rank`，handler 讀它們（`analytics.ArrivalFromRequest`），`GET /policy/data-retention` 對使用者列出它們，整合測試也测它們。**而前端從來沒有送過 `from` 或 `rank`**（`apps/web/src` 全檔 grep 零命中，詳情路由也沒有 `validateSearch`），所以每一列都是 `direct`／0。<br>**這不是謎報**：`direct` 確實被記錄了，揭露面沒有說謊。**也不影響漏斗**：[`funnel.sql`](../../tools/analytics/funnel.sql) 第一段只看 session 有沒有兩種事件（**2026-08-25 起還要求詳情發生在搜尋之後**，但仍不看 `arrival`），**從不讀 `arrival`**。問題是它是一個**分辨不出任何東西的維度**，而它看起來像是活的。<br>**且 `02:O11Y-004` 沒有要求它**——允收準則要的是四個事件與屬性白名單，`arrival` 是白名單上多出來的一個。<br>**兩條路，選一條，不要两邊都不做**：（a）**填它**——搜尋結果連結帶 `from=search&rank=N`、詳情路由加 `validateSearch`、`useSkillDetail` 把它帶進 API。約四個檔。**代價不只是程式**：詳情頁的網址從此帶著「我在第幾名找到它」，複製貼上去給別人時一併帶走，而這是一個**新的網址狀態**，依 AGENTS.md 要先過 [IA §0](../design/information-architecture.md) 那一關。（b）**拆它**——一支 migration 降两欄、`policy.go` 拿掉兩個字、handler 與契約的三個參數一併拿掉。<br>**建議（b）**：沒有規格要求它、沒有查詢讀它，而（a）要的是一個新的公開網址狀態去換一個沒人問的區分。**但這是產品判斷，不是實作判斷，所以記在這裡而不是直接動手。** | 一個選擇（填它或拆它）<br>**✅ 2026-08-25 結案：選了（b），拆掉。** `0040` 降兩欄（分割表的 `DROP COLUMN` 已驗證串到每一個 partition）、ArrivalFromRequest 整支刪除（不加反引號：它已經不存在，而 `doc-identifier` 對帳的正是「被當成宣告名引用」的識別字）、`policy.go` 的揭露改成說「**不記錄到達路徑**」而不是留空——那一格在其他三個事件都在講「這裡刻意沒有記什麼」，空著會讓這個事件看起來是唯一沒有邊界的一個。**契約也一併拿掉 `from`／`rank`**：宣告一個伺服器會忽略的參數比不宣告更糟，client 送 `rank=3` 會拿到 200，然後相信位置被記下來了。<br>**裁定的根據曾經有一個洞，已補**：「每一列都是 `direct`」是**從前端 grep 推出來的，不是從資料查出來的**，而契約公開宣告過那兩個參數——SPA 沒送不等於沒有任何 client 送過，且欄位一旦 drop 就查不回來。`0040` 的檔頭因此寫入一段**套用前必跑的 `GROUP BY` 查詢**：一列代表推論成立且從此有證據，超過一列代表停下來重想。**這不改變裁定**（沒有規格要它、沒有查詢讀它、填它要換一個新的公開網址狀態），它換掉的是裁定的**證據等級**。 |
| ~~**丙-60（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **SEC-009 Suite 1 的 CI 腿從寫出來就沒有成功過一次** | `15a9dd7` 為 `sandbox` job 加了「Sandbox driver tests on gVisor」這一步（安裝 runsc、以 `SKILLHUB_SANDBOX_TEST_RUNTIME=runsc` 重跑 `dockerdrv` 全套）。**GitHub Actions 對一次 push 只跑 head commit 的 workflow**，所以那個 commit 自己沒有跑到；下一次動到 `.github/workflows/ci.yml` 的 push（`b255333`）第一次執行它，**當場紅**，`ff7d934` 再紅一次。<br>**它跟 `platform` 那條紅不是同一件事**：`platform` 從 2026-08-21 的 `9e4fb84`（Go floor 抬到 1.27.0，而 golangci-lint 2.13.1 的預編譯 binary 是用更舊的 Go 編的）起卡在第 5 步，**`go vet`／immutability SQL／`go test ./...`／`go build ./...` 四步是被 skip 不是跑過**；`b255333` 修好之後那四步才第一次真的執行。**`platform` 在 `ff7d934` 已經綠**。<br>**這一列不能靠推理關掉。** 本機是 Windows，Docker Desktop 的 daemon 沒有也裝不了 runsc；job log 需要 repo admin 權限，`check-runs` API 只給得出「failure」三個字。已在 `b255333` 之後的 commit 讓三個測試步驟在失敗時把輸出寫進 `$GITHUB_STEP_SUMMARY`（該欄位 `check-runs` API 讀得到），**所以下一次紅會帶著它自己的證據**。<br>**在讀到那份輸出之前，不知道它是 runsc 的 syscall 落差（ADR-015 指名的那項反覆成本）還是 runner 的環境問題，兩者的處置完全不同。** ADR-022 §附錄把 Suite 1 定義為「每次 `apps/sandbox`／`infra/images` 變更都要跑」的門檻——**一個從來沒綠過的門檻，等於這條規則目前沒有被執行過。**<br>**2026-08-25 結案，同日。** 失敗輸出走 annotation 拿到之後，三次量測把它問完了：①`MaxPIDs=16` 連 sentry 都起不來（`cannot read client sync file`）——fixture 值，不是 C-13 的缺陷，生產預設是 256；②`=64` 起得來但做不了事；③`=128` 之下 trivial workload 跑得動，而 fork bomb 拿到 `sh: sleep: Cannot allocate memory`——**C-13 在 gVisor 上是有生效的**，只是 gVisor 把同一個拒絕回報成 ENOMEM，而 runc 的 busybox 說 `can't fork`。斷言太窄，差一點把一個有效的控制判成失效。<br>**斷言最後改成看退出碼**：文字是 runtime 的、不是 kernel 的契約；而且同一個 commit 連跑兩次，transcript 一次有那兩行、一次全空——任何對文字的斷言在 runsc 上都有一半的時間看不見。數存活數也不行：shell 在第一次 fork 失敗就結束，迴圈之後什麼都不會執行，而第一版的計數用了 command substitution——它需要的正是那個被拒絕的 fork。<br>**`sandbox` job 已綠**（`f5c36c3`）。ADR-022 §附錄 要求「每次 `apps/sandbox`／`infra/images` 變更都跑 Suite 1」這條規則，從今天起是真的在被執行的。 | 已結案 |
| ~~**丙-61（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **Runtime Image 的每週重掃從未跑完過，所以 I-06 的漏洞閘門一次都沒有對已發佈映像執行** | `runtime-image.yml` 的 `rescan` job（`schedule`／`workflow_dispatch` 觸發）宣告 `packages: read`，卻用 `actions/attest` 搭 `push-to-registry: true` —— 那是 registry 寫入。兩次排程執行（08-17、08-24）**都在那一步掛掉**，而每一次 `push` 執行都綠（`publish` job 拿的是 `packages: write`）。<br>**後果有兩層，第二層比較貴**：①`I-04` 的 `scanned_at` 重新見證從來沒寫成功，而 I-04 把「沒有有效期內掃描」視同未掃描；②那一步**排在 「Fail on fixable Critical/High in the published image (I-06)」之前**，所以每週檢查已發佈映像有沒有新的可修補 Critical/High 這件事，**一次都沒有發生過**。已發佈映像的漏洞狀態，停在它被發佈的那一天。<br>**已改為 `packages: write`，但這個修正還沒有被驗證過。** `rescan` 的 `if` 只認 `schedule` 與 `workflow_dispatch`，所以推這個修正本身觸發的是 `push`（跑 `publish`，跳過 `rescan`）。~~**下一次驗證機會是週一 04:31 的 cron**~~，或由有權限的人手動 `workflow_dispatch`。<br>**2026-08-25 訂正兩處，都是這一列自己記錯的**：①cron 是 `17 4 * * 1`，**週一 04:17 UTC**；那個 04:31 是觀測到的實際起跑時間（GitHub 排程本來就會漂），把觀測值寫成排程值會讓下一個人在錯的時間去看。②**「下一次」已經過去了而且不算數**——08-24 04:31 那次排程執行是 failure，但它跑在修正推上去之前，所以它證明不了任何事。**真正的下一次是 2026-08-31（週一）**。<br>**本列因此維持開著，而且它等的東西無法在本機加速**：`rescan` 的 `if` 只認 `schedule` 與 `workflow_dispatch`，推 commit 觸發的是 `push`（跑 `publish`、跳過 `rescan`），而 `workflow_dispatch` 需要有權限的人。<br>**不要把這一列當成已結案，直到看到一次綠的 `rescan`**——`packages: write` 是唯一看得出來的成因，但一次沒跑成功過的 job 可能不只錯一個地方。 | 一次綠的排程 `rescan`（或手動 dispatch）；若仍紅，讀它的 step summary<br>**✅ 2026-08-25 結案，而且是本列自己指定的那種證據。** 不必等 08-31 的 cron：`workflow_dispatch` 是 `rescan` 的第二個觸發者，本列一直把它記成「需要有權限的人」，**而那個權限就在這台機器上**——`git credential` 裡的 PAT 帶 `workflow` scope，一次 API dispatch 即可。**這一列因此不是被時間解開的，是被查證解開的**：它寫著「無法在本機加速」，那句話沒有查過憑證。<br>**逐 step 的證據**（run `32855978261`，`event=workflow_dispatch`，`rescan` job **success**）：第 3 步登入 GHCR ✔、第 4 步解析已發佈 digest ✔、第 5 步對該 digest 做 SBOM 與掃描 ✔、**第 7／8 步重新見證（`actions/attest`，`push-to-registry: true`）✔——就是掛掉兩次的那一步**、**第 9 步 `Fail on fixable Critical/High in the published image (I-06)` ✔——這一步在今天之前一次都沒有執行過**。`publish` 如預期 skipped（它的 `if` 只認 `push`）。<br>**兩層後果都解除**：①`I-04` 的 `scanned_at` 重新見證第一次寫成功，已發佈映像不再被視同未掃描；②已發佈映像的漏洞狀態不再停在它被發佈的那一天。<br>**本列先前的顧慮「一次沒跑成功過的 job 可能不只錯一個地方」已排除**：`rescan` 的十一個 step 全綠，`packages: write` 是唯一的成因。 |
| **丙-62（新入列 2026-08-25 · 記錄，不追蹤）** | **`platform` job 在 CI 閘門恢復後的第一次執行是紅的，而沒有人知道為什麼** | `b255333` 修好 golangci-lint 與 Go 1.27 的不相容之後，`go test ./...` 四天來第一次真的執行——**當場紅**。下一個 commit（`ff7d934`）起就一直是綠的，之後每一次都綠。<br>**三種方式都重現不了**：本機連跑六次全綠；用 `golang:1.27` 容器在 Linux 上對同一個 commit 跑，綠；另建一個全新的空資料庫（CI 每次拿到的就是全新的）再跑一次，也綠。job log 需要 repo admin 權限，當時還沒有 annotation 通道，所以那一次的輸出**已經拿不回來了**。<br>**為什麼記在這裡而不是讓它消失**：那四天之間有八個 commit 在 `go test` 沒有執行的情況下進了 main。紅的那一次很可能是其中之一留下的、後來被同一批修正順手蓋掉的東西——也可能只是一次 flake。**兩者的差別只有在它第二次發生時才看得出來**，而那時候有人會需要知道它不是第一次。<br>**現在有通道了**：三個測試步驟失敗時會把輸出寫進 `$GITHUB_STEP_SUMMARY` 與一則 `::error::` annotation（`tools/ci/report-failure.sh`），annotation 端點唯讀權限就讀得到。**下一次紅會帶著證據。** | 不追蹤——沒有可執行的動作，只有一個值得被找得到的事實 |
| ~~**丙-63（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **刪掉一個 Skill 之後，伺服器逐字告訴使用者「30 天寬限期後清除」，而沒有任何東西會清除它** | `DELETE /skills/{id}` 的回應 `note` 原文是 `version snapshots are retained for the 30-day grace period, then purged`，**顯示在刪除完成的畫面上**。全 repo 唯一硬刪 Skill 的語句是 `PurgeUnreferencedSkills`（[`db/queries/governance.sql`](../../db/queries/governance.sql)），它吃的是 workspace id、**沒有 `deleted_at` 述詞**，且唯一呼叫者是帳號刪除（[`skill/library/purge.go`](../../apps/platform/internal/skill/library/purge.go) 的 `PurgeWorkspace`，註解自陳為「the registry's share of an account deletion」）。**單獨刪掉一個 Skill 的人，`deleted_at` 會永遠留著。**<br>**這是同一週第三次同型事故**（稽核事件 400 天、Run 產出 30 天、這一列），但它多一層：`30 天` 這個數字曾因 PDM-006 未追認而被**刻意從 `/policy`、`/workspace/account` 與 `/workspace/skills` 的確認文案三處清掉**，理由逐字寫在 [`WorkspaceSkills.tsx`](../../apps/web/src/pages/WorkspaceSkills.tsx) 的註解裡——「伺服器自己的 `note` 才是那個由**會強制它的東西**說出寬限期的地方」。那句話不成立，而它正是唯一還留著硬數字的地方。<br>**2026-08-25 已做的一半**：`note` 停止宣告期限（`02` §2.2：不得顯示沒被強制的承諾），常數化為 `deletionNote` 並補上會紅的守門測試（`purge`／`grace`／`30-day` 任一詞回到那句話就 FAIL）；`/workspace/skills` 與 `/policy` 的「再清除」也一併改成不宣稱。**仍開著的一半**：清除工作本身。**刻意不順手實作**——那個 30 天來自 PDM-006 §6.1，仍未追認，在沒人簽名的期限上硬刪使用者內容比不刪更糟。解鎖對象是 PDM-006 追認（乙-15／[`05` R-1](05-pending-rulings.md)）<br>**✅ 同日結案：清除工作已落地，但它拒絕在沒有人簽名的情況下跑。** `maintenance purge-deleted-skills` fail-closed 在 `SKILL_DELETION_GRACE` 上——未設就在建 Service **之前**拒絕啟動，一句 SQL 都不送（測試用 nil pool 證明這一點：碰到資料庫就會 panic）。理由與同檔另外三個保存期變數同一條，但更硬：**這個 job 刪掉的是使用者自己的內容**。<br>**三條排除不是禮貌**：被別的工作區 fork 過、被 Run 用過、或還掛著測試案例的一律不動——硬刪會斷掉第三方的溯源鏈（DISC-003）與鐵律 4，**而且是去罰錯的人**。落 Go 之前先在 SQL 層驗過：60 天且無人依賴的清掉、1 天的留著、60 天但被 fork 的留著。三次突變逐一見紅，每一次都指名哪一列被錯誤地帶走或錯誤地留下。<br>**兩個計數器而不是一個**：`waiting` 歸零是 job 在排空，`kept` 永遠不減是溯源規則正確地在擋——維運者看到一個頑固的非零值，要能分辨自己看的是哪一個。<br>**自開交易**（與同檔的 `PurgeWorkspace` 相反，那一條收 identity 的交易）：`SET LOCAL skillhub.purge = 'on'` 是 0005 trigger 的唯一豁免（0013），作用範圍剛好是那個交易，所以它沒得選。已在 `raw_sql_allow` 與另外兩條同型語句並列登記。<br>**刪除完成畫面上的期限沒有加回去，那是刻意的**：變數未設的部署什麼都不會刪，此時宣告期限就又是一次沒被強制的承諾；設了變數的部署則是少說而不是說謊。PDM-006 §6.1 的 30 天仍未追認，要不要把那句話放回畫面是產品與法務的決定。 |
| **丙-64（新入列 2026-08-25，入列即結案）** | **`datasets` 的 90 天，索引為它而建、註解為它而寫，掃描從來沒有存在過** | [`0004`](../../db/migrations/0004_test_lab_and_runs.sql) 建了 `datasets_expires_at_idx`，正上方的註解逐字描述了要用它的那個 sweep（「scans for expired rows rather than scheduling at creation time」）。欄位有、索引有、設計理由有，**會跑的那一塊沒有**——與稽核事件那一列是同一種四缺一。使用者在上傳前就被告知 90 天（`retention_days`，`testlab.DatasetRetention`），同意書 §3 與 §8.2 也各寫一次；在此之前唯一刪過 `datasets` 的語句是帳號刪除，**沒關帳號的受測者，上傳過的每一個檔案都永久留著**。<br>**同日補齊**：`ListDatasetsPastRetention` ＋ `MarkDatasetPurged`（與 `MarkDatasetObjectLost` 分開兩條語句，因為一條是「檔案不在」一條是「我們because過期移除」，合併會讓一個專門分辨這兩者的檔案分辨不出來）、`testlab` 的 `ExpiredDatasetCandidates`／`MarkDatasetPurged`、`maintenance purge-datasets`，物件與資料列的順序沿用另外兩條共用的 `objreconcile.PurgeExpired`。**測試兩個方向都斷**（過期的檔案要消失、窗內的檔案不准動、第二趟必須是 0），突變證據：拿掉 `expires_at <= now()` 之後 `datasets purged: got 2, want 1`。**部署要多一條 cron**，與 `purge-audit`／`purge-run-artifacts` 同批（見 [m4/release-checklist](mvp/m4/release-checklist.md)） |
| ~~**丙-65（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **`/workspace/import` 只有一條頁內入邊，這是 R3 的現行違規（IA-9）** | [資訊架構](../design/information-architecture.md) §2.3 早就把它記成「1 入邊 ❌ 不適用…第二條頁內入邊仍欠」，但 §5、§8 與本表的丙-52 都寫著「只剩 IA-6 一項開著」——**機器看得到那一列（它解析得出來、數字也對），所以沒有任何東西出聲**。唯一入邊是 `Home.tsx` 的 `no_results`。R3 是 §0 的規則、走在程式前面，所以**方向是改程式不是改規則**；它也不能塞進 R3 的「具名」分支（那一支的判準是脈絡只有一個地方產得出來，對匯入不成立）。同日已補進 §5 並編號 IA-9。不需要產品裁定<br>**✅ 同日結案。** 第二條入邊落在 `/workspace/skills` 的空狀態——那句話本來就在講匯入（「還沒有任何 Skill。到首頁搜尋一個再 Fork，**或匯入自己的套件**」），**它說出了下一步，然後叫你自己去導覽列找**，正是 R3 要抓的形狀。文案一字未改，詞組變成連結。**訪客拿不到打不開的連結，而且這裡不需要登入狀態分支**：`GET /skills` 走 `RequireSession`，所以這個空狀態對訪客結構上不可達。R3 一個字沒動——動的是程式。 |
| ~~**丙-66（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **搜尋結果的截斷揭露只做了一半：說的是下界，規則要的是總數** | [設計系統](../design/system.md) §4.3 要求「共 N 筆」，`Home.tsx` 與 `WorkspaceSkills.tsx` 寫的是「**超過** N 個」。理由那一半成立（使用者知道被截斷了），數量那一半不成立（他不知道被截掉多少）。**缺的不是前端**：伺服器沒有回總數欄位，所以這是契約缺口，補法是 `public.yaml` 加一個 total 再讓兩個畫面讀它<br>**✅ 同日結案。** 三條檢索語句各長出一個 `count(*) OVER ()`，契約與兩個畫面跟上，`超過 N 個` 改成 `共 N 筆`。**刻意不是第二支 COUNT 查詢**：平行的計數得把每一條述詞再寫一次，兩份複述第一次不一致的那天，頁面就會報出一個不描述它底下那張清單的總數，那比它取代的下界更糟；window function 長在同一條 WHERE 之後、同一個 LIMIT 之前，漂移根本表達不出來。<br>**天花板寫進契約與 SQL**：混合檢索兩條腿各取 50 筆候選，所以那條路上算的是至多 100 筆候選裡通過篩選的。索引 45 篇，今天精確；**超過 100 筆之後它變回下界而名字還叫總數**，那時要把篩選推進候選 CTE——與該處 ponytail 註解本來就要的是同一個修改。降級的詞彙路徑永遠精確。<br>釘住漂移的是那條關係斷言（未截斷時 `total` 必須等於列數；截斷時兩頁必須報同一個母體），突變 `total := len(hits)` 兩條同時紅。 |
| ~~**丙-67（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **兩把設計尺自陳的守門，有三處比實際寬** | 逐處：①`system.md` §3 第 16 條原本寫「四項各有守門」，而「markup 裡的 class 在 CSS 裡有規則」**根本沒有測試**（`badge-source-package`／`badge-source-template` 就是這樣沒有 CSS 規則地上線，而它們的兄弟 `badge-source-model` 有——同一個「這句話出自模型」的訊號在三種來源之間長得不一樣）；②`e2e/routes.ts` 是**手寫的 18 列、沒有覆蓋率棘輪**，新增路由不會 FAIL，而隔壁的 `a11y.test.tsx:262` 已經示範了怎麼做（比對 `router.routesById`）；③`design-system.test.ts` 的「index.css 是唯一樣式表」用 `readdirSync(import.meta.dirname)`，**不遞迴**，`src/components/foo.css` 會無聲通過。同日已把三處的**宣稱**改成誠實（不再自稱有守門），**守門本身仍缺**。<br>**另有一處今天才關掉的同型缺口，記在這裡當對照**：`docs/design/**` 不在 `ci.yml` 的 `web` 路徑篩選裡，於是「只改文件」的 PR 會整個跳過 web job——而那兩支測試正是**把文件當政策來源**並對偏離帳、字級與間距表設了只能縮小的上限。換句話說，兩份測試檔頭都寫著「有人必須讀一下」的那一種修改，正是 CI 唯一不會跑的那一種。已補進篩選<br>**✅ 同日結案，三個守門都建了。** ①class 在 CSS 裡有規則——**不是**「每個 class 都要有規則」，因為有七個刻意沒有，而理由分兩種（已定的決策 vs 測試 hook），所以名單裡每一筆都要寫出是哪一種，並附兩道棘輪：只能變短、且**不得腐爛**（某筆後來有了規則或已不在 markup 裡，測試會要求刪掉那一行）。**它第一次跑就抓到一個真的**：`.page` 掛在 11 頁的 14 個 `<section>` 上，沒有規則、沒有測試選它、什麼都不畫——既不是決策也不是 hook，是殘留；同日移除，名單 8 → 7。②375px 的覆蓋棘輪：`ia.test.ts` 把 `e2e/routes.ts` 當文字讀，與 `router.tsx` 雙向比對（收斂成 shape，所以一條路由掃多個位址仍合法），**放在 vitest 而不是 e2e**——只在慢套件裡響的棘輪沒有人感覺得到。③唯一樣式表的斷言改成遞迴。<br>**同批把 `ci.yml` 的 `web` 篩選補上 `docs/design/**`**，因為這兩把尺讀的是文件、而只改文件的 PR 原本會整個跳過 web job（那一列另記在丙-67 的本文裡）。 |
| ~~**丙-68（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **`EVAL-013` 的回歸集換了尺，但還沒有人用新尺量過一次** | 丙-58 的後續：harness 已於同日補齊 ADR-043 的四項行為（NFC／空白摺疊的 `normalized` 比對、內容回驗與改判、`not_checked` 第三態、`evidence_required` 降級），並附九支會紅的測試。**但報告 §5.1 的 90／90、§11 的 22 條、§12 的 27 條全部是舊尺量的**，報告首段自己寫著「用今天的 harness 重跑不會等值重現」。所以缺的是一次**重量**（不是對比），會花錢，自然落點是丙-8 的 B 輪同批。`03:255` 已就地訂正，勾維持——port 之後 harness 確實量產品在跑的東西，取消勾會宣稱「回歸集與流程不存在」，那比缺註記更假<br>**2026-08-25：本列拆成三塊，兩塊今天免費驗掉了，剩下的那一塊不是「等一次重量」而是「等一次儲值」。**<br>①**harness port 之後能不能對真實資料組出請求**——`judge_regression.py --dry-run` 是 live read（讀 PostgreSQL 與 S3、組出完整請求、**不呼叫模型、不產生費用、不寫結果檔**），對今天的 dev 語料跑完 **45/45 全部組成功**（`selection=m2_latest_compatibility`，131 筆 compatibility baseline 裡選出的那批）。**這一塊本來是本列最大的未知**：port 之後沒有任何一次執行證明過 harness 與今天的 schema 對得上。<br>②**ADR-043 的四項行為**——由 `test_judge_regression.py` 的九支測試守著，今日重跑 **9/9 綠**。<br>③**數字本身**——只有這一塊要付費，而且它與丙-56 共用同一個前置（見該列）。<br>**重量指令逐字記在這裡，免得下一個人再推一次**：`apps/llm/.venv/Scripts/python.exe tools/eval-regression/judge_regression.py --note "<why>"`（不帶 `--dry-run`；結果只可 append 到 `results.jsonl`，舊列是可比對的歷史證據，不可覆寫或重排）。<br>**✅ 2026-08-25 結案：新尺量完了，45 筆、$0.8712，結果寫進 [m3/report-judge-regression.md §17](mvp/m3/report-judge-regression.md)（不改 §5.1，那是當時的證據）。**<br>**70／90 ＝ 77.8%，mismatch 仍然 0，降級 20。** 舊尺是 90／90。**而那 20 條有一個完整的解釋，並且不是 Judge**：**20 條全部落在同樣的 10 支被截斷的 Run 上**（10 支 × 每支 2 條計分準則 ＝ 20），降級原因全部是 `incomplete_evidence`，**而那 20 條裡模型自己的 `model_result` 全部是 `passed`**。**新尺不是判得比較差，是在證據不完整時拒絕採信模型說的 `passed`**——ADR-043 的意圖就是這個。未截斷的 35 支、70 條計分準則，**一條都沒有降級**，所以 77.8% 與 100% 的差距沒有殘量。引用 141 條裡 `exact` 135、`not_checked` 6，沒有任何一條回驗失敗。<br>**這一輪指出的下一件事不在本列**：截斷是唯一的降級來源，而 **10／45 ＝ 22% 的 Run 仍然截斷**，且截斷的代價比丙-47 調整當時更高（以前只是讓 Judge 少看一點，現在讓那支 Run 的每一條 `evidence_required` 準則直接降級）。**那是一個可以量的比率不是一個判斷**，寫在報告 §17.3。 |
| ~~**丙-69（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **Token 300K／60K 的唯一強制點在它要限制的那個沙箱裡面** | PDM-005 §5.2a-(4) 逐字把 **Go Worker** 指定為「token 上限的唯一強制點」，而唯一的實作是 `infra/images/runtime-agent-sdk/run.mjs` 的 `ceilingBreach`——**在不受信任的工作負載自己的行程裡**；`RunUsage` 不把數字帶回來，`apps/platform` 沒有任何地方累加 token。`gateway.go:35` 的註解說「that is the worker counting」，`service.go:188` 的註解說「the counting happens in the sandbox harness」，**同一份程式的兩處註解互相矛盾**。<br>計數邏輯本身是對的（逐回應取各欄最大值、跨回應累加、cache token 計入 input，全部符合 §5.2a-(3)）；錯的是位置。工作負載自己拿著 `ANTHROPIC_AUTH_TOKEN` 與 `ANTHROPIC_BASE_URL`，繞過 `run.mjs` 直接打閘道時，實際上界只剩 `max_budget`（$0.50）與 `tpm_limit`——**§5.2a-(4) 自己算過那大約是 2.4M input token，約為畫面上告訴使用者、而使用者按下確認的那個數字的八倍**（`02:TEST-005`）。修法是讓 Go 從閘道回報累加並在超過時取消，不是改 `run.mjs`<br>**✅ 同日結案，強制點移進 Go。** driver 在既有的 2 秒輪詢上多讀一次 LiteLLM 管理 API 的 `GET /spend/logs/v2`（以 `key_alias` 定址，所以 RUN-008 重啟後重新接上的 attempt 也問得到，不需要記憶體裡的任何東西、也不必把 virtual key 落地；該端點刻意不回 `messages`／`response`），超過即走與 wall clock **完全相同**的取消路徑——沒有第二種死法。上限值讀 Run 自己的 `policy_snapshot` 而不是今天的預設值（鐵律 4）。input 與 output 兩半都擋。<br>**查證而非猜測**：三個候選只有一個成立。`RunUsage` 只在 attempt terminal 時到達，停不了任何東西；Trace 事件尚未接上；閘道的 spend log 可用，而且 PDM-003 spike §11.5.3 當初量那個 300K 用的就是這條路。**必須輪詢，沒有推播**，但沒有新增等待。<br>**`run.mjs` 的計數器保留，而且三處註解寫明不要順手清掉這個重複**：合作的工作負載由它早一步、便宜地停下，不合作的由 Worker 停；兩邊都用嚴格大於，剛好停在上限的 Run 兩邊都放行、不會互相打架。<br>**檢查刻意 fail-open**（管理 API 不在沙箱網路上，惡意 Skill 弄不倒它；為一次維運抖動殺掉健康的 Run 比較糟，而 wall clock 與 `max_budget` 仍在），所以同批補了兩個 counter：`skillhub_run_token_ceiling_breached_total` 是**守門在運作**，`skillhub_run_token_usage_unreadable_total` 是**守門靜默停工**——一個沒有序列的 fail-open 守門，與一個正在運作的守門長得一模一樣，那正是本列曾經的狀態。<br>**同批修掉兩處互相矛盾的註解**（`gateway.go` 說「that is the worker counting」，`service.go` 說「the counting happens in the sandbox harness」——同一份程式，兩個答案，那是這個缺陷活下來的方式），並把診斷碼從 `execution` 改為 **`budget_exhausted`**：那個值一直都在 `sandbox-provider.yaml` 的 enum 裡、只是沒有人用，而 PDM-005 §5.2 風險表逐字禁止的正是報成泛用失敗。`runs.failure_class` 維持 `workload_error`（那一欄是重試決定，重試只會再燒一次）。<br>六次突變逐一見紅（含端對端），全套 844 支通過。 |
| **丙-70（新入列 2026-08-25，入列即結案）** | **一個沒設定的環境變數就能把 gVisor 換成 runc，而派送端會接受** | `SKILLHUB_SANDBOX_RUNTIME` 未設或拼錯 ⇒ Docker 預設 runtime（runc）；`sandboxd` **誠實地**把自己宣告成 `isolation: "container"`（註解逐字寫「RUN-005 dispatches on this answer」），但 `schedule.go` 的 `Match` 只拒 `""` 與 `"process"`，`"container"` 一路通過。連帶失效的還有兩道：`refuseDevSettings` 在非 `runsc` 時直接 `return nil`，於是**同一個誤設也解除了 digest 釘選與 caller 自選 entrypoint 的禁令**。唯一訊號是一行啟動 log 與一個沒有東西 gate 的 `isolation_level` 字串。<br>**同日修法**：把預設倒過來——弱隔離一律拒絕，除非部署已宣告自己是離線開發環境，訊號重用既有的 `DEV_LOGIN`（不新增變數：想接受共用核心的沙箱就得同時打開離線登入 provider，沒有哪個生產部署會不小心把這兩件事一起做對）。兩次突變各自見紅。整合測試 harness 同批補上 `t.Setenv("DEV_LOGIN", "1")`——它用的 fake provider 宣告的正是 `container` |
| **丙-71（新入列 2026-08-25，入列即結案）** | **Trace 的擷取憑證是網址的最後一段，而全 repo 沒有一個地方把它當秘密** | 三處後果同一個根因：①沙箱把 `Push` 的原始傳輸錯誤寫進 log，而 Go 的 `*url.Error` 會把整條網址嵌進訊息，於是控制平面重啟或 DNS 抖動就把一枚有效 2 小時、可對該 Run 的時間軸寫入任意事件的 token 印在節點 log 上；②`secretsOf` 只收 `ModelGateway.VirtualKey` 與 `ObjectGrants[].URL` **兩個**，而 driver 注入工作負載環境的是**三個**（`SKILLHUB_TRACE_URL`）——不受信任的 Skill 跑一次 `env` 就把它帶進 `AgentOutput`；③`service.go` 那個註解寫著「provider 錯誤訊息常引用帶簽章的網址」的 masker，`Known` 是空的，而它五條 pattern 沒有一條是位置性的。<br>**同日修法**：①②以 token 註冊進 `secretsOf` ＋ log 改走 `mask()`，兩次突變各自見紅；③查明該處**沒有可用的值在 scope 內**（`RecordOrchestratorEvent` 的簽章裡沒有 per-run 憑證，也無法由 `runID` 反推），補 plumbing 要動四個 call site，**改成誠實的註解而不是假裝有防護**。同批把 trace masker 補齊到套件掃描器的覆蓋（AWS／GitHub／Slack／Google），並加一支**讀掃描器原始碼**的漂移測試，讓下次只補一邊時會紅 |
| **丙-72（新入列 2026-08-25，入列即結案）** | **429 是這份契約裡唯一該被重試的拒絕，而它一個字都沒被寫下來** | `public.yaml` 全檔沒有任何 `"429"`，而 `publicSearchSkills`、`uploadSkillPackage`、`importSkillFromURL`、`generateSkill` 四條路由都被 `limited()` 包著、都會回 429 ＋ `Retry-After`，而且**預設是開的**（只有 `RATE_LIMIT=off` 關得掉）。生成的 client 因此對「什麼時候可以再來」這唯一一個答案沒有分支——受害最廣的是 `/api/skills/search`，每一位封測訪客都會碰到。同批補上 `getRunTrace` 的 400 與 `downloadArtifactContent` 的 503（後者的兩個同儲存兄弟一直都有寫，只有真正吐位元組的那條沒有，於是只讀契約的 client 會把「儲存壞了」講成「你的套件不見了」）。<br>**順帶修掉一個真的行為不一致**：`after` 原本只在 `advanced` 分支裡驗，於是 `?mode=general&after=-5` 回 200 而 `?mode=advanced&after=-5` 回 400——**同一個端點對同一種 schema 違反給兩個答案**，正是 08-24 那批從分頁端點拿掉的形狀。驗證已提到 mode 判斷之前，測試對三種 mode × 兩種壞值全數斷言；突變（把驗證塞回 advanced 分支）讓四格見紅 |
| ~~**丙-73（新入列 2026-08-25）**~~ **已結案 2026-08-25** | **套件物件沒有任何人回收：Skill 清掉之後，它的 zip 還在** | 內容定址的 `package_object_key` 與每一個 Fork 共用，所以刪除路徑一律不碰它——`Delete` 的註解這樣寫、帳號清除也這樣做、同日新增的寬限期清除也這樣做，三處都是對的。**問題是沒有任何一處在後面接手**：`objreconcile` 只掃有 `expires_at` 的 artifacts 與 datasets（`worker/main.go` 只註冊了 `packagingCandidates` 與 `datasetCandidates`），全 repo grep 不到任何回收 `package_object_key` 的路徑。於是一個 Skill 的資料列被清掉之後，它最後一個版本指過的位元組**永遠留在物件儲存裡**——沒有 refcount、沒有 GC、沒有到期欄位。<br>**這不只是儲存成本**：同意書談的是「你的內容會不會消失」，而位元組沒有消失，只是變成無法從任何資料列抵達。<br>**為什麼不順手做**：正確的收法是對 content hash 做參照計數，而**做錯會刪掉別人 Fork 還在讀的位元組**——那是比留著更壞的失敗。這是既有缺口（帳號刪除從第一天就有），不是同日新增的清除造成的，但同日的清除讓它第一次變得明顯。<br>**✅ 2026-08-25 結案：`0039` 的佇列＋兩條 purge 裡的 `enqueued` CTE＋`maintenance collect-objects`。**<br>**producer 的落點是這一列唯一的難處，值得記**：兩條 purge 都是單一 statement，`purgeable` 在同一句裡 select 又 delete，沒有 `RETURNING`，**沒有任何現成 query 列得出它們各自 scope 內的 skill id**（`ListSkills` 帶 `deleted_at IS NULL`，對帳號刪除來說是**危險方向的子集**——已軟刪的 skill 會被漏掉、那些 key 永遠不進佇列）。在 Go 裡重算 `purgeable` 就是在 DELETE 自己的述詞旁邊放第二份定義，**兩者一漂移就靜默漏 key**。所以 enqueue 是掛在同一個 `purgeable` 上的 CTE：同交易、同述詞、零第二定義，鐵律 9 自動成立。<br>**順序是反過來的，而且必須反**：帳號清除是「先移物件、後刪列」（`purgeAccount` 自己的註解寫著為什麼）；套件物件做不到，因為它們是內容定址、與每一個 fork 共用，**能不能移只有在列刪掉之後才知道**。列先物件後就開了一個 crash 視窗，這張表就是關那個視窗的東西。而**收的時機在 sweep 當下判斷、不在 enqueue 當下**——內容定址的 key 會**回來**（重匯相同位元組產生同一個 key），enqueue 時做的決定會刪掉一個當下正在被讀的物件。<br>**量的是佇列深度而不是失敗計數**：失敗計數在「健康」與「根本沒在跑」兩種狀態下都讀 0，而這整個機制存在的原因就是上一個沒人寫的 job。<br>**仍未回收、且不該由這支 sweep 回收的**：`0039` 之前就孤兒的物件沒有佇列列，資料庫裡沒有任何一列知道那些 key 存在——唯一路徑是拿 bucket 清單對 `skill_versions.package_object_key` 做差集後手動 `INSERT`。測試裡的 `enqueueObjectKeys` 保留著，它測的就是「sweep 尊重不是自己建立的佇列列」這條 operator 救援路徑。<br>**另一個已知界**：`cmd/maintenance` 是短命 process，`metrics.Serve` 只在 `cmd/api` 與 `cmd/worker`，所以那個 gauge **目前不會被 scrape**，operator 實際看的是 log 那一行。 |
| ~~**丙-74（新入列 2026-08-26）**~~ **已結案 2026-08-27：收斂允收句的範圍** | **模型 Virtual Key 進的是容器的啟動參數，而 `02:SEC-005` 第 3 條寫著它不該在那裡** | `dockerdrv` 的 `env()` 把 Virtual Key 以 `ANTHROPIC_AUTH_TOKEN` 放進 `container.Config.Env`，**因為 Claude Agent SDK 原生讀這個變數**（PDM-003）——那個選擇本身是對的，程式註解也誠實寫著「它對工作負載可見，所以預算與 TTL 才是真正的控制」。**沒有被寫下來的是另一半**：環境變數是容器的**啟動參數**，Docker daemon 會把它保存進該容器的設定，直到容器被移除。**這是實測不是推論**：`docker run -e … ; docker stop ; docker inspect` 在**程序已結束**之後仍原樣讀得到那個值。<br>**補償控制是真的**（每 Run 專屬、TTL、`max_budget`、teardown 撤銷、容器移除），**但 `02:SEC-005` 第 3 條寫的是「不出現在啟動參數的持久記錄中」**，補償控制不是那句話。**兩條路，而選哪一條是決定不是實作**：(a) 接受補償控制並改寫那一條允收，說清楚它對模型憑證的例外與理由；(b) 改成 tmpfs 檔案 ＋ entrypoint 交接（daemon 的設定裡就不會有它），代價是動 runtime image 的入口。<br>**先記不做**：它不是「有人忘了」，是一條允收與一個已經寫下理由的實作選擇正面衝突，而在一個長工作階段的尾聲改一條安全允收，正是本專案反覆立規則在防的事。<br>**✅ 2026-08-27 取 (a)，經負責人裁定。** `02:SEC-005` 第 3 條補上範圍句：**「持久」指超出該 Run 生命週期而仍然存在**——容器在 teardown 被移除、Virtual Key 在同一次 teardown 被撤銷，**撤銷之後那個值已經不是憑證**；暴露窗正好是 Run 的生命期，而工作負載在那段時間本來就讀得到它（ADR-017 已明確接受）。`03:SEC-005` 同日勾選。<br>**收斂不是開例外，這一句寫進 `02` 了**：本條仍然禁止任何 Run 結束後留著的形態——映像層、節點持久磁碟、Log／Trace、或**一個沒有被移除的容器**。**最後一種才是生產上真正要靠的東西**，而它由 X-01～X-04 承擔，**不是由這句話承擔**——所以這次收斂沒有讓任何一個實際的防護少一層，它只是把責任放回真正在守的那個機制上。<br>**(b) 沒有被否決，只是沒有被選**：tmpfs 檔案 ＋ entrypoint 交接可以讓 daemon 側完全不留紀錄，搬運機制本來就有（`transfer.go` 已把位元組寫進容器），但它要動 runtime image 入口、重跑發佈流水線並重釘 digest，**屬部署期硬化項**。要做的時候這一列就是它的出處 | `03` SEC-005、`02:SEC-005` 第 3 條、威脅模型 Q11 |
| ~~**丙-75（新入列 2026-08-26）**~~ **已結案 2026-08-26（入列即結案）** | **上游內容的「改寫」與「License 變更」偵測不到，因為偵測只發一次 `HEAD`** | `02:SEC-003`／`CONTENT-009` 要求的偵測方式逐字是「**重抓並與保存的內容雜湊比對**」，而 `skill/admission/sources.go` 的 `CheckSources` 只呼叫 `Probe`，那是一次 `http.MethodHead`——**它回答的是「這個網址還在嗎」，不是「它還是同一份東西嗎」**。所以上游**刪除**偵測得到，**改寫與 License 變更偵測不到**，而後兩者正是 `SEC-007` 存在的理由。<br>**要用的東西已經在手上**：`LineageSource.ContentHash` 就存在同一個結構裡，每一筆匯入都寫了，**沒有任何一行程式讀它去比對**。這是「量到的東西沒有落地的地方」的第四個實例。<br>**一次修三個工作項**：`CONTENT-009` 第 1 條、`INGEST-010`（兩者依 `02:533` 共用同一流程）、`SEC-007` 第 2 條。**不等任何人簽名**——`02:536` 逐字說重抓頻率未定值不擋，「本需求只要求流程存在與可追溯」。<br>**✅ 同日落地。** `0041` 加 `content_changed_at`（**邊緣標記，不是狀態**）；`CheckSources` 在 `Probe` 成功之後重抓並以 `sha256` 比對 `content_hash`，不符即寫 `import_source.changed`，與欄位同交易（鐵律 9）。**三個設計點各自對應一個會安靜出錯的形態**：①**`content_hash` 不覆寫**——它是本工作區實際持有的快照的雜湊，覆寫等於讓那一列描述沒有人手上的位元組（鐵律 4）；所以要一個邊緣標記，否則之後每一輪都再寫一筆，把該看的那一筆埋掉（`unavailable_since` 已經解決過同一個問題）。②**抓不到不等於變了**——重抓失敗只記 log 不動那一列，因為這個標記寫下去不會被清掉，而「抓不到」正是排程重抓最常遇到的狀況（限流、部分中斷）。③**整批一起變是產生器不是內容**——同一輪裡每一筆都對不上時記一行 warning：GitHub 改過 repo zip 的產生方式，那天全部釘選雜湊會同時失配而上游一個字都沒動。<br>**三個突變都紅**：拿掉偵測、把抓取失敗當成變更、拿掉邊緣跳過。**仍不足以勾任何一項**：`CONTENT-009` 還缺 `02:534` 的白名單異動記錄，`INGEST-010` 還缺跨工作區下架，`SEC-007` 還等法務判定內容 | `03` CONTENT-009／INGEST-010／SEC-007 |
| 丙-43 | **第五個 `blocked_reason` 有伺服器端、有契約、有 generated client，就是沒有前端那一句話** | `file_removed_by_packager` 於 2026-08-22 的五維對帳批加進 `public.yaml` 與 `packages/api-client-ts`，**但 `apps/web/src/api/packaging.ts` 手寫的 union 仍是四個值**，於是 `PACKAGING_BLOCKED_LABEL[reason]` 取到 `undefined`，畫面印出「不能打包：」後面接空白——**設計系統 §2.1 禁止的空白，出現在讀者最需要一句話的那個位置**。`PACKAGING_BLOCKED_LABEL` 上方的註解自己寫著「兩份對照表會漂」,而漂的正是它。<br>**2026-08-22（下載可達性查證批）同批發現同批結案。** 補上第五個值與它的句子,並加一支**對著 generated enum 逐值檢查**的測試(不是對著這裡手寫的清單——手寫清單就是同一種漂移換一個檔案)。TS 的 `Record<union, string>` 只能綁住 union 與表,**綁不住 union 與契約**,那一段只有這支測試看得到 | 設計系統 §2.1、§4.4；ADR-044 決策 6 |
| 丙-39 | **未知 frontmatter 欄位升級為 error 的殺傷範圍沒有量過** | [ADR-044](../adr/ADR-044-agent-skills-specification-conformance.md) 決策 4 取了選項 (c)（維持 warning，但把後果寫進 finding 訊息與 `INSTALL.md`），**並把升級成 error 的解鎖條件寫死成一次量測**：對 45 個 pin commit 套件跑一次驗證,數出帶未知欄位的比例與欄位名分布。<br>**這是一個查得出來的事實,不是一個要拍板的偏好**——所以它是丙不是乙。量完若殺傷範圍為零,升級不需要再開一次 ADR。<br>**擋住它的只有網路**:`tools/qa/skillpkg-corpus/generate.py` 要抓 3 個 pin commit repo 壓縮檔(`QA002_CORPUS` 未設即跳過,形式比照 `SKILLHUB_TEST_DATABASE_URL`)。<br>**為什麼重要**:規格的參考驗證器與至少一個主要 client 的上傳路徑對未知欄位都是**硬錯**,所以現況是**我們發出去的套件,使用者拿去上傳時才會炸**<br>**2026-08-22（同日補批）結案。** 量了兩次:45 個選定 seed 套件 **0** 項帶規格六欄之外的欄位;再把 11 個 pin commit repo 裡的**全部** 106 個 SKILL.md 重打包一次,一樣 **0**。**第二次才是回答問題的那一次**——決策 4 選項 (a) 擔心的是「為 Claude Code 寫的 Skill 帶著 `argument-hint`／`when_to_use`」,而那 45 個是我們自己挑的、按定義一個都不含那個族群。順帶查出**那兩個欄位名根本不是 Skill 的 frontmatter,是 slash command 的**——(a) 那格的成本估計建立在錯的前提上,而這件事只有量了才知道。<br>殺傷範圍為零 ⇒ 依決策 4 自己寫的解鎖條件,**未知欄位升級為 error**,訊息同時說怎麼修;`INSTALL.md` 從「這裡是警告、別處是硬錯」改成「你下載的套件不會帶著這種欄位」。量測留在 repo(`TestSpecFrontmatterCensus`,`SEED_CORPUS` 閘控)且**刻意不對分布做斷言**——會因數字變動而失敗的普查是一條政策,政策是 ADR 的。<br>**留下一個它答不到的邊界**:106 個全部來自 pin commit repo,使用者**自己寫**的 Skill 不在樣本裡。封測後要回頭數真實上傳的拒絕率,見 ADR-044「後續」末條。<br>**同批順手修掉一個沉默故障**:`qa002_corpus_test.go` 的期望檔相對路徑少了一層——2026-08-20 的邊界重排把 package 從 `internal/ingest` 移到 `internal/skill/admission`(深一層),路徑沒跟著改。**它是 env-gated 的,所以四層往上找不到檔案這件事沒有任何人會看見**,直到本批真的把語料生出來跑了一次。修好後 21 個變體全過(含本批改判為 blocking 的未知欄位那一個) | ADR-044 決策 4；`02:SKILL-002`；`PACK-007` 的對外措辭 |
| 丙-40 | **規格漂移沒有偵測** | Agent Skills 規格**沒有版本號、沒有 tag、沒有 release**（[`contracts/spec/SOURCE.json`](../../contracts/spec/SOURCE.json)），所以釘的是 commit SHA ＋ 規格檔的 blob SHA。**上游改一行,我們的釘選就過期,而沒有任何東西會告訴我們。**<br>**補法**:CI 比對 `https://raw.githubusercontent.com/agentskills/agentskills/<commit>/docs/specification.mdx` 的 hash。<br>**但先有一個決定要下**（ADR-044 待決策第 4 條）:失敗時是 fail 還是提醒?**把我們的 CI 綁在別人的 repo main 上是一個要想清楚的耦合**——所以本項的第一步是那個決定,不是那段 YAML<br>**2026-08-22（同日補批）結案。** 那個「先有一個決定」不用新拍——repo 裡已經有同型答案:`gvisor-baseline.yml`。**不進 merge gate**(gate 判的是我們的程式碼能不能合併;別人的 repo 今早改了一行不是關於我們程式碼的事實,接進 gate 等於把合併的鑰匙交給第三方 main),**但每週排程、漂移開 issue、該次 run 失敗**(看不見比耦合更糟),**連不上是 exit 1 不是綠燈**。唯一會在 PR 擋人的例外:改到 `SOURCE.json` 或檢查器本身的 PR,那時「釘選對不對」確實是我們的事實。<br>實作 `tools/contracts/check_spec_drift.py`＋`.github/workflows/spec-drift.yml`,分得出**釘選本身壞了**(exit 3)與**上游漂移**(exit 2)。首次執行兩者皆無。<br>**這是通知不是保護**:規格漂移了,已經發出去的套件仍然是照舊版驗的 | ADR-044 決策 1 與待決策 4 |
| 丙-41 | **ADR-043 的兩個欄位到不了畫面** | 落地的是伺服器側（`trial/improvement/` 的 `verify()`、`Match` 三態、`ReattributedFrom`）,而 **`apps/web/src/api/evaluation.ts` 與 `RunEvaluation.tsx` 兩邊都還不知道這兩個欄位存在**。生成的 client（`packages/api-client-ts`）已經帶著它們,所以剩下的只有 adapter ＋ 渲染。<br>**ADR-043 §影響 自己寫明**:一個永遠到不了畫面的稽核欄位不是稽核欄位。**G8 那兩筆當時如果有這個欄位,看報告的人一眼就知道發生了什麼**——而那正是它存在的理由。<br>**同時還有一個契約缺口**:ADR-043 §影響 要求 `artifact` 引用以**獨立欄位**附上「僅證明檔案存在」,而本批只加了 `match` 與 `reattributed_from`,所以那句話目前仍在 `excerpt` 裡、由 `match: not_found` 承載語意<br>**2026-08-22（同日補批）結案。** adapter＋渲染都補了:`EvidenceMatch` 型別、`reattributed_from`,以及**四個 match 狀態各自一句話**。措辭**刻意留在 client**,而這不是漏掉設計系統 §4.4——評估報告是不可變且事後很久才讀的,寫進去的 label 會凍結在寫入當下,之後每次改字都只對新報告生效,同一句話會在一份清單裡以兩種樣子出現。enum 是事實(存),句子是呈現(不存)。<br>**`undefined` 自成一格,不併進「已回驗」**:欄位存在之前寫的報告沒有答案,把沉默渲染成通過正是 ADR-043 要治的病。<br>**契約缺口同批補上,但形狀與 ADR-043 §影響 寫的不同**:§影響 要的是「獨立欄位」,落地的是 **`match` 的第四個值 `not_checked`**。理由是兩個欄位會把同一件事編碼兩次並讓它們有機會互相矛盾;而真正的病灶是 artifact 引用被蓋成 `not_found`——**`not_found` 是「我找遍所有可回驗來源,沒有」,接近一句指控;`not_checked` 是「我從來沒打開過那個檔」,是一句關於我們自己的話**。把後者記成前者,會讓報告聽起來對一件它沒看過的事很有把握。`not_found` 保留在 enum 裡(報告不可變,舊報告帶著它)。<br>同批把 `hasVerifiedQuote` 從 `!= not_found` 改成白名單——原寫法會把**欄位存在之前的空字串當成已回驗** | ADR-043 §影響；設計系統 §4.4 |
| 丙-42 | **下載紀錄印的是一串 UUID,不是「第幾版」,也不說有沒有更新版** | 版本模型本身是硬的（版號單調遞增、`content_hash` 唯一索引、`0005_immutability.sql` 的 trigger 全列凍結、manifest 帶著 `skill_version_id`＋`version_number`＋`content_hash`）。**缺的全部在揭露側**:`ListDownloadArtifacts` 的 SQL 已經 join 了 `skill_versions` 卻只取 `sk.skill_id`;`Artifact` 形狀沒有 `version_number`;`withServeState()` 的四個狀態裡沒有第五個叫「已被新版取代」;下載頁只印 `skill_version_id` 這串 UUID。<br>**補法是一個 join ＋ 一個伺服器給措辭的欄位**,資料一項都不缺。<br>**不含上游變更偵測**:那是 `CONTENT-009`／`INGEST-010`,兩項都沒勾,而且**它的重抓頻率與「失效」判準在需求文件裡就寫著「待決策」**——那一半在等人,這一半不在<br>**2026-08-22（同日補批）結案。** 三條 query 各加一個 `sv.version_number` 與一個 `max(version_number)` 子查詢(建立那條寫在 `RETURNING` 裡,**同一個 statement 讀出來**——分兩次就是兩個時間點,而這個數字唯一不能做的事就是跟它旁邊那一列不一致)。<br>**沒有做成第五個 `serve_state`,而是另一條軸 `version_state`**,理由:被新版取代**完全不影響拿不拿得到位元組**,而且下載「你當初打包的那一版」是正當需求。`serve_state` 答「拿不拿得到」,`version_state` 答「是不是最新的內容」;併成一個會讓一列同時 superseded 又 expired 時被迫二選一(設計系統 §2.12 的第三軸)。措辭與**兩個數字**都由伺服器給——「已被取代」不說被誰取代,只會把讀者趕去另一頁查自己落後多少。<br>**不含上游變更偵測**(`CONTENT-009`／`INGEST-010`),範圍與原列一致 | `02:WS-002` 第 1 條的「版本」;`03:147` 自己留的保留意見 |

**不做什麼（同批裁定，免得下一個人重問）**：**不為此引入 LangGraph 或任何 agent framework。** [m3/evaluation-design.md §2.3](mvp/m3/evaluation-design.md) 的條件是「改善建議生成真的需要多步時」，而多步的判定條件是 `skillpkg.Validate`——**驗證器在 Go 的 Shared Kernel，Python 沒有也不該有**（複製一份就是兩個真相）。所以「產提案 → 打補丁 → 驗證 → 沒過帶錯誤碼重來」這個迴圈若要做，**它寫在 Go**：Go 已經有 zip、有驗證器、有重試政策（鐵律 6），呼叫 `apps/llm` N 次，Python 端一行都不用改。而 Go 裡的那個迴圈是一個 `for`。丙-38 的量測正是決定「要不要做那個迴圈」的輸入。

## 移交 M4(打包與封測)

M4 的範圍見 `01` 與 `03` §15／§17／§18。以下只列**M3 已經可以看見的接點**,不代為開工。

> **2026-08-18 更新（M4 對帳批）：六條逐項裁定，四條關閉、兩條轉新殘項。** 依維護規則就地標，不刪列。逐項證據見 [m4/audit.md](mvp/m4/audit.md)。

| # | 接點 | 內容 | **2026-08-18 裁定** |
| --- | --- | --- | --- |
| M4-1 | **`PACK-002`「打包前重新執行規格驗證」的做法已經有前例** | `EVAL-010` 套用建議時對修補後的位元組重跑 `skillpkg.Validate`,任一阻擋級 finding 即整批拒絕且不建版本(`apply.go` 的 `validatePatched()`)——那正是 `PACK-002` 的精神,只是提前發生。M4 應**重用同一條路徑**而不是另寫一套判準:兩套會在「什麼算阻擋級」上漂移 | ✅ **關閉**。`packaging.build()` 走的就是那兩支呼叫（`ingest.PackageFS` → `skillpkg.Validate`），severity 全取 `skillpkg`（`toManifestFindings` 只搬不判），**打包側沒有第二個「阻擋級」的定義存在**；契約面同理（`PackageValidation` 組合既有的 `CategorizedFindings` 而不重述三個嚴重度）。preview 與 create 由同一個 `Plan` 回答，所以「預覽說可以、建置說不行」在結構上不可能。`PACK-002` 已勾 |
| M4-2 | **`PACK-003`「保留衍生關係」對建議生成的版本已有答案** | 由建議建出的版本,其溯源是 `evaluation_suggestions.applied_skill_version_id` 的反向查詢(「哪幾條建議造出它、出自哪一份評估」)。打包時要保留的是**這個關係**,不是一個 `derived_from_evaluation_id` 欄位——後者不存在,理由見 [m3/evaluation-design.md §5.3](mvp/m3/evaluation-design.md) 的 2026-08-17 更正 | ✅ **關閉**。`manifest.go` 的 `originOf` 三形態（`import`／`fork`／`improvement`）＋ `walkLineage`／`rootSourceOf`（上限 32 跳，**追到原始來源不是最近一跳**，走不到記 `"unavailable"` 而非省略），三者皆無即**回 error 拒絕打包**。`improvement` 形態記的是「哪幾條建議造出它、出自哪一份評估」而**不帶任何建議 prose**（測試 `TestTheImprovementOriginCarriesNoSuggestionProse`）。**`PACK-003` 仍不勾，但不是因為這一條**——不勾的是「作者」與「授權檔隨包」的斷言，見丙-17 |
| M4-3 | **`PACK-005` 與丙-12 是同一件事的兩半** | 「可散布的 Test Case 與範例資料」這條準則已經預設 Test Case 可以被搬運;丙-12 說的是策展側目前沒有可種入的形式。兩者一起處理才划算 | ✅ **關閉，而且兩半在同一次驗證裡接上了**。共用同一份 `portable-test-case.schema.json`（一份 schema 兩個方向）：`seed_testcases.py` 種進去、`packaging` 匯出來。實測見 [m4/README §14.2](mvp/m4/README.md)——種入的 Test Case 被 `packaging/preview` 列為 `included`，下載的位元組裡確有 `case.json`（帶 `rubric_version` 與 4 個 item）與兩個 Dataset。**丙-12 已結案、`PACK-005` 已勾**。**轉記錄一項**：「不含使用者上傳位元組」以 `workspaces.is_catalog` 當代理，是一條**組織性**判準不是技術性判準（已寫進 `03` PACK-005 行內） |
| M4-4 | **`PACK-004`「排除 Secrets 與 Run 資料」要涵蓋評估產物** | 建議的 `problem`／`expected_impact`／證據 `excerpt` 全部是模型寫的或從 trace 抄的文字,而 rubric 是使用者寫的。打包時這些**都不該進套件**——它們是 Run 資料不是 Skill 內容。目前沒有任何規則說這件事 | ✅ **關閉**。規則寫成了**白名單**而不是禁止清單：只有該 Skill Version 套件位元組裡的檔案 ＋ 平台自產三樣能進包，所以評估 prose、證據引文、rubric、Trace、Run artifact **不進包不是因為有人擋，是因為它們不在來源集合裡**。契約面另加一道（三份 schema 的 `additionalProperties` 全域封閉）。符號連結那一半由丙-15 一併關閉（**揭露在匯入端、排除在匯出端**）。`PACK-004` 已勾 |
| M4-5 | **封測(`03` §17)會第一次讓非團隊成員讀到評估報告** | ADR-025 的待決策(兩列狀態的實際文案與版面,由 `DESIGN-010`／`011` 承接)在那之前應該有答案,否則「執行完成 ＋ 未符合」這個組合會由每個使用者自己解讀。另:`EVAL-013` 報告 §2 的三個空格(主觀判定、注入抵抗、證據殘缺)在封測前補上一格算一格——**注入抵抗那一格的樣本可以合成,不需要真 Run**,是三格裡最便宜的 | ⚠️ **三件事：兩件關閉、一件轉殘項。** ①ADR-025 的兩列狀態**已由 `RunEvaluation.tsx`／`RunCompare.tsx` 回答**並就地回填該 ADR（M3 已處理）；丙-10 的呈現面亦於 2026-08-18 結案。②**注入抵抗那一格已測**——13 個合成樣本、27 條判定、**讓步 0／27**，閘道實付 $0.05688（[m3/report-judge-regression.md §12](mvp/m3/report-judge-regression.md)）。③**轉殘項：乙-13（G7＋G8）仍待拍板**，而它正是「封測者會讀到什麼」的核心——目前平台分不出「歸錯類」與「編的」，A 輪 9 筆 `passed` 有 6 筆只靠一種從未被回驗的引用。**這一條擋 `QA-006` 與 `RELEASE-007`，且擋封測不擋打包** |
| M4-6 | **甲類四項仍是外部使用者開放的硬前提** | 依 ADR-015 定案紀錄,**`SEC-009`／`SBX-010` 未通過不得開放外部使用者提交 Skill 執行**。封測若涉及外部人員實際跑 Run,甲-1～甲-4 就從「部署期驗收」變成封測的阻擋項,不再是平行工作 | ⚠️ **轉殘項：乙-14 仍待拍板。** M4 計畫已提出建議裁定（[m4/README.md §4](mvp/m4/README.md)：四項全部到期），替代路徑「只讀不跑的封測」的兩條代價也寫清楚了（北極星指標量不到、`02` §7 DoD 第一條走不完）。**但那是建議不是裁定**——它決定的是封測日期取決於誰，所以留給負責人。逐項的「誰做什麼驗什麼」已備妥於 [m4/release-checklist.md §2.1](mvp/m4/release-checklist.md) |

## 移交下一階段（封測期與 MVP 之後）

**M4 是 MVP 的最後一個里程碑，所以這一節不是「移交 M5」而是「移交封測期與其後」。** 只列**M4 已經可以看見的接點**，不代為開工。

| # | 接點 | 內容 |
| --- | --- | --- |
| N-1 | **封測會產生第一批「不是團隊寫的」內容與行為資料** | 分析事件（`analytics_events`）、質性回饋（`feedback_reports`）、外部使用者上傳的 Dataset 與建立的 Skill 版本。**三者的保存與刪除語意已定但未追認**（PDM-006 ＋ ADR-029 決策 5），而**帳號刪除的去識別化保留**（`purge.go` 的 `DetachWorkspaceAnalytics`／`DetachWorkspaceFeedback`）是第一次會被真人行使的承諾。同意書 §6 的那張表就是它的對外形式 |
| N-2 | **`skills.redistribution` 會第一次遇到「不是策展的 Skill」** | 目前的 90 列全部來自策展目錄與其 fork，判準從 `license_expression` 推導。**使用者自己匯入的 Skill 只會有那一個欄位**，所以 `unknown`（＝擋）會是常態，而使用者看到的是「你的 Skill 不能下載」。**這不是缺陷，是預設值的正確方向**（ADR-021 §5.3 的誤判錯在放行方向），但它會產生一個產品問題：**沒有任何路徑讓使用者或 operator 把它改成 `allowed`**（丙-16 (b)）。第一個需求訊號會從 `POST /feedback` 進來<br>**2026-08-23 更新：訊號沒有等到封測就來了,而且是從裡面來的。** 一次查證（丙-44）確認這條路在實測上是 422,[ADR-045](../adr/ADR-045-self-supplied-content-is-not-redistribution.md) 已處理**使用者自己帶進來的**那一半（`self_supplied`）。**本列不因此關閉**:它記的是「不是策展的 Skill 會撞到這個欄位」,而現在撞到的族群換人了——**使用者上傳的別人的內容**（他自己拿得回去,但平台不會替他判定能不能散布給第三人）與**日後的發佈到目錄路徑**（必須面對一個不是 `allowed` 的值）。 |
| N-3 | **`BETA-005` 的範圍複審是 MVP 之後所有優先級的唯一輸入** | `01` §7.3 明列不含公開 Beta、Marketplace、付費；`Local Runner` 與遠端 MCP 已移出首發並**依需求訊號啟動**（ADR-006）。封測的 `need_signal` 那一類回饋就是那個訊號的第一批樣本 |
| N-4 | **漏斗第七段（首次使用後再回來）在 14 天內量不到** | [m4/pdm-009-beta-proposal.md §4.2](mvp/m4/pdm-009-beta-proposal.md) 已裁定它**不列入判準但列入報告**，結論只能當下界讀。要量真正的留存需要一次 30 天以上的觀察窗，**那屬公開 Beta 而非封測** |
| N-5 | **`CONTENT-011` 的增強重跑在 D 日之後到期** | 負責人已裁定 D 日前不執行（凍結標的不動）。**D 日一過、閘門結案，凍結解除，這一項就該排入**——它要把被拒收依賴造成的能力缺口寫進「限制」欄，對象三筆已具名。它屬內容批不屬打包批 |
| N-6 | **丙-13（歷史 M2 Run 的 `artifacts` 表全空）仍開著，而順序是硬的** | 先回填 `artifacts`，再評估。反過來會得到 43 份帶著一句錯誤指控的**不可變**評估。M4 不做它（`m4/README §2.2`），警告維持開著；**今天不會自動發生**（評估只由終態轉移排入，M2 的 73 筆早已終態） |
| N-7 | **Platform 的 DDD 邊界治理**（負責人 2026-08-19 裁示；**2026-08-22 邊界收斂與拓撲遷移完成**） | ADR-032 已 Accepted 並全面落地：stable Boundary ID、depguard、query ownership、raw-SQL tripwire 與產品價值流路徑均已收斂；`allow:`／`read_allow:` 均為空，**裸 SQL 只剩九個具名技術豁免**（raw-SQL tripwire 的白名單，與 `allow:` 同一個性質：**是存量不是擴充點**），`shared/skillpkg` 是唯一 Shared Kernel，Foundation 與 Entrypoint 依 ADR-040 收納。凍結摘要見 [M4 DDD 邊界收斂報告](mvp/m4/report-platform-ddd-boundary-convergence-2026-08-19.md)；現行規則以 ADR-032～035／038／040 為準。 |
| N-8 | **技術債修復後仍待決策／部署的五項** | `DEPLOY-IAC-001`、`RUNTIME-PYTHON-001`、`LLM-EVAL-007`、`LLM-RES-001`（partial）、`SUPPLY-RUNTIME-LOCK-001`。部署項由部署負責人依 [M4 release checklist §2.8](mvp/m4/release-checklist.md#28-仍待定值或部署驗證的技術債) 執行並留下 IaC／image／負載或 clean-build evidence；`LLM-EVAL-007` 由負責人決定 Judge／Suggest usage 與 cost 的唯一事實來源，再以 OpenAPI／Go／Python contract test 證明。不得把原始盤點裡的 `fixed` 項重新列為 active。凍結摘要與逐項落點見 [技術債修復報告](mvp/m4/report-technical-debt-remediation-2026-08-19.md)。 |
