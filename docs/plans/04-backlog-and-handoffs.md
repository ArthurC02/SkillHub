# 待辦與移交：目前還缺什麼、誰在等誰

## 這份文件是什麼

**活文件**，與 [01 目標與計畫](01-goals-and-plan.md)／[02 規格與允收準則](02-specifications-and-acceptance-criteria.md)／[03 工作項目](03-work-items.md) 並列，是「**目前還缺什麼、誰在等誰**」的**唯一入口**。開工前先看這份。

與其他文件的分工：`03` 記工作項本身與勾選狀態，**本文件記擋住工作項的東西**——環境上結構性做不到的、等人拍板才動得了的，以及沒有任何工作項承接的。它專收「**已定值但沒有強制**」與「**已量到但沒查根因**」那一類洞，因為那正是逐項對帳最容易漏掉的一種。

里程碑目錄（`mvp/mX/`）記的是**某一時點的帳**，里程碑完結即凍結；**本文件不凍結，隨時更新**。

維護方式：

- **一項解除就把那一列刪掉**，並把解除的理由與證據寫進 commit message。歷史在 git，不留在本文件裡——**本文件只回答「現在還缺什麼」**。
- 解除時若留下了新的缺口，那個缺口**另立新列**，不寫成舊列的續段。
- 新殘項直接加列，同時更新該類的計數與 `<!-- open: … -->`。
- 里程碑交替時把新里程碑的殘項**併進本文件**，不另開一份——兩份清單一定會漂移。
- 逐項證據留在各里程碑的對帳文件（`mvp/mX/*-work-items-audit.md`），本文件只做入口與現況。

## 殘項清單（三類）

分類原則：**甲**只能在真實部署環境驗收，本機結構性做不到；**乙**缺的是一個決策不是一段程式，寫程式之前得先有人拍板；**丙**是實作或資料工作，不是等人拍板的東西。

**這三個數字有一台機器在對帳**（`devctl automation-check` 的 `backlog-tally`）。每一格末尾的 `<!-- open: … -->` 是該類宣稱還開著的列，機器比對三件事：**數字等於清單長度**、**清單上的每一列都真的存在**、**沒有一列被列為未結卻在自己的狀態欄寫著已結案**。它**故意不檢查**一個沒在清單上的列是不是真的結了——那需要逐列的狀態欄，而本文件的解除規則是刪列，所以「不在檔案裡」就是結案的形式。

這是全 repo 唯一的殘項總數來源；[`01` §10](01-goals-and-plan.md) 與 `AGENTS.md` 都只指過來、不複製數字。

| 類 | 未結案 | 內容 |
| --- | --- | --- |
| 甲 | **5** | 部署期驗收：沙箱逃逸與資源測項、生產出口強制層、映像掃描門檻的節點查證、節點 IaC 與上線最低配。**全部在等同一件事——一台真實的 Linux 節點**，不因程式面收斂而放寬一分 <!-- open: 1,2,3,4,5 --> |
| 乙 | **5** | 待負責人決策：受測者同意書、跨工作區證據彙總的隱私前置、清除路徑的資料庫權限、策展範例資料的交付路徑、平台增強產物要不要進交付物 <!-- open: 16,19,27,28,30 --> |
| 丙 | **14** | 實作與資料工作：Judge 準確度重量、意圖分析與查詢改寫、分析事件的兩個缺口、demo 的能力服務執行期、互動創作後續驗收、搜尋 F1 的生產重跑、Credit 金流、上游映像漏洞、花費明細 <!-- open: 8, 76, 78, 79, 113, 166, 175, 176, 178, 179, 180, 185, 236, 284 --> |

## 甲、部署期驗收

**⚠️ 一個共同前提**：gVisor 的 `systrap` 平台**不需巢狀虛擬化**（只有 `--platform=kvm` 才需要），因此多數測項在一般 Linux runner 即可跑。此點須由第一台節點實測確認。

| # | 需求 | 還缺什麼 | 等誰 |
| --- | --- | --- | --- |
| 甲-1 | **`SEC-009`** | 46 項覆蓋基線、10 個測項，判準是**全數 pass 且 0 unknown**，證據存 [`mvp/m4/sec-009-acceptance/`](mvp/m4/sec-009-acceptance/)。T1（逃逸嘗試）、T2（syscall fuzz）、T5（網路外洩）已寫成可重跑的腳本並在巢狀開發機上跑過；T3 的三項行為測試（記憶體、pids、開檔上限）隨 CI 的 gVisor leg 在真 `runsc` 上通過；T8 的映像半（I-01～I-06）有機器可查的證據。**剩 25 項沒有證據，而它們的受測物就是那台機器**：T6、T7、T9、T8 的節點半，以及 N-01～N-08 的生產強制層——渲出的 ruleset 沒有任何東西執行 `nft -f`，探針的每一次讀數到今天為止都來自 fake 或 dev Docker。**「實作了」與「在受測物上跑過」不是同一件事**，兩者對這 25 項的判定同樣是沒有證據。<br>**巢狀環境的讀數不能當驗收**：Windows → WSL2 VM → privileged 容器，核心不是生產那個，目的地是假的，`pinned_ip` 是實驗室自己填的。<br>**T4 缺的不是 Linux 是模型憑證**（判準是與 runc 基準逐筆一致，指派給 CI ＋人工確認）。<br>**寫這些腳本的過程反覆抓到「綠燈其實什麼都沒量」**：負對照不存在時，一個從沒執行過的測試會給出 exit 0；只跑正向時，「被 DROP」與「連接埠關著」對 `nc -z` 是同一件事。**每一個測項都要有會讓它變紅的輸入，否則它量的是零。** | Linux 節點 |
| 甲-2 | **`SBX-010`** | 甲-1 的工作項側。現有的真實容器驗證（非 root、唯讀 rootfs、無主機掛載、pids／記憶體／暫存／開檔上限、逾時強停、清理冪等）**不等於**逃逸測試通過——多的是 T3，不是 T1 的 CVE PoC，也不是任何要節點的測項 | 同甲-1 |
| 甲-3 | **`SBX-005`／`SBX-007` 的生產網路面** | 出口強制層已渲染：`tools/egress/render.py` 從 `infra/egress/allowlist.yaml` 一次渲出 nftables ruleset、DNS 設定與 sandboxd 讀的准入清單，`Config.accept()` 比對 **host＋port**（不比對 purpose——purpose 是呼叫端自選的字串，比對它等於「自稱 `model_gateway` 就能連到任何主機」）。**規則順序在這裡是安全屬性不是排版**：東西向、節點 loopback、metadata 三段 drop 必須排在允許清單的 accept 之前，accept 必須排在 RFC1918 drop 之前。P-02 常駐探針與 N-06 的目的地記錄（conntrack 事件 ＋ journal 上傳 ＋ 90 天保存）都已落地。<br>**仍缺**：①**節點上實際套用** ruleset；②**每 Run netns 與東西向阻擋**——`dockerdrv` 的 `HostConfig` 至今不指定 namespace，`skillhub_egress` 上的沙箱可互相連通，那是**不需逃逸**的跨 Run 橫向路徑；③模型閘道移到沙箱面專屬節點；④`pinned_ip` 填實際位址（今天全是 `unset`，渲出零條放行，那是 fail-closed 的正確狀態）；⑤在真實節點上看到一個 Run 的三份記錄在控制平面對得起來。<br>**⚠️ 節點准入必須檢查 `br_netfilter`**：東西向阻擋與 `--icc=false` 都依賴它，而它在部分雲端映像上預設不載——兩個手段會一起、安靜地失效。CI 上第一次跑就紅在這一條，抓到一個沙箱連到了同一台節點上的另一個沙箱 | 生產網路。決策已定（nftables default-deny ＋固定 DNS，不部署代理），剩下的是實作 |
| 甲-4 | **`SBX-002` 的節點查證** | 門檻已定值（可修的 Critical／High 阻擋且無豁免路徑、不可修者具名豁免、複審日 ＝ 首次豁免＋90 天；掃描有效期 30 天、到期前 7 天告警），發佈流水線把 SBOM 與掃描結果以 attestation 掛在 digest 上，每週重掃已發佈的 digest，每日檢查到期並開 issue。節點准入探針 [`tools/sec009/t8-node-probe.py`](../../tools/sec009/t8-node-probe.py) 已寫，`unknown` 視同 fail。<br>**仍缺**：在真實節點上跑一次（它在開發機上的判定是誠實的紅——基線檔 `unset` 沒有東西可比對、這台機器混排工作負載、dockerd 沒有註冊 runsc），以及驗證 issue 通知真的送到負責人 | 部署批的閘門 A 探針 |
| 甲-5 | **沙箱節點的 IaC 與上線最低配（`DEPLOY-IAC-001`）** | 目標已定值（[`05` R-43](05-pending-rulings.md)）：Hetzner Cloud／Falkenstein、第一批 1 台 CPX31、每月預算上限 US$60。**不選低延遲區域的理由**：Run 是非同步佇列任務，端對端時間由模型呼叫主導、不由節點的地理位置主導。cloud-init 的輸入契約定案（[`05` R-17c](05-pending-rulings.md)）：`/etc/skillhub/node.json` 五欄，`build_phase` 取代閘門 A 探針原本那個 2 秒容差的啟發式。<br>三種角色的 IaC 都已進 repo——控制平面（TLS、備份與還原演練、監控與告警路由、秘密權限檢查）、模型閘道、沙箱節點（baseline 版本的 `runsc`、`icc: false`、`br_netfilter`、從允許清單渲染 nftables、`sandboxd` 以非 root 服役後才把 `node.json` 改成 `serving`），步驟在[三份 runbook](../runbooks/README.md)，CI 驗 cloud-init schema、systemd unit、compose 與 shellcheck。已在一個模擬 Ubuntu 的容器裡從 user-data 跑到服役。<br>**仍缺**：**一台真機**。容器不是真機，而本列要到真機上的 SEC-009 證據才結。**交付定義多一列**（[`05` R-6](05-pending-rulings.md)）：Suite 1 必須在該節點上跑過一次並存檔，在它之前不得有第一位外部使用者的 Run。<br>**一個由本列造成、但不在本列的後果**：受測者的程式碼會在德國執行，那是一次跨境傳輸，同意書已補上對應條款並因此要重走法務確認（見乙-16） | 部署負責人 |

## 乙、待負責人決策（**寫程式之前先要有人拍板**）

| # | 要決定什麼 | 現況與已查到的事實 | 不決定會擋住什麼 |
| --- | --- | --- | --- |
| 乙-16 | **受測者同意書與資料保存政策的法務確認與填值** | 草稿在 [gate-test/consent-and-data-policy.md](mvp/gate-test/consent-and-data-policy.md)（放 `gate-test/` 是因為它由 M1 閘門與 M4 封測共用，不屬於任何一個 `mX/`）：一份政策、兩份同意書（A 版＝閘門的 40 分鐘一對一；B 版＝封測的 14 天自用），逐項列舉蒐集什麼與**不**蒐集什麼、四個分析事件的逐欄公開、模型呼叫與成本、八項風險與各自的緩解、四條退出機制、聯絡方式。<br>**非法務的部分已裁定**：未成年受測者**不補條款，改用招募端排除**（限年滿 18 歲，把那個法律問題移除而不是回答它）；§9 的十一項待填值分兩堆別一起等（三項要等保存期限追認，其餘八項不必等任何人）；未回填前寫「尚未定值」而不是留白，並註明回填前不得對受測者宣告任何期限。<br>**法務要看的縮小為五件**：用語與法域、個資法規對應、四條退出機制的可執行性、分析事件逐欄公開的措辭，以及**受測者的程式碼會在一個尚未完成逃逸驗收的環境中執行**這項事實揭露（封測與甲類驗收並行是既定裁定，所以招募或同意書裡任何一句宣稱隔離強度已驗證的話都是不實陳述）。跨境傳輸條款隨甲-5 補上，要一起看 | **招募寄確認信時沒有東西可簽** ⇒ `BETA-001`；連帶 M1 閘門的 D 日 |
| 乙-19 | **試跑證據彙總的同意書條款** | 評分與排行榜永久不做，替代品是跨工作區的試跑證據彙總（「在 12 個工作區的 37 次試跑中，28 次符合各自的驗收條件」）。**它跨工作區，所以它是同意書的事情，不是分析事件的事情**——分析邊界管的是漏斗量測，本項是由使用者資料推導出的產品內容。<br>**資料模型已定**：opt-in 掛在**工作區**不掛在使用者（Skill 與 Run 都是工作區範圍，掛在使用者會讓「哪些 Run 算數」在多工作區時無法回答）；欄位是 `opted_in_at` ＋同意書版本（版本沒對上等於沒同意）。**撤回取「即時且只往前」**：彙總一律從目前 opt-in 的工作區即時計算，不做回溯抹除——彙總從不保存個別工作區的身分、`k ≥ 5` 保證每格至少五個來源，**沒有可以被抹除的個別痕跡**，而承諾回溯抹除會逼出一張記錄「誰貢獻過哪一格」的表，**那張表比它要保護的東西更敏感**。預設是關的，未 opt-in 的工作區在畫面上是「尚未定值」不是空白。<br>**剩下的只有同意書條款**，與乙-16 同一份文件、同一次法務確認 | MVP 之後的社會證明功能整條 |
| 乙-27 | **purge 與資料庫層的清除權限屬於誰**（[`05` R-25](05-pending-rulings.md)） | 角色、逐表授權、環境變數與切換 runbook 都已在 repo 裡，**沒有被做的是操作者那兩步**（指派登入帳號、收回 API 角色的 DELETE）；變數未設時 `maintenance` 退回 API 角色並在啟動日誌說出來，所以今天 `maintenance` 的九個子命令全部跑在與 API 相同的資料庫角色上（角色在建連線池時一次決定，連要做 DDL 的 `rotate-partitions` 也跟著走，而 `skillhub_purge` 沒有建分割表的權限——切過去必須把它拆成另一次呼叫），**那個角色對每一張表都有 DELETE**。清除路徑是這個系統唯一會不可逆銷毀使用者資料的東西，而它與服務請求的路徑共用同一組憑證。<br>要決定的是**切過去並收回 DELETE** 還是**維持退回、把理由寫下來**。建議後者，理由是封測 12 人、operator 一人，拆角色的縱深買不到相稱的風險降低；退回機制讓淨測試模式那條唯一連線不受影響，原本反對拆角色的理由不再成立 | 不擋任何人。**它擋的是「有沒有人想過」這件事被記下來** |
| 乙-28 | **策展的範例 Dataset 事實上永遠到不了下載者手上**（[`05` R-26](05-pending-rulings.md)） | `02:PACK-001` 寫「使用者可選擇是否包含**可散布的** Test Case 與範例資料」，第二層表格明列策展 Dataset 可以進包，`CONTENT-007` 的允收理由逐字是「供 `PACK-001` 選用」。**實際上這條路只有 operator 走得通**：判準是工作區的 `IsCatalog`，而打包讀的是呼叫者自己的個人工作區；一般使用者要拿到策展 Skill 只能先 Fork，**Fork 進的是個人工作區**。結果是永遠拿到一份 `not_curated` 的排除清單，**M2 為 45 個精選做的範例資料對下載者是零價值** | **封測第一天就會被撞到**：Fork 一個精選 Skill、打包、勾選「包含範例資料」，拿到一份空的排除清單。`NFR-001` 禁止 UI 誤導，所以「現狀不動且不寫」不是一個選項 |
| 乙-30 | **白話摘要與任務範例句要不要、以什麼形式交給下游**（[`05` R-28](05-pending-rulings.md)） | 索引時增強（**每個版本一次模型呼叫**）的全部價值只兌現在平台內的搜尋列上；詳情頁自己就這樣告訴使用者：「你的 Agent 讀的不是這一段……下載回去的套件裡也不會有這一段」。**價值鏈斷在最後一格**：平台幫使用者找到 Skill，卻沒有幫他讓 Agent 用上它。不主張推翻套件 frontmatter 的欄位禁令 | 不擋任何人。**排在 D 日之後**——閘門量的正是「平台的理解對人有沒有用」，那個讀數應該先於這個決定 |

## 丙、實作與資料工作

| # | 缺口 | 現況與解除條件 | 落點 |
| --- | --- | --- | --- |
| 丙-8 | **`EVAL-013` 的所有讀數都是舊尺量的** | 回歸集、轉換規則與**與現行 Judge 同尺的 harness** 都在；**不存在的是一份用這把尺量出來的讀數**。`02:EVAL-013` 要求「逐筆可比對，並產出一份含符合率、逐筆差異清單與差異歸因的報告」——一個讀者看到它已勾會相信今天有一份 Judge 準確度的讀數，而沒有。<br>**同批要一起量的第二件事**：**Judge 的取樣雜訊**。沒有任何一次模型呼叫釘住 `temperature` 或 `seed`，而重評、回歸與生成來源紀錄三者都建立在「同條件可重現」上。最小量法是同一份輸入重複跑 N 次、記不一致率——**不必先改程式**，量到之後才知道要不要釘。**不量它，回歸報告的每一個差異都歸不了因**：分不出是尺變了還是模型當天心情不同。<br>**證據殘缺下的保守性仍是零覆蓋**（注入樣本刻意全部完整，否則判定歸不了因）。<br>**解除條件**：一次付費重跑 ＋ 一份報告。要一套跑得動的 dev stack；harness 的取樣只從 `skill_runtime_compatibility` 拿 Run，那張表不隨新 Run 增長，所以跑完要補一步把新 Run 納入，否則它會安靜地重評舊 Run 而且不會有任何提示 | **不擋任何工作項的勾選，但它擋「Judge 判得準」這句話能不能被說出口** |
| 丙-76 | **核心旅程的第二步在程式裡不存在** | `01` §6.1 步驟 2「系統分析輸入、輸出、工具、資料與環境需求」——十六步裡唯一一步連半邊都沒有，查詢原文直接送 embedding。**同一個洞的第二半**：查詢改寫也沒有實作，而向量腿失效時的降級路徑正是提案逐字禁止的「原句 FTS」。<br>需求 ID 與允收已立（`02:DISC-005`、`03:DISC-011`），**四件允收**：①結構化意圖（輸入／輸出／工具／資料／環境五欄）；②改寫成關鍵詞 ＋ 篩選條件，值域限於已實作的維度；③**降級不得繞過向量腿**，要一支把改寫器換成永遠失敗的具名反證測試；④**中文 FTS 的下界要有明文決定**（擇一擴充並記實測命中率，或明文寫「中文只走向量腿」）。<br>**⏱ 這一項若在閘門之前沒有落地，閘門不通過時沒有人分得出來它敗在搜尋品質還是敗在一步從未實作** | `01` §6.1 步驟 2、[意圖搜尋](../adr/README.md#意圖搜尋) |
| 丙-78 | **`feedback_reports` 沒有 `session_id`** | 缺的不是一支查詢是 schema——表只有 `workspace_id`／`user_id`／`run_id`／`page_path`。要決定的是**要不要真的把 session 蓋章到回報上**，那牽動分析邊界刻意剪斷的那條線，**是產品決定不是實作** | [產品分析與稽核邊界](../adr/README.md#產品分析與稽核邊界)、`03` BETA-004／RELEASE-009 |
| 丙-79 | **`session_started` 是一個沒有讀者的事件** | 四個分析事件裡只有它沒有被漏斗查詢引用過一次（第 7 段走的是領域表的 UTC 日分桶）。它也永遠不帶 `workspace_id`（多數觸發點在未登入的公開頁）。<br>**補法是一個選擇不是工時**：要嘛給它一個讀者（第 7 段改以它為母體，代價是母體從 workspace 變成 session、與第 3／5 段不可串接），要嘛承認它今天只為 `BETA-004` 而存在並把這件事寫在分析邊界旁邊。**收一個沒有人讀的事件，與不收它，在隱私上不是同一件事** | [產品分析與稽核邊界](../adr/README.md#產品分析與稽核邊界)、`01` §11.2 第 7 段 |
| 丙-113 | **demo 的四項能力住在一個沒有人為那台機器規劃過的執行期** | `apps/llm` 擁有 `/embed`、`/judge-run`、`/enrich-skill`、`/match-reasons`——**跨語言意圖搜尋、評估判定、匯入增強、推薦理由**四樣，平台一律經它、沒有繞過去直連閘道的路。它是 Python ＋ uv ＋ FastAPI。<br>**而受限環境的問卷一題都沒問 Python**，三個機制也都沒有涵蓋它：啟動器不起 `apps/llm`；能力表檢查的是變數有沒有被設、不是那個位址答不答得出來；離線打包沒有 Python 那一半。<br>**要做的兩件事都不是程式**：①補一題問那台機器能不能跑 Python／uv／建虛擬環境；②把既有問卷裡已經有答案的那幾格回填——文件把讀者送去那張表，而那張表現在說「沒人答過」，這是錯的。<br>**如果答案是不能**，退路已經寫好：播放真實錄下的 Run，或以本機 Driver 現跑策展素材 | `apps/llm/`、`tools/cleanmode/`、[m6/environment-probe.md](mvp/m6/environment-probe.md) |
| 丙-166 | **互動創作的完整驗收待收齊** | LangGraph、會話與 Worker、三個入口與精確保存都已接線；**規劃完成不等於功能完成**。依 `02:GEN-007`～`012` 與 `03` §19.1 逐項收齊證據才可結案。免費證據與真實模型／真人量測的缺口見[開發手冊](../development/interactive-creation.md)；**曝光仍未放行**（`01` §10 ⛔ 邊界 1） | [互動創作](../adr/README.md#互動創作)、`03` §19.1 |
| 丙-175 | **互動創作候選在自己的驗收條件下還沒穩定過門檻** | 門檻依 [`05` R-45](05-pending-rulings.md)：`met` 算一輪修訂內，文字＋參考 10 場 ≥ 6；流程圖組退出分母（另見丙-176）。最近一次量到文字＋參考三輪內 7／10，首次過門檻；逐次讀數見[報告](mvp/m5/creation-measure/report.md)。<br>**量測路上定下來的三件設計**：條件必須在這份樣本上可判、Skill 要動手不反問、樣本＝一句請求＋材料。**換執行者不抬 `met`**（旗艦執行反而更低，失敗方向換成加入輸入裡沒有的日期與假設）——**槓桿在作者側不在執行者側**。<br>**剩**：`kept` 的人工判讀要人讀 30 份；Run 的模型費用 harness 記不到（每個 Run 自己的 Virtual Key，閘道沒有按 key 報表） | `creation.py` 的 compose／review 提示、[`05` R-45](05-pending-rulings.md) |
| 丙-176 | **流程圖入口要改成「先描述、再拆解、不懂必問」，而今天不確定處沒有任何閘門** | 現況：模型一步就回 JSON，人按一次確認就能出草稿；Go 對不確定處只有數量上限，沒有「還有不確定處就不得起草」的規則。量測裡流程圖組跨模型等級都是零。<br>**要做**：①第一步只描述圖上有什麼（形狀、文字、箭頭），人確認描述；②確認後才拆成節點／條件／分支；③每一條不確定處都要人回答，Go 在還有未回答時拒絕起草；④流程圖組退出門檻分母、另立自己的數字。<br>**實驗性功能，白話與參考優先**（八成用量在那兩個入口） | `creation.py` 的 understand 相、`job.go` 的起草閘門 |
| 丙-178 | **Re-Use 三關卡** | 三件都已落地（[`05` R-48](05-pending-rulings.md)／R-49／R-50）：①**公開搜尋**加 bigram 覆蓋腿，覆蓋全部 token 的列不受距離截斷、排在向量命中之前；②**會話首則訊息先查目錄**，有命中就三選一（採用它 fork 進工作區／以它為參考／不要，清空參考）；③**保存前查重**，命中則扣住 materialize 等人確認。<br>**離線重放沒有退步**（golden 三項前後不變，特定詞的 Top-1 從 7 提到 23）。<br>**量測路上抓到的三個 harness 脆弱點都已修**（匯入的參考沒做索引時增強所以語意查不到、跨場同名、草稿取了參考的名字）；最近一次十五場裡參考組首則訊息查目錄 5／5 命中。<br>**剩**：真人使用；「仍然建立之後的同名」提示要改 | `discovery/service.go`、`creation.go`、`public.yaml` |
| 丙-179 | **OWASP Top 10 for LLM 對照：創作迴圈的注入與投毒** | 對照表與威脅模型的四條新威脅（同意後抓取的 SSRF、抓回頁面／參考／觀察的注入、投毒經 Re-Use 被採用、快照裡的個資與金鑰）都已寫；`02`／`03` 的 `SEC-013` 已立。已落地：提示層圍欄、會話快照遮罩、materialize 的路徑穿越反證、參考的層級與掃描狀態揭露、查重後改名不重跑查重——四處新守門都以突變驗過紅。<br>**紅線量在產品層**：12 案例語料由 Go 守門重放，決定性、在 CI 裡、不花錢；模型層的 harness 數字只列觀測，至少三次取樣並記全距。**投毒分不開**，目錄維持策展＝唯一結構性緩解，投毒量測列常設紅線。<br>**剩**：同意書互動創作那一列的法務（乙-16） | `02`／`03` 的 `SEC-013`、威脅模型 §2.10、`creation.py` 的圍欄 |
| 丙-180 | **搜尋 F1 的生產目錄還沒重跑** | 目標是 F1 ≥ 95%。F1 的定義已寫死在量測腳本（k ＝ 正解數；舊的 F1@1 對兩份正解的題上限 0.67，永遠到不了 0.95）。增強提示 v7（例句分布與 tags 格式）把公開搜尋從 0.927 提到 **0.955**，創作搜尋改跑公開規則後同樣到 **0.955**，干擾拒答全程 12／12。入庫順序已裁定（[`05` R-52](05-pending-rulings.md)）：資安確認 → 抽 metadata → 入庫，做法是**沒有 metadata 的文件不進公開目錄**（版本仍建立、擁有者仍看得到，每小時 backfill 補進）。<br>**剩**：**生產目錄的 45 份仍是舊提示**。要由負責人在部署環境執行重做（`REINDEX_REENRICH`），約 45 份、US$2。本機 dev 資料庫 schema 落後，代理沒有對它跑。<br>golden 剩下的 miss 是近義對的第二名，天花板在標註與查詢改寫 | `enrich.py` v7、`cmd/reindex`、[`05` R-52](05-pending-rulings.md) |
| 丙-185 | **Credit 的金流還開著：唯一入帳入口是 operator 授予** | 帳本、路由、五種付費呼叫的成本事件、畫面、每日與事件驅動的統計都已落地；試跑依 [`05` R-74](05-pending-rulings.md) 開始扣點（開始前以閘道上界檢查、清理時扣實付），搜尋不扣。開始前門檻、會話終結摘要、負債下限都已在真模型上驗過一次：帳戶 80 點 → 第一步實付進位扣 1 點 → 第二步要預留 130 點，79 − 130 低於下限，**在呼叫模型之前停下**，餘額不動。<br>**一個只在單連線才出現的死鎖已修**：授予與扣點在自己的交易裡卻向連線池要第二條連線查帳號狀態，淨測試模式只有一條，所以永遠逾時。改成帳號檢查走呼叫者的交易。<br>面額、加成、保守門檻與滾動窗維持現值（[`05` R-75](05-pending-rulings.md)）；清除帳號時 Credit 紀錄全部保留、永不過期（[`05` R-76](05-pending-rulings.md)）。<br>**剩的就是金流**：MVP 期間沒有真正的付費儲值機制，使用者沒有自行充值的路徑 | [Credit 計量與扣款](../adr/README.md#credit-計量與扣款)、`02` §4.11、`03` §21 |
| 丙-236 | **四顆上游映像都有可修的 High，而四顆都已經是最新的 tag** | 物件儲存三項、模型閘道一項、監控六項、資料庫二十五項（全是映像內某個 Go 二進位的 stdlib）。**四顆的 pin 都已經是該 tag 的最新 digest，今天沒有可以升的目標**，修法只有等上游重建。這些只在每週排程那一跑變紅，不擋任何人的 PR。<br>**這一列存在的意義是讓那個週紅有一個可以對照的清單**，不是拿來調鬆閘門：每次週紅時比對，只有出現**新的**項目才需要動作；清單上的項目全部消失時刪掉這一列。閘道的開發頻道 tag 會修掉其中一項，**不採用** | `infra/compose/docker-compose.yml`、`.github/workflows/image-scan.yml` |
| 丙-284 | **使用者看不到自己的花費明細，只有一個滾動的餘額** | 真正被扣的分錄在 `credit_entries`，查詢與營運者端的整條路徑都已齊備，但 `GET /me/credits` 只回餘額；唯一讀得到真實分錄的畫面在 operator 後台。使用者側散落在四個畫面的點數數字，沒有一個是從真實分錄算出來的。`02:CRED-001` 從來沒要求過使用者可讀的明細，**所以這是準則層的缺口不是實作漏做**。<br>**工程面可以直接做，但三個問題是產品決定**：明細要不要能點回是哪一筆花費（要新欄位，且牽動實作鐵律 3 的 Workspace Scope）、要給使用者看真實分錄還是 Trace 下界（兩個數字並陳看起來像自相矛盾）、要不要出現美金（契約明寫美金永不出現在這條路由上，要顯示就是推翻既有決定）。等 [`05` R-86](05-pending-rulings.md) | `contracts/openapi/public.yaml`、`apps/platform/internal/creator/credit/` |

## 移交下一階段（封測期與 MVP 之後）

只列**已經可以看見的接點**，不代為開工。

| # | 接點 | 內容 |
| --- | --- | --- |
| N-1 | **封測會產生第一批「不是團隊寫的」內容與行為資料** | 分析事件、質性回饋、外部使用者上傳的 Dataset 與建立的 Skill 版本。三者的保存與刪除語意已定，而**帳號刪除的去識別化保留**是第一次會被真人行使的承諾；同意書的那張表就是它的對外形式 |
| N-2 | **可散布性會第一次遇到「不是策展的 Skill」** | 目前的判定全部來自策展目錄與其 fork。使用者自己匯入的 Skill 只會有授權運算式那一個欄位，所以 `unknown`（＝擋）會是常態，而使用者看到的是「你的 Skill 不能下載」。**這是預設值的正確方向**（誤判要錯在擋的方向），但它會產生一個產品問題：**沒有任何路徑讓使用者或 operator 把它改成允許**。<br>使用者**自己帶進來**的那一半已經有答案（`self_supplied`）；仍然撞得到的是**使用者上傳的別人的內容**（他自己拿得回去，但平台不會替他判定能不能散布給第三人），以及**日後的發佈到目錄路徑**（必須面對一個不是 `allowed` 的值並停下來） |
| N-3 | **封測後的範圍複審是 MVP 之後所有優先級的唯一輸入** | `01` §7.3 明列不含公開 Beta、Marketplace、付費；Local Runner 與遠端 MCP 已移出首發並依需求訊號啟動。封測回饋裡的需求訊號那一類就是第一批樣本 |
| N-4 | **漏斗第七段（首次使用後再回來）在 14 天內量不到** | 已裁定它**不列入判準但列入報告**，結論只能當下界讀。要量真正的留存需要一次 30 天以上的觀察窗，**那屬公開 Beta 而非封測** |
| N-5 | **`CONTENT-011` 的增強重跑** | 要把被拒收依賴造成的能力缺口寫進「限制」欄，對象三筆已具名。它屬內容批不屬打包批；動到閘門標的的，數字依 gate-test §3.2 分開統計 |
| N-6 | **Platform 的 DDD 邊界治理** | 邊界收斂與拓撲遷移已完成並全面落地：stable Boundary ID、depguard、query ownership、raw-SQL tripwire 與產品價值流路徑均已收斂；`allow:`／`read_allow:` 均為空，裸 SQL 只剩九個具名技術豁免（**是存量不是擴充點**），`shared/skillpkg` 是唯一 Shared Kernel。現行規則以 [Platform Bounded Context 與 Context Map](../adr/README.md#platform-bounded-context-與-context-map) 與 [Query 與寫入所有權](../adr/README.md#query-與寫入所有權) 為準 |
| N-7 | **技術債修復後仍待決策／部署的五項** | `DEPLOY-IAC-001`、`RUNTIME-PYTHON-001`、`LLM-EVAL-007`、`LLM-RES-001`（partial）、`SUPPLY-RUNTIME-LOCK-001`。部署項由部署負責人依 [M4 release checklist §2.8](mvp/m4/release-checklist.md) 執行並留下 IaC／image／負載或 clean-build 證據；`LLM-EVAL-007` 由負責人決定 Judge／Suggest 用量與成本的唯一事實來源，再以契約測試證明。**不得把原始盤點裡已修的項目重新列為 active** |
