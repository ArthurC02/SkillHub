# 前端設計系統與 UI/UX 評估準則

本文件是 [ADR-039](../adr/ADR-039-frontend-design-system-and-ui-evaluation-criteria.md) 與 [ADR-041](../adr/ADR-041-trust-signal-vocabulary-typed-absence-and-rule-precedence.md) 的操作手冊，**活文件**。ADR 記的是「為什麼要有一把尺」與否決了哪些做法；本檔記的是**現在這把尺長什麼樣**，隨 `apps/web` 一起改。

**允許值在本檔，實際值在 [`apps/web/src/index.css`](../../apps/web/src/index.css)，[`design-system.test.ts`](../../apps/web/src/design-system.test.ts) 比對兩者。** §4.1／§4.2／§5 的粗體 px 儲存格是測試直接讀的政策來源——改 CSS 而沒改這裡，或改這裡而沒改 CSS，測試都會 FAIL。其餘章節不重抄 CSS 的值。

**這份文件管一頁之內；一頁與一頁之間——有哪些位址、怎麼互相到達、叫什麼名字——見 [資訊架構](./information-architecture.md)。**

五層，由上往下：**優先序**（§0，規則互相牴觸時誰贏）→ **義務**（§1，產品推出來的，不可談判）→ **原則**（§2，十三條）→ **系統**（§4，字級、間距、表面、狀態、版面）→ **強制對照表**（§6，誰在把關，以及誰沒有）。§3 的 checklist 把上面全部折成一張逐頁可用的表；§5 是偏離帳；§7 是這把尺對**還沒寫的功能**說得出什麼。

---

## 0. 優先序

**原則會互相牴觸，而且已經在正式畫面上牴觸過。** 一份沒有優先序的原則清單不是系統，是願望表。

首頁手機版是實例：三個停用的篩選各自帶一整段理由（原則 2.4 正確執行），加總把第一筆搜尋結果推到 **1180px**，也就是義務 1.2 所禁止的「為了拿到頭條而費力」。兩條規則都對，畫面壞了。

牴觸時的順位，由上而下：

| 順位 | 誰 | 為什麼排這裡 |
| --- | --- | --- |
| 1 | **安全與不誤導**（§1.1、§2.2、§2.3、§2.9） | 讀者據此決定要不要執行別人的程式碼。錯了的代價不對稱：多看一眼是麻煩，看漏一項是執行了不該執行的東西 |
| 2 | **§2.10 的「永不折疊」清單** | 上面那條的具體化。它是封閉清單，不是判斷題 |
| 3 | **可及性硬需求**（`02:NFR-007`、WCAG AA） | 不是品味，是允收準則 |
| 4 | **速度與版面**（§1.2） | 讓步的方式是**改變揭露的形狀，不是刪掉內容** |
| 5 | 一致性、尺度、美觀 | 前四項都成立之後才輪到 |

**解法的形狀，不是二選一。** 首頁那個例子的正解不是「刪掉停用理由」（違反 2.4），也不是「忍受 1180px」（違反 1.2），而是把六個篩選在窄螢幕收進一個 `<details>`，`<summary>` 上留「3 個條件目前無法篩選」——**數量留在外面，段落收進去**。順位低的規則讓步時，讓的是形式；順位高的規則的**內容一個字都不能少**。

> **2026-09-03：這個裁定落地了，而它躺了多久是有代價的。** 落地前實測，手機上第一筆結果在 **y1437**——比本節記下的 1180 還遠，也就是這個例子在沒有人實作它自己寫好的解法的期間**繼續惡化**。現在 `<summary>` 上是「篩選條件（**5 項可用、1 項目前無法篩選**）」〔2026-09-03 稍晚訂正：原寫 4／2，類別維度同日隨 migration `0053` 成為可用控制項，不可用的只剩 MCP〕，**兩個數字都是數出來的、不是打上去的**（`disc.test.tsx` 把它們釘在實際渲染的控制項上——一個停止描述底下清單的數量，比沒有數量更糟，因為讀者不會有理由去打開它）。手機 **y1437 → y734**。<br>~~**桌面沒有一起收**，因為本節的裁定寫的是「窄螢幕」：桌面預設仍然展開，第一筆結果在 y958（同一批量測），也就是桌面**現在也踩到 §1.2**，而那要的是一次新的裁定而不是把舊的偷偷放大。記在 `04` 丙-130。~~<br>**✅ 2026-09-03 同日稍晚結案（[`04` 丙-132](../plans/04-backlog-and-handoffs.md)，不是丙-130——那個號碼在同日的改號後屬於別的一列）**：負責人指示「UI/UX 是關鍵要素」後桌面一起收，**而收的方式與原本設想的不一樣，這一點值得留著**：起始開合**由「有沒有篩選在作用」決定，不由視窗寬度決定**。寬度測試是「窄螢幕」三個字最直覺的讀法，也錯了兩次——它讓桌面實測不合格，而且回答了一個沒有人問的問題。真正決定這排控制項值不值得 330px 的，不是螢幕多寬，是它們**有沒有在做事**：有篩選時它不是一個提議，是正在從答案裡拿掉列的東西（§2.2）；沒有篩選時就收起來。落地後只會單向再打開（後到的篩選會展開它，沒有任何東西會關上它），理由同 §1.3／ADR-042 決策 5：**只會展開的規則是預設值，會關上的規則才是「精簡模式」**。程式與理由在 `apps/web/src/pages/Home.tsx` 的 `narrowing`。

---

## 1. 三個 UX 義務

這三條不是風格偏好，是 [`01-goals-and-plan.md`](../plans/01-goals-and-plan.md) 直接推出來的。違反它們是產品缺陷，不是品味爭議。

> **更正（2026-08-22）：本文件前一版在這裡寫錯了。** 它說 `01` §2.1（「可展開的進階技術資訊」）與 §5 原則 7（「**一般模式**／**進階模式**」）互相矛盾，並裁定本產品「不做模式切換」。查證後：**不矛盾，而且模式切換早就出貨了。** `02` §DISC-003 與 §TRACE-001 各自定義兩種模式的內容，`03` 的 `TRACE-006`／`TRACE-007` 都已勾選（`?mode=advanced` 是 API 參數），`m0` 威脅模型把「進階模式」當成一個安全情境，而 [`RunTrace.tsx`](../../apps/web/src/pages/RunTrace.tsx) 有一個真的切換器（兩顆帶 `aria-pressed` 的按鈕）。錯的是設計文件，不是 `01`。§1.3 已改寫成三種機制與它們的選用判準。

### 1.1 可判斷

願景是「成為個人與團隊探索、驗證、改善及發佈 Agent Skill 的**可信任基礎平台**」（`01` §3），而四個問題裡的第二個是「找到 Skill 後，**難以判斷是否可信**、是否適合自己的 Agent 與環境」（`01` §1）。使用者的核心動作是**決定要不要信任並執行別人寫的程式碼**。

所以每個畫面必須讓證據看得見，**也必須讓證據的缺席看得見**。缺席比證據更容易出錯，因為它的預設呈現方式（空白）會被讀成「通過」。

**這一條有外部實驗背書，不只是推理。** Song & Szafir（IEEE VIS 2018）量到**零值填補得到最低的資料品質感與信心**——把缺失當成零不只是語意錯誤，讀者會把它當真值採信；而把缺失標示出來反而提高信心。`02:DISC-004`「缺少資料的欄位顯示未知，**不得自行推定為通過**」是同一句話的產品版本。

### 1.2 快到第一個判斷

MVP 承諾是「**10 分鐘內**找到 → 驗證 → 下載」（`01` §3）。10 分鐘要走完整個閉環，代表沒有任何一個畫面可以讓讀者為了拿到頭條而費力。

**判準的視窗要講明，否則兩個人會給相反的結論**（那正是 ADR-039 要終結的事）：**桌面 1280×900、手機 375×900**，與 `e2e/shots.spec.ts` 拍的兩個尺寸一致。

### 1.3 一個介面服務三種成熟度

`01` §2.1 明文：「MVP 預設介面以**非技術使用者可理解的任務語言**呈現，並提供**可展開的進階技術資訊**」，且同節說目標使用者「**可能處於不同成熟度階段，而非固定角色**」（學習者／改善者／精深者）。

產品實際用了**三種**揭露機制，而且它們選用的判準不是「這東西有多技術」，是**被藏起來的東西是什麼**：

| 機制 | 現行用處 | 什麼時候選它 |
| --- | --- | --- |
| **模式切換**（一般／進階） | `RunTrace` 的兩顆按鈕 | **同一份事實的兩種保真度**：摘要 vs 經遮罩的原始事件。兩邊回答的是同一個問題，只是解析度不同 |
| **另一個位址** | `/skills/:id/files` | 進階內容是**另一個物件**（套件的檔案），不是同一份事實的另一種呈現。位址可以分享，這是它勝過前兩者的地方 |
| **`<details>`** | 版本 id、內容雜湊、產生摘要的模型 | 一個**屬於本頁某個事實的細節**。它不是一個獨立的視圖 |
| **Tip**（點擊觸發的 toggletip；[ADR-065](../adr/ADR-065-hot-path-text-budget-and-the-fourth-disclosure-mechanism.md) 決策 3，2026-09-03） | [`components/Tip.tsx`](../../apps/web/src/components/Tip.tsx)（2026-09-04 落地，丙-142 第二批）。**今天只有一個實例**：`InFlight` 的「為什麼可以關掉這一頁」——推導原本住在程式註解裡等這個元件。同日的稽核提出另外七個候選（模式切換句、分頁排序句、Rubric 兩句、詳情頁的 Test Case 句、打包頁的冪等句、首頁的排序 `<details>`），**三個反駁子代理以第 6 條全數駁回**：去重之後那些句子都已經是平鋪且合規的短句，為了技術上符合六條而加開關是反方向。預定的另外兩處（引文回驗方式的解釋、「為什麼這一格沒有分數」）留在清單層級的去重，因為一頁三份清單各一個 Tip 會撞 ≤3 | **一個已經說完的句子的理由**。`<details>` 藏的是事實的細節，Tip 藏的是「為什麼」；判準是**把一頁上所有 Tip 刪掉之後，每一個判斷仍然做得出來**。入場六條見 §2.13；不得是 `title`、不得是 hover-only（§2.4 第 3 項的失效正是 hover） |

**四者共同的約束只有一條，而它比機制的選擇重要得多：§2.10 清單上的十項，哪一個都不准進去。**〔2026-09-03：原寫「三者」，Tip 隨 ADR-065 加入成四者〕 這一條在切換器上尤其容易破——降級自述（第 9 項）必須在**兩個模式下都渲染**，而不是只在進階模式裡。目前成立：`IncompleteNotice` 在一般模式與進階模式各渲染一次。

**持久化的「總是展開技術細節」偏好不是第四種機制**（[ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 5）：**偏好只能改變「預設開合」，不能改變「有沒有這個東西」**。單向展開、不動文字與版面、可在一處反轉 → 可做。一旦它會**收起**任何東西（「精簡模式」），它就是模式，禁止。

**已知缺口：`RunTrace` 的模式是 component state，不在 URL 裡**（`useState<"general" | "advanced">`），所以一個進階視圖無法被連結、被分享、被貼進 issue。`03` `TRACE-007` 把 `?mode=advanced` 定為 API 參數，UI 從來沒有把它提到位址上。這削弱了「另一個位址」那一列本來提供的好處，而 Trace 恰好是最需要把「你看，這裡少了三筆」寄給別人的畫面。

---

## 2. 原則

十二條。前八條標了出處——它們不是新發明的，是已經散在程式碼註解、規格與殘項清單裡的判斷。接著三條（2.9～2.11）是 2026-08-22 依外部調查與逐頁對帳新增的，出處是外部證據加本產品的推導，見 ADR-041。**第十二條（2.12「進行中是第三軸」）隨 ADR-042 加入。第十三條（2.13「說明可移形，判斷不可」）隨 [ADR-065](../adr/ADR-065-hot-path-text-budget-and-the-fourth-disclosure-mechanism.md) 加入（2026-09-03）。**〔**2026-09-03 訂正**：本段與 §0 原本都寫「十一條」，而 §3 早已寫「十二條原則」——同一份文件裡的兩個數字。〕

**每一條都要能被反對。** 「簡單」「清楚」這種形容詞不是原則；一條沒有人可能不同意的規則，不會擋掉任何東西。

### 2.1 未知不是空白

缺資料的欄位顯示「未知」，不留白。空白會被讀成「通過」。

出處：[`02:DISC-004`](../plans/02-specifications-and-acceptance-criteria.md)；實作與推理見 [`Compare.tsx`](../../apps/web/src/pages/Compare.tsx) 檔頭——每一列宣告 `signature` 回傳 `undefined` 表示「沒有資料」，而不是空字串，正是為了讓「沒有」有一個渲染得出來的形狀。

**缺席有型別，見 §2.9。** 只寫「未知」已經不夠了。

同型例：[`RunEvaluation.tsx`](../../apps/web/src/pages/RunEvaluation.tsx) 的「**未評估不等於通過，也不等於未通過**」。

**這一條的強形式已經是既有實作，也一併寫進規則**：空狀態要說出這個「空」**不是**什麼。`WorkspaceSkills.tsx`「這裡是空的代表你還沒有建立過，不是清單讀取失敗」、`WorkspaceRuns.tsx`「代表沒有發生過，不是紀錄被清掉了」、`RiskIndicator.tsx`「這不等於安全」——三處以上的一致行為，只寫「說出來」會讓下一個人退回弱形式。

### 2.2 顯示與強制成對——**三向**

**不強制的東西不要顯示；會擋住人的東西必須在他撞上之前顯示；而每一條限制都要說得出誰在強制它。**

- **顯示但不強制**是最壞的一種：它讓使用者按下「我確認」。出處：[`04` 乙-2](../plans/04-backlog-and-handoffs.md)——`TokenBudget` 曾經寫進 `policy_snapshot`、顯示在權限摘要上要求確認，而平台與沙箱都不強制；原文是「**現狀（顯示但不強制）是兩者中最壞的一種**」。相同形狀後來又出現兩次（丙-18、丙-25）。
- **強制但不顯示**是第二壞的。匯入頁一個數字都沒有，而平台強制五項：32 MiB zip 上限、256 MiB 解壓上限、1 MiB 單檔掃描上限（**這一項還會靜默地限縮頁面回報的證據**）、三個 host 的來源白名單、https-only 且 URL 不得帶憑證。使用者只會在 4xx 訊息裡遇到它們。頁面上寫著「GitHub 或**允許的** URL」——點名了一份白名單，然後不給看。
- **強制了、也顯示了，但沒有出路**是第三種，`04` N-2 說它即將變成常態而非例外：使用者匯入的 Skill 一律 `redistribution: unknown`（＝擋），且目前沒有任何路徑可以改。**擋住人的訊息必須說下一步是什麼，或誠實說目前沒有下一步。**

**第三向：強制者必須具名**（[ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 4）。`01` §7.3 的 Local Runner 與遠端 MCP 會讓前兩向結構性失效——**平台對使用者自己的機器強制不了任何東西**，但那些限制是真的存在，隱藏它們比顯示它們更糟。

- 每一條限制帶一個強制者：**`平台`／`你的機器`／`第三方服務`**，三者措辭不同，而且**平台不得為它強制不了的事情背書**。
- **平台強制不了的事情不得用完成式。** 不寫「已阻擋」「不允許」，寫「這個 Skill 會做 X」＋「要不要讓它做，由你的機器決定」。
- **預設拒絕，而且拒絕狀態必須是可用的**（VS Code Restricted Mode 的形狀：未授權時內容仍可讀，只是不執行）。
- **授權是一次阻斷式的決定，不是被動通知。** 這條有實測撐著：VS Code 原本刻意做成不打擾的被動提示，結果授權率極低、而且使用者花時間以為自己弄壞了東西，最後改回強制對話框。本產品在這裡沒有比他們更好的條件。
- **執行前顯示命令**（`01` §9.2）。遠端 MCP 的名稱驗證要說出上限（§2.11(c)）：「這個名稱屬於 X」沒有說它做什麼。

判定的做法在 §3 第 11 條：**指出強制它的那一行**。指不出來就移除顯示。

> 引用校正：舊版本文把 `02:NFR-001` 寫成「UI 不得誤導」。`02:NFR-001` 標題是「安全」，其 UI 條款很窄——「靜態掃描通過不等於安全保證，UI 必須避免此類誤導」。本原則的權威來自 `04` 乙-2 那三次裁定與該窄條款，不是一條泛用的反誤導準則。

### 2.3 狀態先給文字，顏色是第二訊號

出處：[`02:NFR-007`](../plans/02-specifications-and-acceptance-criteria.md) 第 3 條「風險、相容與評估狀態必須**同時提供文字，不只依賴顏色**」。

實作上這條逐條寫進 CSS 註解：`.compare-differs` 的色底旁邊永遠有「有差異」徽章；`.badge-unverified` 的註解寫明色調是第二通道；`.criterion-unverifiable` 的虛線邊框註明「a third channel, **never the only one**」。

**外部佐證：** Chrome 與 Firefox 先後**移除**了網址列的鎖頭與 EV 綠條，因為量到 89% 的使用者把它讀成「這個網站可信」。正向的安全指示器被讀得比它的主張大，是有紀錄的失效模式，不是假想。

### 2.4 停用要說原因，**而且原因不能只活在 `title` 裡**

停用而不說原因會被讀成 bug。**原因才是這個功能誠實的部分。**

出處：[`index.css`](../../apps/web/src/index.css) `.filter-bar` 上方註解——「a disabled control with no stated cause reads as a bug, and the cause is the honest part of the feature」。

**這一條的範圍比原本寫的寬，因為同一個失效有四種形狀：**

1. 停用的控制項沒有原因（原本就涵蓋）。
2. 控制項被**拿掉**而不是停用，替代文字只說「目前不提供」不說為什麼。
3. 但書只存在於 `title=`：`LabelledBadge` 把伺服器給的每一句限定都放進 tooltip，包括「收錄不等於精選。」——那句話的存在理由就是阻止讀者把徽章當背書，而它在觸控裝置上不存在。
4. 徽章渲染原始 enum 而不是標籤。

`title` 只能重複已經看得見的東西。QA-009 拔掉那段文字上的 `opacity: 0.65` 正是因為它一度是整個 filter bar 裡**最難讀**的東西（2.18:1）。

### 2.5 執行狀態與任務判定是兩軸

執行成功 ≠ 任務完成。兩者永遠兩列，**任務判定排在前面**。

出處：[ADR-025](../adr/ADR-025-run-terminal-state-and-evaluation-verdict-separation.md)；實作在 [`RunEvaluation.tsx`](../../apps/web/src/pages/RunEvaluation.tsx) 的 `RUN_STATUS_LABEL`，宣告上方註明「Execution wording for a run's terminal state. **Never a pass/fail of the task.**」。

這條是全 app 被破壞得最頻繁的一條：`RunCompare` 曾把執行狀態排在判定之前（連註解都把錯的順序寫成本意）；`/runs/:id` 的執行狀態一度落在「有幫助／沒幫助」按鈕與版本選單之間，讀起來像回饋表單的一部分。

### 2.6 平面答案優先，識別符折疊

白話結論在上面；版本 id、內容雜湊、產生摘要的模型折進 `<details>`。

出處：`01` §2.1 與 §5 原則 7；實作在 [`SkillDetail.tsx`](../../apps/web/src/pages/SkillDetail.tsx) 的三個折疊區。

**推論一條：模型寫的東西不是答案。** SkillDetail 曾經把 AI 產生的白話摘要放在第一屏、把風險揭露推到 1.4 個視窗之後。在一個以「可判斷」為義務的產品上，模型的複述不能排在人可以查核的判定之前。

### 2.7 禁止用 `opacity` 弱化文字，也禁止色彩字面值

**次要文字只用字級降階，不用 `opacity`，也不另立一支更淡的灰。所有顏色都是 token。**

出處：`index.css` 的 QA-009 註解與 [`contrast.test.ts`](../../apps/web/src/contrast.test.ts) 檔頭。舊值把 `--text` 壓到 **2.18:1 ~ 3.71:1**，全數低於 AA。理由是「whatever value `--text` holds, **a multiplier lands somewhere else**」——`opacity` 是**任何色彩 token 都攔不住**的破壞方式，而 `contrast.test.ts` 量的是靜態 hex，量不到乘數（該檔「What it does not prove」第 2 點自己寫明）。

**這條原本是全文件唯一沒有任何守門、也沒有出現在 §6 對照表裡的原則。** 2026-08-22 補上：`design-system.test.ts` 現在對 token 區塊以外的任何 `#hex`／`rgb(`／`opacity:` FAIL。

### 2.8 毀滅性動作兩段式，且第二段要說會毀掉什麼

出處：[`ConfirmDelete.tsx`](../../apps/web/src/components/ConfirmDelete.tsx) 檔頭。機制共用（**七個呼叫點**），**只有範圍那句話每次不同，而那句話就是整個揭露**。

**四段式範本**，見 [`WorkspaceSkills.tsx`](../../apps/web/src/pages/WorkspaceSkills.tsx) 的 `scope`：會消失什麼、**還救得回來多久**、**什麼不受影響**、**還要另外刪什麼**。四段缺一段就是缺一段，寫的時候逐段對。**沒有救回期就寫「沒有」**——驗收條件刪掉就是沒有，不准為了湊滿四段而發明一個。

無障礙的兩個行為是承重的：確認鈕 `autoFocus`（前一顆按鈕離開 DOM，焦點會掉回 `<body>`），以及 `aria-describedby` 指向範圍句。**範圍句排在兩顆按鈕之前**——`aria-describedby` 帶得到螢幕閱讀器，帶不到眼睛，而確認鈕已經 `autoFocus` 了。

> 引用校正：`02:SEC-006` 的四條允收準則**沒有一條提到刪除範圍揭露**（它談的是刪除任務狀態、沙箱拆除與上傳授權）。`02:WS-002` 第 3 條寫的是「系統**應**說明刪除範圍」，沒有「在執行前」，而且是「應」不是「必須」。本條的實作比它引用的規格嚴——那是好事，但不要把它講成規格要求的。

### 2.9 缺席有型別，而且型別是資料層的列舉，不是渲染層的 falsy

**每一個「沒有值」的位置都要渲染一個具名詞，且該詞來自固定表。**

| 詞 | 意思 |
| --- | --- |
| `0` | 真值。零筆、零項、零位元組 |
| `不適用` | 這個物件在結構上沒有這個欄位 |
| `未測量` | 檢查沒有跑，或這個伺服器版本不回報 |
| `測量失敗` | 檢查跑了而且錯了 |
| `無權檢視` | 你的 Workspace scope 看不到（ADR-011） |
| `尚未定值` | **平台自己還沒決定**。與 §2.2 成對：這裡不准印一個沒被強制的數字，也不准留白 |

判法（單張截圖）：找 `—`、`N/A`、空欄、空判定區。**出現任何一個非表列的缺席呈現即 FAIL。「0」與「未測量」在同一張截圖上必須長得不一樣。**

出處與佐證：Grafana 把「查詢成功但零筆」（`No Data`）與「查詢失敗」（`Error`）分成兩個具名狀態，Datadog 另有 `Not Reporting`——兩家獨立收斂到同一條線。**沒有任何觀測工具處理 `無權檢視`，因為它們沒有這個問題；本產品有。** 學術上最接近的前例是 Eaton et al. (2011) 的 Coded 呈現：缺失處掛一個圖示說明**為什麼**缺，受試者明確偏好它；2024 的綜述直說這一塊「缺乏總覽與設計指引」。**沒有現成元件可抄，但形狀一致：缺席是列舉值。**

**繼承來的量測不是缺席。** 位元組相同的複本（Fork）沿用祖先的靜態掃描時，它是**有值且有歸屬**，不進上面那張表；規則與它的四個前提見 [ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 6。這裡只留一條邊界：**可繼承的只有「對位元組成立」的量測**（靜態掃描、規格驗證、License 判定）；相容性、試跑與評估量的是內容在某個環境下的行為，換工作區就換條件，**不可繼承，缺席就是 `未測量`**。

已知的兩個實例：`RunPreflight` 的成本區註解寫「Absent on an older server: rendered as nothing rather than as a zero」——避開了零值填補（對），落到了空白（本條 FAIL）；Run 產出的保存期限因為 PDM-006 未追認而不能印數字，正解是`尚未定值`，兩邊都不是。

**2026-09-04 沿試跑主線再量到三個**（[`04` 丙-147、丙-148](../plans/04-backlog-and-handoffs.md)）：評估成本的 `source` 從伺服器來是 `unreported`、契約沒有這個值、前端因此印「未測量（模型閘道實付）」——缺席詞對了，括號把它推翻；`RunEvaluation` 的「Judge prompt 版本：」沒跑 Judge 時是空白；`RunPreflight` 把 Skill 清單的讀取失敗印成「不在你的清單裡」——**這一個比空白更壞：它不是缺席呈現，是一個錯的主張**。同一條線上做對的更多，值得當範本：`failure_class` 缺席印「未記錄」、`status_reason` 缺席說「伺服器沒有給，不是被省略」、`cost_usd` 為 null 印「未測量」而不是 `US$0`、產出清單被截斷時說「無法據此判定沒有產生檔案」、`expires_at` 缺席印「尚未定值」。**判法補一條**：缺席詞後面跟著的括號或副句，不得推翻那個詞。

**讀取失敗不是缺席**（同日，[`04` 丙-145](../plans/04-backlog-and-handoffs.md)）：`RunTrace` 對自己的 `GET /runs/{id}` 失敗一個字都不說，失敗類別退成「未記錄」——那是把「讀不到」渲染成「沒記錄」，正是這張表把 `未測量` 與 `測量失敗` 分成兩格的理由。讀取失敗走 `ReadFailure`（§2.1 的 401 匯流點），不走這張表。

### 2.10 判準是後果不對稱，不是使用頻率——「永不折疊」清單

`<details>` 對**識別符與推導過程**是好工具，對**判斷依據**是設計缺陷。三種已記錄的失效：GOV.UK 的使用者研究發現有人不敢點（以為會離開本頁）；NN/g 說折疊內容無法被掃視，而本產品的十分鐘承諾整個建立在掃視上；NVDA 不播報 `<details>` 的開合狀態。

**文獻給的判準是「多少人需要看」，對本產品是錯的軸。** 風險摘要不是多數人需要，是**看漏了會出事**。改用後果不對稱推導出的封閉清單——以下每一項必須在**不互動**的情況下可見，不得進 `<details>`、`title`、hover 或第二個 tab：

1. **風險摘要的存在與最高嚴重度**（細項可折，「有 3 項風險，最高為 error」不可折）
2. **驗證狀態**（已驗證／未驗證／**未檢查**——三者是三個詞，見 §4.4）
3. **License 與可散布性判定**
4. **任何「不能打包／不能下載」的理由**（人工 hold、不可散布、授權未知、驗證未過）
5. **任一停用或被移除控制項的原因**（§2.4）
6. **執行前的權限、網路、工具與 MCP 清單**（`01` §5 原則 3；限額細節可折，權限語意不可折）
7. **任務判定**，含`未評估`（ADR-025；`未評估`尤其不可折，它的預設呈現是空白）
8. **毀滅性動作的範圍句**（折疊會讓 `aria-describedby` 的指向失效）
9. **平台的降級自述**（部分索引、證據無法回驗、已被取代、部分事件未送達）
10. **缺席的型別詞**（§2.9）。缺席的「為什麼」可以折；缺席「是哪一型」不可折

### 2.11 不做綜合分數，不把人氣放在信任主張旁邊，證據要說出自己的上限

三條，同一個推理，見 ADR-041。

**（a）不做綜合分數。** 最強的反對理由不是它不準，是**綜合分數必然把「沒檢查」映射到與「檢查過、沒事」同一條軸上**——OpenSSF Scorecard 的空 repo 在某些檢查上拿 10 分，就是義務 1.1 的失效在正式產品裡的樣子；Zahan et al.（ICSE-SEIP 2023）甚至量到分數與實際漏洞的關聯方向不對。分數是**把缺席轉換成看起來像核可**最有效率的機器。支持它的理由（十分鐘旅程裡只有一個數字活得下來、讓學習者不必讀六塊證據、讓排序可解釋）是真的，但輸給上面那一條。

**（b）風險用具名能力表達，不用等級；人氣不得與信任主張相鄰。** Socket.dev 的做法是對的形狀：它點名一個行為（安裝腳本、shell、網路、環境變數、混淆），不給等第——那天然滿足 §2.3。反面是下載數與星數：Aqua 的 Prettier typosquat 48 小時得到 1500 次安裝，惡意 VS Code 擴充達到數百萬次安裝，而安裝數是攻擊者可以灌的。`01` §5 原則 2 早已寫「不只依賴 Star」；本條把它變成禁令——**人氣數字不得出現在信任主張旁邊，也不得是預設排序**。

**（d）社會證明用試跑證據，不用評分**（[ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 1）。評分與排行榜是**永久不做**，不是延後：名次必須可比較，而本產品的證據天生不可比較——A 的驗收條件不是 B 的驗收條件。替代品是 MVP 之後的**試跑證據彙總**（「在 12 個工作區的 37 次試跑中，28 次符合**各自的**驗收條件」），四個入場條件缺一不可：**帶分母且在同一句話裡**、**永遠不是預設排序**、**同區塊說出它不涵蓋什麼**、**少於 5 個不同工作區時顯示 `未測量` 而不是隱藏**（且擁有者自己的工作區不計入分母）。它跨工作區，所以前置是同意書與逐工作區 opt-in；在那之前正確的呈現是 `尚未定值`。唯一允許的「排名」是逐次查詢的候選比較（現有的 Compare 畫面）。

**（c）每一個驗證徽章要在同一個區塊、以文字說出它不涵蓋什麼。** npm 的文件裡有「provenance 不保證套件沒有惡意程式碼」這句話，**頁面上的徽章沒有**；2026 年 5 月的 TanStack worm 帶著合格的 provenance 發了 84 個惡意版本。SLSA 等級的已記錄失效是同一個形狀（「L3 所以安全」）。這條是 §2.2 的手足：§2.2 擋掉顯示沒在強制的東西，本條擋掉顯示會被讀得比它主張更大的東西。

### 2.12 進行中是第三軸，不是判定的一個值

ADR-025 把執行狀態與任務判定分成兩軸（§2.5）。**進行中再切一刀**：它不是判定的一個較淺的值，也不是狀態的一個特例。全文件原本沒有時間軸——`RUN_STATUS_LABEL` 有五個非終態，而 §4.4 的四種視覺全是終態判定，app 裡的載入狀態當時是十二處各寫一次的 `<p>載入中…</p>`，那是慣例不是規則（**2026-08-25 訂正：慣例已經換掉了**——現在是 `components/Loading.tsx` 一個元件、~~24 個~~ **30 個呼叫點**〔2026-09-03 重數〕，每一個都帶 `role="status"` 與一個具名的主詞 `載入{what}中…`。`04` 丙-35 裁定這些**不是** §2.12：200ms 的讀取不需要「會不會自己結束」與「能不能離開」。剩下的兩處字面「載入中…」是「載入更多」按鈕的進行中標籤，不是頁面的載入狀態；`Compare` 另有一條帶分母的 `載入中…（N 個裡讀到 M 個）`）。規則如下（[ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 2）：

1. **進行中不得產生判定。** 還在跑的 Run，任務判定是 `未評估`（§2.9、§2.10 第 7 項），不是空白、不是樂觀猜測、不是一個較淺的顏色。**這一條對「評審還在跑」同樣成立**——2026-09-04 量到現行違規：評估 `pending` 時 `RunEvaluation` 的橫幅說進行中、底下的報告仍印「任務判定：無法判斷」，因為 `overall` 在建列時就被寫成 `undetermined`，而報告對任何有資料的評估都渲染。一句「以下結果尚未定案」的但書不能救它：讀者看到的第一個粗體仍是一個判定詞（[`04` 丙-146](../plans/04-backlog-and-handoffs.md)）。既有測試的 fixture 是 `pending` 疊在 `not_met` 上，一個伺服器不可能回的形狀，所以綠。
2. **進行中的畫面必須說出三件事**：現在在哪一步、這一步**會不會自己結束**、**使用者能不能離開**。第三件是十分鐘承諾裡最重要而最常被忘記的——可以關掉分頁就要說可以（而且依 §2.2，說之前要有人確認過那是真的）。
3. **必須有一個會變的量，以及一個「多久沒動了」的事實。** 永遠不動的轉圈分不出「在跑」與「卡住」；最後更新時間是最便宜的反卡死訊號，Trace 事件本來就帶時間戳。
4. **逾時、上限與配額在進行中畫面上同樣受 §2.2 約束。**
5. **部分資料必須標記為部分，而且分母要在**（與 §4.3 的截斷規則同一條）。
6. **含有進行中項目的清單，本身就是進行中的畫面**（2026-09-04 補，從第 3 條推出來：一列在跑，整頁的「多久沒動了」就成立）。所以清單要有兩件事：一個**可見的重新整理控制項**（或自動輪詢，但輪詢要說出間隔），以及**「上次取得於」的時間**——沒有它，一個停在「執行中」的列分不出是還在跑、還是這一頁三十分鐘沒更新。對照來源：另一個團隊的內部平台在每一份清單的工具列上放了「重新整理」，而我們的 Run 歷史（`WorkspaceRuns`）在有 Run 進行中時只靠焦點回來時的重抓，畫面上沒有任何東西說它多舊。**今天沒有機器**（§6）。

### 2.13 說明可以移形，判斷不可以；同一句話一頁講一次、一份清單講一次

出處：[ADR-065](../adr/ADR-065-hot-path-text-budget-and-the-fourth-disclosure-mechanism.md)（2026-09-03，負責人指示「熱路徑上的元件盡可能避免大量文字」）。**這一條是先量再寫的**：九個子代理逐檔數過 17 個頁面的每一段可見文字，結論與直覺相反——**畫面的重量不是教學，是同一句話印了 N 次**。首頁目錄態 12,118 字裡教學只佔 0.3%，但書佔 53%，而但書裡壓倒性的部分是四句跟著每一列逐位元相同地印 45 次的 `note`（約 6,000 字；`risk.note` 一句就 1,665 字）；評估頁八筆證據各掛一句同樣的回驗解釋（232 字，19%）；試跑前頁伺服器固定送六則 `notes[]`，四則是平鋪文字的複述（193 字，22%）；打包頁 381 字是詳情頁證據區塊的逐字複本。**§4.5 那個「65～83% 的字元屬於 `.note`」的指標因此不可行動**——`.note` 同時裝但書（順位 1）與教學（順位 5），詳情頁上但書還比教學多。

**先分類，預算只下在能動的類別上：**

| 類 | 是什麼 | 入不入預算 |
| --- | --- | --- |
| **A 判斷依據** | §2.10 十項；權限、額度、期限與判定 | **不入**。只能因為合併同義句而變少 |
| **B 原因** | 停用、缺席、被擋的原因（§2.4、§2.9）；`aria-describedby` 的目標 | **不入** |
| **C 但書** | 「這個徽章不涵蓋什麼」（§2.11(c)）、限制的強制者（§2.2 第三向） | **不入** |
| **H 限制** | 上限、白名單、保存天數——平台真的會擋的值 | **不入**（值與型別詞不動；推導可移形） |
| **G 進行中** | §2.12 的三件事、會變的量、多久沒動了 | **不入**（**會變的量永遠平鋪，不會變的理由才可以折**） |
| **D 說明** | 讀過一次就懂的教學。判準：**同一個人第二次來看，這段文字不改變他這次要按什麼** | **入預算** |
| **F 識別符與推導** | 雜湊、UUID、digest、映像名、「為什麼是這個順序」 | **入預算**（§2.6 本來就要折） |
| E 操作回饋 | 按鈕字、`role="status"` 的一句話 | 不計 |

**預算**：一條路由在預設狀態下（已登入、讀取成功、清單一列、未展開任何確認、旗標關），**平鋪的 D＋F 不得超過同頁 A＋B＋C，且單一 D 區塊不超過 100 字**。字數算法與 `FeedbackEntry.tsx` 的 `runes()` 同（Unicode code point）。門檻是 2026-09-03 的量測給的（四個試跑相關畫面只有 `/lab/run` 卡線），**先量再定值**；今天沒有機器（§6）。`02` 的允收準則釘住的解釋段（Token 輪數換算表，`02` 第 266 行的義務）算進預算但具名豁免，豁免清單只能變短。

**三條去重，前兩條是 §3 第 14 條的延伸：**

1. **逐列複述**：一段文字在同一份清單內逐位元相同地渲染超過一次，它就不是那一列的事實，是那份清單的事實；**提到清單層級講一次**。§2.10 的十項仍逐列可見，搬走的只是與列無關的那一句——§2.10 保護的是**能區分這一列與那一列的事實**，一句在 45 列上完全相同的話，讀者從第 2 列起就不可能因為它而作出不同判斷。樣板已在 repo：`Home.tsx` 目錄態的 `rankNoteInList={false}`、`RunVerdict.tsx` 的 `NOTE_SHOWN`（註解逐字「On a page of fifty rows a sentence per row is noise」）、`CompatibilityStatus.tsx` 對區塊句已涵蓋的軸送空 `note`。

   > **⚠️ 這條規則有一個前提，而它是落地當天靠突變才發現的，不補上去會弄丟 §2.9。**
   >
   > **一句話只有在「每一列身上都還戴著一個對得回去的詞」時才准離開那一列。** 首頁的 `risk.note` 有兩種值（「掃過了」與「從未掃描」），而**從未掃描的那一列身上一個標記都沒有**；把兩句一起提到清單上方，畫面會同時印出兩種說法而讀者分不出眼前這一列屬於哪一種——那不是精簡，是把一個 §2.9 的型別詞弄丟。第一版程式就是這樣寫的，`DISC-002` 那支測試當場變紅。
   >
   > 所以判準是三分的：**(a)** 整份清單逐位元相同 → 提上去一行；**(b)** 有幾種值，但每一列都戴著挑出自己那一行的詞（`tier`、`category` 的徽章標籤）→ 每個相異值一行 `{facet}「{標籤}」：{note}`；**(c) 沒有那個詞、又有多種值 → 不准搬。** 徽章與它的標籤永遠留在列上，搬走的只有句子。
   >
   > **反過來，兩列剛好長一樣也不等於它是清單的事實**：一句掛在某個風險旗標上的解釋，會因為兩列碰巧帶同一個旗標而重複，但第三列換一個旗標就換一句話——那是**旗標**的事實，留在列上。機器分不出這兩種，所以它有一份具名、寫理由、只能變短的豁免清單（§6）。

   **✅ 2026-09-03 落地**（丙-142 第一批）：首頁四句 **−8,493 字**（真實伺服器字串 × 45 列 seed 目錄；遠大於原估，因為原估引的是 fixture 的短字串）、評估頁 `MATCH_NOTE` 徽章化、Trace 產出兩句、Run 比較兩句、下載紀錄五句、`WorkspaceSkills` 的相容性缺席句、打包頁每個目標的 INSTALL.md 句。**機器同日補上**（§6）。
2. **同頁同義句**：`title=` 與可見 `.note` 同字算兩次。**§2.4 的補句：原因搬出 `title` 之後，`title` 要拿掉**——那條規則寫的是「不可以**只**在 tooltip 裡」，它允許 tooltip，沒有要求 tooltip；句子一旦看得見，tooltip 只讓螢幕閱讀器把它念第二遍。<br>**✅ 2026-09-03 落地**：`LabelledBadge`、`RiskIndicator`、`Compare`、`SkillDetail`（三處）、`WorkspaceRuns`、`DownloadArtifactFacts` 的 `title` 全數移除。**其中 `LabelledBadge` 那一個在去重之後會變成錯的**：一列的 note 被提到清單層級之後，殘留的 `title` 就是那一列**唯一**的副本，也就是 §2.4 第 3 項本身——而 `design-system.test.ts` 看不到（它比對的是同一個檔案裡的字串，另一個分支仍然渲染 `{value.note}`）。<br>同一條規則在一頁講兩次以上的也留一份：Test Case 兩頁的快照規則（**三份，不是四份**——原本記成四份的第四處其實是「開始 Run 前會再確認一次權限摘要」的預告，屬 §2.2，不可刪）、「刪東西去哪裡」的地圖四份 457 字、試跑前頁重複的伺服器 `notes[]`。
3. **跨頁複本**：§3 第 14 條只管同一頁；打包頁 381 字的詳情頁複本這一格**沒有人裁過**，記 `05` R-42 (c)，本檔不代簽。

**Tip 的六條入場條件（缺一不可；它是 §1.3 的第四種機制）：**

1. **只准裝 D 與 F。** A／B／C／G 一律不准進去，與 `<details>` 同一條。
2. **不得是 `title=`、不得是 hover-only。** 點擊／Enter／Space 觸發，內容在 DOM 裡、可鍵盤到達、觸發鈕帶 `aria-expanded`（ARIA APG Disclosure 的形狀）；Esc 關閉；開合不改變其他內容的版面高度——會改的就是 `<details>`，走那一列。
3. **錨點必須自己成立**：被限定的那個東西本身（徽章、數字、一句完整的話）或帶主詞／帶數量的短語。「為什麼可以關掉這一頁」合格；「詳情」「說明」「?」與沒有文字的圖示不合格。
4. **觸發鈕永遠帶可見文字**，命中區 ≥24×24 CSS px（WCAG 2.5.8）、有描述用途的 accessible name（1.1.1）；`aria-label` 不能代替可見字。
5. **一頁至多三個。** 十個問號和沒有問號一樣沒有指向（同 §4.6.3「一頁一個主要動作」的理由）。
6. **需要很多 Tip 是設計訊號不是解法**：先去重、先改文案，Tip 是最後一步。

**標籤與圖示**：一段文字的內容是一個狀態、一個分類或一個計數，就用帶字的徽章或 `a.chip`，不用句子（評估頁的引文回驗結果改成 `已逐字回驗`／`正規化後比對`／`找不到`／`未回驗引文` 四個徽章，解釋進 Tip，省 232 字而 §2.11(c) 的第一訊號完整保留）。圖示的規則在 §4.7：**永遠是第三訊號、永遠伴隨可見文字**。

**「看過一次就收起來」＝對所有人預設收合，不記住誰看過**（ADR-065 決策 6；ADR-042 決策 5 不變）。一段 D 類文字對所有人預設就收在一個帶主詞的錨點後面，第一次來的人多按一下，第二次來的人不用按，兩者看到的畫面一樣；「記住已讀並自動收起」的持久化變體記 `05` R-42 (a)，待負責人簽。

**與 §4.5 同日裁定的關係**：§4.5 的「一個字都沒有刪、沒有折、沒有改寫」是 40em 那一批對**行長**的裁定，它對 A／B／C／H／G 仍然成立；本條對 D／F 與複述的**量**下裁定。兩份管不同的軸。

---

## 3. 評估準則（checklist）

**這是拿著逐頁走的那把尺。** 每一條都要能回答「這一頁過或不過」。視窗固定為**桌面 1280×900、手機 375×900**。標記：`自動` = 有測試會 FAIL；`手動` = 目前只能靠人看。

舊版本只折了十一條規則裡的五條——**§2.2、§2.5、§2.7、§2.8 一條 checklist 也沒有**，而 §2.2 是全文件唯一有三次獨立事故紀錄的原則。以下十七條把十二條原則全部涵蓋（第 18 條 2026-09-03 隨 §4.6 加入，守的不是原則是視覺層的一條硬規則；**第 19 條同日隨 §2.13 加入，涵蓋第十三條原則**）。

| # | 問題 | 不過的樣子 | 誰在看 |
| --- | --- | --- | --- |
| 1 | **頭條在第一屏，而且是第一屏裡的第一個東西嗎？** | 識別碼、版本號、麵包屑或一整排控制項排在答案前面；**或這一頁根本沒有頭條** | 手動（§1.1＋§1.2） |
| 2 | **答案有被標記成答案嗎？** 它有自己的標題、live region 或明確的第一句話嗎？ | 答案在畫面上，但沒有任何東西說它是答案（匯入被擋下卻只印一份清單） | 手動（§1.1） |
| 3 | **每個「沒有資料」的地方，用了 §2.9 那六個詞之一嗎？** 「0」和「未測量」長得不一樣嗎？ | 空欄、`—`、`N/A`、被 `.filter()` 濾掉的零 | 手動（§2.9） |
| 4 | **手上有、判斷用得到的資料，有顯示出來嗎？** | 型別裡有、伺服器送了、頁面丟掉（`size_bytes`、`status_reason`、`details`） | 手動（§1.1） |
| 5 | **每個狀態拿掉顏色之後還讀得懂嗎？** | 只有色底／只有邊框顏色／只有紅字，沒有詞 | 手動（§2.3；`02:NFR-007`） |
| 6 | **每個不能用、不適用或被拿掉的控制項，有說原因嗎？原因是看得見的文字，不是 `title`？** | 灰掉的 select 旁邊什麼都沒有；但書只在 tooltip 裡 | 部分**自動**（`[disabled][title]` 必須有 `aria-describedby`），其餘手動（§2.4） |
| 7 | **§2.10 清單上的十項，有任何一項被折疊、藏進 `title` 或推到第二個 tab 嗎？** | 一個關閉的 `<summary>` 後面藏著風險、驗證狀態或停用理由 | 手動（§2.10） |
| 8 | **進階／識別符資訊折疊了嗎？平面答案在上面嗎？** 適用於**以敘述回答一個問題**的畫面；以欄位對照為主體的表格不受此條約束，但識別符欄位不得是第一列 | 雜湊、UUID、版本號、原始 enum、機器格式時間戳與白話結論平鋪同一層 | 手動（§2.6） |
| 9 | **標題層級反映真實結構嗎？** 從屬段落是 `h3` 而不是第二個 `h2`；分節標題是 heading 而不是 `<p>`；這一頁有 `h1` 嗎？ | 一頁八個 `h2` 互為兄弟／分組標題是 `<p>`／整頁沒有 `h1`／最重要的區塊只有 `<caption>` | 半**自動**（`__outlines__/` 快照使變更成為必須核可的 diff），判讀仍手動 | 
| 10 | **清單項目該是卡片嗎？** 判準見 §4.3 | 帶著自己的動作或自己的子清單的一則資料，長得像一疊段落 | 手動（§4.3） |
| 11 | **畫面上每個上限、額度、逾時、鎖、期限與權限摘要，指得出強制它的那一行嗎？反過來，會擋住人的限制在他撞上之前看得見嗎？** | 指不出強制點；或指得出但值不一樣；或限制只在 4xx 訊息裡出現；或擋住了而沒說下一步 | 手動（§2.2）——**指不出來就移除顯示** |
| 12 | **執行狀態與任務判定是兩列，判定在前嗎？** | 只有一列；或判定被寫成腳註；或兩者被別的東西隔開 | 手動（§2.5） |
| 13 | **毀滅性動作是兩段式的嗎？第二段的四段式範圍句齊了嗎？範圍句排在按鈕之前嗎？** | 一顆「刪除」按下去就沒了；或範圍句缺「什麼不受影響」 | 手動（§2.8） |
| 14 | **同一個事實，這一頁講了幾次？講得一樣嗎？** | 一列上同時寫著「狀態：可下載」「已過期」「目前不提供下載」 | 手動（§2.2＋`02:NFR-007`） |
| 15 | **有任何元素蓋住另一個元素的文字嗎？兩段文字之間有間距嗎？** | `inline-flex` 沒宣告 `gap`，兩個 `<span>` 黏成 `SKILL.md42 bytes`；chip 溢出自己的 cell 蓋到鄰欄 | **部分自動**（2026-09-03：`rendered.spec.ts` 現在守「`.note` 緊貼前一個徽章」這一種形狀，見 §6），其餘手動——**而 `.badge` 那次是在桌面 1280px 發生的，不要只在手機上找** |
| 16 | **375px 下不橫捲；字級與間距在尺度上；沒有色彩字面值與 `opacity`；markup 裡的 class 在 CSS 裡有規則** | 見 §6 | **自動（四項）**（2026-08-25 一日之內兩次訂正：上午查出第四項從來沒有守門、把數字改成三；當日稍晚補上守門，數字回到四。中間那半天是這一列**第一次說實話**——在那之前它宣稱了一個不存在的檢查） |
| 17 | **進行中的畫面說得出「在哪一步／會不會自己結束／能不能離開」嗎？有一個會變的量與一個「多久沒動了」嗎？進行中有沒有偷偷產生判定？** | 一個永遠不動的 `<p>載入中…</p>`；或一列還在跑的 Run 已經帶著判定色；或截斷的清單沒有總數 | 手動（§2.12＋§4.3 截斷條款） |
| 18 | **這一頁有幾個填色的主要動作？是「完成這一頁的工作」的那一個嗎？有沒有任何主張（徽章）被填了色？** | 兩顆填色按鈕；或「刪除」是填色的；或一個填色的「已驗證」徽章 | **部分自動**（`rendered.spec.ts`：全部路由、至多一個、只能是 `.action`、`.badge` 不得填色——**只在瀏覽器層**，jsdom 不解析 `var()` 所以結構上量不到，放回第二個 `.action` 時 342 支 jsdom 全綠而它紅）；「是不是這一頁的工作」仍手動（§4.6.3） |
| 19 | **這一頁有沒有一句話在同一份清單裡逐字印了 N 次？平鋪的說明與識別符（D＋F）有沒有超過判斷依據（A＋B＋C）？每一個 Tip 裡裝的都只是「為什麼」、錨點自己成立、一頁不超過三個嗎？拿掉圖示之後每個狀態還讀得懂嗎？** | 45 張卡各帶同一句 `risk.note`；一段 118 字的教學平鋪在按鈕上方；一個 `?` 圖示後面藏著停用理由；徽章與它的 `title` 同字 | 手動（§2.13）——**四個子項今天一個都沒有機器**，建議形狀見 §6 |

第 7、10、11、14 條是這張表目前最有價值的四條，因為**它們是沒有任何機器在看、而且各自都已經出過事的**。~~第 17 條是唯一一條**還沒有任何實例**的~~ **（2026-08-25 訂正：它有實例了。）** [`components/InFlight.tsx`](../../apps/web/src/components/InFlight.tsx) 就是照這一條寫的，渲染在 `/runs/$runId`（`RunTrace`）上、Run 進入終態就不再出現：哪一步（`RUN_STATUS_LABEL`）、會不會自己結束（會）、能不能離開（可以，而且說得出憑什麼——River job 在 Postgres、Trace 由沙箱直接回報，兩者都不經過瀏覽器〔**2026-09-03 訂正**：這段推導自丙-142 起**不在畫面上**了，它搬進了 `InFlight.tsx` 的 JSDoc。畫面上是 `可以關掉這一頁（平台在跑，不是你的瀏覽器）`——§2.2 第三向要的強制者具名仍然滿足，而§2.13 的判準是「會變的量永遠平鋪，不會變的理由才可以折」：這條路徑對每一個 Run 都一樣，是後者〕，那是 §2.2 要的那一行）、一個會變的量（已記錄的事件數）與一個「多久沒動了」（最後一個事件的時間）。**它原本是為 `01` §7.3 的長時間工作與串流先寫好的（見 §7），結果第一個用到它的是本來就在的 Run 頁。**

**2026-09-04 外部對照：拿這張表量別人的畫面。** 另一個團隊的內部平台（Agent／知識庫／MCP／應用／排程五種「版本庫」的清單頁，每個版本庫一張卡）走一遍這張表：第 3 條抓到一個沒有型別的「—」（複製圖示後面）；第 14 條抓到五個分頁各印一次「版本庫」而標題已經是「版本庫」；§2.12 抓到「更新於 2026/8/28 上午9:08:33」這種到秒的原始絕對時間；§4.7 抓到卡片右下角只有「…」的按鈕；§4.3 抓到描述在句子中間被「…」截斷而沒有任何不互動就看得到的版本。**它做對、而這份文件原本沒寫的有兩件，已補進 §2.12 第 6 條與 §4.3**：清單工具列上的「重新整理」，與跟著分頁換的搜尋範圍。它做的、而我們刻意不做的有一件：導覽列放「探索」（IA-1 的裁定相反，理由在 IA R7）。這段的用處不是評分，是證明這張表對不是我們寫的畫面也說得出話，而且說出來的每一條都指得回上面某一列。

---

## 4. 系統

### 4.1 字級尺度

`index.css` 宣告 **11** 個 `font-size` 值。其中 **3** 個只在 `@media (max-width: 1024px)` 出現（36、30、16），其餘 8 個是桌面實際使用的階。20px 與 18px 各自**同時**是某一階的桌面值與另一階的 ≤1024px 值，所以「12 減 5」那種算法是錯的（舊版本文如此，結論對而算式錯）。

| 階 | 桌面 | ≤1024px | 工作 | 不要用在 |
| --- | --- | --- | --- | --- |
| hero | **56px** | **36px** | **只有首頁**那一個 `h1` | 任何其他路由。它曾套在每一條路由上，讓「資料保存政策」跟搜尋頁的一行邀請語一樣大 |
| page | **40px** | **30px** | 一般 `h1` | — |
| section | **24px** | **20px** | `h2` | — |
| sub | **20px** | **18px** | `h3`、`.verdict` | — |
| body | **18px** | **16px** | 內文、`h4` | — |
| ui | **15px** | — | 控制項、`.app-nav`、行內 `code` | — |
| meta | **14px** | — | `.note`／`caption`／label／徽章／次要資訊 | — |
| micro | **12px** | — | **只給等寬字** | **非等寬字**。mono 在同字級下視覺偏大（12px mono ≈ 14px sans），非等寬降到 12px 就是把 §2.7 的問題換個做法再犯——`.filter-unavailable .note` 已經犯過一次 |

**hero 與 page 是兩個工作，不是同一個尺寸的兩次品味判定。** 同一個 `h1` 前後被判兩次而結論相反，就是因為當時沒有這一行。

`.badge` 原本的 13px 已於 2026-08-22 併入 meta。

### 4.2 間距尺度

4px 網格，七階：**4px** ／ **8px** ／ **12px** ／ **16px** ／ **20px** ／ **24px** ／ **32px**。

`index.css` 的 `padding`／`margin`／`gap` 目前出現 **9** 個值，上面 7 個在網格上，另外 **`5px` 與 `10px`** 是 §5 登記的兩條偏離，兩條都有推導。

原本的 `2px`（三處視覺微調）與 `6px`（八處「緊密清單間距」）已於 2026-08-22 全數收掉——**8px 這一階涵蓋得了它們，所以它們沒有資格成階**（§5 的入場規則）。

### 4.3 表面（surface）

| 表面 | 樣式 | 什麼時候用 | 不要用在 |
| --- | --- | --- | --- |
| **卡片** | `border: 1px solid var(--border)` ＋ `border-radius: 8px` ＋ `padding: 10px 12px` ＋ **`background: var(--surface)`**（2026-09-03，§4.6.2：卡片是「面」，頁是「地」） | 見下方判準 | 導覽清單。整份確認裡的條目（判斷單位是整份摘要，不是它的一行） |
| **notice** | `border-left: 3px solid var(--accent)` ＋ `background: var(--accent-bg)` ＋ `padding: 8px 12px`；**阻斷版 `.notice-danger`**（2026-09-03）換 `--danger`／`--danger-bg`，`[role="alert"]` 的區塊同款 | **平台對這一頁講的一個持續狀態**：降級、部分索引、未評估、已被取代；**不能打包的理由、授權審查中功能關閉、無法套用建議**三處是阻斷版 | 使用者自己動作的**當下結果**（那是 `role="status"` 的一句話）；**內含按鈕的確認對話**；單純的補充說明（那是 `.note`）。不是「非阻斷」的同義詞 |
| **裸區塊** | 無 | 敘述文字、說明段落 | — |
| **右欄**（`aside.detail-rail`，2026-09-03） | 與卡片**同一份配方**（`--surface`、`1px solid var(--border)`、`8px`、padding 12px）＋ ≥1024 `sticky` | **一頁的操作與各自的理由**：試跑、以此為起點、檔案樹、上傳新版本。判定不進去（§2.10 的東西留在主欄），主要動作也不進去 | 任何不是操作的東西；第二種卡片樣式（它不是第五種樣式，是卡片配方的另一個用法） |
| **chip**（`a.chip`） | 次要按鈕的盒（描邊、藥丸）；選中＝凹面＋字重 | **導覽用的分類列**：它是控制項 | 任何主張（那是 badge）；填色（那是 `.action`） |

**卡片判準（2026-08-22 改寫）：**

> **一列是卡片，若且唯若它帶著（a）自己的控制項或連結，或（b）自己的巢狀子清單。**

**為什麼換掉舊判準。** 舊的「一則可以被單獨採信或拒絕的東西」不可證偽——任何清單都能論證成可獨立判斷。曾經提過的收緊是「自己的動作**或**自己的識別」，那更糟：**每一列都有識別碼**，這個析取式對全部十二個清單都成立，其中七個答錯（檔案樹、Trace 事件、`.tag-list`、`.risk-list`、`.compat-list`、`.evidence-list` 都不該是卡片）。新判準對十二個清單全部給出正確答案，**而且它抓到了一個當時還沒修的缺陷**：`.finding-list` 的每一則都帶著自己的 `<EvidenceList>`，就排在 `.criterion` 卡片正下方，卻是一疊裸段落——那是 commit `90ade82` 修掉的「Test Case 列不是卡片」的現行未修版本。

**現行卡片族與它們的真實用途**（舊版本文按 class 名稱猜用途，猜錯兩個）：

| class | 誰在用 |
| --- | --- |
| `.search-result` | 搜尋結果、Test Case 列、Workspace Skill 列、**Skill 詳情的逐版歷史**（**四處**；2026-09-03 訂正，原寫三處） |
| `.criterion` / `.suggestion` | 驗收條件、改善建議、**匯入與打包的驗證發現**（`.finding-list` 於 2026-08-22 套用） |
| `.packaging-target` | 打包目標 |
| `.download-item` | **這是全 app 的通用卡片，不是「下載紀錄」**——**八個呼叫點裡七個不是下載**：Run 產出、Workspace Run 歷史、Test Case 執行歷史、Run 比較、以及建立中心的三張卡（2026-09-03 訂正，原寫四個呼叫點；**論點沒有被推翻，是變強了**） |

**修法一律是套用既有的卡片族，不是發明第五種樣式。**

**清單長到數百列時怎麼辦**（[ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 決策 3）。判準**不是列數**——卡片在大清單上塌掉的原因不是視覺密度，是 §2.10：一則卡片要放得下風險摘要、驗證狀態、可散布性與停用理由，那些一項都不准折疊，而把卡片壓扁成表格列的唯一方法就是把它們擠掉。判準是**這一頁在問什麼**：

- 問「**這一則行不行**」→ 卡片（逐則判斷，§2.10 的十項要放得下）。
- 問「**哪一則最好**」→ 表格（跨則比較；此時 §2.10 的項目改以**欄**存在，每一則仍然看得到）。
- 一個螢幕裝不下時，正解是**把它縮小到可判斷的數量**（篩選、搜尋、範圍），不是把每一則削薄。**而縮小它的那個控制項要說出它縮小的是哪一份清單**（2026-09-04 補）：搜尋框的 placeholder 與篩選器的標籤要帶清單的名字（「搜尋 Test Case」而不是「搜尋…」）。理由與 IA R5 同一條——「搜尋」單獨出現是容器詞，而一頁有兩份清單時（`/lab/test-cases` 的清單與建立表單的選單）讀者分不出它作用在哪一份；IA-10 那次事故正是「範圍參數與選擇器對不同集合說話」。對照來源：另一個團隊的內部平台，五個分頁共用一個搜尋框，placeholder 跟著分頁換成「搜尋 排程 版本庫」。**今天沒有機器**，現行三個 placeholder（首頁、Run 比較、Test Case 的 slug）都是例句而不是範圍，因為那三個框都只對一份清單說話。

**外加一條硬規則：任何被截斷的清單都必須說出總數與截斷理由**——「共 N 筆，這裡顯示 M 筆，因為 X」。本產品四種分頁策略並存而只有 Trace 做對；策略收斂為三種：**「載入更多」**用於使用者自己的、只增不減的時間序資料；**cursor 上一頁／下一頁**用於需要穩定位置的證據流；**硬截斷**只有在同時給出總數與理由時才允許。~~**「完全不設限」不再是選項**——搜尋結果目前落在這一格。~~ **（2026-08-25 訂正：半格，不是整格。）** 搜尋結果與 Workspace Skill 清單現在都**說出自己被截斷了**（`pages/Home.tsx`、`pages/WorkspaceSkills.tsx`，各一段 `notice` `role="status"`），所以「完全不設限」那一半已經不成立。**但另一半還在**：兩處說的都是「**超過 N 個**」，那是一個**下界**，而同一段硬規則要的是「**共 N 筆**」——一個下界說不出「共」，讀者無法從 `超過 20 個` 分辨 21 與 2100。**截斷理由那一半是齊的**（兩處都寫了為什麼、也都誠實說目前沒有翻頁），**總數那一半沒有**，而缺的是伺服器的一個計數欄位，不是頁面的一句話。~~**這一格記成半關，不是關。**~~ **2026-08-25 稍晚：整格關閉。** 伺服器補上 `total`，兩個畫面都改說「**共 N 筆**」。計數是 `count(*) OVER ()` 長在檢索語句自己裡面，**不是第二支 COUNT 查詢**——平行的計數得把每一條述詞再寫一次，而兩份複述第一次不一致的那天，頁面就會報出一個不描述它底下那張清單的總數，那比它取代的下界更糟。<br>**它的天花板照樣寫出來**（一個悄悄不再是總數的總數，正是這個欄位存在的理由）：混合檢索的兩條腿各取 50 筆候選，所以那條路上算的是「在至多 100 筆候選裡通過篩選的」。目前索引 45 篇，對每一個查詢都是精確值；**超過 100 筆候選之後它會變回下界，而名字還叫總數**——那時要把篩選推進候選 CTE，與該處 ponytail 註解本來就要的是同一個修改、同一個觸發條件。降級的純詞彙路徑沒有候選窗，永遠精確。<br>`truncated` 為假時 `total` 必須等於列數，那是釘住漂移的那條斷言。

### 4.4 狀態語彙

| 視覺 | 主張 | 現行 class（**完整**） |
| --- | --- | --- |
| **badge（永遠帶文字）** | 一個事實的標籤 | `LabelledBadge` 的文案由伺服器給——**但這條規則只對 `LabelledBadge` 的三種 kind 成立**，見下方 |
| **`--danger` 邊框＋文字** | 這件事不通過／會擋住你 | `.badge-danger`、`.badge-expired`、`.badge-compat-failed`、`.badge-risk`、`.badge-risk-flag`、`.badge-criterion-failed`、`.badge-severity-error`、`.script-tag`；`.criterion-failed` **只有邊框沒有文字色** |
| **`--accent-border`（僅邊框）** | 這件事未知、未驗證或**未檢查** | `.badge-license-unknown`、`.badge-trust-unknown`、`.badge-compat-unverified`、`.badge-source-model`、`.badge-untested`、`.badge-unverified`、`.badge-criterion-undetermined`、`.badge-severity-warning`、`.criterion-undetermined`、**`.badge-source-unknown`**；`button:hover` 也用它。〔**2026-09-03 訂正**：補上 `.badge-source-unknown`（`index.css` 與 `.badge-license-unknown` 同一條規則），並移走 `.badge-criterion-unverifiable`——它在 CSS 裡**只有 `border-style: dashed`、沒有 `--accent-border`**，屬於下一列的虛線邊框。這一格的標題自稱「完整」，所以錯一個就是錯的〕 |
| **虛線邊框** | 這個東西**不是完整有效的**：平台自己降級了判定，或控制項現在不能用 | `.criterion-unverifiable`、`button:disabled` 等 |
| **無修飾的 badge** | 通過／正常。**這是合法的**——§2.3 只要求詞在前面，沒要求每個狀態都有顏色 | `.badge` 本身 |

**四條規則：**

1. **一個檢查產生的徽章，必須是 `通過` / `未通過` / `未執行` 三者之一，而 `未執行` 要說出是哪個檢查沒跑。** 業界的主流失效正是「沒跑的檢查」與「跑過而通過」長得一樣（Hugging Face 的安全標記自陳 best-effort；OpenSSF 空 repo 拿 10 分）。目前 `--accent-border` 同時承載「未驗證」與「未檢查」，**詞分開了、視覺沒分開**——§2.3 允許這樣，但那正是為什麼詞不能省。
2. **顏色與線型永遠是第二或第三訊號。** `--accent`（`#aa3bff`）在 `--bg` 上是 4.39:1，低於 AA，所以它可以描邊，**不能當文字顏色**；連結另有 `--link`。
3. **class 名字要命名主張，不要命名某一個領域的理由。** `.badge-expired` 的宣告與 `.badge-severity-error` 完全相同，但名字寫死了「過期」，於是「清理失敗」需要同一個視覺時無處可去。`.badge-danger` 是主張的名字；兩個舊名保留，因為使用它們的頁面命名的是真實的東西。
4. **`--accent-border` 目前有一個名不副實的用法**：`.badge-differs` 指的是「有差異」，那是一個肯定的發現，不是未知。這一格待裁。

**§4.4 第 1 列的規則與契約現在是矛盾的，這裡直說。** 全 app 有 **20 個**前端 enum→中文對照表，因為契約在 `tier`／`trust`／`license`／`redistribution` 上送 `{value, label, note}`，在 `status`／`cleanup_status`／compatibility 三軸／risk flags／artifact status 上只送裸 enum。**刪掉對照表會讓 `pending` 直接出現在畫面上，那違反 `02:NFR-007` 第 3 條**（「風險、相容與評估狀態必須同時提供文字」——它剛好點名了風險與相容）。所以：規則對，契約沒跟上，這是**契約缺口不是前端缺陷**。逐項裁定與補法見 [丙-29](../plans/04-backlog-and-handoffs.md)。

**兩件要記住，免得把這一格想得比它大或比它小：**

- **引用更正**：本檔前一版把「兩個平面不得把同一件事講成兩種話」記成 `02:NFR-001`。`02:NFR-001` 標題是「安全」，其唯一的 UI 條款是「靜態掃描通過不等於安全保證，UI 必須避免此類誤導」，**沒有一句談兩平面措辭一致**。那句話是詮釋，站得住腳但不是規格文字；真正承重的是 `NFR-007` 第 3 條。
- **五個缺口只解釋 20 份對照表裡的 8 份。** 補完契約還會剩 12 份。這一格不是靠一次契約改動關掉的。

~~**這條規則不是理論——它已經抓到兩處現行分歧，兩處都不需要契約改動就修得掉**：風險旗標有兩份清單，搜尋列那份把「**可執行**」拿掉了（不是比較短的標籤，是比較小的主張）；`Home.tsx` 用行內三元把 `spec_validation` 的三個值壓成兩個，`failed` 會被講成「未驗證」。~~

**這條規則不是理論——它抓到的兩處現行分歧都已經修掉了**（2026-08-25 複驗於程式，原文以現在式留在上面當證據）：

- **風險旗標的兩份清單併成一份**，而且是**伺服器措辭**的那一份：`components/RiskIndicator.tsx` 現在只渲染 `risk.disclosures` 的 `label`，兩個端點讀同一份目錄。少掉「可執行」那件事因此不可能再發生——它不再是前端的兩個 boolean 集合。附帶效果是一個 `keyof` 聯集拿不到的性質：**這個 build 沒見過的代碼也渲染得出來**。
- **`Home.tsx` 的行內三元換成了伺服器給的標籤**：`hit.compatibility.spec_validation.label`，三個值原封不動，`failed` 不再被講成「未驗證」。

**這一格因此從「規則抓到了兩件事」變成「規則抓到的兩件事修完了」，但上一格的 20 份對照表沒有跟著少到零**——這兩處走的是契約補完那條路（`04` 丙-29），不是前端自己改字。

### 4.5 版面

- `#root`：`width: 1126px`、`max-width: 100%`、置中、左右各一條 `--border`。
- `main`：`padding: 0 24px`。
- 斷點兩個：**1024px**（字級降階）與 **640px**。640px 的區塊有**兩條**規則，不是一條：`main`／`.app-header`／`.app-footer` 的 `padding-inline` 改 12px，**以及**比較表列首欄由 220px 縮為 140px。640px 是後補的——註解寫「The file had three breakpoints before this and all three were 1024px, i.e. **the app had no phone layout at all**」。
- `.filter-bar` 的 320px grid track 是從 1126px 反推的：要讓一頁落三軌而不是四軌。改頁寬要一起重算。
- **行長上限 `40em`**（`main :is(p, li, dd, dt, summary)`，2026-09-03 新增）。**`em` 不是 px，這是規則本身**：40em 在字級尺度的任何一階上都是 40 個中文字，同時也是約 80 個西文字元——一個值把兩種文字的舒適行長一起服務了。表格刻意不在選擇器裡：儲存格的寬度是那張表的決定，把它夾窄只會把留白往裡面搬一層。<br>**它是量出來才加的，不是品味**：`#root` 是 1126px，而在這之前**每一段也都是**。2026-09-03 逐頁量的結果——18 條路由的最長行落在 **60～104 個中文字**（`packaging` 的 104），對照舒適的 25～40；同一批量到 8 條路由有 **65～83% 的字元屬於 `.note`**。兩件事合起來就是「整頁文字非常滿」的形狀:**問題在文字的形狀，不在它的量**，所以修法是 §0 的「讓步的是形式」——一個字都沒有刪、沒有折、沒有改寫。〔**2026-09-03 稍晚補記**：這一句管的是**行長**那一批，仍然成立；同日 [ADR-065](../adr/ADR-065-hot-path-text-budget-and-the-fourth-disclosure-mechanism.md) 對**量**另下裁定（§2.13）——A／B／C／H／G 的量一個字不動，D／F 與逐列複述要動。「65～83% 屬於 `.note`」這個數字自此只當量級看：`.note` 同時裝但書與教學，詳情頁上但書 316 字比教學 194 字還多，把它當減量目標會先砍到順位最高的那類文字〕<br>**代價要寫出來**：行變窄，頁就變高（`home-results` 1683→1944、`skill-detail` 2845→2932、`run-trace` 2913→3070）。那讓 §1.2 更依賴「操作要在一個地方」，不是更不依賴。
- **兩欄與右欄（2026-09-03，ADR-064 待決策 2 的裁定）。** 1024px 這個斷點從此不只降字級，也切版面：`/skills/$id` 的四項判定在 ≥1024 排成 `.verdict-grid` 兩欄（`repeat(2, minmax(0, 1fr))`，gap 16／24），操作與各自的理由收進 `aside.detail-rail`（320px，`position: sticky; top: 16px`——**全 app 第一個 `position` 宣告**，理由是 §1.2：讀到哪裡操作都在手邊；主要動作**不**進右欄，它留在判定格裡，一頁一個的棘輪不變）；結果卡的 `.result-facets` 在 ≥1024 排兩欄（五列 → 三列，卡高 442 → 約 330px）。**為什麼與 40em 不衝突**：量出來的是 `article` 1078px 而最長排版行 720px ＝ 40em，右側 358px 永遠空白；兩欄各約 519px **比上限窄**，所以行變短不變長——這是 40em 留下的空間的正當用途，不是繞過它。**代價**：≤1024 全部回到單欄，右欄跟在主欄之後；375 下四項判定進不了第一屏，而且**沒有合法做法讓它們進去**（要砍的會是 §2.10 第 1 項或 §2.11(c) 的但書，順位 1 與 2）——手機的正確目標是判定之前沒有別的東西、摺線落在區塊中間讓讀者知道下面還有，不是四項全進第一屏。
- **首頁的分類列與精選書架（同日）。** 搜尋框正下方一列 `nav.category-nav` 的 `a.chip`（全部／文件／寫作／資料，各帶伺服器數出來的計數，`aria-current="page"` 由 router 標）；chip 是**控制項不是主張**，所以它長得像次要按鈕（描邊、`--border-strong`、藥丸），選中時用凹面＋字重，**永遠不填色**。目錄狀態下清單分成 `精選（N）`／`其餘目錄（M）` 兩段，同一個卡片元件、同一份措辭，N 與 M 都是數出來的；精選那一段的但書四句（誰讀的、不是安全保證也不是推薦、審查綁在這一版的位元組上、已索引不等於從沒被審）寫在同一區塊（§2.11(c)），而第四句刻意與 `filter-why-tier` 的那句措辭不同（§3 第 14 條的機器）。

### 4.6 視覺層：層級由 token 承載（[ADR-064](../adr/ADR-064-the-visual-layer-is-hierarchy-carried-by-tokens.md)）

~~**本節在 CSS 落地前是政策不是事實**~~ **2026-09-03 同日：R-38 放行、第一階段落地，本節自此與 `index.css` 同步是事實。** 把關：§4.6.1 的每一對由 `contrast.test.ts` 的 `PAIRS`（25 對）守；§4.6.3 的「一頁一個」由 `rendered.spec.ts` 在三引擎守（§6）。落地時與本節不同的三件小事記在 ADR-064「落地紀錄」：行內 `<span role="alert">` 三處不套框（版面）、`--dur` 宣告未用、`/skills/$id` 在版本清單讀取中是零個主要動作。

**為什麼有這一節。** 2026-09-03 的盤點：全 app `box-shadow`／`transition`／`:active`／`position` 各 **0** 條、`:hover` **2** 條、四個卡片族三族逐位元相同、**沒有按鈕階級**（`建立下載套件` 與同頁另外 8 顆按鈕是同一個灰框）、`role="alert"` 沒有任何規則、輸入框邊框在白底上 **1.27:1**（WCAG 1.4.11 要 3:1）。線框感的來源是「**畫面上沒有任何東西比別的東西重要**」——那是 §1.2 與順位 3 的問題，不是順位 5 的問題；裝飾是修它的副產品。**每一項視覺變更要答得出「它讓哪個東西比別的重要、為什麼該」，答不出的是裝飾，裝飾要單獨放行。**

#### 4.6.1 色彩角色

| token | 工作 | 亮 | 暗 | 不要用在 |
| --- | --- | --- | --- | --- |
| `--bg`（改值） | **地**：頁面底 | `#f7f6fa` | `#121218` | — |
| `--surface`（新） | **面**：卡片、控制項、頁首——「這是一個東西」 | `#ffffff` | `#1b1c24` | 整頁 |
| `--code-bg`（改值） | **凹**：程式碼、徽章底、停用態 | `#f1eef6` | `#24252f` | **整張卡的底**（那格留給所有權軸，見 4.6.5） |
| `--surface-hover`／`--surface-active`（新） | 互動態，**預混平色**，不是疊層 | `#f3f0f8`／`#efebf5` | `#23242e`／`#2a2b36` | 靜態區塊 |
| `--border`（改值） | 分隔線、卡片邊 | `#e3dfea` | `#31303d` | 控制項邊界（不夠 3:1） |
| `--border-strong`（新） | **控制項邊界**（≥3:1） | `#8f87a0` | `#6e6a80` | 裝飾性強調 |
| `--cta`／`--on-cta`（新） | **一頁一個**主要動作的填色與其上的字 | `#9b2ae6`／`#ffffff` | `#c084fc`／`#0d0812` | 任何徽章、任何第二顆按鈕 |
| `--danger-bg`（新） | 阻斷性 notice 的底（§5.3 缺口 ①） | `#fdecec` | `#3b1a1a` | 非阻斷的降級說明（那是 `--accent-bg`） |

**全部配對已算過**（2026-09-03）：文字／`--text-h`／`--danger`／`--link` 在地、面、凹、hover、active 五種底上都 ≥4.5:1（亮色最緊的一對是 `--link` 在 active 上 4.62）；`--on-cta` 在 `--cta` 上 5.43／7.49；`--border-strong` 在面上 3.42／3.26；`--accent` 描邊在地上 4.08／7.06（描邊只要 3:1，§4.4 規則 2 不變）。**沒有進 `PAIRS` 的配對等於沒有被守**——落地時逐對加。

`--accent`、`--accent-bg`、`--accent-border`、`--danger`、`--link`、`--text`、`--text-h` **不改名不改值**。

#### 4.6.2 深度：三層表面，不用陰影

地／面／凹。亮色模式面比地白，暗色模式面比地亮——與 Material 3 的 `surface-container-*` 同向，而且是**深色模式唯一成立的做法**（陰影在深底上會消失）。陰影只給「真的浮在別的東西上面」的物件，**本 app 今天一個都沒有**，所以沒有陰影 token；第一個浮起物出現時再加兩個（key ＋ ambient），alpha 只能住在 token 裡。`#root` 的兩條直線隨之退場——地與面的色差就是框。`.app-header` 是面。

#### 4.6.3 主張用描邊，動作用填色；一頁一個主要動作

- **徽章（主張）永遠是描邊**，底只能是 `--code-bg`。填色徽章讀起來像背書（§2.3、§2.11(c)），而路線圖上第一個會要填色徽章的是付費標章。**填色這個頻道屬於動作，不屬於主張。**
- **主要動作**（`.action`：`--cta` 底、`--on-cta` 字，`<a>` 與 `<button>` 同一個 class）**一頁至多一個**——**完成這一頁的工作的那一個**：

  | 頁 | 主要動作 | 備註 |
  | --- | --- | --- |
  | `/` | 搜尋 | — |
  | `/skills/$id` | 打包並下載這個版本 | 不可打包時退為「Fork 到你的工作區」；版本清單讀取中或失敗時**零個**（不先給再收回）。~~今天兩個 `a.action` 同頁，落地時要收成一個~~ 已收成一個（Test Case 連結降為一般連結） |
  | `/skills/$id/package` | 建立下載套件 | 今天它與同頁 8 顆按鈕同色 |
  | `/lab/run` | 同意並開始試跑 | 授權確認**可以**是主要動作：§2.2 引用的 VS Code 實測正是「不顯眼的授權提示會失敗」 |
  | `/lab/test-cases`、`/workspace/import` | 建立、匯入 | — |
  | `/workspace/skills` | 建立中心的「匯入 Skill」 | 2026-09-03 新增：這一頁本來零個；「建立一個 Skill」區塊三張卡（匯入／從目錄挑一個來改／生成——第三張只在 `generate_skill` 旗標開時存在，IA §2.4），匯入是唯一填色的那一顆 |
  | `/compare`、`/runs/$id`、`/policy` | **零個** | 沒有「完成這一頁的工作」的動作時，零個是合法的 |

- **毀滅性動作永遠不是主要動作**：`ConfirmDelete` 的確認鈕維持 `--danger` 描邊——§2.8 的兩段式靠的就是它不顯眼。
- 其餘按鈕＝次要：`--surface` 底、`--border-strong` 邊、hover 換 `--surface-hover`、`:active` 換 `--surface-active`。停用態不變（虛線，§4.4）。

#### 4.6.4 動效

`--dur-fast: 120ms`、`--dur: 200ms`、`--ease: cubic-bezier(0.2, 0, 0, 1)`；`@media (prefers-reduced-motion: reduce)` 把兩個時長設 `0ms`，**一個區塊關掉全部**。只轉場 `background-color`／`border-color`／`color`／`outline-color`／`transform`。**不加 `@keyframes`、不加進場動畫、不加 spinner**——§2.12 逐字點名「永遠不動的轉圈分不出在跑與卡住」，`Loading.tsx` 維持文字。

#### 4.6.5 為路線圖保留的頻道（本節不准用掉）

| 頻道 | 留給誰 |
| --- | --- |
| 第四個語意色相（attention／in-progress） | §2.12 進行中；乙-13 評估報告的第二個「未驗證」（§7：「這格要在釘板被塞滿之前先決定」） |
| 凹面當**整張卡**的底 | 多租戶的所有權軸（「別人的」「唯讀」，§7 第一列） |
| 填色徽章 | **永遠不給任何主張** |
| 三種強制者的 notice 變體 | Local Runner／遠端 MCP（§2.2 第三向）；本節只加 `--danger-bg` 一種 |

#### 4.6.6 字體、記號與不做的事

- **不引入 webfont**：外部請求要進同意書的第三方清單、字型載入會跳版、ADR-039 否決的是依賴面。槓桿是**字重**：`h1`／`h2` 600、`.app-title` 700（今天全部 500／600），GOV.UK 與 USWDS 的視覺品質就是這樣來的。
- favicon 以 inline SVG data URI 寫在 `index.html`（不開 `public/`）；`.app-title` 前一個 `--accent` 方塊記號（`::before`，全 app 第一個 pseudo-element，只此一處）。
- **不做**：漸層、主題切換按鈕（`prefers-color-scheme` 就是偏好；IA R4、凍結）、~~圖示集（等第一個「沒有它會誤讀」的實例）~~〔2026-09-03 由 [ADR-065](../adr/ADR-065-hot-path-text-budget-and-the-fourth-disclosure-mechanism.md) 決策 5 回答：允許至多六個形狀的 inline SVG、一列一個、永遠伴隨文字，規則在 §4.7；「不做圖示**集**」仍成立〕、成功綠、任何「Verified」填色、視覺回歸截圖基準線（§6 已證偽兩次）。

#### 4.6.7 凍結下的分類與時間窗

綁得到既有規則的三件（`--border-strong` → 1.4.11；一頁一個主要動作 → §1.2 與閘門分析 §5.1 的 P1；`--danger-bg` 與 `role="alert"` 的第二訊號 → §5.3 ①、§2.3、§3 第 2 條）是**修缺陷**。其餘（三層表面、染色灰階、字重、favicon、hover／active、轉場）**綁不到——那是順位 5，要負責人一句放行（R-38）**。第一階段不動版面、不動任何文案，所以 09-10 前落地不會讓閘門不可比；**09-11～09-20 一律不動**；動版面的第二階段（右側三分之一的空白、操作欄）09-20 後與 `04` 丙-132 同一次裁。

---

### 4.7 Tip、標籤與圖示的語彙（[ADR-065](../adr/ADR-065-hot-path-text-budget-and-the-fourth-disclosure-mechanism.md)，2026-09-03；**2026-09-04 落地，丙-142 第二批**）

本節寫下時 CSS 還沒落地，所以是政策不是事實；**2026-09-04 落地之後，下表每一列都有對應的檔案**：`components/Tip.tsx`（觸發鈕＋內容）、`components/StateIcon.tsx`（四個形狀）、`index.css` 的 `.tip`／`.tip-trigger`／`.tip-content`。落地的配方過了 §5.1 的入場關：**沒有新增任何字級或間距的階**（內容用 `.notice` 那組 `8px 12px` 與 14px；觸發鈕直接是基礎控制層的次要按鈕，32px 那一階），沒有新 token（內容底色是 `--surface`、邊是 `--border-strong`），唯一新的宣告是 `position: absolute`——它是規則本身（開合不推動鄰居），不是便利；`opacity`、`#hex`、第二份樣式表照舊禁止。

| 語彙 | 是什麼 | 規則 | 不要用在 |
| --- | --- | --- | --- |
| **Tip 觸發鈕** | 一顆帶可見文字的次要按鈕（`--surface` 底、`--border-strong` 邊，同其餘次要按鈕），`aria-expanded` 指向內容 | 命中區取既有的 `min-height: 32px` 那一階；一頁至多三個；不得填色（`rendered.spec.ts` 會把它算成第二個主要動作） | 任何 §2.10 項目；停用理由；但書 |
| **Tip 內容** | 一個 block，在 DOM 裡、預設 `hidden`，開合不推動鄰居 | 用 `[hidden]`，不用 `opacity`；內容是 `<p>`，所以 §4.5 的 40em 與 `rendered.spec.ts` 的行長斷言看得到它——**不要用 `<div>` 躲掉行長**（那是一個新的無人看管的角落） | 連結以外的互動元件 |
| **標籤** | 既有的 `.badge`（主張）與 `a.chip`（導覽），不新增第三種 | 一個狀態、一個分類、一個計數→標籤；一段推理→不是標籤 | 一句話塞進藥丸裡 |
| **圖示** | inline SVG 寫在元件裡，`aria-hidden="true"`，顏色取所在徽章的 token | **至多六個形狀**，對應 §4.4 的列（不通過／未知或未檢查／降級／通過）與 Tip 觸發鈕；**永遠伴隨可見文字**；拿掉圖示畫面每個狀態仍讀得懂 | 不帶字的 icon-only 按鈕；新的語意色（§4.6.5 的四個頻道一個都不用） |

**與既有守門的交界，落地時要一起改**：`rendered.spec.ts` 的「`.badge + .note` 間距」靠 `nextElementSibling`，在徽章與但書之間插 Tip 觸發鈕會讓它靜默失效——要改成找下一個 `.note` 而不是下一個兄弟；`a11y.test.tsx` 的 `.note` 重複守門明文豁免 `li` 內的 `.note`，逐列複述因此**曾經**沒有機器——2026-09-03 已在同一支測試裡補上（§6），Tip 若把但書包進一個不是 `.note` 的容器，那台機器就看不到它了；`design-system.test.ts` 的 `UNSTYLED`／`TOOLTIP_ONLY` 兩份豁免清單已滿（7/7），Tip 的 class 一定要有 CSS 規則、Tip 的內容一定不是 `title`。

---

## 5. 偏離帳

**形狀比照 [`db/query-owners.yaml`](../../db/query-owners.yaml) 的 `allow:`：具名、有理由、只能縮短不能增加。**

### 5.1 入場規則（新的值想成為一階，要過這一關）

> **兩處才成階。** 新的字級／間距／表面／狀態樣式只有在 `apps/web/src` 有 **≥2 個 call site**、**且既有的階涵蓋不了**時，才進 §4。只有一處，或既有的階蓋得住，就改回尺度上。

這條直接裁掉了兩個懸而未決的案子：`6px` 有八個 call site（過第一關），但 `8px` 蓋得住（第二關不過）→ **收掉**；`2px` 只有三處視覺微調且無推導 → **收掉**。

### 5.2 現存偏離（2 條，均為保留）

| 值 | 出現在 | 判定 |
| --- | --- | --- |
| **5px** | `button/select/textarea/input` 的 `padding: 5px 10px` | **保留，有推導**：`20px` line-height ＋ 上下 5px ＋ 上下各 1px 邊框 = `min-height: 32px`（WCAG 2.2 2.5.8 的 24px 下限之上）。不是隨手挑的值 |
| **10px** | 同上的水平 padding，以及卡片的 `padding: 10px 12px` | **保留，單一來源**：一組值服務四個卡片族，改它就是改全部卡片 |

已收（2026-08-22）：`13px`（`.badge` → meta 14px）、`12px 非等寬`（`.filter-unavailable .note`，規則整條移除）、`2px`（三處）、`6px`（八處）。**五條縮到兩條。**

### 5.3 開放缺口（不是偏離）

- ~~**沒有 `--danger` 版的 notice**，所以阻斷性訊息（`Packaging` 的「不能打包」）與降級說明用同一個外觀。目前靠文字分辨（§2.3 允許），但這是 §4.3 那張表講不清楚的一格。~~ **已關（2026-09-03）：`.notice-danger`，§4.6.1 的 `--danger-bg`，三處套用（§4.3 表）。**
- **`.badge-differs` 用 `--accent-border` 表達一個肯定的發現**（§4.4 規則 4）。
- **`.verdict-*` 的 class 插值已於 2026-08-22 移除**：四個判定只有兩個 token 可用，「部分符合」與「無法判斷」必得共用一個，比不上色更糟。`.criterion-passed` 刻意不存在——安靜的預設就是它的視覺。

---

## 6. 強制對照表

**這一節的價值在誠實，所以每一列都要寫覆蓋範圍。** 舊版本文有三列連續高估：焦點環與 Tab 順序寫成「同上＝全部 18 條路由」，實際各只跑 `/` 一條；合成像素對比寫成自動而沒說是 2 條路由。

| 規則 | 把關者 | 覆蓋範圍 |
| --- | --- | --- |
| 色彩對比（token 層） | [`contrast.test.ts`](../../apps/web/src/contrast.test.ts) | ~~全部 token 配對~~ **（2026-09-03 訂正：是 `PAIRS` 手列的 9 對，不是全部）**；值直接讀 `index.css`，不留第二份副本。帶 alpha 的 token（`--accent-bg`、`--accent-border`）結構上量不到（下一列補）。~~§4.6 落地時要逐對擴充~~ **同日擴到 25 對**（§4.6.1 的每一個新底各與四種前景配對，另加 `--border-strong` 兩對 3:1）；**沒進 `PAIRS` 的 token 仍然等於沒有被守** |
| 色彩對比（合成像素、alpha） | [`e2e/rendered.spec.ts`](../../apps/web/e2e/rendered.spec.ts)（[ADR-036](../adr/ADR-036-real-browser-verification-tier.md)） | **2 條路由**（`/?q=pdf`、`/policy`），三引擎。理由見該檔檔頭：`--accent-bg` 是唯一落在文字後面的 alpha token，兩頁都有它 |
| 375px 不橫向溢出 | 同上 | **全部路由**（今天是 18 個位址／17 條路由，`/` 出現兩次）。~~**2026-08-25 訂正**：那張表是手寫的、沒有棘輪，所以只保證「這 18 個過了」。~~ **同日稍晚補上棘輪**：`ia.test.ts` 把 `e2e/routes.ts` 當文字讀，與 `router.tsx` 的 `path` **雙向**比對——兩邊都先收斂成 shape（去掉 query，`${SKILL}` 與 `$skillId` 都變 `*`），所以一條路由對多個位址仍然合法，而少一條或多一條都會 FAIL。**守門放在 vitest 而不是 e2e**：Playwright 那層不是每次改動都跑，只在慢套件裡響的棘輪沒有人感覺得到 |
| 焦點環真的被畫出來 | 同上 | **`/` 一條**，三引擎 |
| 寬表格在自己的容器裡捲，頁面本身不橫捲 | 同上 | **`/policy` 一條**（375px），三引擎。〔2026-09-03 補：這三列的把關者一直存在，只是沒有進表——而本節自己說「沒有進表＝沒有被守」，反過來也讓已經被守的看起來沒守〕 |
| 主要動作連結是控制項尺寸的目標（≥32px、要有 box） | 同上 | **`/skills/$id` 一條**（1280×900），三引擎 |
| 事實與它的但書在結果卡上不同色 | 同上 | **`/?q=…` 一條**（1280×900），三引擎；比對 `.result-facets dd` 與其中 `.note` 的實際顏色 |
| Tab 順序不倒退 | 同上 | **`/` 一條**，三引擎；斷言「不往回跳」而非固定清單，因為 WebKit 預設不把連結放進序列 |
| 正 `tabindex` 不存在 | [`a11y.test.tsx`](../../apps/web/src/a11y.test.tsx) | 全部路由。**上面那一列的 Tab 斷言靠它成立**（DOM 順序＝焦點順序） |
| `<details>` 一定有 `<summary>` | 同上 | 全部路由。這是 §2.6 唯一的機器 |
| 表單標籤、鍵盤可達、**100 條** axe 規則 | 同上 | **全部路由的成功態 ＋ 三個代表性非成功態**，新路由沒加案例會 FAIL。**數字是 axe-core 4.13.0 在六個 tag 下的實際值；舊文件寫 88 是 2026-08-18 的快照，已陳舊**。<br>~~全部路由~~ **（2026-08-29 訂正：這一欄的標題是「覆蓋範圍」，而「全部路由」被讀成了「全部畫面」。）** 每條路由只掃**一個**狀態，而 `fixtures/platform.ts` 明文說那刻意是**忙碌的成功態**——理由對了一半：空頁確實證明得少，但**讀取失敗不是空頁**，它是 `role="alert"`、`role="status"`、一條登入連結與一份被抽掉的表單，也就是新的、沒被掃過的互動 markup，而那正是 a11y 缺陷最容易長出來的地方。`Loading` 有 ~~24~~ **30** 個呼叫點、`ReadFailure`／`LoginRequired` 散在 ~~13~~ **21** 個檔案〔2026-09-03 重數；**這一類手數的數字在本檔已經漂過三次**，引用它們時請當作「量級」而不是事實〕，在此之前 axe 一次都沒看過它們。<br>**同日補三個代表**（不是 17×4，補的是三種**形狀**）：`/workspace/skills` 的 401、`/lab/run` 的載入中、`/workspace/runs` 的空清單，各多存一份 `__outlines__/` 快照 |
| 標題**不跳級** | 同上（axe `heading-order`） | 全部路由 |
| **標題該降級而沒降／該有而沒有** | [`__outlines__/`](../../apps/web/src/__outlines__/) 快照 | 全部路由。**不判斷對錯，只讓變更變成必須核可的 diff**。真正的判讀仍是 §3 第 9 條 |
| 停用控制項的原因不只在 `title` | `a11y.test.tsx` | 全部路由（`[disabled][title]` 必須有 `aria-describedby`） |
| 徽章一定有詞 | 同上 | 全部路由（`.badge` 的 `textContent` 非空） |
| 搜尋的 live region 是筆數不是整份清單 | 同上 | `/` |
| 字級／間距在尺度上 | [`design-system.test.ts`](../../apps/web/src/design-system.test.ts) | `index.css` **全部**；看不到 `style={{…}}` 與元件內樣式（目前全 app 皆無，且下一列讓這件事成為不變式） |
| `index.css` 是唯一的樣式表 | 同上 | 一條 `readdirSync` 斷言，**遞迴**，所以它是全 `src/` 的不變式。~~2026-08-25 訂正：它是 `src/` 頂層的不變式，不是全 app 的~~——那句話當天成立過半天：`readdirSync(import.meta.dirname)` 原本不遞迴，`src/components/foo.css` 會無聲通過，而上面兩列的尺度守門只讀 `index.css`。**同日補上 `{ recursive: true }`**，缺口關閉，上面兩列的「目前全 app 皆無」自此才真的是全 app 的不變式 |
| 色彩字面值與 `opacity`（§2.7） | 同上 | token 區塊以外一律 FAIL。**這條原本連一列都沒有** |
| 偏離清單只能縮短 | 同上 | 長度斷言。**已知弱點**：它擋不住把值搬進 `TYPE_SCALE`／`SPACE_SCALE`，而 §5 明文說的退場路徑就是「改回尺度上」——修法見 §7 待辦 |
| markup 裡的 class 在 CSS 裡有規則 | [`design-system.test.ts`](../../apps/web/src/design-system.test.ts) | ~~**沒有**——一列都沒有~~ **2026-08-25 同日補上。** 掃 `src/` 下全部非測試 `.tsx` 的 `className=`（不掃整檔字串——原型版把 `=== "scanned"` 的比較字面值報成缺規則），**單向 used → defined**。<br>**它不是「每個 class 都要有規則」**，因為有七個 class 刻意沒有，而理由分兩種：**已定的決策**（`badge-source-package` 不吃 `--accent-border`——作者原文是既成事實，套上未知色會讓顏色與字面互相矛盾，純 `.badge` 就是它的視覺）與**測試 hook**（六個，各有測試真的選它）。所以名單裡每一筆都要寫出是哪一種，並附兩道棘輪：**長度只能變短**，以及**不得腐爛**——某一筆若後來有了規則、或已經不在 markup 裡，測試會要求刪掉那一行。<br>**它第一次跑就抓到一個真的**：`.page` 掛在 11 個頁面的 14 個 `<section>` 上，沒有規則、沒有測試選它、什麼都不畫——既不是決策也不是 hook，是殘留。同日移除，名單 8 → 7。<br>**看不到的範圍**：`` `badge-${kind}-${value}` `` 這類動態組出來的 class（`LabelledBadge` 一族） |
| **段落行長不超過 §4.5 的 40em** | [`e2e/rendered.spec.ts`](../../apps/web/e2e/rendered.spec.ts)（2026-09-03 新增） | `/skills/$id` **一條**，三引擎。斷言量的是**排版出來的行**（`Range.getClientRects`）而不是元素的 box——卡片的 padding 與藥丸的邊框不是行長，量 box 會把它們當成行長報上來（第一版就這樣報了三個假的）。jsdom 沒有 line box，這一條在那一層**結構上量不到** |
| **徽章與它後面的但書之間有間距** | 同上（2026-09-03 新增） | `/` **一條**，三引擎。同樣只有真引擎看得到：藥丸自己有 12px padding，所以**字**隔了 12px 而**邊框**貼著——缺陷落在一個元素的 box 與鄰居的字之間，jsdom 兩者都沒有 |
| **一頁至多一個填色的主要動作、只能是 `.action`、`.badge` 不得填色**（§4.6.3） | [`e2e/rendered.spec.ts`](../../apps/web/e2e/rendered.spec.ts)（2026-09-03 新增） | **全部路由**（18 個位址），三引擎，桌面 1280、**只有亮色模式與靜止態**。比的是 `getComputedStyle` 的 `background-color` 與 `--cta` 解析後的值——同一個引擎自己的序列化對自己，所以 `rgb()`／`rgba()` 的拼法差異進不來；`--cta` 解析不出來時報「這條路由什麼都沒量到」而不是報 0；全部路由加起來一個填色都沒有時 FAIL（否則刪掉 `.action` 的規則整套照樣綠）。**看不到**：hover／active／focus 的底、登入後或旗標後的狀態、暗色模式（`--cta` 在暗色與 `--accent` 同值，而唯一畫 `--accent` 底的是 `.app-title::before`——pseudo-element，`querySelectorAll` 拿不到） |
| **§2.10 十項不進 `<details>`**（2026-09-03） | [`detail.test.tsx`](../../apps/web/src/detail.test.tsx) | **`/skills/$id` 一條**：風險計數、三個相容判定、License、可散布性、工具清單、來源層級但書各查一次 `closest("details") === null`；把風險計數包進 `<details>` 即紅（突變已做）。其餘路由的 §2.10 仍是人看 |
| **分類 chip 的計數等於伺服器的 `total`；精選（N）＋其餘（M）＝總數**（2026-09-03） | [`disc.test.tsx`](../../apps/web/src/disc.test.tsx) | `/` 目錄狀態；把任一數字寫死即紅（兩個突變都做了） |
| **建立中心的生成卡只在旗標開時存在**（2026-09-03） | [`workspace.test.tsx`](../../apps/web/src/workspace.test.tsx)＋[`ia.test.ts`](../../apps/web/src/ia.test.ts) 的 `FLAG_OFF_ASSERTED` | `/workspace/skills`；旗標關時沒有那張卡、沒有「生成」二字；無條件渲染即三支紅 |
| 卡片規則（§4.3） | **沒有** | 只能靠人看截圖 |
| **§2.13 去重 1：同一份清單裡逐字相同的 `.note`** | [`a11y.test.tsx`](../../apps/web/src/a11y.test.tsx)（2026-09-03 新增） | **全部路由**。逐個 `ul`／`ol` 收它**直屬 `li`** 裡的 `.note`（巢狀清單歸巢狀清單、`<label>` 不算——那是控制項的名字不是但書），同一字串出現在同一份清單的兩個以上的列即 FAIL。<br>**它上線第一次就抓到五筆**：打包頁每個目標各印一次的 INSTALL.md 句（真違規，同日修掉）、一個表單欄位的 `<label>`（因此有了上面那條排除）、以及三筆它**天生分不出來**的——所以有一份具名、寫理由、只能變短的 `LIST_NOTE_REPEATS`（今天兩筆：掛在風險旗標上的解釋、以及兩筆下載剛好同一版本的版本字串）。<br>**已知弱點**：豁免是逐字字串，其中兩筆的值來自 fixture，fixture 一改就無聲腐爛。它短所以還可以忍；長了就要補上 `TOOLTIP_ONLY` 同日拿到的那個「每一筆都要真的被遇到」棘輪 |
| **`title` 掃描的 sentinel 不再是一個會過期的數字**（2026-09-03） | [`design-system.test.ts`](../../apps/web/src/design-system.test.ts) | 原本是 `scanned > 8`，而 §2.13 去重 2 當天拿掉五個 tooltip，一個健康的掃描本來就會找到更少——**數字分不出「解析壞了」與「app 的 tooltip 變少了」**。改成斷言 `TOOLTIP_ONLY` 的**每一筆豁免都真的被遇到**：解析壞了一筆都遇不到，而一筆不再命中就是腐爛的豁免。這是別的清單早就有、這一份一直沒有的棘輪 |
| **§2.13 預算：平鋪的 D＋F ≤ A＋B＋C** | **一半**（2026-09-04）：[`rendered.spec.ts`](../../apps/web/e2e/rendered.spec.ts) 加總每條路由上 `[data-role="teaching"]` 的平鋪字數 | **D 的那一半有機器，A＋B＋C 沒有**——所以它守的不是那條不等式，是兩件較弱的事：單一平鋪 D 區塊 ≤100 字（規則自己的數字）、每條路由的平鋪 D 總字數不超過 2026-09-04 第一次量到的值（**棘輪**，ADR-065 待決策 1 說門檻從第一輪分佈訂、只能往嚴）。標記由當天的稽核放（九個讀檔子代理逐句分類、三個反駁子代理改判三句 D→C），**機器不判標記放得對不對**：標在但書上只會讓但書被算進去，不會把它藏起來。收在 Tip 裡的 D 沒有 client rect，算 0——那正是搬進去的理由 |
| **§2.13 Tip 不含 §2.10 項目、不是 `title`、一頁 ≤3** | **有**（2026-09-04）：[`a11y.test.tsx`](../../apps/web/src/a11y.test.tsx) 逐路由（≤3 個 `[data-tip]`、觸發鈕文字不在「詳情／說明／?」清單、內容是 `<p>`、內容裡沒有 `.note`／`.badge`／`role=status`／`role=alert`、`aria-controls` 對得上）；[`detail.test.tsx`](../../apps/web/src/detail.test.tsx) 對 §2.10 十項各問一次 `closest("[data-tip]") === null`；[`tip.test.tsx`](../../apps/web/src/tip.test.tsx) 守元件形狀（DOM 常駐、預設 `hidden`、`aria-expanded`、Esc 關且焦點不走）；`rendered.spec.ts` 在真引擎裡量「開合不移動鄰居、頁高不變、命中區 ≥24px」 | 「錨點必須自己成立」機器只能拒絕規則點名的幾個詞，判斷仍在人。今天全 app 只有一個 Tip（`InFlight` 的「為什麼可以關掉這一頁」），所以 ≤3 那條還沒被撞過 |
| **§4.7 圖示永遠伴隨可見文字；至多六個形狀** | **有**（2026-09-04）：`a11y.test.tsx` 逐路由查每個 `svg[aria-hidden="true"]` 的父元素去掉空白後有字、且沒有任何不是 `aria-hidden` 的 `<svg>`；[`design-system.test.ts`](../../apps/web/src/design-system.test.ts) 數元件裡的 `<svg` 站點 ≤6（去掉註解再數——`StateIcon.tsx` 的檔頭就提到 `<svg`，第一版把它算成第六個）並拒絕 `src/` 下出現 icon 目錄 | 形狀住在兩個檔：[`StateIcon.tsx`](../../apps/web/src/components/StateIcon.tsx) 四個（不通過 ×／未知 ?／降級 —／通過 ✓）與 `Tip.tsx` 的觸發鈕一個，共五站點。「一列一個」與「顏色取所在徽章的 token」沒有機器：前者靠 review，後者靠 `currentColor` 讓它結構上不可能不成立 |
| **§2.12 第 6 條：含進行中項目的清單要有可見的重新整理與「上次取得於」** | **有**（2026-09-04 同日落地）：[`workspace.test.tsx`](../../apps/web/src/workspace.test.tsx)、[`testcases.test.tsx`](../../apps/web/src/testcases.test.tsx) | 兩條會列出 Run 的路由（`/workspace/runs`、`/lab/test-cases/$id`）各用一筆 `running` 的列渲染，斷言有一顆「重新整理」按鈕、同一段裡有 `<time>`、按下去會再抓一次；反向一支：沒有進行中的列時，按鈕與「上次取得於」都不出現。實作是一個元件 [`ListFreshness.tsx`](../../apps/web/src/components/ListFreshness.tsx)，時間取 TanStack Query 的 `dataUpdatedAt`（這個瀏覽器最後一次收到清單的時刻，不是伺服器欄位）。**它只守這兩頁**：第三份會列出進行中項目的清單出現時要自己加一支 |
| **§4.3 縮小清單的控制項要說出它縮小的是哪一份清單** | **沒有**（2026-09-04 新增） | 現行三個 placeholder 都只對一份清單說話，沒有實例可守；第一個「一頁兩份清單各有搜尋框」的畫面出現時，斷言 placeholder 含該清單的 `h2` 文字 |
| **§3 第 14 條的限定語那一半** | [`a11y.test.tsx`](../../apps/web/src/a11y.test.tsx)（2026-09-03 新增） | 全部路由。**只看 `.note`，而那個範圍就是規則本身**：初版數的是每一個重複的文字節點，回報十四筆而幾乎全是合法的——比較表每一欄各講一次同一個判定、導覽列說出你在哪一頁、訪客的 Skill 頁上有三個「使用 GitHub 登入」因為 §2.2 第三向要求每一個被擋的動作各自帶著下一步。那些是「一個主體一次」，不是「一個事實兩次」。**不能合法重複的是限定語**：`.note` 是本 app 用來承載「這個徽章不涵蓋什麼」（§2.11(c)）的那個 class，而同一個主體不會需要同一句但書兩次。`li`／`td`／`th` 內的一律不計，理由同上——二十張卡各帶一個「未測量」是二十個事實。<br>**豁免清單目前是空的**，而它與本檔其他清單同一條規矩：具名、有理由、只能變短。<br>**它補的是 04 丙-137**：Skill 詳情頁用同一個元件、同一組 props 渲染了兩次 `LicenseBadge`，於是伺服器那句限定語在一個畫面上印了兩遍，而在此之前這一列寫著「沒有」 |
| §2.9 缺席型別、§2.10 永不折疊、§2.2 顯示與強制（含強制者具名）、§2.5 兩軸、§2.8 四段式、§2.12 進行中、**§3 第 14 條的其餘部分**、§3 第 15／17 條、**§3 第 18 條的「是不是這一頁的工作」那一半**（數量那一半上一列有機器） | **沒有** | 逐頁的判斷題。第 14 條「同一個事實講幾次」的**非限定語那一半**（例如兩個區塊各印一次同一個數字）仍然沒有機器——上一列只守 `.note` |

**兩條已證偽的自動化路線，寫在這裡免得有人再走一次：**

- **在 `filter: grayscale(1)` 下重跑 axe 什麼都證明不了。** axe 讀 `getComputedStyle`，不讀畫出來的像素，而且[它根本不處理 `filter`](https://github.com/dequelabs/axe-core/issues/3214)。結果會與未加濾鏡的那次逐位元相同——一個什麼都沒證明的綠勾，正是 `rendered.spec.ts` 用「先斷言 `color-contrast` 有做出判定」擋掉的那種失效。
- **`Emulation.setEmulatedVisionDeficiency` 拿得到但沒用。** Playwright 沒有原生 API，走 CDP 可行但**只有 Chromium**，而且它是算繪階段的濾鏡，改不了本 repo 任何一條斷言讀得到的東西——它只能餵截圖，於是又回到基準線圖片的問題。

**視覺回歸基準線（`toHaveScreenshot`）維持不採用**，理由見 `e2e/shots.spec.ts` 檔頭。一處算術更正：CI 跑的是 **4 個組合**（ubuntu × 三引擎、windows × chromium），不是六個。結論不變。

---

## 7. 這把尺對還沒寫的功能說得出什麼

**一份設計系統如果第一次遇到新功能就啞了，它就沒有達成它宣稱的目的。** `01` §7.3 列了 MVP 之後要做的事；逐項問這份文件現在說得出什麼。

**2026-08-22 更新：下表原本有六格寫著「這是產品決策，要在 ADR 裡先解決」。[ADR-042](../adr/ADR-042-roadmap-product-rulings-evidence-aggregate-in-flight-axis-and-enforcer-attribution.md) 把那六格全部裁定了，規則已寫進上面的章節；下表保留缺口的原始描述作為證據，並在「具體缺口」欄註明現在的答案落在哪裡。** 剩下真正沒有答案的只有兩格（多租戶的所有權軸、評估報告的第二個「未驗證」狀態）。

| 即將到來的能力 | 這份系統已經有的 | **具體缺口** |
| --- | --- | --- |
| **團隊協作、多租戶** | 無 | §4.4 的狀態語彙**沒有所有權這條軸**：沒有「別人的」「共用」「唯讀」的視覺；§4.3 的卡片表沒有署名的位置。而且已經有一個現行實例走在錯誤的方向——`04` 丙-14 記載，別人的 Skill 的版本清單回空陣列，也就是**「不是你的」目前渲染成「沒有版本」**，正是 §2.9 的 `無權檢視` 該擋掉的那一格 |
| **完整發佈與創作者 Marketplace、付費 Skill** | §2.11 已經先禁掉綜合分數與人氣訊號 | 商業壓力會壓在徽章上：Docker Hub 與 VS Code Marketplace 的「Verified Publisher」實際只證明付了錢或擁有網域六個月。**§2.2 的「顯示與強制成對」要延伸成「徽章的謂詞必須是它字面上的意思」**——這一條要在有商業誘因之前先寫好。**已部分回答**：§2.2 第三向（強制者具名）與 §2.11(c) 合起來就是這條的骨架；付費徽章的具體措辭仍未寫 |
| **社群留言、評分與排行榜** | §2.11(b) 直接禁止 | 但它只禁「相鄰」與「預設排序」，沒有規定評分**自己**怎麼呈現。排行榜與 `01` §5 原則 2（證據優先，不只依賴 Star）正面衝突。**已回答**：§2.11(d)——評分與排行榜**永久不做**，替代品是帶分母、非預設排序、有 k 門檻的試跑證據彙總 |
| **GPU、長時間工作** | 無 | **全文件沒有時間軸。** `RUN_STATUS_LABEL` 已經有五個非終態（queued／provisioning／preparing／running／evaluating），而 §4.4 的四種視覺全部是終態判定。沒有規則說：跑到一半的頁面長什麼樣、進行中的判定可不可以顯示（`RunEvaluation` 已經自己發明了答案）、部分資料怎麼標記。app 裡十二處載入狀態都是 `<p>載入中…</p>`，那是一個沒人寫下來的慣例，第一個串流畫面會把它取代掉。**已回答**：§2.12 與 checklist 第 17 條——**而且兩邊都已經有實作**（2026-08-25）：那十二處慣例換成了 `components/Loading.tsx` 一個元件、24 個呼叫點、各帶 `role="status"` 與一個具名主詞；§2.12 本身的實例是 `components/InFlight.tsx`（`/runs/$runId`）。**取代它的不是第一個串流畫面，是一次逐處收斂** |
| **大規模平行 Benchmark；任何會長到數百列的清單** | 無 | 已經有**四種**並存的策略：「載入更多」無限捲（三處）、cursor 上一頁／下一頁（Trace）、硬截斷前 100 筆（Trace 重複事件）、完全不設限（搜尋結果）。沒有規則說哪個用在哪；也沒有規則說**截斷的清單必須說出總數**（Trace 說了，搜尋沒說）。更根本的是：**§4.3 對卡片的整個論證是「每一則要能被單獨判斷」，那在 50 列以上會塌掉，而文件沒講**。**已回答**：§4.3 的尺度條款——判準不是列數而是「這一頁在問什麼」，截斷一律要說出總數與理由，策略收斂為三種 |
| **Local Runner／本機工具絕對路徑** | 無 | 這會是這個產品史上風險最高的畫面，而它的需求 `01` §9.2 已經寫好：「明確授權、限制路徑、**顯示命令**與可中止執行」。更麻煩的是 **§2.2 在這裡會結構性地失效——平台對使用者自己的機器強制不了任何東西**。所以需要一條新規則：**當強制的一方不是平台時，畫面必須說出誰在強制**。**已回答**：§2.2 第三向，含「不得用完成式」「預設拒絕且拒絕狀態可用」「授權是阻斷式決定而非被動通知」 |
| **遠端 MCP Server** | `RunPreflight` 的權限摘要與 `summary_hash` 綁定 | 同上一格，加上一件事：MCP Registry 的 namespace 驗證只證明名稱歸屬，**什麼都沒說行為**。§2.11(c)（證據要說出自己的上限）就是為這種徽章寫的。**已回答**：§2.2 第三向最後一項給了措辭形狀——「這個名稱屬於 X」沒有說它做什麼 |
| **更豐富的評估報告**（`04` 乙-13 G7／G8 待決） | §4.4 的虛線邊框已經被 `.criterion-unverifiable` 用掉 | 若選項 (b) 成立（「在 UI 上明確標示『此引文未經回驗』」），**第二個「未驗證」狀態會落在同一頁而語彙沒有空頻道**：顏色已經三分（danger／accent-border／無），而 §2.3 禁止只靠顏色。這格要在釘板被塞滿之前先決定 |

**兩條 `01` 明文寫了、而 §1 沒有推導出來的義務**，兩者都已經有實作、也都沒有指引：

- **`01` §5 原則 3「權限透明：執行前說明預計使用的資料、工具、MCP 與網路權限」**——`RunPreflight` 整頁是它，而它的操作形式（同意綁定當下摘要的雜湊，舊的同意永不重送，`02:TEST-005`）是全 app 最強的一條慣例，卻只寫在那個檔案的檔頭。**它應該升成原則，並推廣到每一個確認：第二段確認必須綁定畫面上此刻顯示的事實。**
- **`01` §11.3「所有『通過』或『未通過』結論都能展開查看依據」**——`EvidenceList` 是它，但 §2.10 只說判定不可折疊，沒說**判定的依據必須可展開**。

### 7.1 已知待辦

1. ~~**進階 Trace 視圖無法被連結。**~~ **已裁定為「維持現狀」（2026-08-23），本項關閉。** 本文件此前把它寫成待辦，而 [`router.tsx`](../../apps/web/src/router.tsx) 的檔頭把同一件事寫成一項決定（「閱讀偏好，不值得一個可分享的位址」）——**兩份文件對同一件事各說各話**。裁定依據是[資訊架構 §0.1 R4](./information-architecture.md)：**「你在看哪一份東西」進網址，「你偏好怎麼看」不進**，而該規則正是從現行七條 `validateSearch` 實際遵守的判準推出來的。閱讀模式是後者，所以程式是對的、本文件是錯的。<br>**順帶更正本項原文的一個誤會**：`03` 的 `TRACE-007` 定的 `?mode=advanced` 是 **API 參數**（`GET /runs/{id}` 取遮罩後的原始事件），該工作項自己寫著「UI 為 `apps/web` 的 `/runs/$runId`，一般／進階切換」。**它從來沒有要求那個切換上網址**，所以這件事只有兩種立場而不是三種。
2. **五件契約缺口已逐項寫成殘項 [丙-28～丙-32](../plans/04-backlog-and-handoffs.md)。2026-08-22 稍晚拓撲遷移落地後，兩項半已結案**：那個寫錯的列舉值（`cleaning` → `cleaning_up`）、`Me.deletion_scope`（刪除範圍句現在活得過一次重新整理），以及 `GET /skills` 的三件——兩個本來就在列上卻被丟掉的鎖、靜默截斷在 100 筆這件事終於說出來、以及同一個 endpoint 的第二個更窄型別（缺口就藏在那裡）。**留著的是要先做決定的那幾件**：風險 facet 需要 ADR-034 式的反轉（`read_allow:` 是零條且明文不是擴充點）；`verified_at` 對 Fork 出來的 Skill 會印出按下 Fork 的時間而那時什麼都沒掃過（裁定為 §2.9 的 `未測量`）；Run 歷史的判定軸同樣卡在跨 context 讀取。
