version: "2"
sql:
  - engine: postgresql
    schema: migrations
    queries: queries
    gen:
      go:
        package: gen
        out: ../apps/platform/internal/platform/db/gen
        sql_package: pgx/v5
        emit_pointers_for_null_types: true
