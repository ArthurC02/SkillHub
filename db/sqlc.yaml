version: "2"
sql:
  - engine: postgresql
    schema: migrations
    queries: queries
    gen:
      go:
        package: gen
        out: ../apps/platform/internal/foundation/persistence/db/gen
        sql_package: pgx/v5
        emit_pointers_for_null_types: true
