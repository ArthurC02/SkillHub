---
paths:
  - "apps/sandbox/**"
---

動 `apps/sandbox` 之前，先讀 [`apps/sandbox/AGENTS.md`](../../apps/sandbox/AGENTS.md)。以服務 README、`cmd/sandboxd/main.go` 與 `contracts/openapi/sandbox-provider.yaml` 為事實來源；local green 不等於部署安全，SEC-009／SBX-010 與 `runsc` 驗收仍須依權威文件執行。
