---
paths:
  - "docs/design/**"
---

這兩份設計文件有機器直接解析它們（`apps/web/src/design-system.test.ts` 讀 system.md 的粗體數值與豁免名冊；`src/ia.test.ts` 讀 IA §0）。改文件不改程式會紅，反之亦然。

哪一段對到哪支測試，見 [`apps/web/AGENTS.md`](../../apps/web/AGENTS.md) 那張表。判準在被指的那一份，不在這裡。
