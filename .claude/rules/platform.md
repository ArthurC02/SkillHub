---
paths:
  - "apps/platform/**"
---

動 `apps/platform` 之前，先讀 [`apps/platform/AGENTS.md`](../../apps/platform/AGENTS.md)。若目標在 `internal/`，再讀 [`apps/platform/internal/AGENTS.md`](../../apps/platform/internal/AGENTS.md) 與**你要改的那個套件自己的 `doc.go`**——它寫了這個 Bounded Context 擁有什麼、允許哪些協作方向、刻意不放什麼。

判準永遠在被指的那一份，不在這裡。
