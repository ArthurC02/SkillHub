---
paths:
  - "apps/platform/**"
---

動 `apps/platform` 之前，先讀 [`apps/platform/AGENTS.md`](../../apps/platform/AGENTS.md)。若目標在 `internal/`，再讀 [`apps/platform/internal/AGENTS.md`](../../apps/platform/internal/AGENTS.md) 與**你要改的那個套件自己的 `doc.go`**（一句話說這個 Bounded Context 擁有什麼；擁有的表在 `db/query-owners.yaml`，協作方向在 ADR-032 附錄 A）。

判準永遠在被指的那一份，不在這裡。
