[project]
name = "skillhub-api-stub"
version = "0.0.0"
description = "Generated Pydantic models for Skill Hub internal APIs"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["pydantic>=2"]

[build-system]
requires = ["uv_build>=0.12.13,<0.13"]
build-backend = "uv_build"
