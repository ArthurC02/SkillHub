/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PublicSearchResult } from './PublicSearchResult';
import {
    PublicSearchResultFromJSON,
    PublicSearchResultFromJSONTyped,
    PublicSearchResultToJSON,
    PublicSearchResultToJSONTyped,
} from './PublicSearchResult';

/**
 * 02:DISC-006. Four fields and no more: this response is a page of a list, and
 * every field search carries that is missing here is missing because it
 * would have been a constant (see the operation's description).
 * 
 * @export
 * @interface CatalogResponse
 */
export interface CatalogResponse {
    /**
     * The same row type search returns, and that is deliberate: the same
     * card renders both states of the same screen, and 02:NFR-007 第 3 條
     * does not allow one surface to word a fact two ways. Every row here
     * carries `rank: null` plus a `rank_note` naming the ordering.
     * 
     * @type {Array<PublicSearchResult>}
     * @memberof CatalogResponse
     */
    results: Array<PublicSearchResult>;
    /**
     * How many rows this page carries at most — the `limit` parameter if
     * given, otherwise the default. Named only because it is enforced
     * (設計系統 §2.2).
     * 
     * @type {number}
     * @memberof CatalogResponse
     */
    limit: number;
    /**
     * How many skills are in the catalogue under the filters given, before
     * `limit` cut the page down. 設計系統 §4.3 requires a truncated list to
     * say 「共 N 筆，這裡顯示 M 筆，因為 X」, and a lower bound cannot say 共.
     * 
     * `count(*) OVER ()` inside the retrieval statement, not a second COUNT
     * query, for the reason the search route documents at length: a
     * parallel count restates every predicate and the first disagreement
     * makes the page report a total that does not describe the list under
     * it. **Exact on this path always** — unlike search's hybrid leg, a
     * browse has no candidate window, so there is no size at which this
     * quietly becomes a lower bound.
     * 
     * `total == len(results)` whenever `truncated` is false.
     * 
     * @type {number}
     * @memberof CatalogResponse
     */
    total: number;
    /**
     * True when the catalogue holds more than this page shows. ADR-042
     * 決策 3: a truncated list must say so, or it reads as the whole answer.
     * 
     * @type {boolean}
     * @memberof CatalogResponse
     */
    truncated: boolean;
}

/**
 * Check if a given object implements the CatalogResponse interface.
 */
export function instanceOfCatalogResponse(value: object): value is CatalogResponse {
    if (!('results' in value) || value['results'] === undefined) return false;
    if (!('limit' in value) || value['limit'] === undefined) return false;
    if (!('total' in value) || value['total'] === undefined) return false;
    if (!('truncated' in value) || value['truncated'] === undefined) return false;
    return true;
}

export function CatalogResponseFromJSON(json: any): CatalogResponse {
    return CatalogResponseFromJSONTyped(json, false);
}

export function CatalogResponseFromJSONTyped(json: any, ignoreDiscriminator: boolean): CatalogResponse {
    if (json == null) {
        return json;
    }
    return {
        
        'results': ((json['results'] as Array<any>).map(PublicSearchResultFromJSON)),
        'limit': json['limit'],
        'total': json['total'],
        'truncated': json['truncated'],
    };
}

export function CatalogResponseToJSON(json: any): CatalogResponse {
    return CatalogResponseToJSONTyped(json, false);
}

export function CatalogResponseToJSONTyped(value?: CatalogResponse | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'results': ((value['results'] as Array<any>).map(PublicSearchResultToJSON)),
        'limit': value['limit'],
        'total': value['total'],
        'truncated': value['truncated'],
    };
}

