/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { RunCostEstimate } from './RunCostEstimate';
import {
    RunCostEstimateFromJSON,
    RunCostEstimateFromJSONTyped,
    RunCostEstimateToJSON,
    RunCostEstimateToJSONTyped,
} from './RunCostEstimate';
import type { RunQuota } from './RunQuota';
import {
    RunQuotaFromJSON,
    RunQuotaFromJSONTyped,
    RunQuotaToJSON,
    RunQuotaToJSONTyped,
} from './RunQuota';
import type { RunPermissionSummaryContent } from './RunPermissionSummaryContent';
import {
    RunPermissionSummaryContentFromJSON,
    RunPermissionSummaryContentFromJSONTyped,
    RunPermissionSummaryContentToJSON,
    RunPermissionSummaryContentToJSONTyped,
} from './RunPermissionSummaryContent';

/**
 * Everything a run will be able to touch, shown before it starts
 * (TEST-005/008). Built for a (skill version, test case draft) pair and
 * never stored: it is recomputed on every read, so it always describes the
 * run as it would start right now.
 * 
 * @export
 * @interface RunPermissionSummary
 */
export interface RunPermissionSummary {
    /**
     * 
     */
    summary: RunPermissionSummaryContent;
    /**
     * SHA-256 over a canonical encoding of `summary` and nothing else. Both
     * the confirmation and the run request quote it back, and the server
     * recomputes it rather than trusting either, so it is an integrity
     * check on what the user saw and not a token.
     * 
     */
    summaryHash: string;
    /**
     * What the run is predicted to cost. Outside the hash, like `notes` and
     * for the same reason: the hash covers what the run is *allowed to
     * touch*, and a prediction is not a permission. Recalibrating the
     * numbers against a larger sample must not silently revoke every
     * confirmation a user has outstanding.
     * 
     */
    estimatedCost: RunCostEstimate;
    /**
     * What the account has left (PDM-010), shown here because it answers
     * the same kind of question `estimated_cost` does — what this run will
     * use up — and the screen where a user decides to start one is where
     * that belongs.
     * 
     * Outside the hash, for the same reason and by the same rule TEST-011
     * set for `estimated_cost`: the hash covers what the run is *allowed to
     * touch*, and an allowance is a state, not a permission. A run
     * finishing elsewhere in the workspace must not invalidate a
     * confirmation the user is holding.
     * 
     * Absent when the deployment enforces no allowance. Absent rather than
     * zeroed or filled with nulls: a number on this screen is a claim that
     * it is applied (04 乙-2), so a build with no enforcement shows nothing
     * at all.
     * 
     */
    quota?: RunQuota;
    /**
     * Why this pair cannot start a run at all, absent when it can. A code
     * rather than a sentence: the wording belongs to the interface, and an
     * English message from the server would be the only English on the
     * screen.
     * 
     * Outside the hash, like `quota` and for the same reason: it is a
     * state of the deployment, not something the run is allowed to touch.
     * A deployment that releases a version must not invalidate a
     * confirmation someone is already holding.
     * 
     * The values are the refusal reasons the run gates already count, so
     * the screen and the enforcement cannot drift apart: the summary asks
     * the same functions `POST /runs` enforces. `access_restricted` is a
     * licence hold on the skill, `capability_mismatch` a deployment with
     * no model outlet or no sandbox that fits, `scan_blocked` and
     * `scan_unavailable` the static scan refusing or being unable to read
     * the package, and `content_not_curated` the clean test mode refusing
     * material that is neither in the public catalogue nor curated at
     * this exact version.
     * 
     * Every one of these is also refused when the run is created; this
     * field exists so the refusal arrives before the user has read a
     * permission summary and confirmed it.
     * 
     */
    blocked?: RunPermissionSummaryBlockedEnum;
    /**
     * Plain-language explanation of the summary, deliberately outside the
     * hash. Rewording a sentence must not invalidate every outstanding
     * confirmation, and the facts are in `summary` where the hash covers
     * them.
     * 
     */
    notes: Array<string>;
}


/**
 * @export
 */
export const RunPermissionSummaryBlockedEnum = {
    AccessRestricted: 'access_restricted',
    CapabilityMismatch: 'capability_mismatch',
    ScanBlocked: 'scan_blocked',
    ScanUnavailable: 'scan_unavailable',
    ContentNotCurated: 'content_not_curated',
} as const;
export type RunPermissionSummaryBlockedEnum = typeof RunPermissionSummaryBlockedEnum[keyof typeof RunPermissionSummaryBlockedEnum];


/**
 * Check if a given object implements the RunPermissionSummary interface.
 */
export function instanceOfRunPermissionSummary(value: object): value is RunPermissionSummary {
    if (!('summary' in value) || value['summary'] === undefined) return false;
    if ((!('summaryHash' in (value as Record<string, any>)) && !('summary_hash' in (value as Record<string, any>))) || ((value as Record<string, any>)['summaryHash'] === undefined && (value as Record<string, any>)['summary_hash'] === undefined)) return false;
    if ((!('estimatedCost' in (value as Record<string, any>)) && !('estimated_cost' in (value as Record<string, any>))) || ((value as Record<string, any>)['estimatedCost'] === undefined && (value as Record<string, any>)['estimated_cost'] === undefined)) return false;
    if (!('notes' in value) || value['notes'] === undefined) return false;
    return true;
}

export function RunPermissionSummaryFromJSON(json: any): RunPermissionSummary {
    return RunPermissionSummaryFromJSONTyped(json, false);
}

export function RunPermissionSummaryFromJSONTyped(json: any, ignoreDiscriminator: boolean): RunPermissionSummary {
    if (json == null) {
        return json;
    }
    return {
        
        'summary': RunPermissionSummaryContentFromJSON(json['summary']),
        'summaryHash': json['summary_hash'],
        'estimatedCost': RunCostEstimateFromJSON(json['estimated_cost']),
        'quota': json['quota'] == null ? undefined : RunQuotaFromJSON(json['quota']),
        'blocked': json['blocked'] == null ? undefined : json['blocked'],
        'notes': json['notes'],
    };
}

export function RunPermissionSummaryToJSON(json: any): RunPermissionSummary {
    return RunPermissionSummaryToJSONTyped(json, false);
}

export function RunPermissionSummaryToJSONTyped(value?: RunPermissionSummary | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'summary': RunPermissionSummaryContentToJSON(value['summary']),
        'summary_hash': value['summaryHash'],
        'estimated_cost': RunCostEstimateToJSON(value['estimatedCost']),
        'quota': RunQuotaToJSON(value['quota']),
        'blocked': value['blocked'],
        'notes': value['notes'],
    };
}

