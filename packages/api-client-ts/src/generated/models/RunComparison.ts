/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { RunComparisonCriterionMatrixInner } from './RunComparisonCriterionMatrixInner';
import {
    RunComparisonCriterionMatrixInnerFromJSON,
    RunComparisonCriterionMatrixInnerFromJSONTyped,
    RunComparisonCriterionMatrixInnerToJSON,
    RunComparisonCriterionMatrixInnerToJSONTyped,
} from './RunComparisonCriterionMatrixInner';
import type { RunComparisonRunsInner } from './RunComparisonRunsInner';
import {
    RunComparisonRunsInnerFromJSON,
    RunComparisonRunsInnerFromJSONTyped,
    RunComparisonRunsInnerToJSON,
    RunComparisonRunsInnerToJSONTyped,
} from './RunComparisonRunsInner';

/**
 * Two runs of the same test case, side by side (EVAL-003). A read: neither
 * run is written to, and both sides are immutable snapshots (iron rule 4).
 * 
 * Each side reports execution and task judgement in separate fields, and
 * an unevaluated side says so rather than leaving the reader to infer a
 * pass from `succeeded` (ADR-025).
 * 
 * @export
 * @interface RunComparison
 */
export interface RunComparison {
    /**
     * The run named in the path first, the `against` run second.
     * @type {Array<RunComparisonRunsInner>}
     * @memberof RunComparison
     */
    runs: Array<RunComparisonRunsInner>;
    /**
     * One row per acceptance criterion with both runs' verdicts, so a
     * regression shows up per criterion instead of only in the overall.
     * Criteria are matched by `criterion_id`, which the two snapshots
     * share when the same test case was re-run (equal `content_hash`); one
     * present on only a single side carries a null verdict for the other
     * rather than being dropped.
     * 
     * @type {Array<RunComparisonCriterionMatrixInner>}
     * @memberof RunComparison
     */
    criterionMatrix: Array<RunComparisonCriterionMatrixInner>;
    /**
     * Where to see what changed between the two skill versions: the
     * existing GET /skills/{id}/diff with `from` and `to` filled in
     * (WS-003), not a second diff implementation. Absent when both runs
     * used the same version, and absent when they used versions of
     * different skills — that diff does not exist, and inventing a
     * cross-skill one would be a second diff implementation for a
     * comparison nobody asked for.
     * 
     * @type {string}
     * @memberof RunComparison
     */
    versionDiffUrl?: string;
}

/**
 * Check if a given object implements the RunComparison interface.
 */
export function instanceOfRunComparison(value: object): value is RunComparison {
    if (!('runs' in value) || value['runs'] === undefined) return false;
    if (!('criterionMatrix' in value) || value['criterionMatrix'] === undefined) return false;
    return true;
}

export function RunComparisonFromJSON(json: any): RunComparison {
    return RunComparisonFromJSONTyped(json, false);
}

export function RunComparisonFromJSONTyped(json: any, ignoreDiscriminator: boolean): RunComparison {
    if (json == null) {
        return json;
    }
    return {
        
        'runs': ((json['runs'] as Array<any>).map(RunComparisonRunsInnerFromJSON)),
        'criterionMatrix': ((json['criterion_matrix'] as Array<any>).map(RunComparisonCriterionMatrixInnerFromJSON)),
        'versionDiffUrl': json['version_diff_url'] == null ? undefined : json['version_diff_url'],
    };
}

export function RunComparisonToJSON(json: any): RunComparison {
    return RunComparisonToJSONTyped(json, false);
}

export function RunComparisonToJSONTyped(value?: RunComparison | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'runs': ((value['runs'] as Array<any>).map(RunComparisonRunsInnerToJSON)),
        'criterion_matrix': ((value['criterionMatrix'] as Array<any>).map(RunComparisonCriterionMatrixInnerToJSON)),
        'version_diff_url': value['versionDiffUrl'],
    };
}

