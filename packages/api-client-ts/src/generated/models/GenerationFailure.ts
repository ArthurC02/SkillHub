/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * One generation that produced nothing, as the workspace reads it back.
 * 
 * Every field except `occurred_at` is best-effort. These rows are 400-day
 * history written by whichever version of the code was running at the time,
 * so a missing or re-typed key produces a row with less in it rather than an
 * error — the alternative is a screen that goes blank because of something
 * that happened last year.
 * 
 * @export
 * @interface GenerationFailure
 */
export interface GenerationFailure {
    /**
     * 
     * @type {Date}
     * @memberof GenerationFailure
     */
    occurredAt: Date;
    /**
     * What went wrong. `quota` never reached the gateway; `gateway` is the
     * model service or the proxy; `unpackageable` is an answer that parsed
     * but cannot be made into an archive; `rejected` is admission refusing
     * it (a name collision, most often); `blocked` is a validation finding.
     * Empty when a row's metadata could not be decoded — the row still
     * happened, and its timestamp is the part the screen needs most.
     * 
     * @type {string}
     * @memberof GenerationFailure
     */
    failure: GenerationFailureFailureEnum;
    /**
     * How many gateway calls that failure cost. 0 for a refusal that never
     * reached the gateway, which is exactly what `quota` is.
     * 
     * @type {number}
     * @memberof GenerationFailure
     */
    attempts: number;
    /**
     * Blocking finding codes, present only for `blocked`. **Codes and never
     * values**: a finding's message never carries the matched text (iron
     * rule 11), and this must not become the place that reintroduces it.
     * 
     * @type {Array<string>}
     * @memberof GenerationFailure
     */
    codes?: Array<string>;
    /**
     * The model hit the token ceiling. One of the two failures a user can
     * act on — the action is "make the task smaller".
     * 
     * @type {boolean}
     * @memberof GenerationFailure
     */
    truncated?: boolean;
    /**
     * This workspace already had a skill of that name and the two are not
     * the same kind of content. The other actionable one — rename or delete
     * the existing skill.
     * 
     * @type {boolean}
     * @memberof GenerationFailure
     */
    collision?: boolean;
}


/**
 * @export
 */
export const GenerationFailureFailureEnum = {
    Quota: 'quota',
    Gateway: 'gateway',
    Unpackageable: 'unpackageable',
    Rejected: 'rejected',
    Blocked: 'blocked',
    Empty: ''
} as const;
export type GenerationFailureFailureEnum = typeof GenerationFailureFailureEnum[keyof typeof GenerationFailureFailureEnum];


/**
 * Check if a given object implements the GenerationFailure interface.
 */
export function instanceOfGenerationFailure(value: object): value is GenerationFailure {
    if (!('occurredAt' in value) || value['occurredAt'] === undefined) return false;
    if (!('failure' in value) || value['failure'] === undefined) return false;
    if (!('attempts' in value) || value['attempts'] === undefined) return false;
    return true;
}

export function GenerationFailureFromJSON(json: any): GenerationFailure {
    return GenerationFailureFromJSONTyped(json, false);
}

export function GenerationFailureFromJSONTyped(json: any, ignoreDiscriminator: boolean): GenerationFailure {
    if (json == null) {
        return json;
    }
    return {
        
        'occurredAt': (new Date(json['occurred_at'])),
        'failure': json['failure'],
        'attempts': json['attempts'],
        'codes': json['codes'] == null ? undefined : json['codes'],
        'truncated': json['truncated'] == null ? undefined : json['truncated'],
        'collision': json['collision'] == null ? undefined : json['collision'],
    };
}

export function GenerationFailureToJSON(json: any): GenerationFailure {
    return GenerationFailureToJSONTyped(json, false);
}

export function GenerationFailureToJSONTyped(value?: GenerationFailure | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'occurred_at': value['occurredAt'].toISOString(),
        'failure': value['failure'],
        'attempts': value['attempts'],
        'codes': value['codes'],
        'truncated': value['truncated'],
        'collision': value['collision'],
    };
}

