/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { SearchResultRisk } from './SearchResultRisk';
import {
    SearchResultRiskFromJSON,
    SearchResultRiskFromJSONTyped,
    SearchResultRiskToJSON,
    SearchResultRiskToJSONTyped,
} from './SearchResultRisk';
import type { SkillVerification } from './SkillVerification';
import {
    SkillVerificationFromJSON,
    SkillVerificationFromJSONTyped,
    SkillVerificationToJSON,
    SkillVerificationToJSONTyped,
} from './SkillVerification';

/**
 * A row of the caller's own skill list. `Skill` plus the two facets that
 * make the list decidable rather than merely enumerable: this is a page of
 * code the caller owns and will run, and without them there was nothing on
 * it to decide by (設計系統 §1.1, 04 丙-31). Not folded into `Skill`,
 * because the fork reply shares that schema and a fork one second old has
 * neither.
 * 
 * The two are one fact seen from two sides. Both come from the import
 * scan, and a fork has neither, for the same reason: a fork copies the
 * bytes and not the measurement.
 * 
 * @export
 * @interface OwnSkill
 */
export interface OwnSkill {
    /**
     * 
     * @type {string}
     * @memberof OwnSkill
     */
    skillId: string;
    /**
     * 
     * @type {string}
     * @memberof OwnSkill
     */
    name: string;
    /**
     * 
     * @type {string}
     * @memberof OwnSkill
     */
    summary: string;
    /**
     * Whether a Download Artifact may be produced from this skill. Two of
     * its four values release and two refuse, so on the owner's own list
     * this is the difference between a skill they can take away and one
     * they cannot. It was already on the row and dropped in serialisation;
     * surfacing it is 02:NFR-001 in the direction that says a limit which
     * will block you has to be visible before you hit it.
     * 
     * `self_supplied` is what a user's own import carries since 0036. It
     * was `unknown` before that, which refused — so the answer to "may I
     * download the Skill I just wrote" was permanently no, over a licensing
     * question nobody could resolve (ADR-045).
     * 
     * @type {string}
     * @memberof OwnSkill
     */
    redistribution: OwnSkillRedistributionEnum;
    /**
     * Reason code for a licensing hold on the package materials, null when
     * there is none. Also copied onto forks at fork time, which is why it
     * belongs on a list of skills the caller owns rather than only on the
     * detail view.
     * 
     * @type {string}
     * @memberof OwnSkill
     */
    accessRestriction?: string;
    /**
     * 
     * @type {string}
     * @memberof OwnSkill
     */
    forkedFromSkillId?: string;
    /**
     * 
     * @type {string}
     * @memberof OwnSkill
     */
    forkedFromVersionId?: string;
    /**
     * The same block, from the same projection, that a search row
     * carries for the same skill — deliberately the same schema and not
     * a second one shaped like it, because the rule this facet exists
     * to keep is that the two planes word one fact the same way
     * (02:NFR-007 第 3 條). `scan_status: unavailable` is the usual
     * answer for a fork; see `verification` for why.
     * 
     * @type {SearchResultRisk}
     * @memberof OwnSkill
     */
    risk: SearchResultRisk;
    /**
     * 
     * @type {SkillVerification}
     * @memberof OwnSkill
     */
    verification: SkillVerification;
}


/**
 * @export
 */
export const OwnSkillRedistributionEnum = {
    Allowed: 'allowed',
    Blocked: 'blocked',
    Unknown: 'unknown',
    SelfSupplied: 'self_supplied'
} as const;
export type OwnSkillRedistributionEnum = typeof OwnSkillRedistributionEnum[keyof typeof OwnSkillRedistributionEnum];


/**
 * Check if a given object implements the OwnSkill interface.
 */
export function instanceOfOwnSkill(value: object): value is OwnSkill {
    if (!('skillId' in value) || value['skillId'] === undefined) return false;
    if (!('name' in value) || value['name'] === undefined) return false;
    if (!('summary' in value) || value['summary'] === undefined) return false;
    if (!('redistribution' in value) || value['redistribution'] === undefined) return false;
    if (!('risk' in value) || value['risk'] === undefined) return false;
    if (!('verification' in value) || value['verification'] === undefined) return false;
    return true;
}

export function OwnSkillFromJSON(json: any): OwnSkill {
    return OwnSkillFromJSONTyped(json, false);
}

export function OwnSkillFromJSONTyped(json: any, ignoreDiscriminator: boolean): OwnSkill {
    if (json == null) {
        return json;
    }
    return {
        
        'skillId': json['skill_id'],
        'name': json['name'],
        'summary': json['summary'],
        'redistribution': json['redistribution'],
        'accessRestriction': json['access_restriction'] == null ? undefined : json['access_restriction'],
        'forkedFromSkillId': json['forked_from_skill_id'] == null ? undefined : json['forked_from_skill_id'],
        'forkedFromVersionId': json['forked_from_version_id'] == null ? undefined : json['forked_from_version_id'],
        'risk': SearchResultRiskFromJSON(json['risk']),
        'verification': SkillVerificationFromJSON(json['verification']),
    };
}

export function OwnSkillToJSON(json: any): OwnSkill {
    return OwnSkillToJSONTyped(json, false);
}

export function OwnSkillToJSONTyped(value?: OwnSkill | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'skill_id': value['skillId'],
        'name': value['name'],
        'summary': value['summary'],
        'redistribution': value['redistribution'],
        'access_restriction': value['accessRestriction'],
        'forked_from_skill_id': value['forkedFromSkillId'],
        'forked_from_version_id': value['forkedFromVersionId'],
        'risk': SearchResultRiskToJSON(value['risk']),
        'verification': SkillVerificationToJSON(value['verification']),
    };
}

