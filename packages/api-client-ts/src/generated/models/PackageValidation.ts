/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { Finding } from './Finding';
import {
    FindingFromJSON,
    FindingFromJSONTyped,
    FindingToJSON,
    FindingToJSONTyped,
} from './Finding';

/**
 * The re-validation of the bytes packaging would produce (PACK-002), in
 * the same three-severity shape an import refusal uses — errors, warnings
 * and infos apart (02:SKILL-002). It is that shape rather than one like
 * it: the findings come from the same `skillpkg` validator, and a second
 * severity vocabulary would drift from it in the lenient direction.
 * 
 * Warnings never block. They do have to be shown: PACK-001 blocks on
 * errors only, but hiding the warnings would break 02:SKILL-002, so they
 * travel in the download manifest and onto the download page as well.
 * 
 * @export
 * @interface PackageValidation
 */
export interface PackageValidation {
    /**
     * 
     * @type {Array<Finding>}
     * @memberof PackageValidation
     */
    errors: Array<Finding>;
    /**
     * 
     * @type {Array<Finding>}
     * @memberof PackageValidation
     */
    warnings: Array<Finding>;
    /**
     * 
     * @type {Array<Finding>}
     * @memberof PackageValidation
     */
    infos: Array<Finding>;
    /**
     * True when `errors` is non-empty. Stated rather than left to the
     * client to derive, so no surface can decide for itself what
     * counts as blocking.
     * 
     * @type {boolean}
     * @memberof PackageValidation
     */
    blocked: boolean;
}

/**
 * Check if a given object implements the PackageValidation interface.
 */
export function instanceOfPackageValidation(value: object): value is PackageValidation {
    if (!('errors' in value) || value['errors'] === undefined) return false;
    if (!('warnings' in value) || value['warnings'] === undefined) return false;
    if (!('infos' in value) || value['infos'] === undefined) return false;
    if (!('blocked' in value) || value['blocked'] === undefined) return false;
    return true;
}

export function PackageValidationFromJSON(json: any): PackageValidation {
    return PackageValidationFromJSONTyped(json, false);
}

export function PackageValidationFromJSONTyped(json: any, ignoreDiscriminator: boolean): PackageValidation {
    if (json == null) {
        return json;
    }
    return {
        
        'errors': ((json['errors'] as Array<any>).map(FindingFromJSON)),
        'warnings': ((json['warnings'] as Array<any>).map(FindingFromJSON)),
        'infos': ((json['infos'] as Array<any>).map(FindingFromJSON)),
        'blocked': json['blocked'],
    };
}

export function PackageValidationToJSON(json: any): PackageValidation {
    return PackageValidationToJSONTyped(json, false);
}

export function PackageValidationToJSONTyped(value?: PackageValidation | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'errors': ((value['errors'] as Array<any>).map(FindingToJSON)),
        'warnings': ((value['warnings'] as Array<any>).map(FindingToJSON)),
        'infos': ((value['infos'] as Array<any>).map(FindingToJSON)),
        'blocked': value['blocked'],
    };
}

