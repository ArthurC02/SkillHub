/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * PDM-005 §5.3's 預估成本區間. A range rather than a number because §5.2a-6
 * makes one wrong every time: prompt caching puts a first run and a repeat
 * run of the same skill about 8x apart, so any single figure misdescribes
 * one of them.
 * 
 * An estimate, never a quote and never a commitment. The actual charge is
 * the gateway's own per-key spend. It is also not the per-run budget
 * ceiling - that is `resource_limits` and the gateway's max_budget, and
 * showing the ceiling here would tell every user their run costs half a
 * dollar when the median is six cents.
 * 
 * @export
 * @interface RunCostEstimate
 */
export interface RunCostEstimate {
    /**
     * In Credit, the platform's only unit of account (ADR-068 decision 1).
     * 
     * The field this replaced was `low` beside a `currency` fixed at USD,
     * whose description argued that converting would "present an exchange
     * rate the platform does not own". That is true of a foreign currency
     * and false of Credit: the rate is US$0.001 per credit and the markup
     * is the platform's own constants, and decision 2 requires the markup
     * to be recorded on every entry. The platform owns this rate; it does
     * not own the euro.
     * 
     * @type {number}
     * @memberof RunCostEstimate
     */
    lowCredits: number;
    /**
     * 
     * @type {number}
     * @memberof RunCostEstimate
     */
    typicalCredits: number;
    /**
     * Rounded up past the observed maximum, not a bound: the baseline is 45
     * runs and a sample that size does not establish one.
     * 
     * @type {number}
     * @memberof RunCostEstimate
     */
    highCredits: number;
    /**
     * Where the numbers came from, in the user's language, so nobody reads
     * them as a quote. Display it with them rather than beside them.
     * 
     * @type {string}
     * @memberof RunCostEstimate
     */
    basis: string;
}

/**
 * Check if a given object implements the RunCostEstimate interface.
 */
export function instanceOfRunCostEstimate(value: object): value is RunCostEstimate {
    if (!('lowCredits' in value) || value['lowCredits'] === undefined) return false;
    if (!('typicalCredits' in value) || value['typicalCredits'] === undefined) return false;
    if (!('highCredits' in value) || value['highCredits'] === undefined) return false;
    if (!('basis' in value) || value['basis'] === undefined) return false;
    return true;
}

export function RunCostEstimateFromJSON(json: any): RunCostEstimate {
    return RunCostEstimateFromJSONTyped(json, false);
}

export function RunCostEstimateFromJSONTyped(json: any, ignoreDiscriminator: boolean): RunCostEstimate {
    if (json == null) {
        return json;
    }
    return {
        
        'lowCredits': json['low_credits'],
        'typicalCredits': json['typical_credits'],
        'highCredits': json['high_credits'],
        'basis': json['basis'],
    };
}

export function RunCostEstimateToJSON(json: any): RunCostEstimate {
    return RunCostEstimateToJSONTyped(json, false);
}

export function RunCostEstimateToJSONTyped(value?: RunCostEstimate | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'low_credits': value['lowCredits'],
        'typical_credits': value['typicalCredits'],
        'high_credits': value['highCredits'],
        'basis': value['basis'],
    };
}

