/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PackagingTargetId } from './PackagingTargetId';
import {
    PackagingTargetIdFromJSON,
    PackagingTargetIdFromJSONTyped,
    PackagingTargetIdToJSON,
    PackagingTargetIdToJSONTyped,
} from './PackagingTargetId';
import type { PackagingTargetEnvVarsInner } from './PackagingTargetEnvVarsInner';
import {
    PackagingTargetEnvVarsInnerFromJSON,
    PackagingTargetEnvVarsInnerFromJSONTyped,
    PackagingTargetEnvVarsInnerToJSON,
    PackagingTargetEnvVarsInnerToJSONTyped,
} from './PackagingTargetEnvVarsInner';

/**
 * One packaging target and what installing it involves (PACK-002,
 * PACK-006/007/008).
 * 
 * @export
 * @interface PackagingTarget
 */
export interface PackagingTarget {
    /**
     * 
     * @type {PackagingTargetId}
     * @memberof PackagingTarget
     */
    id: PackagingTargetId;
    /**
     * `standard_package` — the Agent Skills spec package itself, with no
     * agent-specific adaptation. `profile` — a versioned set of install
     * settings for one agent.
     * 
     * Two values rather than one flat list of profiles, because the
     * standard package is what makes "Skill Hub is not tied to one agent"
     * checkable (PDM-008): its SKILL.md is unchanged from the source
     * version, byte for byte, and a profile's is not.
     * 
     * @type {string}
     * @memberof PackagingTarget
     */
    kind: PackagingTargetKindEnum;
    /**
     * The profile's own version, recorded in the package manifest so a
     * downloaded package says which settings produced it. The standard
     * package reports the packager's version instead — there is no profile
     * to version.
     * 
     * @type {string}
     * @memberof PackagingTarget
     */
    version: string;
    /**
     * 
     * @type {string}
     * @memberof PackagingTarget
     */
    displayName: string;
    /**
     * Where the package goes, in the words the install instructions use.
     * Absent for `standard_package`, which makes no claim about any
     * agent's layout.
     * 
     * The two profiles resolve to the same path today and differ in *which
     * layer* it is — a user-level or project-level skills directory versus
     * an agent's working directory — so the copy has to state the
     * difference or the two read as one option listed twice (PDM-008 v4).
     * 
     * @type {string}
     * @memberof PackagingTarget
     */
    installLocation?: string;
    /**
     * `verified` — Skill Hub has actually installed and run a package
     * through this target. `unverified` — it has not, and nothing here
     * promises the package will work (02:PACK-002 第 2 條). The standard
     * package is `unverified` by construction: any spec-compliant agent may
     * try it, and Skill Hub has not tried it on yours.
     * 
     * It is a property of the *target*, not of a skill. Whether a
     * particular version was ever measured is the `compatibility` axes on
     * the skill, and passing format validation is never permission to say
     * it installs (ADR-012).
     * 
     * @type {string}
     * @memberof PackagingTarget
     */
    supportStatus: PackagingTargetSupportStatusEnum;
    /**
     * A prompt to run against the target agent once the package is
     * installed, to see whether the Skill was actually picked up
     * (02:PACK-002 第 3 條). Absent for `standard_package`, which names no
     * agent to run a prompt against — its check is `verification_steps`.
     * 
     * @type {string}
     * @memberof PackagingTarget
     */
    verificationPrompt?: string;
    /**
     * Post-install checks, in order. Every target has at least one of
     * these two fields; both are optional here because which one a target
     * carries depends on whether it names an agent.
     * 
     * Served rather than left to the INSTALL.md inside the package: a
     * check that only exists inside a package the user has not built yet
     * is not available on the page where they are choosing a target, and
     * 02:PACK-002 第 3 條 asks for the check itself, not for a promise
     * that one ships.
     * 
     * @type {Array<string>}
     * @memberof PackagingTarget
     */
    verificationSteps?: Array<string>;
    /**
     * The environment variables this target needs, and the other half of
     * 02:PACK-002 第 1 條's "依賴與環境變數需求". A property of the target
     * rather than of the skill: the SDK target needs its API key whichever
     * Skill is inside, and the skill's own dependencies are on the
     * packaging preview, which is where the package's bytes are known.
     * 
     * Served rather than left to the INSTALL.md inside the package, for
     * the same reason as `verification_steps`: a requirement the user only
     * discovers after building the package is not on the page where they
     * choose a target. Empty array for a target that needs none — the
     * standard package names no agent and therefore no variable.
     * 
     * @type {Array<PackagingTargetEnvVarsInner>}
     * @memberof PackagingTarget
     */
    envVars: Array<PackagingTargetEnvVarsInner>;
    /**
     * Known limitations and the steps a path alone does not cover — for
     * the SDK target, that `cwd` and `setting_sources` are part of
     * installing and a skill silently never loads without them (ADR-023).
     * Server-side so every surface states them identically (NFR-001).
     * 
     * @type {Array<string>}
     * @memberof PackagingTarget
     */
    notes: Array<string>;
}


/**
 * @export
 */
export const PackagingTargetKindEnum = {
    StandardPackage: 'standard_package',
    Profile: 'profile'
} as const;
export type PackagingTargetKindEnum = typeof PackagingTargetKindEnum[keyof typeof PackagingTargetKindEnum];

/**
 * @export
 */
export const PackagingTargetSupportStatusEnum = {
    Verified: 'verified',
    Unverified: 'unverified'
} as const;
export type PackagingTargetSupportStatusEnum = typeof PackagingTargetSupportStatusEnum[keyof typeof PackagingTargetSupportStatusEnum];


/**
 * Check if a given object implements the PackagingTarget interface.
 */
export function instanceOfPackagingTarget(value: object): value is PackagingTarget {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('kind' in value) || value['kind'] === undefined) return false;
    if (!('version' in value) || value['version'] === undefined) return false;
    if (!('displayName' in value) || value['displayName'] === undefined) return false;
    if (!('supportStatus' in value) || value['supportStatus'] === undefined) return false;
    if (!('envVars' in value) || value['envVars'] === undefined) return false;
    if (!('notes' in value) || value['notes'] === undefined) return false;
    return true;
}

export function PackagingTargetFromJSON(json: any): PackagingTarget {
    return PackagingTargetFromJSONTyped(json, false);
}

export function PackagingTargetFromJSONTyped(json: any, ignoreDiscriminator: boolean): PackagingTarget {
    if (json == null) {
        return json;
    }
    return {
        
        'id': PackagingTargetIdFromJSON(json['id']),
        'kind': json['kind'],
        'version': json['version'],
        'displayName': json['display_name'],
        'installLocation': json['install_location'] == null ? undefined : json['install_location'],
        'supportStatus': json['support_status'],
        'verificationPrompt': json['verification_prompt'] == null ? undefined : json['verification_prompt'],
        'verificationSteps': json['verification_steps'] == null ? undefined : json['verification_steps'],
        'envVars': ((json['env_vars'] as Array<any>).map(PackagingTargetEnvVarsInnerFromJSON)),
        'notes': json['notes'],
    };
}

export function PackagingTargetToJSON(json: any): PackagingTarget {
    return PackagingTargetToJSONTyped(json, false);
}

export function PackagingTargetToJSONTyped(value?: PackagingTarget | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': PackagingTargetIdToJSON(value['id']),
        'kind': value['kind'],
        'version': value['version'],
        'display_name': value['displayName'],
        'install_location': value['installLocation'],
        'support_status': value['supportStatus'],
        'verification_prompt': value['verificationPrompt'],
        'verification_steps': value['verificationSteps'],
        'env_vars': ((value['envVars'] as Array<any>).map(PackagingTargetEnvVarsInnerToJSON)),
        'notes': value['notes'],
    };
}

