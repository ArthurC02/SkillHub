/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * One rubric line, strengthening exactly one acceptance criterion
 * (CONTENT-007). Same shape as llm-internal.yaml's RubricItem, because it
 * is the same object: what is stored here is what /judge-run is sent.
 * 
 * @export
 * @interface RubricItem
 */
export interface RubricItem {
    /**
     * The id of the acceptance criterion this item strengthens, not an id
     * of its own. /judge-run answers one verdict per criterion id and the
     * platform drops any id it did not send, so an item naming something
     * else could never produce a stored verdict. An item whose criterion is
     * deleted is removed with it.
     * 
     * @type {string}
     * @memberof RubricItem
     */
    id: string;
    /**
     * 
     * @type {string}
     * @memberof RubricItem
     */
    text: string;
    /**
     * The author's relative-importance signal, passed to the model as
     * written. Nothing in the platform does arithmetic on it: the overall
     * verdict is recomputed from the per-criterion results, so this is not
     * a score. Absent means the author gave none.
     * 
     * @type {number}
     * @memberof RubricItem
     */
    weight?: number;
    /**
     * Whether this item's verdict must come back with a quotation. Per item
     * rather than a global switch, because a quote can show that something
     * is present and cannot show that something is absent.
     * 
     * @type {boolean}
     * @memberof RubricItem
     */
    evidenceRequired: boolean;
}

/**
 * Check if a given object implements the RubricItem interface.
 */
export function instanceOfRubricItem(value: object): value is RubricItem {
    if (!('id' in value) || value['id'] === undefined) return false;
    if (!('text' in value) || value['text'] === undefined) return false;
    if (!('evidenceRequired' in value) || value['evidenceRequired'] === undefined) return false;
    return true;
}

export function RubricItemFromJSON(json: any): RubricItem {
    return RubricItemFromJSONTyped(json, false);
}

export function RubricItemFromJSONTyped(json: any, ignoreDiscriminator: boolean): RubricItem {
    if (json == null) {
        return json;
    }
    return {
        
        'id': json['id'],
        'text': json['text'],
        'weight': json['weight'] == null ? undefined : json['weight'],
        'evidenceRequired': json['evidence_required'],
    };
}

export function RubricItemToJSON(json: any): RubricItem {
    return RubricItemToJSONTyped(json, false);
}

export function RubricItemToJSONTyped(value?: RubricItem | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'id': value['id'],
        'text': value['text'],
        'weight': value['weight'],
        'evidence_required': value['evidenceRequired'],
    };
}

