/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { SearchResultRisk } from './SearchResultRisk';
import {
    SearchResultRiskFromJSON,
    SearchResultRiskFromJSONTyped,
    SearchResultRiskToJSON,
    SearchResultRiskToJSONTyped,
} from './SearchResultRisk';
import type { SkillCompatibility } from './SkillCompatibility';
import {
    SkillCompatibilityFromJSON,
    SkillCompatibilityFromJSONTyped,
    SkillCompatibilityToJSON,
    SkillCompatibilityToJSONTyped,
} from './SkillCompatibility';
import type { Labelled } from './Labelled';
import {
    LabelledFromJSON,
    LabelledFromJSONTyped,
    LabelledToJSON,
    LabelledToJSONTyped,
} from './Labelled';

/**
 * 
 * @export
 * @interface PublicSearchResult
 */
export interface PublicSearchResult {
    /**
     * 
     * @type {string}
     * @memberof PublicSearchResult
     */
    skillId: string;
    /**
     * 
     * @type {string}
     * @memberof PublicSearchResult
     */
    name: string;
    /**
     * Plain summary (DISC-002). The index-time LLM summary when the skill
     * has been enriched (ADR-013 §1, model-generated), otherwise the
     * package's own frontmatter description. `summary_source` says which,
     * because until it existed this sentence was documented as sometimes
     * model-written and had no field a client could act on.
     * 
     * @type {string}
     * @memberof PublicSearchResult
     */
    summary: string;
    /**
     * Who wrote `summary`. ADR-013 requires model-generated content to be
     * labelled, and this row was already labelling `match_reason` while
     * printing the model's rewrite of the summary unmarked — the footnote
     * carried the badge and the sentence a reader decides on did not.
     * 
     * `package` also carries a second fact worth knowing: it is the
     * `description` an agent actually reads when it decides whether to
     * load this Skill. A `model` summary is not that text, so a Skill can
     * read well here and be passed over by the agent, and nothing about
     * the enriched summary changes what the downloaded package says.
     * 
     * @type {string}
     * @memberof PublicSearchResult
     */
    summarySource: PublicSearchResultSummarySourceEnum;
    /**
     * Cosine similarity to the query, 0..1, higher is better (DISC-002 —
     * never star count). Results are ordered by it.
     * 
     * Null when this page was not ranked by similarity, and the ordering
     * is then something else entirely — see `rank_note`. Two cases reach
     * it: the whole answer came from the lexical leg (`degraded`), or this
     * one row has no embedding yet (`partial_index`). The lexical score is
     * not returned in its place: `ts_rank_cd` is unbounded (a local answer
     * measured 1.4) and is not the same quantity as a cosine similarity,
     * so squeezing it into 0..1 would be false precision.
     * 
     * @type {number}
     * @memberof PublicSearchResult
     */
    rank: number | null;
    /**
     * Why `rank` is null and what ordered the page instead. Present only
     * when `rank` is null.
     * 
     * @type {string}
     * @memberof PublicSearchResult
     */
    rankNote?: string;
    /**
     * Collection tier (DISC-002 來源層級). Always `indexed` here for the
     * same reason as on the detail view: curation is a recorded human
     * review and nothing records one yet.
     * 
     * @type {Labelled}
     * @memberof PublicSearchResult
     */
    tier: Labelled;
    /**
     * 
     * @type {SearchResultRisk}
     * @memberof PublicSearchResult
     */
    risk: SearchResultRisk;
    /**
     * Dependency tags from the index-time enrichment (DISC-002 依賴).
     * Model-extracted from what the package's own documentation states —
     * not a resolved dependency graph, and empty while enrichment is
     * pending.
     * 
     * @type {Array<string>}
     * @memberof PublicSearchResult
     */
    dependencies: Array<string>;
    /**
     * Same three axes as the detail view (DISC-002 Agent 相容狀態).
     * `spec_validation` is `passed` for any row with a saved version:
     * static validation is what admits a package to the index, so being
     * listed here is the evidence. It is `unverified` only for a skill
     * with no version yet. `capability` and `runtime` carry the measured
     * verdict when the projection holds one, and stay `unverified` when it
     * does not — that is the 「尚未試跑」 state DISC-002 requires to be
     * explicit, and it is what `runtime_image` being absent means.
     * 
     * @type {SkillCompatibility}
     * @memberof PublicSearchResult
     */
    compatibility: SkillCompatibility;
    /**
     * When the listed content was last validated (DISC-002
     * 最近驗證時間): the creation time of the latest saved version, which
     * is the import that scanned it. Versions are immutable, so this
     * cannot drift from the content it describes. Absent for a skill with
     * no saved version.
     * 
     * @type {Date}
     * @memberof PublicSearchResult
     */
    verifiedAt?: Date;
    /**
     * Human-readable explanation of why this skill matches the query
     * (DISC-002). Template-based fallback when LLM polish times out.
     * 
     * @type {string}
     * @memberof PublicSearchResult
     */
    matchReason?: string;
    /**
     * Provenance of match_reason. ADR-013 requires model-generated content
     * to be labelled, so the UI can mark it as such. `template` reasons are
     * assembled from the query/document lexical overlap, or state plainly
     * that the hit came from semantic similarity with no shared keywords.
     * 
     * @type {string}
     * @memberof PublicSearchResult
     */
    matchReasonSource?: PublicSearchResultMatchReasonSourceEnum;
}


/**
 * @export
 */
export const PublicSearchResultSummarySourceEnum = {
    Model: 'model',
    Package: 'package'
} as const;
export type PublicSearchResultSummarySourceEnum = typeof PublicSearchResultSummarySourceEnum[keyof typeof PublicSearchResultSummarySourceEnum];

/**
 * @export
 */
export const PublicSearchResultMatchReasonSourceEnum = {
    Model: 'model',
    Template: 'template'
} as const;
export type PublicSearchResultMatchReasonSourceEnum = typeof PublicSearchResultMatchReasonSourceEnum[keyof typeof PublicSearchResultMatchReasonSourceEnum];


/**
 * Check if a given object implements the PublicSearchResult interface.
 */
export function instanceOfPublicSearchResult(value: object): value is PublicSearchResult {
    if (!('skillId' in value) || value['skillId'] === undefined) return false;
    if (!('name' in value) || value['name'] === undefined) return false;
    if (!('summary' in value) || value['summary'] === undefined) return false;
    if (!('summarySource' in value) || value['summarySource'] === undefined) return false;
    if (!('rank' in value) || value['rank'] === undefined) return false;
    if (!('tier' in value) || value['tier'] === undefined) return false;
    if (!('risk' in value) || value['risk'] === undefined) return false;
    if (!('dependencies' in value) || value['dependencies'] === undefined) return false;
    if (!('compatibility' in value) || value['compatibility'] === undefined) return false;
    return true;
}

export function PublicSearchResultFromJSON(json: any): PublicSearchResult {
    return PublicSearchResultFromJSONTyped(json, false);
}

export function PublicSearchResultFromJSONTyped(json: any, ignoreDiscriminator: boolean): PublicSearchResult {
    if (json == null) {
        return json;
    }
    return {
        
        'skillId': json['skill_id'],
        'name': json['name'],
        'summary': json['summary'],
        'summarySource': json['summary_source'],
        'rank': json['rank'],
        'rankNote': json['rank_note'] == null ? undefined : json['rank_note'],
        'tier': LabelledFromJSON(json['tier']),
        'risk': SearchResultRiskFromJSON(json['risk']),
        'dependencies': json['dependencies'],
        'compatibility': SkillCompatibilityFromJSON(json['compatibility']),
        'verifiedAt': json['verified_at'] == null ? undefined : (new Date(json['verified_at'])),
        'matchReason': json['match_reason'] == null ? undefined : json['match_reason'],
        'matchReasonSource': json['match_reason_source'] == null ? undefined : json['match_reason_source'],
    };
}

export function PublicSearchResultToJSON(json: any): PublicSearchResult {
    return PublicSearchResultToJSONTyped(json, false);
}

export function PublicSearchResultToJSONTyped(value?: PublicSearchResult | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'skill_id': value['skillId'],
        'name': value['name'],
        'summary': value['summary'],
        'summary_source': value['summarySource'],
        'rank': value['rank'],
        'rank_note': value['rankNote'],
        'tier': LabelledToJSON(value['tier']),
        'risk': SearchResultRiskToJSON(value['risk']),
        'dependencies': value['dependencies'],
        'compatibility': SkillCompatibilityToJSON(value['compatibility']),
        'verified_at': value['verifiedAt'] == null ? value['verifiedAt'] : value['verifiedAt'].toISOString(),
        'match_reason': value['matchReason'],
        'match_reason_source': value['matchReasonSource'],
    };
}

