/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PackagingTargetId } from './PackagingTargetId';
import {
    PackagingTargetIdFromJSON,
    PackagingTargetIdFromJSONTyped,
    PackagingTargetIdToJSON,
    PackagingTargetIdToJSONTyped,
} from './PackagingTargetId';
import type { PackageValidation } from './PackageValidation';
import {
    PackageValidationFromJSON,
    PackageValidationFromJSONTyped,
    PackageValidationToJSON,
    PackageValidationToJSONTyped,
} from './PackageValidation';
import type { PackagingBlockedReason } from './PackagingBlockedReason';
import {
    PackagingBlockedReasonFromJSON,
    PackagingBlockedReasonFromJSONTyped,
    PackagingBlockedReasonToJSON,
    PackagingBlockedReasonToJSONTyped,
} from './PackagingBlockedReason';
import type { PackagingPreviewIncludedTestCasesInner } from './PackagingPreviewIncludedTestCasesInner';
import {
    PackagingPreviewIncludedTestCasesInnerFromJSON,
    PackagingPreviewIncludedTestCasesInnerFromJSONTyped,
    PackagingPreviewIncludedTestCasesInnerToJSON,
    PackagingPreviewIncludedTestCasesInnerToJSONTyped,
} from './PackagingPreviewIncludedTestCasesInner';
import type { PackagingPreviewExcludedTestCasesInner } from './PackagingPreviewExcludedTestCasesInner';
import {
    PackagingPreviewExcludedTestCasesInnerFromJSON,
    PackagingPreviewExcludedTestCasesInnerFromJSONTyped,
    PackagingPreviewExcludedTestCasesInnerToJSON,
    PackagingPreviewExcludedTestCasesInnerToJSONTyped,
} from './PackagingPreviewExcludedTestCasesInner';
import type { PackagingPreviewExcludedFilesInner } from './PackagingPreviewExcludedFilesInner';
import {
    PackagingPreviewExcludedFilesInnerFromJSON,
    PackagingPreviewExcludedFilesInnerFromJSONTyped,
    PackagingPreviewExcludedFilesInnerToJSON,
    PackagingPreviewExcludedFilesInnerToJSONTyped,
} from './PackagingPreviewExcludedFilesInner';

/**
 * What POST .../packaging would do, without doing it (PACK-001). Computed
 * against the package as it stands, so it can change when a new version is
 * saved in between; the packaging call checks again and is the one that
 * decides.
 * 
 * @export
 * @interface PackagingPreview
 */
export interface PackagingPreview {
    /**
     * 
     * @type {PackagingTargetId}
     * @memberof PackagingPreview
     */
    target: PackagingTargetId;
    /**
     * Whether packaging would be permitted right now.
     * @type {boolean}
     * @memberof PackagingPreview
     */
    allowed: boolean;
    /**
     * Present exactly when `allowed` is false.
     * @type {PackagingBlockedReason}
     * @memberof PackagingPreview
     */
    blockedReason?: PackagingBlockedReason;
    /**
     * The reason in the user's language, safe to display as-is and served
     * rather than assembled client-side, so the preview and the 422 from
     * the packaging call say the same sentence.
     * 
     * @type {string}
     * @memberof PackagingPreview
     */
    blockedMessage?: string;
    /**
     * 
     * @type {PackageValidation}
     * @memberof PackagingPreview
     */
    validation: PackageValidation;
    /**
     * What the package needs installed, as the download page's half of
     * 02:PACK-002 第 1 條. These are the exact lines the produced
     * INSTALL.md carries, from the same `skillpkg` findings, so the page
     * and the packaged document cannot disagree about what a Skill needs.
     * 
     * It includes the dependencies the package never declares but its
     * scripts import (`undeclared-dependency`): that is the first reason a
     * reader follows the instructions and the Skill still does not run.
     * 
     * Empty when a licensing gate closed before any bytes were read —
     * there is no package yet to have dependencies — and empty when the
     * package genuinely declares and imports nothing. The preview's
     * `allowed` and `blocked_reason` tell those two apart.
     * 
     * @type {Array<string>}
     * @memberof PackagingPreview
     */
    dependencies: Array<string>;
    /**
     * The test cases that would travel in the package (PACK-005). Empty
     * when the caller did not ask for them, and empty is not the same as
     * "there are none" — `excluded_test_cases` says which were left out
     * and why.
     * 
     * @type {Array<PackagingPreviewIncludedTestCasesInner>}
     * @memberof PackagingPreview
     */
    includedTestCases: Array<PackagingPreviewIncludedTestCasesInner>;
    /**
     * Test cases that will not travel, each with its reason. Listed rather
     * than omitted so the user is told what they are not getting — most
     * often that a dataset they uploaded cannot be redistributed, which is
     * a decision about licensing and not a defect in their test case.
     * 
     * @type {Array<PackagingPreviewExcludedTestCasesInner>}
     * @memberof PackagingPreview
     */
    excludedTestCases: Array<PackagingPreviewExcludedTestCasesInner>;
    /**
     * Source files the packager removed, each with its reason.
     * 
     * The exporter has always dropped these — `.git/`, `node_modules/`,
     * `.ssh/`, `.env`, symlinks — and until now it dropped them
     * **silently**: nothing in the manifest, nothing on this page, nothing
     * in INSTALL.md. `excluded_test_cases` existed for exactly this reason
     * one field over, and the argument is the same one 02:PACK-001 makes
     * about keeping the Skill's directory structure: a package missing a
     * file the Skill needs is not the same product as one that never had
     * it, and only one of those two is worth telling the author about.
     * 
     * Empty for almost every package. It is not empty for the case that
     * matters — a Skill that vendored its dependencies, or one whose
     * `SKILL.md` points at something the exporter would not carry.
     * 
     * @type {Array<PackagingPreviewExcludedFilesInner>}
     * @memberof PackagingPreview
     */
    excludedFiles: Array<PackagingPreviewExcludedFilesInner>;
    /**
     * How long the package this preview describes would be kept, in whole
     * days, before the platform deletes it (03:PACK-011).
     * 
     * **On the preview and not on the packaging response, on purpose.**
     * 02:NFR-001 asks that a limit which will affect the user be visible
     * before they hit it - the same reasoning that put `redistribution` on
     * the owner's list - and the packaging response arrives after the
     * decision it would be informing. It is not on GET /packaging/targets
     * either: retention is one deployment number, not a property of a
     * target, and three targets would put three copies of it on the wire.
     * 
     * It is the value the create call writes into the artifact's
     * `expires_at`, read from the same `DOWNLOAD_ARTIFACT_RETENTION` the
     * deployment configures. Clients render this number and never a
     * constant of their own (設計 §2.2 顯示與強制成對).
     * 
     * Required, because a deployment with no ratified period answers 503
     * for this whole endpoint rather than serving a preview with an unknown
     * one: no ratified number, no number on the screen, and no build
     * either. Truncated rather than rounded - the value is a promise about
     * how long the bytes survive, so the error falls on the side of
     * promising less. A deployment configured shorter than a day answers 0,
     * which 02:NFR-002a 第 1 條 already forbids.
     * 
     * @type {number}
     * @memberof PackagingPreview
     */
    retentionDays: number;
}



/**
 * Check if a given object implements the PackagingPreview interface.
 */
export function instanceOfPackagingPreview(value: object): value is PackagingPreview {
    if (!('target' in value) || value['target'] === undefined) return false;
    if (!('allowed' in value) || value['allowed'] === undefined) return false;
    if (!('validation' in value) || value['validation'] === undefined) return false;
    if (!('dependencies' in value) || value['dependencies'] === undefined) return false;
    if (!('includedTestCases' in value) || value['includedTestCases'] === undefined) return false;
    if (!('excludedTestCases' in value) || value['excludedTestCases'] === undefined) return false;
    if (!('excludedFiles' in value) || value['excludedFiles'] === undefined) return false;
    if (!('retentionDays' in value) || value['retentionDays'] === undefined) return false;
    return true;
}

export function PackagingPreviewFromJSON(json: any): PackagingPreview {
    return PackagingPreviewFromJSONTyped(json, false);
}

export function PackagingPreviewFromJSONTyped(json: any, ignoreDiscriminator: boolean): PackagingPreview {
    if (json == null) {
        return json;
    }
    return {
        
        'target': PackagingTargetIdFromJSON(json['target']),
        'allowed': json['allowed'],
        'blockedReason': json['blocked_reason'] == null ? undefined : PackagingBlockedReasonFromJSON(json['blocked_reason']),
        'blockedMessage': json['blocked_message'] == null ? undefined : json['blocked_message'],
        'validation': PackageValidationFromJSON(json['validation']),
        'dependencies': json['dependencies'],
        'includedTestCases': ((json['included_test_cases'] as Array<any>).map(PackagingPreviewIncludedTestCasesInnerFromJSON)),
        'excludedTestCases': ((json['excluded_test_cases'] as Array<any>).map(PackagingPreviewExcludedTestCasesInnerFromJSON)),
        'excludedFiles': ((json['excluded_files'] as Array<any>).map(PackagingPreviewExcludedFilesInnerFromJSON)),
        'retentionDays': json['retention_days'],
    };
}

export function PackagingPreviewToJSON(json: any): PackagingPreview {
    return PackagingPreviewToJSONTyped(json, false);
}

export function PackagingPreviewToJSONTyped(value?: PackagingPreview | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'target': PackagingTargetIdToJSON(value['target']),
        'allowed': value['allowed'],
        'blocked_reason': PackagingBlockedReasonToJSON(value['blockedReason']),
        'blocked_message': value['blockedMessage'],
        'validation': PackageValidationToJSON(value['validation']),
        'dependencies': value['dependencies'],
        'included_test_cases': ((value['includedTestCases'] as Array<any>).map(PackagingPreviewIncludedTestCasesInnerToJSON)),
        'excluded_test_cases': ((value['excludedTestCases'] as Array<any>).map(PackagingPreviewExcludedTestCasesInnerToJSON)),
        'excluded_files': ((value['excludedFiles'] as Array<any>).map(PackagingPreviewExcludedFilesInnerToJSON)),
        'retention_days': value['retentionDays'],
    };
}

