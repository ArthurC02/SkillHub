/* tslint:disable */
/* eslint-disable */
/**
 * Skill Hub Public API
 * Web to Go control plane API. This file is the single source of truth for the contract (implementation rule 12); the TS client under packages/ is generated from it and must never be hand-edited. 
 *
 * The version of the OpenAPI document: 0.1.0
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { SkillRisk } from './SkillRisk';
import {
    SkillRiskFromJSON,
    SkillRiskFromJSONTyped,
    SkillRiskToJSON,
    SkillRiskToJSONTyped,
} from './SkillRisk';
import type { SkillCompatibility } from './SkillCompatibility';
import {
    SkillCompatibilityFromJSON,
    SkillCompatibilityFromJSONTyped,
    SkillCompatibilityToJSON,
    SkillCompatibilityToJSONTyped,
} from './SkillCompatibility';
import type { SkillEnrichment } from './SkillEnrichment';
import {
    SkillEnrichmentFromJSON,
    SkillEnrichmentFromJSONTyped,
    SkillEnrichmentToJSON,
    SkillEnrichmentToJSONTyped,
} from './SkillEnrichment';
import type { SkillSource } from './SkillSource';
import {
    SkillSourceFromJSON,
    SkillSourceFromJSONTyped,
    SkillSourceToJSON,
    SkillSourceToJSONTyped,
} from './SkillSource';
import type { SkillAccessRestriction } from './SkillAccessRestriction';
import {
    SkillAccessRestrictionFromJSON,
    SkillAccessRestrictionFromJSONTyped,
    SkillAccessRestrictionToJSON,
    SkillAccessRestrictionToJSONTyped,
} from './SkillAccessRestriction';
import type { Labelled } from './Labelled';
import {
    LabelledFromJSON,
    LabelledFromJSONTyped,
    LabelledToJSON,
    LabelledToJSONTyped,
} from './Labelled';
import type { SkillLicense } from './SkillLicense';
import {
    SkillLicenseFromJSON,
    SkillLicenseFromJSONTyped,
    SkillLicenseToJSON,
    SkillLicenseToJSONTyped,
} from './SkillLicense';
import type { SkillLimitation } from './SkillLimitation';
import {
    SkillLimitationFromJSON,
    SkillLimitationFromJSONTyped,
    SkillLimitationToJSON,
    SkillLimitationToJSONTyped,
} from './SkillLimitation';
import type { SkillDetailDerivation } from './SkillDetailDerivation';
import {
    SkillDetailDerivationFromJSON,
    SkillDetailDerivationFromJSONTyped,
    SkillDetailDerivationToJSON,
    SkillDetailDerivationToJSONTyped,
} from './SkillDetailDerivation';
import type { SkillDetailVersion } from './SkillDetailVersion';
import {
    SkillDetailVersionFromJSON,
    SkillDetailVersionFromJSONTyped,
    SkillDetailVersionToJSON,
    SkillDetailVersionToJSONTyped,
} from './SkillDetailVersion';

/**
 * 
 * @export
 * @interface SkillDetail
 */
export interface SkillDetail {
    /**
     * 
     * @type {string}
     * @memberof SkillDetail
     */
    skillId: string;
    /**
     * 
     * @type {string}
     * @memberof SkillDetail
     */
    name: string;
    /**
     * The package's own frontmatter description, never the model's rewrite.
     * @type {string}
     * @memberof SkillDetail
     */
    summary: string;
    /**
     * Which read answered — the public catalog, or the caller's own workspace.
     * @type {string}
     * @memberof SkillDetail
     */
    scope: SkillDetailScopeEnum;
    /**
     * curated | indexed | external. Always `indexed` today: curation is a
     * recorded human review (PDM-002) and CONTENT-003's promotion workflow
     * is what would record one. Catalog membership is not that review, and
     * labelling it 精選 would be the endorsement PDM-002 warns against.
     * 
     * @type {Labelled}
     * @memberof SkillDetail
     */
    tier: Labelled;
    /**
     * 
     * @type {SkillEnrichment}
     * @memberof SkillDetail
     */
    enrichment: SkillEnrichment;
    /**
     * 
     * @type {SkillDetailVersion}
     * @memberof SkillDetail
     */
    version?: SkillDetailVersion;
    /**
     * 
     * @type {SkillSource}
     * @memberof SkillDetail
     */
    source?: SkillSource;
    /**
     * 
     * @type {SkillLicense}
     * @memberof SkillDetail
     */
    license: SkillLicense;
    /**
     * allowed | blocked | unknown — whether this skill's content may be
     * handed on to someone else, which is what decides whether a download
     * package can be built from it at all (02:SEC-007, ADR-012).
     * 
     * Required, and required for every skill, because the question has an
     * answer for every skill: `unknown` is where one starts and where
     * anything unclassifiable stays. **Only `allowed` releases**; `unknown`
     * is treated exactly like `blocked` at the packaging gate, since
     * 02:DISC-003 forbids implying that an unestablished licence may be
     * modified or redistributed.
     * 
     * A separate axis from `license.status` and never derivable from it:
     * 02:CONTENT-002 states plainly that a manually confirmed licence is
     * not thereby a redistributable one, so `confirmed` is not a release
     * condition. Also separate from `access_restriction`, which is a
     * reviewer's temporary hold on a handful of known skills; this is a
     * property of the content that every skill has. Reading the absence of
     * a hold as permission would be asserting that whatever nobody
     * objected to may be redistributed.
     * 
     * The copy in `label`/`note` says what was and was not established,
     * server-side so every surface words it the same way (NFR-001).
     * 
     * @type {Labelled}
     * @memberof SkillDetail
     */
    redistribution: Labelled;
    /**
     * 
     * @type {SkillDetailDerivation}
     * @memberof SkillDetail
     */
    derivation: SkillDetailDerivation;
    /**
     * What the skill does not do or needs in order to work (DISC-003
     * 一般模式「限制」). Assembled from both sources — the enrichment's
     * reading of the document and the static scan of the package — each
     * labelled. An empty list means neither source found anything to
     * state, never that the skill is unconstrained.
     * 
     * @type {Array<SkillLimitation>}
     * @memberof SkillDetail
     */
    limitations: Array<SkillLimitation>;
    /**
     * The manifest's allowed-tools, as declared by the package.
     * @type {Array<string>}
     * @memberof SkillDetail
     */
    allowedTools?: Array<string>;
    /**
     * 
     * @type {SkillRisk}
     * @memberof SkillDetail
     */
    risk: SkillRisk;
    /**
     * 
     * @type {SkillCompatibility}
     * @memberof SkillDetail
     */
    compatibility: SkillCompatibility;
    /**
     * The 0023 licensing hold, present only while a licensing question
     * about the package is open. Absent is the normal state and means no
     * hold - it is omitted rather than nulled, so a client must treat
     * "field missing" as unrestricted.
     * 
     * Every other field still answers when it is present: the hold is on
     * the *materials*, not on the platform's own description of them. What
     * it does close is GET /api/skills/{id}/files, which answers 403 while
     * this is set. A client that sees it should say so before offering the
     * advanced view rather than letting the user walk into the refusal.
     * 
     * @type {SkillAccessRestriction}
     * @memberof SkillDetail
     */
    accessRestriction?: SkillAccessRestriction;
}


/**
 * @export
 */
export const SkillDetailScopeEnum = {
    Catalog: 'catalog',
    Private: 'private'
} as const;
export type SkillDetailScopeEnum = typeof SkillDetailScopeEnum[keyof typeof SkillDetailScopeEnum];


/**
 * Check if a given object implements the SkillDetail interface.
 */
export function instanceOfSkillDetail(value: object): value is SkillDetail {
    if (!('skillId' in value) || value['skillId'] === undefined) return false;
    if (!('name' in value) || value['name'] === undefined) return false;
    if (!('summary' in value) || value['summary'] === undefined) return false;
    if (!('scope' in value) || value['scope'] === undefined) return false;
    if (!('tier' in value) || value['tier'] === undefined) return false;
    if (!('enrichment' in value) || value['enrichment'] === undefined) return false;
    if (!('license' in value) || value['license'] === undefined) return false;
    if (!('redistribution' in value) || value['redistribution'] === undefined) return false;
    if (!('derivation' in value) || value['derivation'] === undefined) return false;
    if (!('limitations' in value) || value['limitations'] === undefined) return false;
    if (!('risk' in value) || value['risk'] === undefined) return false;
    if (!('compatibility' in value) || value['compatibility'] === undefined) return false;
    return true;
}

export function SkillDetailFromJSON(json: any): SkillDetail {
    return SkillDetailFromJSONTyped(json, false);
}

export function SkillDetailFromJSONTyped(json: any, ignoreDiscriminator: boolean): SkillDetail {
    if (json == null) {
        return json;
    }
    return {
        
        'skillId': json['skill_id'],
        'name': json['name'],
        'summary': json['summary'],
        'scope': json['scope'],
        'tier': LabelledFromJSON(json['tier']),
        'enrichment': SkillEnrichmentFromJSON(json['enrichment']),
        'version': json['version'] == null ? undefined : SkillDetailVersionFromJSON(json['version']),
        'source': json['source'] == null ? undefined : SkillSourceFromJSON(json['source']),
        'license': SkillLicenseFromJSON(json['license']),
        'redistribution': LabelledFromJSON(json['redistribution']),
        'derivation': SkillDetailDerivationFromJSON(json['derivation']),
        'limitations': ((json['limitations'] as Array<any>).map(SkillLimitationFromJSON)),
        'allowedTools': json['allowed_tools'] == null ? undefined : json['allowed_tools'],
        'risk': SkillRiskFromJSON(json['risk']),
        'compatibility': SkillCompatibilityFromJSON(json['compatibility']),
        'accessRestriction': json['access_restriction'] == null ? undefined : SkillAccessRestrictionFromJSON(json['access_restriction']),
    };
}

export function SkillDetailToJSON(json: any): SkillDetail {
    return SkillDetailToJSONTyped(json, false);
}

export function SkillDetailToJSONTyped(value?: SkillDetail | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        'skill_id': value['skillId'],
        'name': value['name'],
        'summary': value['summary'],
        'scope': value['scope'],
        'tier': LabelledToJSON(value['tier']),
        'enrichment': SkillEnrichmentToJSON(value['enrichment']),
        'version': SkillDetailVersionToJSON(value['version']),
        'source': SkillSourceToJSON(value['source']),
        'license': SkillLicenseToJSON(value['license']),
        'redistribution': LabelledToJSON(value['redistribution']),
        'derivation': SkillDetailDerivationToJSON(value['derivation']),
        'limitations': ((value['limitations'] as Array<any>).map(SkillLimitationToJSON)),
        'allowed_tools': value['allowedTools'],
        'risk': SkillRiskToJSON(value['risk']),
        'compatibility': SkillCompatibilityToJSON(value['compatibility']),
        'access_restriction': SkillAccessRestrictionToJSON(value['accessRestriction']),
    };
}

